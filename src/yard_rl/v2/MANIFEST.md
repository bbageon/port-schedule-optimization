# v2 — 비용 채점 세대 (Q 회귀)

> 세대 분리 2026-08-20. **설계 정본**: [`.claude/docs/architecture/README.md`](../../../.claude/docs/architecture/README.md)

## 한 줄

"이 선택의 **비용이 얼마**"를 회귀하고 `argmin` 으로 고른다.
**규칙 대비 +39.92 — 세 세대 중 최선이지만 여전히 졌다.**

## 이 폴더에 있는 것

| 파일 | 무엇 | 어디서 왔나 |
|---|---|---|
| `features.py` | `block_features` · `candidate_features` · `BLOCK_DIM` · `KEEP_Q` | `integrated/transfer_head.py` 분리 |
| `sell_q.py` | `SellQNet`(5,633) · `QCoordScorer` · `QSellPolicy` · `coord_features` · `EpochCache` | `integrated/sell_q.py` 이동 |

`features.py` 는 v1 에도 같은 식의 사본이 있다. **의도된 중복**이다.

## 무엇을 세대 폴더에, 무엇을 무대에 두나

가르는 기준은 **"세대가 바뀌면 이게 달라져야 하나"** 하나다.

| | 어디 | 왜 |
|---|---|---|
| **판정에 쓰는 것** — 평가 잣대 Φ · 규칙 팔 · 에피소드 실행 · 시뮬레이터 | `integrated/` | **같아야 비교가 성립한다.** 판정이 짝비교라 세 세대가 같은 무대·같은 자를 받아야 한다 |
| **학습에 쓰는 것** — 특징 · 보상 라벨 · 정책망 · 학습 루프 | `v1/` `v2/` `v3/` | **달라지는 게 세대다.** 세대마다 사본을 갖는다 |

`shared/` 같은 중간 계층은 두지 않는다 — 만들면 결국 거기로 다 모인다
(사용자 지시 2026-08-20). 기계 검사: `tests/v3/test_v3_isolation.py`


## 아직 안 옮긴 것 (2차 대상)

| 파일 | 무엇 | 걸림돌 |
|---|---|---|
| `experiments/yr189_q_train.py` | Q 회귀 학습 | 하네스(`yr151` Φ · `yr170` 에피소드 · `yr174_txn_reward` 신용)를 씀 |
| `experiments/yr189_q_eval.py` | 판정 | 〃 |
| `experiments/yr195_margin.py` | 문턱 (최선 성적) | 〃 |
| `experiments/yr200_axis_margin.py` | 축별 문턱 | 〃 |
| `experiments/yr174_txn_reward.py` | 차분 신용 — **학습 잣대라 v2 사본이 필요** | v1 도 같은 것을 쓴다 |

Φ·에피소드 실행·규칙 팔은 **무대로** 가고, 차분 신용은 **세대마다 사본**을 갖는다.

## 판정 이력

| 판정 | 무엇 | 규칙 대비 | 안 팔기 대비 |
|---|---|---|---|
| YR-189 | Q 전환 | +211.41 | +86.32 |
| YR-195 | 문턱 | **+39.92 ← 최선** | −105.41 |
| YR-200 | 축별 문턱 | +40.57(C) · 주판정 +61.66 | −106~−127 |

**안 팔기는 YR-195 부터 확실히 넘는다** — 재배치는 배우지만 규칙만큼 잘 고르지 못한다.

## 무엇을 해결했고 무엇이 남았나

**해결**: 구조 결함 3종(좌표 부재로 인한 부호 오류·기준 불일치·critic 붕괴)이 소멸했다.

**남음**:

| | |
|---|---|
| 시야 40분 | 학습 잣대가 트럭 한 대의 방문만 본다. 규칙도 근시안이라 **우위가 없다** |
| 예측 한계 | YR-199 — 오차의 **95%가 지금 특징으로는 못 줄이는 잡음** |
| 라벨이 추정 | "밀렸을 것이다"라는 가정을 정답으로 쓴다 |

→ v3 는 네 축(데이터·보상·구조·정보)을 **함께** 바꾼다.

## 재현 주의

판정 산출물은 **파일이 옮겨지기 전 경로**로 만들어졌다. 과거 결과를 그대로
재현하려면 `repro_stamp` 의 `git_head` 로 체크아웃한다.

**평가 대역 재사용 금지**: 9,200,000 · 9,300,000~ · 9,600,000~ · 9,700,000~ ·
9,800,000~ · 비판정 9,900,0xx
