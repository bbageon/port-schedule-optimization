"""v2 — 비용 채점 세대 (Q 회귀).

■ 한 줄
  "이 선택의 **비용이 얼마**"를 회귀한다. **규칙 대비 +39.92 — 세 세대 중 최선이지만 여전히 졌다.**

■ 무엇이 핵심이었나
  · 확률이 아니라 **비용**을 낸다 — 낮을수록 싸다. 선택은 `argmin`.
  · 행에 **목적지 좌표가 들어간다**(21차원 = 블록 7 + 후보 6 + 좌표 8) —
    구판은 배정기가 좌표를 따로 계산하다 부호 오류·기준 불일치를 냈다.
  · **정책과 배정기가 같은 망으로 같은 행**을 채점한다 → "다른 자로 잰다"가 성립 불가.
  · critic 이 없다 — 붕괴할 critic 이 없다.

■ 무엇이 남았나 (v3 로 넘어간 이유)
  · 학습 잣대의 시야가 **트럭 한 대의 방문(약 40분)** 이고 규칙도 근시안이라
    **RL 에게 구조적 우위가 없다.**
  · YR-199: 모델 오차의 **95%가 지금 특징으로는 못 줄이는 잡음** →
    "같은 시야에서 더 정교하게"는 소진됐다.
  → v3 는 데이터·보상·구조·정보 **네 축을 함께** 바꾼다 (`yard_rl.v3`).

■ 이 패키지의 것
  sell_q.py   SellQNet · QCoordScorer · QSellPolicy · coord_features · EpochCache

■ 세대 목록과 판정 이력: `MANIFEST.md`
"""

__all__: list[str] = []

GENERATION = "v2"
