"""v3 — 통합 아키텍처 (재배치 축).

■ 이 패키지의 규칙
  1. **v1·v2 코드를 수정하지 않는다.** 필요하면 읽기만 하고, 고쳐야 하면
     여기에 새로 짠다 (사용자 지시 2026-08-20).
  2. **v3 는 v3 것만 갖는다.** 특징·보상·정책을 v1/v2 에서 가져오지 않고 여기에
     새로 짠다. 식이 같아도 사본을 따로 둔다 — 세대를 얼리기 위한 의도된 중복이다.
     `shared/` 같은 중간 계층도 만들지 않는다 (사용자 지시 2026-08-20).
  3. **무대도 v3 것이다** (사용자 지시 2026-08-22 — 예외 없음). 엔진·무대 생성기·
     배정기까지 `v3/world/` 에 **사본**으로 갖는다. 세대 간 짝비교는 그 사본이
     원본과 물리적으로 같다는 전제 위에 서므로, 사본은 **v3 전용 확장 세 파일을
     빼면 바이트 동일**해야 한다 — `tests/v3/test_world_clone.py` 가 검사한다.
  4. **한 번에 한 축씩** 넣고 축마다 판정한다 (설계 원칙 2).

■ 네 축 = 네 하위 패키지 (+ 무대 결합 `stage/` · 세계 사본 `world/`)
  schema/    ① 데이터 — 오더 6필드 · 실행 기록(터미널 전송 이벤트)
  reward/    ② 보상   — 반사실 교사 (H = 1시간)
  actors/    ③ 구조   — Seller · Buyer · 배정기
  features/  ④ 정보   — 시각 · 실제 대기 대수 · 크레인 예상 서비스 시각

  train/     학습 진입점 (교사가 라벨을 만들고 학생이 배운다)
  eval/      판정 진입점 (학생만 돈다 · 교사 호출 0회)

■ 설계 정본
  `.claude/docs/architecture/README.md` — 문서가 to-be 이고 코드가 여기 맞춘다.
  문서와 코드가 다르면 **코드가 아직 안 따라온 것**이다.

■ 진행 상황은 기계가 센다
  PYTHONPATH=src python .claude/docs/architecture/verify_architecture.py
"""

__all__: list[str] = []

#: 아키텍처 세대. 판정 산출물·체크포인트 경로에 박아 세대를 구분한다.
GENERATION = "v3"

#: 반사실 rollout 지평(초). 사용자 결정 2026-08-20 — 근거는 연산량.
#: 오더 1건당 rollout 1회이므로 H 가 곧 계산 배수다 (40분 ×1 · 1시간 ×1.5 · 3시간 ×4.5).
CF_HORIZON_S = 3600.0
