# v3 — 통합 아키텍처 코드

> 설계 정본: [`.claude/docs/architecture/README.md`](../../../.claude/docs/architecture/README.md)
> 문서가 **to-be** 이고 코드가 여기에 맞춘다.

## 이 폴더의 규칙 (사용자 지시 2026-08-20)

1. **v1·v2 코드를 수정하지 않는다.** 읽는 것은 자유, 고쳐야 하면 여기에 새로 짠다.
2. **v3 는 v3 것만 갖는다.** v1/v2 에서 import 하지 않고, `shared/` 같은 중간
   계층도 만들지 않는다. 식이 같아도 사본을 따로 둔다 — **의도된 중복**이다.
3. **무대만 공유한다** — `integrated/`(엔진·비용·레이아웃·배정기)는 복제하지
   않는다. 판정이 **짝비교**라 세 세대가 같은 무대를 받아야 비교가 성립한다.
4. **한 번에 한 축씩** 넣고 축마다 판정한다.

## 무엇을 v3 에, 무엇을 무대에 두나

가르는 기준은 **"세대가 바뀌면 이게 달라져야 하나"** 하나다.

| | 어디 | 왜 |
|---|---|---|
| **판정에 쓰는 것** — 평가 잣대 Φ · 규칙 팔 · 에피소드 실행 · 시뮬레이터 | `integrated/` | **같아야 비교가 성립한다** |
| **학습에 쓰는 것** — 특징 · 보상 라벨 · 정책망 · 학습 루프 | **여기 v3/** | **달라지는 게 세대다** |

**⚠️ v1·v2 는 아직 물리적으로 분리되어 있지 않다** — `integrated/transfer_head.py`
(v1 PPO 망)와 `integrated/sell_q.py`(v2 Q 망)가 무대 폴더에 섞여 있다.
분리 작업은 [[YR-209]] 로 **보류**됐다(브랜치 `refactor/generation-split` 에 보존).
v3 를 짤 때는 **그 둘을 참조하지 않으면** 규칙을 지킨 것이다.

## 네 축 = 네 폴더

| 폴더 | 축 | 무엇 | 설계 문서 |
|---|---|---|---|
| `schema/` | ① 데이터 | 오더 6필드 · 시나리오 파라미터 · 실행 기록 | [01](../../../.claude/docs/architecture/01-오더-스키마.md) |
| `reward/` | ② 보상 | 반사실 교사 (H=1시간) · 원화 환산 | [04b](../../../.claude/docs/architecture/04b-학습-잣대.md) |
| `actors/` | ③ 구조 | Seller · Buyer · 배정기 | [03](../../../.claude/docs/architecture/03-결정층.md) |
| `features/` | ④ 정보 | 시각 · 실제 대기 대수 · 크레인 예측 | [05](../../../.claude/docs/architecture/05-정보경계.md) |
| `train/` | — | 학습 진입점 (교사 → 학생) | [06](../../../.claude/docs/architecture/06-학습과-판정.md) |
| `eval/` | — | 판정 진입점 (**학생만** · 교사 호출 0회) | [06](../../../.claude/docs/architecture/06-학습과-판정.md) |

## 무엇을 어디에 두나

**`schema/`** 오더 레코드 규격과 시각 셋(통지·예정·실현)의 분리. 지금 구현은
셋이 한 값이라 예약 준수 축이 없다 → 여기서 갈라 놓는다.

**`reward/`** 그 거래만 취소한 세계를 H=1시간 굴려 총비용 차이를 재는 교사.
**학습에서만** 불린다 — 배포 경로에서 부르면 하드가드 위반이다.

**`actors/`** 채점망을 파는 쪽(Seller)·사는 쪽(Buyer) 둘로 나눈다. 배정기가
`Seller(src) − Buyer(dst) − ΔC_route` 로 조립한다.

**`features/`** 경계 안쪽인데 안 주던 정보를 넣는다. 넣기 전에 **잡음 하한을
먼저 재서** 값어치를 확인한다(학습 불필요).

## 아직 비어 있다

착수 순서는 설계 정본의 리팩토링 순서를 따른다. 지금 가능한 것은 1~4단계이고
5~8단계는 **performance 게이트 해소 전 착수 금지**다.
