# 부산항 다중 터미널의 공동 이용 외부 컨테이너 장치장을 활용한 강화학습 기반 반입 경로·출고 시점 최적화 연구 설계안

> 개정일: 2026-09-26. **기존 터미널 내부 재배치 연구를 참고한 확장 연구 제안**이다.
> 공용 장치장 모형의 구현·성능 검증 결과를 보고하는 문서가 아니다.
> 상세: [문제 정식화와 비용 모형](docs/research/buffer-yard/01-problem-formulation.md) · [실험 설계와 판정 기준](docs/research/buffer-yard/02-experimental-design.md).

## 1. 연구 목적과 범위

본 연구는 목적 터미널이 지정된 수출 반입 컨테이너에 대해 **직접 반입과 외부 장치장 경유를 선택하고, 장치한 컨테이너의 출고 시점을 결정하는 문제**를 다룬다. 강화학습(reinforcement learning, RL: 행동 결과로부터 장기 비용이 낮은 의사결정 규칙을 학습하는 방법)을 이용하여, 추가 하역·보관·운송을 포함한 시스템 총비용을 줄일 수 있는지 검증한다.

공동 이용 외부 컨테이너 장치장(shared off-dock container yard)은 여러 터미널을 목적지로 하는 컨테이너를 일시 장치하는 시설로 정의한다. 이하 **공용 장치장**으로 표기한다. “공동 이용”과 “혼잡 완충”은 본 연구의 운영 가정이며, 부산항에 해당 시설·협약이 이미 존재한다는 뜻은 아니다. 영어 기본 명칭은 항만 당국의 [off-dock container yard 용례](https://www.portoftacoma.com/business/contracting/procurement/thorne-road-dock-container-yard-2025)를 참고한다.

연구 대상은 **목적 터미널 변경이 없는 시간적 유입 조절**이다. 다른 터미널의 여유 공간으로 화물의 최종 목적지를 바꾸는 문제와 구분한다. 개별 터미널의 내부 작업 규칙은 비교 실험 동안 동일하게 유지하며, 실제 터미널 운영시스템(Terminal Operating System, TOS) 연동은 평가 범위에 포함하지 않는다. 시설 부지선정·신설 투자경제성은 범위 밖이며, 장치장 운영에 따른 고정·변동비는 포함한다.

## 2. 기존 연구와의 관계

기존 연구는 야드크레인 작업순서와 **동일 터미널 내 블록 간 작업 배정 변경**을 다뤘다. 이번 제안은 터미널 외부의 장치·운송 과정이 추가되므로 기존 정책이나 개선율을 그대로 적용할 수 없다.

| 기존 연구에서 확인한 사항 | 이번 설계에 반영하는 원칙 |
|---|---|
| 21블록 모형의 16개 평가 시나리오에서 규칙 기반 재배치는 두 크레인 배차 조건 모두에서 비용·체류시간을 줄였다 | 물류 운영방식의 효과와 학습 알고리즘의 추가 효과를 분리한다 |
| 같은 평가에서 학습 기반 재배치는 규칙 기반 재배치보다 불리했다 | “강화학습을 쓰면 개선된다”를 전제로 두지 않는다 |
| 후속 학습에서는 학습 오차 감소와 정책 성능 개선이 일치하지 않았다 | 학습 손실, 미학습 자료의 예측 오차, 실제 정책 총비용을 따로 평가한다 |
| 정책 입력의 미래정보 누출과 시스템 밖 대기 누락이 검증 쟁점이었다 | 정보 공개시점과 물류 전체의 시간·비용 기록을 유지한다 |
| 터미널 프로파일 등록과 실제 운영 재현은 서로 다른 단계다 | 터미널 수를 늘리는 것만으로 부산항 실운영 검증을 주장하지 않는다 |

따라서 기존 성과는 **평가 방법과 문제 설정의 근거**다. 공용 장치장이나 강화학습의 우월성을 입증한 근거로 사용하지 않는다.

기존 잠금평가(비교 조건·자료·판정법을 미리 고정한 평가)에서는 규칙 배차에 재배치를 추가했을 때 실제 터미널 체류시간이 차량당 **143.78초, 약 2.40분** 줄었다. 총비용 차이를 트럭 시간으로 환산한 **약 2.64분**과는 다른 수치다. 이는 내부 재배치 조건의 결과이며 외부 장치장 효과가 아니다. [원자료](outputs/reports/yr180_scorecard/scorecard.json) · [기존 논문 초안](docs/paper/v1/01-주장과-근거.md).

최신 학습 관련 근거는 [학습 루프 진단](.claude/docs/dashboard-task-specs/YR-218-training-loop.md)과 [검증 표본 분리 명세](.claude/docs/dashboard-task-specs/YR-222-sample-expansion.md)를 따른다. 진단 중간값을 최종 평가 성적으로 인용하지 않는다.

## 3. 용어와 측정 경계

| 용어 | 이 문서에서의 정의 |
|---|---|
| 야드 장치율(yard occupancy ratio) | 장치된 컨테이너의 환산 물량 ÷ 사용 가능한 장치용량 |
| TEU(twenty-foot equivalent unit) | 20피트 컨테이너 기준 환산 단위. 기본 모형에서 20피트는 1, 40피트는 2로 센다 |
| 트럭 대기시간(truck waiting time) | 게이트·장치장·작업장에서 서비스를 기다린 시간. 주행·작업시간과 별도 기록 |
| 터미널 내 트럭 체류시간(in-terminal truck time) | 해당 터미널의 게이트 진입부터 게이트 진출까지의 시간 |
| 터미널 방문시간(truck visit time) | 게이트 외부 대기열 진입부터 터미널 게이트 진출까지의 시간 |
| 컨테이너 반입 소요시간(container delivery lead time) | 공통 운송 의사결정 지점부터 목적 터미널 반입 절차 완료까지의 시간 |
| 장치장 체류시간(buffer dwell time) | 컨테이너가 장치장에 하차·장치된 뒤 출고를 위해 반출될 때까지의 시간 |
| 반입 개시시각·마감시각 | 해당 선적 건의 컨테이너 반입이 허용되는 가장 이른 시각·최종 시각 |
| 추가 취급(additional handling) | 장치장 경유로 추가되는 하차·상차 등 필수 취급 |
| 재취급(rehandling) | 목표 컨테이너 접근 등을 위한 부가 이동. 장치장 하차·상차와 별도 집계 |

트럭이 컨테이너를 내려놓고 떠났다면 **컨테이너 보관시간 전체를 기사 대기로 계산하지 않는다**. 대신 실제 운송차량·셔틀의 작업시간과 컨테이너 보관시간을 각각 비용에 반영한다. TEU는 용량 단위이고 차량 대수·취급 횟수와 같지 않다.

“Turn Time”은 측정 범위가 문헌마다 다르므로 단독 표기를 피하고 시작·종료 사건을 명시한다. 위 시간 구분은 [METRIS 트럭 시간 용어 정의](https://metris.us/services/turntime/TTGlossary.htm)를 참고한다.

선박의 입항예정시각(Estimated Time of Arrival, ETA), 접안예정시각(Estimated Time of Berthing, ETB), 출항예정시각(Estimated Time of Departure, ETD)을 구분하고, 차량 ETA에는 차량임을 표시한다. 초안의 ETW는 자료 제공자의 정의를 확인하기 전 사용하지 않고 **예정 본선 하역작업 개시시각**으로 명시한다.

컨테이너 반입 마감은 본선 하역 개시·출항시각과 같지 않다. DCSA(디지털 컨테이너 해운협회)의 **FCL delivery cut-off(컨테이너 단위 화물 반입 마감)**와 반입 개시시각을 참고하되, 실제 자료가 뜻하는 접수 완료 사건을 확인한다. [DCSA 마감 코드](https://models.dcsa.org/2024Q4/EARoot/EA5/EA2/EA6/EA7/EA1/EA1/EA2565.htm) · [Port Call 표준](https://dcsa.org/standards/port-call).

## 4. 운영 가정과 제어 범위

기본 실험 대상은 목적 터미널과 선적 일정이 지정된 **수출용 일반 컨테이너**다. 초안의 출발지→장치장→목적 터미널 경로가 이 흐름에 해당한다. 수입 반출·환적·냉동·위험물은 별도의 인도 조건과 시설 제약이 필요하므로 기본 평가에 혼합하지 않는다.

| 구분 | 기본 모형의 계약 |
|---|---|
| 직접 반입 | 외부 운송차량이 지정 터미널에 컨테이너를 인도 |
| 장치장 경유 | 외부 운송차량이 장치장에서 하차 후 이탈하고, 별도 셔틀이 지정 터미널로 운송 |
| 차량 흐름 | 적재 운송, 하역 중 점유, 빈차 회송, 셔틀 복귀와 재가용 시점을 추적 |
| 의사결정 권한 | 최초 경로 선택과 장치 컨테이너의 출고 여부·순서 |
| 고정 사항 | 목적 터미널·선적 대상, 터미널 내부 작업 규칙, 운송망과 자원 수량 |
| 시설 제약 | 장치공간, 장비·게이트 처리능력, 셔틀 대수, 접수시간, 화물 취급 가능 여부 |

공용 장치장이 컨테이너를 내리지 않는 트럭 대기장이라면 차량을 계속 점유한다. 이는 별도 운영 모형이므로 위 가정과 섞지 않는다.

~~~mermaid
flowchart LR
    O["수출 컨테이너 운송 의사결정"] -->|"직접 반입"| T["지정 목적 터미널"]
    O -->|"장치장 경유"| B["공용 장치장: 하차·보관"]
    B -->|"출고 결정·상차·셔틀 운송"| T
    T --> V["예정 본선 작업"]
~~~

두 경로 모두 같은 화물을 같은 터미널로 인도한다. 장치장 경유로 줄어드는 터미널 대기와 추가되는 물류 부담을 함께 비교한다.

## 5. 연구 질문과 검증 가설

가설은 결과를 보고 채택하는 설명이 아니라 사전에 검증할 명제다. 강건성은 “어떤 조건에서도 우수함”이 아니라 **미리 정한 조건 범위에서 성능·서비스 기준을 충족함**으로 정의한다.

| 연구 질문 | 가설과 핵심 비교 |
|---|---|
| 공용 장치장 이용 자체가 유효한가? | **H1:** 동일 수요·서비스 조건에서 규칙 기반 장치장 이용이 직접 반입보다 시스템 총비용을 줄인다 |
| 강화학습의 추가 가치가 있는가? | **H2:** 동일 정보·행동·제약을 가진 강한 비학습 정책보다 RL 정책의 시스템 총비용이 낮다 |
| 사전정보가 정책에 도움이 되는가? | **H3:** 현재 관측만 사용하는 RL보다 통지된 차량 도착정보를 추가한 RL이 비용을 줄인다 |
| 효과가 유지되는 조건은 어디까지인가? | **H4:** 사전 지정한 용량·부하·예측오차 범위에서 비용 개선과 서비스 비열등성을 함께 충족한다 |

서비스 비열등성은 반입 마감 준수·처리량·긴 대기 등에서 **미리 정한 허용 악화폭을 넘지 않는 것**을 뜻한다. H1이 지지돼도 H2는 기각될 수 있다. 본선 계획정보의 추가 효과는 H3 이후 별도 단일축 비교로 확인한다.

## 6. 의사결정 모형

정책은 의사결정 시점에 공개된 정보만 관측한다. 초기 입력은 현재 시각, 대상 컨테이너의 목적지·규격·반입 가능시간, 터미널 대기·장치율, 장치장 재고·예약량, 장비·셔틀 가용상태로 제한한다.

행동은 컨테이너의 진행 상태에 따라 구분한다.

- **최초 경로 선택:** 직접 반입 또는 장치장 경유.
- **장치 중 출고 결정:** 계속 장치 또는 지정 터미널로 출고.

출고 가능한 컨테이너가 여러 개면 동일 정책이 공통 후보 집합에서 선택하며, 모든 비교군에 같은 자원 제약을 적용한다. 이산사건 시뮬레이션(discrete-event simulation: 도착·작업완료 등 사건에 따라 상태를 갱신하는 모형)으로 물류 흐름을 재현한다. 세부 관측·행동·정보 경계는 [문제 정식화](docs/research/buffer-yard/01-problem-formulation.md)에 정의한다.

기본 정책에서는 미래정보 입력을 늘리지 않고 최소 구성을 먼저 평가한다. 미래 도착·본선 계획은 한 번에 한 축씩 추가한다. 실제로 나중에 발생한 대기시간·도착시간은 정책 입력에 넣지 않는다.

## 7. 목적함수와 비용 평가

정책은 다음 **원화 환산 시스템 총비용**의 기대값을 최소화한다.

$$
C^\pi =
C_{\mathrm{truck}}+
C_{\mathrm{handling}}+
C_{\mathrm{storage}}+
C_{\mathrm{transport}}+
C_{\mathrm{rehandling}}+
C_{\mathrm{vessel}}+
C_{\mathrm{late}}.
$$

각 항은 순서대로 차량·기사의 구속시간, 생산적 취급, 장치·보관, 시간비용을 제외한 운송비, 재취급, 본선 지연·유휴, 반입 마감 위반의 비용이다. 모든 항은 같은 평가 구간·단위로 산출한다. 단가는 실측 원가·외부 근거·연구 가정으로 구분하며 **실제 회계비용이나 법정 청구액과 동일하다고 주장하지 않는다**.

장치율은 혼잡 진단과 용량 제약에 사용한다. 근거 없는 장치율 제곱 벌점을 대기·취급비와 중복 가산하지 않는다. 장치장 사용률에도 임의의 목표 비율을 부과하지 않는다. 최적 이용률은 시설·운송비·수요에 따라 달라지며 실험으로 판단한다.

보상은 각 의사결정 구간에서 발생한 **증분비용의 음수**로 정의한다. 이미 누적된 총비용을 매번 보상으로 다시 넣지 않는다. 비용의 자원별 경계와 종료시점 미완료 처리 규칙은 [상세 모형](docs/research/buffer-yard/01-problem-formulation.md)에 따른다.

## 8. 비교 실험과 평가 지표

비교군은 직접 반입, 장치율 임계 정책, 대기시간 임계 정책, 예측 기반 비용 비교 정책, RL 정책으로 구성한다. 장치장 진입 규칙뿐 아니라 **출고 규칙까지 정의된 완결된 정책**을 비교한다.

주평가 지표는 동일 물량 기준의 시스템 총비용이다. 다음 지표를 함께 보고해 대기가 다른 장소로 이동했거나 미완료 화물이 숨겨졌는지 확인한다.

- 트럭 총 구속시간, 터미널 내 체류시간, 방문시간의 평균·95백분위수.
- 컨테이너 반입 소요시간, 장치장 체류시간, 마감 위반율.
- 터미널·장치장 장치율, 게이트 대기열, 설정 임계치 초과 지속시간.
- 처리량, 평가 종료시점 미완료 물량, 셔틀 주행거리·빈차 운행, 비용 항별 분해.

95백분위수(P95)는 관측값의 95%가 그 이하인 시간으로, 오래 기다리는 차량의 부담을 보여준다. 평균만 개선되고 긴 대기나 마감 위반이 악화되면 이를 별도 결과로 보고한다.

정상·혼잡·과부하, 특정 터미널 수요 집중, 복수 터미널 동시 피크, 용량 부족, 정보 오차, 장비 장애를 구분한다. 각 조건은 실제 자료 또는 공개 근거로 범위를 정하며, 임의의 장치율·사용률을 정상 운영의 사실로 제시하지 않는다.

## 9. 통계·재현성·주장 기준

같은 외생 수요와 난수 조건에서 정책을 짝지어 비교한다. 독립 표본은 차량 수가 아니라 **독립적으로 생성한 운영 시나리오**이며, 학습 초기화에 따른 변동도 별도로 평가한다. 학습·검증·최종 평가 자료를 분리하고 비교군·표본 수·최소 개선량·서비스 허용폭을 평가 전에 고정한다.

총비용 감소는 불확실성 범위와 함께 제시한다. 유의한 차이가 없다는 사실을 동등성이나 비열등성의 증거로 해석하지 않는다. 예측오차·용량·비용 단가의 민감도 분석에서도 기준 평가의 비용 장부를 유지한다. 상세 통계·도표·판정 절차는 [실험 설계](docs/research/buffer-yard/02-experimental-design.md)에 따른다.

**허용되는 결론의 범위는 자료 수준에 따른다.** 합성 수요와 가정 단가를 사용한 결과는 시뮬레이션 조건에서의 방법론 검증이다. 실제 도로망을 모형화하지 않았다면 도로 혼잡 완화를, 선박 영향을 모형화하지 않았다면 선박 지연 감소를 주장하지 않는다.

## 10. 자료 요구사항과 문서의 위치

실제 적용 검토에는 터미널별 게이트·야드 처리 기록, 시점별 재고, 차량 통지·예약·실현 도착, 선적별 반입 개시·마감, 본선 작업계획, 장치장 취급능력·비용, 셔틀 운행·도로 이동시간 자료가 필요하다.

저장소의 [부산항 터미널 레지스트리](configs/terminals/busan/README.md)에 10개 대상이 등록돼 있다는 사실은 10개 터미널의 운영이 모두 재현됐다는 뜻이 아니다. 3개·10개 터미널은 **규모를 비교하는 제안 조건**이며, 실제 운영사 수나 검증 범위를 확정하는 수치로 쓰지 않는다.

이번 문서 개정은 Dashboard 작업 **YR-225(연구 설계안 개정)**에 해당한다. **YR-226(확장 연구 성립조건 검토)**에는 운영·자료·비용 경계 확인을 조건부 후속으로 등록했다. 현재 연구의 성능·증거 정합·현실성 판정과 착수 규칙을 충족하기 전 새 확장 실험을 시작하는 계획으로 해석하지 않는다. [작업 명세](.claude/docs/dashboard-task-specs/YR-226-buffer-extension-feasibility.md).

연구 기여는 사전정보를 활용한 공동 장치·출고 문제의 정식화와 공정한 효과 분해로 설정한다. 기존 문헌 대비 신규성, 강화학습 우월성, 부산항 실운영 개선율은 해당 검증이 완료된 범위에서만 서술한다.
