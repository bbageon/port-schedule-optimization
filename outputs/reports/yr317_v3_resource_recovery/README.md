# 독립 반복의 병렬 자원 예약 보정

2026-09-19 · YR-317-d(독립 반복 비교). **실행기 복구 준비 기록이며 새 성능 실험 설계가 아니다.**

## 발견한 대기 원인

- 08:13 기준 주요 세 정책 54/60개, 블록 전용 6/20개가 완료됐다.
- 주요 실험은 6개만 실행하고 새 대기 작업은 없었지만, 추가 실행기는 처음의 13개 자리를 계속 예약했다.
- 당시 실제 가용 메모리는 약 41.40GiB였다. 실행하지 않는 7자리에도 21GiB를 예약하면서 추가 실행 허용 수가 0이 됐다.
- 주요 실행과 감독 프로세스는 살아 있었다. 모델 계산 실패나 결과의 좋고 나쁨 때문에 중단한 것이 아니다.
- 12:05 확인에서는 주요 실험 55/60개 완료·5개 실행, 블록 6개 완료·14개 대기였다.

## 원래 절차와 이번 운영 변경

기존 [추가 사전등록](../yr317_v3_block_only/prereg.md)은 주요 실행이 끝날 때까지 13코어를 예약했다.
원래 사전등록·설정·자원 파일의 내용과 지문은 변경하지 않는다. 아래는 완료된 작업의 예약을 해제하기 위한 별도 운영 변경이다.

1. 주요 실행에 대기 작업이 남으면 기존 예약을 유지한다.
2. 주요 대기 작업이 0개로 확인되면 현재 실행 중인 작업의 코어와 메모리만 예약한다.
3. 감독 상태가 오래되었거나 실패하면 새 추가 작업을 시작하지 않는다.
4. CPU 0~19, 작업당 3GiB 예약, 전체 16개 작업 및 기존 메모리 여유 규칙을 유지한다.
5. 기존 외부 작업도 메모리 계산에 포함하며 그 작업과 주요 실험을 중단하거나 바꾸지 않는다.

변경 대상은 동시에 시작할 수 있는 작업 수뿐이다. 입력·시드 순서·모델·비용·정책·30일 길이·통계 비교는 유지한다.
수평·수직 환경의 성능 평가나 새 학습을 자동으로 추가하는 변경이 아니다.

## 완료분을 보존하는 복구 조건

- 원래 추가 실행의 자식 작업이 0개이고 실행 중인 작업이 없음을 확인한다.
- 완료 6개의 요청·가중치·결과 지문·비용 합계와 저장 검사를 재검산한다.
- 실제로 시작하지 않은 14개만 이어서 실행한다. 부분 실행이나 실패 자료가 발견되면 조용히 덮어쓰지 않는다.
- 새 출력 폴더에 검증된 완료분과 원래 실행 기록을 보존하고, 이전·새 감독기와 소스 커밋의 연결을 남긴다.
- 실행 중인 주요 5개는 유지한다. 기존 추가 감독기의 신원을 확인한 뒤 자식이 없는 감독기만 교체한다.
- 새 감독기가 실행된 뒤 코어 중복·메모리·완료분 지문·실제 새 자식 실행을 확인한다.

## 상태

코드 보정과 복구 검증을 준비 중이다. 실제 재개 여부·검사 결과·새 출력 경로는 확인 후 추가한다.

## 수평·수직 작업과의 관계

YR-317-h의 코드 점검·명세 준비는 CPU를 오래 점유하지 않으므로 기존 평가와 병행할 수 있다.
새 구조의 장기 학습·평가는 구조 구현과 검증, 별도 실행 계약 이후에만 배정한다.
현재 자원 복구를 수평·수직 실험 착수나 완료로 보고하지 않는다.

## Code verification

- WSL: 60 tests passed, 0 failures or errors. Evidence: [validation.json](validation.json), [tests.xml](tests.xml).
- Runtime and raw completed-run preflight will execute from a clean frozen checkout before supervisor replacement.
