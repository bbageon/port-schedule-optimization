# v6 조각 6 구현 명세 — 다중블록 조정자 — 블록 vmap × 에폭 scan·트럭 투입·전역 원장·재배정/이연 원자 확정·전역 큐 키

원본: outputs/v6/port_plan_wf_9e7366e6.json (plan.pieces[5])

files_to_port:
- integrated/multiblock.py:118-210 (run 루프), 212-240 (_sync_locks·free_slots), 243-291 (admit_external_job), 294-374 (defer), 377-488 (prepare·validate), 490-650 (commit·rollback·try_*), 669-690 (_namespace_jobs → 색인이라 no-op)
- stage/orders.py:96-188 (V3Announcer), terminal_stream.py:481-507 (_job_from_entry), 767-777·827-831 (ensure_time_ledger·admission_epochs)
- stage/cargo_runtime.py:63-276 (CargoTerminal 전역 시각 루프·컨테이너 정체성 장부), cargo_moves.py:39-103
- integrated/lane.py:74-117, block_congestion.py:156-189

hard_parts:
- 블록 간 순서: MultiBlockTerminal 은 시계 최소·동률 dict 삽입순(192행), CargoTerminal 은 (우선순위, 블록 이름) 최소 순으로 하나씩(cargo_runtime.py:226-231) — 전역 큐 키를 (time, prio, block_idx, seq) 4단으로
- 검토 시각 사이 블록 독립이 병렬화 전제(투입·이송은 review 에서만, 249-250행) — CargoTerminal 의 원격 본선 인계(49-60행)는 블록 간 쓰기라 예외
- 같은 에폭 안 투입은 순차 의존(앞 투입이 뒤 free_slots 에 영향, 806-825행) → 오더 색인 오름차순 scan
- commit 의 스냅샷/복원 원자성(490-620행) → new=f(state); state'=tree_map(where(ok,new,old)) 로 롤백 공짜

array_technique:
블록 축 B 를 맨 앞에(WorldArrays 묶음 (B,…)); 바깥 lax.scan(에폭), 안쪽 vmap(블록) × lax.while_loop('다음 에폭까지'); 투입 = 오더 열 갱신 + masked push; 원장 = OrderArrays 열 자체(owner=block); 장부 포인터 3종은 닫힌 식 적분으로 소멸.

equivalence_test:
layout=terminal_layout().subset(('Y01',)), obs=ObservationContract(0, 7200, 300), build_diurnal(build_h21_profile(), seed=9_900_777, load_4h=20, day_total=20, n_streams=0, drain 1200) — 오더 20·본선 0·크레인 2. v5 = test_world_equivalence.py:38-56 골격(MultiBlockTerminal + ScheduledAnnouncer(lead 600) + ResolverPolicy(SF_SPT)). 비교 ① 블록별 event_log 전열 정확 ② ann.ledger ADMIT/SKIP 순열·n_admitted==20 정확 ③ a_to_o 표본 정렬열 |Δ|≤1e-6 ④ terminal_total·totals 상대 1e-6 ⑤ terminal_area/block_area 상대 1e-9. 2단계: Y01+Y21·40대, t=1800 try_pre_gate_transfer 1건·t=2400 try_defer 1건 강제 → owner/version/transfer_history/entry_deferrals/route_cost_s(220s) 정확. PYTHONHASHSEED=0 고정(vessels.py:176-178 set max).

예상 규모: ~600줄

의존: ['2', '4', '5']

## 조사 지도: multiblock

one_line: 블록 21개짜리 시뮬레이터를 하나의 공용 시계로 번갈아 굴리면서, 트럭 명단을 정해진 시각에 각 블록으로 투입하고, 블록 간 재배정(공간 판매)·진입 시각 이연(시간 판매)을 "전부 되거나 전부 안 되게" 확정하며, 터미널 전체의 게이트인→게이트아웃(A→O) 장부를 쥔다. 부속으로 블록 안 이송차(YT, 야드 트랙터) 대기열, 레인(크레인 주행로) 혼잡 적분, 블록 혼잡 스냅샷이 붙어 있다.
determinism: ■ 난수 — 전부 시나리오 생성(호스트)에서만 소비되고 런타임 루프는 난수를 안 쓴다(예외 하나 아래). terminal_stream.py 의 문자열 시드 CPython random.Random 스트림: h21:stream/adh/exit/eta/mix/resid/assign/tgt(L193, L309, L314-318, L324), h21d:arr(L264)·vblk(L405)·tgt(L715)·assign(L723)·flow/size/exit/resid(L724-728), h21w:vsel/vslot/vtype(L461-465)·tgt(L560)·flow/size/exit/resid(L566-569)·fill(L603)·pool(L628)·hspot(L661). .gauss() 는 Box–Muller 두 값 캐시를 쓰므로 jax.random 으로 재현 불가 → 생성은 CPython 에 두고 결과 배열(arrival_s round(·,3)·block·flow·size·travel_s·exit_travel_s·target)을 넘긴다. 런타임 예외: WIP 모드 adherence_sigma>0 일 때 random.Random(f\"h21w:adh:{job_id}\").gauss (L917-919) — 작업 id 만의 함수이므로 호스트에서 (N,) 사전 계산. trunc_normal(scenario_gen L121-135)은 최대 16회 재추출 후 fallthrough — 호스트 유지. ■ 동시각 사건 타이브레이크 — v5 블록 큐는 (time, _PRIORITY[kind], seq) 3단 (integrated/events.py L46-50, L61-65; 우선순위표 L30-43: JOB_COMPLETED 0 < EQUIPMENT 1 < TRANSFER_ARRIVE·STS_MOVE 2 < BLOCK_ARRIVAL·JOB_RELEASED·VESSEL_RELEASED 3 < VESSEL_START 4 < PLAN_CHANGE 5 < ETA_UPDATED 6 < HORIZON 7). seq 는 블록마다 독립 카운터(L62-63)이고, 투입·이송·이연이 push 하는 BLOCK_ARRIVAL 도 그 블록 카운터를 쓴다 → seq 값은 '그 블록에 push 된 순서' 로 결정되며, 검토 시각 안에서의 투입 순서(명단 오름차순)가 그대로 seq 순서가 된다. 힙 재구성(heapify, L339, L574, L684)은 키를 안 바꾸므로 마스크 제거와 등가. ★gpu/events.py L88 의 time+seq×1e-9 는 float32 눈금(t=10s 에서 9.5e-7, 86,400s 에서 7.8e-3 — numpy.spacing 으로 확인)보다 작아 완전히 소실되고, 종류 우선순위 단이 없다 → 배열판은 t_min 동률 칸 안에서 prio×2^24+seq 의 argmin 으로 3단 키를 재현해야 v5 와 같은 답이 난다. 시각은 f64 또는 정수 ms 로. ■ 블록 전진 순서 — min(movable, key=clock) (L192): Python min 은 동률에서 첫 원소 = dict 삽입 순서 = layout.ids 순(Y01→Y21, L124). 검토 시각 사이에서는 블록끼리 독립이라 결과에 영향이 없고, 영향 있는 곳은 totals 의 블록별 누적(블록별 칸이라 무관)과 마지막 sum(totals.values()) 의 덧셈 순서(Y01→Y21 순으로 더해 float 순서 재현) 뿐. ■ 검토 시각 안 처리 순서 — Announcer 는 by_epoch[slot] 리스트 순서 = 명단 순서 = diurnal_arrivals 의 out.sort() 오름차순(L273) 을 slots 와 zip 한 순서(L730) → 오더 번호 오름차순. 같은 에폭의 앞 투입이 뒤 투입의 free_slots 검사에 영향을 주므로 이 순서는 순차(scan)로 지켜야 한다. 판매 다리는 res.resolve.trades 순서(bridge L223) — 시장 하위 시스템 소관. ■ 검토 시각 자체 — set(round(a,6)) ∪ round(extra,6) 을 sorted (L155-165); 격자는 i×60.0 (L831-832). 엔진은 review_epochs[0] 이 '다음 사건 nt 와 wake wt 이하' 일 때만 그 시각으로 전진해 ReviewEpoch 를 돌려주고(engine L152-160), 그 전에 현재 시각의 사건·wake·결정을 전부 소진한다 — 배열판도 '같은 시각 사건 다 처리 → 결정 없음 확인 → 에폭 반환' 순서를 지켜야 한다. ■ 허용오차 상수 — engine _EPS=1e-9(L35), transfer _EPS=1e-9(L11), multiblock 1e-9(L267, L398, L447)·1e-6(L316, L390, L438), on_grid 1e-6(time_grid L12-15), wip_now 는 gate_in > t+1e-9 / gate_out > t 비대칭(L882-884). 배열판은 같은 상수를 같은 부호로 쓴다. ■ 정렬·집합 순서 의존 — allocate 의 sorted(key=(−잔여, 블록이름)) (L138): 이름 Y01..Y21 = 번호 순. 반출 대상 후보 sorted(set(containers)−used) 뒤 시드 shuffle (L559-560, L714-715): 컨테이너 id 문자열 정렬이 기준 — 호스트 유지. dict 순회는 harvest·check_invariants·wip_now 처럼 쓰기만 하거나 계수만 해서 순서 무관. ■ 시간 장부 — integrate 동시각 'A 먼저' (time_contract L100) 는 적분값에 영향 없음(측도 0). 닫힌 식 Σ max(0, min(hi,O)−max(lo,A)) 로 바꾸면 값은 같고 덧셈 순서만 달라 f64 에서 1e-9 수준 차이. ■ 부동소수점 — v5 는 Python float64; gpu/state.py·events.py 는 float32. 도착시각 round(t,3)(ms) 은 86,400 부근 float32 로 표현 불가(눈금 7.8ms) → 사건 순서가 뒤집힐 수 있다. 등가성 시험은 jax_enable_x64 로 돌리거나 시각을 정수 ms(int32) 로 둔다.
equivalence_test: ■ 입력(작게) — layout = terminal_layout().subset((\"Y01\",)) (yard_layout L146-152; 부분집합도 절대좌표 유지라 Y01 게이트 주행 190초 그대로). obs = ObservationContract(warmup_s=0.0, measure_s=7_200.0, snapshot_s=300.0) (2시간, L54-68 검사 통과). build_diurnal(build_h21_profile(), seed=9_900_777, obs=obs, layout=layout, params=TerminalStreamParams(load_4h=20), day_total=20, n_streams=0, drain_s=1_200.0, background_seed=9_900_777) → 오더 20대(diurnal_arrivals 가 day_s=7200 위에 층화 배치), 본선 0(n_streams=0 이면 vessel_schedule_24h 가 빈 리스트, L403), 크레인은 프로파일 그대로 2대(YC-L·YC-W, profiles L104) — 1대 축소는 replace(prof, cranes=prof.cranes[:1]) 로 가능하나 validate 의 cranes[0] 사용(L485) 외에 엔진 쪽 계약은 다른 하위 시스템이 확인. 블록 2개 판(subset((\"Y01\",\"Y21\")), day_total=40)은 이송 경로용 2단계. ■ v5 실행 — test_world_equivalence.py L38-56 골격 그대로: sims = {b: ensure_time_ledger(TerminalSimulator(prof, scn, check_invariants=True); info_level=PRE_ADVICE)}, mbt = MultiBlockTerminal(sims, extra_review_epochs=admission_epochs(obs)) (121 에폭), ann = ScheduledAnnouncer(built[\"schedule\"], lead_s=600.0, end_s=built[\"sim_end_s\"]=8400), 정책 = ResolverPolicy(ServiceFirstSPTPreference(), \"SF\") 규칙팔(신경망 없음, L49-54). out = mbt.run(exec_policy, review_fn=ann.review). 2단계: review_fn 을 감싸 t=1800 에 try_pre_gate_transfer(아직 미진입인 반입 1건, dst=\"Y21\", travel_s=layout.gate_to_block_s(\"Y21\"), route_delta_s=layout.pre_gate_route_delta_s) 1회, t=2400 에 try_defer_admitted_entry(미진입 1건, delta_s=600) 1회를 강제 실행해 commit·defer 경로를 연다. ■ 배열판 실행 — 같은 built[\"schedule\"] 을 호스트에서 OrderArrays 로 변환(job_id→행 번호 표 보관), 같은 에폭 배열·같은 규칙 정책(배열판 규칙팔이 없으면 v5 정책을 호스트 콜백으로 꽂아 1블록만 굴려도 됨 — 이 시험의 대상은 조정자·투입·장부이지 정책이 아니다). ■ 비교 대상과 허용오차 — (1) 블록별 event_log (time, kind, payload) 전 순열: payload 를 행 번호로 사상한 뒤 **정확 일치**(순서 포함; 타이브레이크 3단 키 검증). (2) ann.ledger 의 ADMIT/SKIP/SKIP_TAIL 순열과 reason, n_admitted==20: **정확 일치**. (3) sorted(mbt.ledger.a_to_o_samples_s(obs.observe_s)) ↔ censored_turn_time_s(orders, 7200) 의 유한값 정렬: f64 면 |Δ|≤1e-6 s, f32 면 ≤1e-2 s. (4) out[\"terminal_total\"]·블록별 totals: 상대 1e-6(f64)/1e-4(f32). (5) 블록별 time_ledger.terminal_area_s·block_area_s·block_tail_area_s ↔ 닫힌 식 적분: 상대 1e-9(f64). (6) 2단계에서 rec.owner/version/transfer_count/transfer_history/entry_deferrals/entry_deferred_s/a_gate_in 와 route_cost_s(=Y21−Y01 주행차 220 s): 정수 정확, 실수 1e-6. (7) transfer_wait_accum_s·cong_area_s 는 본선 0 이라 0 — 본선 1스트림 변형(n_streams=1)에서 상대 1e-6. (8) 사건 큐 overflow==0, WorldArrays.overflow==0 (칸 수 계약). ■ 통과 판정 — (1)(2)(6) 정확 일치가 먼저이고, 여기서 갈리면 (3)~(5) 는 볼 필요 없다(순서가 다르면 수치는 우연히 같을 수 있다).
gpu_hostile:
- [매우 어려움] multiblock.py L179-206 run() 주 루프: while len(_terminal)<len(blocks): min(movable,key=clock) 로 블록 하나 골라 run_until_decision(); 반환값 None/ReviewEpoch/TerminalDecision 을 isinstance 로 분기 → 검토 시각 사이에서는 블록끼리 상호작용이 없다(투입·이송은 검토 시각에서만, L249-250). 따라서 바깥은 lax.scan(에폭 E개), 안쪽은 vmap(블록 B개) 으로 각 블록을 lax.while_loop 로 '다음 에폭까지' 굴린다. 결정 요청은 while_loop 안에서 정책 배치 호출(policy.py choose_batch)로 처리. 반환 타입은 정수 코드 (0=계속,1=에폭,2=결정,3=끝)
- [쉬움] multiblock.py L146-147 now, L187 min(parked): list comprehension + min → jnp.min(jnp.where(~terminal, clock, inf)); parked 동일
- [쉬움] multiblock.py L155-167 _schedule_review_epochs: set 에 round(a,6) 넣고 sorted, 블록마다 [t for t in eps if t<=end] → 호스트에서 1회 계산해 epochs (E,) 고정 배열 + 블록별 유효 개수 (B,) i32. round(·,6) 은 그대로 적용해 v5 와 같은 값이 되게
- [쉬움] multiblock.py L61-64 records dict, L15-16·L669-689 _namespace_jobs: 문자열 job_id "Y05:D-00042" 로 dict 조회·f-string 개명·힙 재구성·_eta_wakes 개명 → 오더 = 정수 행 번호(구성 시 1회 호스트에서 str→int 표 생성). origin_block (N,) i32 가 접두어 역할. 사건 target 은 정수(gpu/events.py 는 이미 그렇다)
- [보통] multiblock.py L243-291 admit_external_job: 6개 raise 검사(L257-274) 뒤 sim.jobs[jid]=job dict 삽입(L278), queue.push(L280), bisect.insort 로 _a_sorted 삽입+포인터 보정(L285-289), ledger.register(L290) → ok = 등록안됨 & 블록유효 & exit_travel유효 & (gate_in≥clock−1e-9) & (arr>clock+1e-9) & (arr≤end) & (반입이면 free_slots>margin) & (반출이면 target 존재). 오더 행은 이미 있으므로 where(ok) 로 gate_in_s/block_in_s/owner/stage 갱신 + push_event(where 로 조건부). _a_sorted 는 닫힌 식 적분으로 대체(아래 항목)
- [보통] time_contract.py L45-48·L89-105 와 그것을 만지는 multiblock L285-289·L351-361·L575-585·L599-610: 정렬 리스트 _a_sorted + 포인터 _a_idx·_n_inside + O 최소힙으로 구간을 조각내며 while 적분 → terminal_area 증가분[lo,hi] = Σ_j [owner_j==b]·max(0, min(hi,O_j)−max(lo,A_j)) (O 미확정=+inf). 상태는 A/O 열뿐 — 삽입·삭제·포인터가 전부 사라진다. 동시각 'A 먼저' 규칙(L100)은 적분값에 영향 없음(측도 0)
- [보통] multiblock.py L294-366 defer_admitted_entry: 힙 리스트 필터+heapify(L337-339), 재push(L340), setattr 루프(L345-348), bisect del/insert+포인터 보정(L351-361) → hit = (kind==BLOCK_ARRIVAL)&(target==j) 마스크로 그 칸을 빈칸(+inf,-1)으로, 이어 push_event. 시각 열은 where(ok, old+delta, old). appointment_gate_time 은 안 건드린다는 규약을 열 분리로 구조화
- [쉬움] multiblock.py L230-240 free_slots: sum(1 for j in sim.jobs.values() if …) O(N) 스캔, len(stk.containers) → pending (B,) = segment_sum((stage==NOTICE)&(flow∈{GATE_IN,DISCHARGE}), owner); used (B,) 는 스택 하위 시스템 카운트. free = phys − used − pending − reserved
- [어려움] multiblock.py L377-406·L408-455 prepare_*, L475-488 validate: 8~9개 순차 raise; validate 의 stacks.find_slot(…) is None (자료 의존 탐색) → 모든 검사를 bool 열로 만들어 ok = &; find_slot 존재 여부는 스택 하위 시스템이 (B, size) bool 'has_slot' 표를 제공해야 한다(의존)
- [보통] multiblock.py L490-511 _precommit, L513-531 _snapshot, L533-554 _restore, L556-620 commit try/except: 객체 스냅샷·복원으로 원자성 흉내; jobs.pop/insert 로 소유권 이동; 힙 재구성; dict 복사(L579,L603); tuple append(L615) → 순수 함수: new_state = f(state); state' = tree_map(where(ok, new, old)). 롤백이 공짜. 소유권 이동 = owner[j]=dst 한 칸 갱신(jobs dict 이동 없음). transfer_history (N,MAX_T=1,3) 에 transfer_count 위치로 .at[].set
- [쉬움] multiblock.py L622-629 _release / L130 _open_txn set: set 멤버십으로 멱등 보장 → prepare+commit 융합으로 제거. 남기려면 (MAX_OPEN,) i32 슬롯 + any(==txn_id)
- [쉬움] multiblock.py L653-666 check_invariants: dict seen 으로 이중 소유·소유자 없음 검사 → owner 열이 단일 진실이라 이중 소유는 구조적으로 불가. 검사 = all((owner==-1)|(0≤owner<B)) 및 live 수 보존
- [쉬움] multiblock.py L66-82 harvest, L212-227 _sync_locks: 블록 장부 → 전역 원장 복사, status 스캔으로 locked 결정 → 장부가 하나(OrderArrays)라 harvest 는 사라진다. locked = stage!=NOTICE 로 파생
- [쉬움] terminal_stream.py L193, L264, L309, L314-318, L324, L405, L461-465, L560-569, L603, L628, L715, L723-728 (문자열 시드 random.Random): random.Random(f"h21d:…:{seed}") 메르센 트위스터를 문자열로 시드, .random()/.gauss()/.shuffle()/.sample() → 시나리오 생성은 호스트(CPython)에 남기고 결과만 배열로 넘긴다(arrival_s, block, flow, size, travel_s, exit_travel_s, target). GPU 결정론은 '입력 배열 동일' 로 확보
- [쉬움] terminal_stream.py L253-274 diurnal_arrivals: 누적분포를 리스트로 만들고 while j+1<len(cum) and cum[j+1]<q 로 역CDF 훑기, out.sort() → 호스트 유지. 옮긴다면 j = searchsorted(cum, q, side='right')−1 이 같은 j (cum[j+1]<q 인 동안 전진 = cum[j]<q≤… 경계에서 strict/non-strict 를 시험으로 고정)
- [쉬움] terminal_stream.py L134-143 allocate: 최대잉여법 — sorted(p, key=(−잔여, 블록이름)) → 호스트 유지. 배열이면 lexsort((block_idx, −rem)); 이름 Y01..Y21 정렬 = 번호 정렬이라 동일
- [쉬움] terminal_stream.py L793-798·L807 by_epoch dict[float,…] 과 round(t,6) 조회: 부동소수점 키 dict → notify_slot (N,) i32 = floor((max(0,arrival−lead))/60) (round(…,6) 후 //period 와 같은 정수). 에폭 e 처리 = 마스크 notify_slot==e
- [보통] terminal_stream.py L806-825 Announcer.review 루프 (stage/orders.py L156-187 동일 골격): 명단 순서대로 admit 시도, 실패는 try/except 로 SKIP → 같은 에폭 안 오더는 오름차순 lax.scan(캐리: 세계 상태) 으로 순차 처리 — 상한 = 한 에폭 최대 명단 수(부하 15,000 이면 60초당 최대 ≈ 40; 상한 64 로 패딩). 에폭 간은 scan 바깥
- [보통] terminal_stream.py L874-886 wip_now, L904-931 deficit while: 블록×레코드 이중 루프 계수; while admitted<deficit and tries<max_tries → inside/pipeline = 마스크 합; 투입은 길이 max_tries(200) 고정 scan 에 (admitted<deficit)&(cursor<P) 마스크
- [쉬움] terminal_stream.py L917-919: 런타임에 random.Random(f"h21w:adh:{job_id}").gauss — 작업별 문자열 시드 → 호스트에서 adh_eps (N,) f64 사전 계산(작업 id 만의 함수라 실행 순서 무관 — 문서 L856-857 이 이미 그렇게 설계)
- [쉬움] terminal_stream.py L481-507 _job_from_entry, L502-506 setattr 루프: dict → Job 객체 생성·setattr → 행이 이미 존재. 투입 = stage/시각 열 갱신뿐
- [쉬움] transfer.py L28-32 _free_index, L38·L50 pending.append/pop(0): for 루프로 첫 빈 차 찾기, 리스트 FIFO → free = busy_until ≤ now+1e-9; has = any(free); i = argmax(free)(첫 참 = v5 첫 인덱스와 같음). pending 은 (P,) 링버퍼 + head/tail
- [쉬움] lane.py L79-84 _adj dict/frozenset, L96-110 occupancy 루프, L92-94 conflict_set: 문자열 레인 id, frozenset 연산, 리스트 누적 후 sum/max → adj (L,L) bool; occ (L,) bool; load = occ + adj@occ; vals = load/(1+deg); mean/max. conflict_set(l) = adj[l] | onehot(l). ids 비면 (0,0)
- [쉬움] block_congestion.py L161-185: sim.jobs.values() 를 4번 순회, status.name 문자열 비교, getattr None 분기, statistics.fmean → ext&waiting 마스크로 Q; W = sum(now−block_in_s)/max(count,1)/ref (block_in 미도착=now→0 기여); L = (Σmax(0,available_at−now) + svc_ref·count(WAITING|RELEASED))/(K·horizon); F = 스택 카운트/용량; D = 마스크 평균(est−appt 유효 칸만). (B,8) 한 번에
- [쉬움] gpu/events.py L82-88 _sort_key (배열판 자체의 결함 — 이 하위 시스템이 큐를 직접 다루므로 여기 기록): time + seq×1e-9 를 float32 로 더해 동시각 타이브레이크 → 2단 argmin: t_min=min(time); cand=(time==t_min); i=argmin(where(cand, prio[kind]*2^24+seq, INF)). 시각은 f64(jax_enable_x64) 또는 정수 밀리초 int32(93,600,000<2^31)로 두어 round(·,3) 도착시각을 정확히 담는다
- [쉬움] multiblock.py L195-198 cost_fn / sim.cost.cut(): 블록별 비용 원장 절단·외부 콜백 → 비용 하위 시스템이 (B,항목) 누적 열을 두면 cut = 차분. 이 하위 시스템은 totals (B,) += 로만 관여
- [쉬움] multiblock.py L133 _parked dict + L185-191 '전원 park' 판정: dict 비었나로 분기 → all_parked = all(terminal | (parked_at<inf)); 배열판에서는 전 블록을 동시에 같은 에폭까지 굴리므로 '전원 park' 가 구조적으로 보장되어 판정 자체가 사라진다

## 조사 지도: stage — 에피소드 구성 (시나리오→세계, 진입점) [key=stage]

one_line: 시드(seed·난수 씨앗) 하나에서 "오늘 어떤 트럭이 언제 어느 블록으로 오고, 어떤 배가 붙고, 야드에 처음 뭐가 쌓여 있나" 를 전부 미리 뽑아(무대 세우기), 그것을 블록별 시뮬레이터 21개·오더 장부·통지 스케줄러로 조립한 뒤 하루를 굴려 비용 Φ(파이·총 원화 비용)를 내는 곳이다. 난수는 여기서만 소비되고, 굴러가는 엔진은 결정론 입력만 먹는다.
determinism: ■ 난수 사용처 (전부 파이썬 random.Random, 문자열 시드 → 메르센 트위스터; 소비 순서가 곧 결과)
① orders.py:62 `Random(f\"v3:lead:{seed}:{load}:{lead_mode}\")` — 명단 순서대로 항목당 rng.random() 1회 (DIST 만; FIXED 는 소비 0, 67행).
② vessels.py:123 `Random(f\"v3:fleet:{seed}\")` — DAY_FLEET 순서(SMALL×2, MEDIUM×1, LARGE×1)로 배마다: random()[출현 판정, prob=1.0 이어도 소비] → random()[물량] → uniform(0, room)[접안 시작].
③ vessels.py:147 `Random(f\"v3:vblk:{seed}\").shuffle(blocks)` — 스트림→블록 순서.
④ vessels.py:214 블록 i 의 배경 시드 = bseed + 1000·(i+1) → scenario_gen.py:291 `Random(seed)`(gaussian=False 라 상단 추출 없음) → _place_containers(226-241: randrange·random 거절 루프) → 306-307 sorted(containers) 후 shuffle → 트럭 1대 루프(324-378): random()[arrival], trunc_normal(rng)[gate_travel, ≤16회 gauss], eta_rng.uniform, adh_rng.gauss(v2 ON), g2b_rng trunc_normal(gate_block ON), exit_rng trunc_normal, random()[gate_out 판정, share=0 이라 항상 반입], random()[규격] → 본선 루프(382-452): 양하는 vdis_rng.random() move 당 1회, 적하는 free_targets.pop(). 전용 스트림: eta:{seed}·vdis:{seed}·adh:{seed}·gtx:{seed}·exit:{seed}·g2b:{seed}·fault:{seed}(315-323, 481). 그 뒤 _background 가 트럭을 제거(terminal_stream.py:167)해도 난수는 이미 소비됨.
⑤ vessels.py:227 `Random(f\"v3:tgt:{bseed}:{b}\").shuffle(cand)`, cand = sorted(set(containers) − 본선 사용분).
⑥ 트럭 명단: terminal_stream.py:264 `Random(f\"h21d:arr:{seed}\")` 층화 분위 → 역CDF(60초 격자 사다리꼴 누적, 선형보간) → out.sort() (273); vessels.py:235 `Random(f\"h21d:assign:{seed}\").shuffle(slots)`; 236-239 flow/size/exit/resid 스트림. 항목당 소비 순서: resid.gauss → flow.random → size.random → exit trunc_normal (243-260).
⑦ episode.py:357 torch.manual_seed(seed) (망 없을 때만) · Seller/Buyer(seed) · make_preference(dispatcher, seed) (206).
⑧ 30일 경로: month.py plan_month 가중 추첨 · month_engine.py:146 `Random(seed).shuffle` · :230 `Random(f\"{size_seed}:size\")` · seed_inventory.py:59 `Random(f'v3:month:{seed}:{key}:size')` · fixed_seed.py:131 `Random(f'v5:fixed-cargo-v2:{seed}:{b}')` randrange + swap-pop.
⇒ 배열판이 이 난수열을 장치에서 재현하는 것은 비현실적. 무대는 호스트에서 v5 코드 그대로 돌려 배열로 내보낸다. 그러면 무대 단계의 동등성은 '같은 파이썬 함수' 라 자동으로 성립하고, 검증할 것은 그 뒤(조립·투입·루프·집계)로 좁혀진다.

■ 동시각 타이브레이크 (배열판이 반드시 재현해야 하는 규칙)
· 블록 안 사건 큐: 힙 키 = (time, _PRIORITY[kind], seq) — 종류 우선순위(JOB_COMPLETED 0 < EQUIPMENT 1 < TRANSFER/STS 2 < BLOCK_ARRIVAL/JOB_RELEASED/VESSEL_RELEASED 3 < VESSEL_START 4 < PLAN_CHANGE 5 < ETA_UPDATED 6 < HORIZON 7), 그다음 발행 순서 (world/integrated/events.py:29-65). ★기준 gpu/events.py:82-88 의 `_sort_key` 는 (time, seq) 뿐이라 **kind 우선순위가 빠져 있다** — 같은 시각에 완료와 도착이 있으면 v5 는 완료 먼저인데 배열판은 넣은 순서가 된다. 키를 (time, priority, seq) 로 바꿔야 한다.
· ★기준 배열 큐의 `time + seq·1e-9` 합성은 float32 에서 **동시각을 전혀 못 가른다** (수치 확인: t=1.0 에서 ulp 1.19e-7, t=86400 에서 7.8e-3; t+5e-9 == t 가 모든 시각에서 True). 지금 시험이 통과하는 것은 push 가 빈 칸 중 가장 앞을 쓰고 argmin 이 가장 앞 색인을 돌려줘 우연히 삽입 순서와 같기 때문이며, 칸이 재활용되면(꺼낸 칸을 나중 사건이 차지) 깨진다. 해법: 시각을 int64 ms 로 두고 key = time_ms·2^20 + priority·2^16 + seq 같은 정수 합성(또는 lexsort).
· t=0 씨앗 순서: 작업은 job_id 문자열 정렬, 본선은 vessel_id 정렬, injected 는 (time, kind, target), 마지막 HORIZON (engine.py:227-243). 배열판은 이 순열을 호스트에서 계산해 저장.
· 투입 순서: 같은 epoch 버킷 안은 명단 순서 = 도착 시각 오름차순(diurnal_arrivals 의 sort) (orders.py:114-118, 156); 30일은 schedule.sort(key=arrival_s) 안정 정렬 (month.py:281).
· 블록 간 순서 (MultiBlockTerminal.run): 시계가 가장 이른 블록, 동률이면 dict 삽입 순서 = layout.ids 순 (Y01…Y21) (multiblock.py:192); 검토 시각 = min(parked) (187).
· 블록 간 순서 (CargoTerminal.run): due 사건을 (사건 priority, 블록 이름) 최소 순으로 하나씩 (cargo_runtime.py:226-231) — seq 를 안 봄; affected 는 sorted(블록 이름) (239); grid 시각엔 전 블록 (237-238).
· 시간 장부 적분: A 와 O 동시각이면 진입 먼저 (time_contract.py:100); _a_sorted 는 bisect_left 삽입 (multiblock.py:285).
· 배분: 최대잉여법 정렬 키 (−잉여, 블록 이름) (terminal_stream.py:138).
· 스트림 초과 시 제거: `max(set(vessel_id), key=max start_s)` (vessels.py:176-178) — ★start_s 가 같으면 **set 순회 순서**(문자열 해시 → PYTHONHASHSEED 에 따라 프로세스마다 다름)에 의존. 24시간 무대는 uniform(0,room) 이라 동률 확률 ≈0 이지만, 관측창이 짧아 room=0 이 되는 소형 시험에서는 모든 배가 0.0 에서 시작해 동률이 확정된다 → 2 블록 이상 소형 시험은 비결정론. 1 블록이면 어차피 전부 제거되어 결과 무관.
· ETD/계획 완료: _retime_vessels 는 sorted(starts) 와 scn.vessels 생성 순서를 zip (terminal_stream.py:419).
· 반올림: arrival_s round 3 (vessels.py:251), notice/reserve round 3 (orders.py:87-88), epoch 키 round 6 (orders.py:117, multiblock.py:163, episode.py:301, 398), on_grid 허용 1e-6 (time_grid.py:14-15), 엔진 _EPS 비교 (engine.py:287-288).
· dict/set 순회 의존: vessel_idle_of 는 sorted 로 고정(episode.py:265-266); namespace_initial_inventory 는 built['scenarios'] 삽입 순서(=layout.ids) (container_contract.py:44-54); free[b] 후보는 sorted 후 shuffle (vessels.py:225-227); YardStacks 는 sorted(containers) (sim/stack.py:18); 유일한 위험은 위 ⑦ plan_streams 의 set max.

■ 수치 정밀도
v5 는 float64. arrival_s 가 1 ms 단위인데 float32 는 t≥16,384 s 에서 이미 1 ms 를 못 담고(86,400 s 근처 7.8 ms), 7,500대/일이면 8 ms 안에 붙는 도착 짝이 하루 수 건 생겨 사건 순서가 뒤집힐 수 있다. 동등성 시험은 jax_enable_x64 또는 int64 ms 시각으로 해야 하고, float32 는 학습 속도용 별도 모드로 둔다.
equivalence_test: ■ 최소 입력 (v5 조각을 직접 조립 — run_episode 는 프로파일·레이아웃을 내부 고정(episode.py:343)이라 못 씀; tests/v6/test_cargo_runtime.py:27-49 make_world 와 같은 조립법)
· layout = terminal_layout().subset((\"Y01\",))  — 블록 1개 (게이트 190 s, 지원범위 [180,420] 안이라 __post_init__ 통과, yard_layout.py:63-67). 1 블록이면 모든 배가 스트림≥2 라 전부 제거되어 본선 0 → 위 set 순회 비결정론 회피.
· profile = dataclasses.replace(build_h21_profile(), cranes=build_h21_profile().cranes[:1]) — 크레인 1대 (YC-L).
· obs = ObservationContract(warmup_s=0.0, measure_s=7200.0, snapshot_s=300.0) — 2시간 (measure % snapshot == 0 통과).
· load = 5, seed = 9_900_306 (비판정 대역), lead_mode=\"FIXED\" (리드 난수 소비 0, 전원 30분), arm=\"NO_REALLOC\", dispatcher=\"SF_SPT\", budget 없음.
· 조립: built = build_stage(load=5, seed, profile, layout, obs, lead_mode=\"FIXED\") → orders, records = orders_from_schedule(built) → mbt = MultiBlockTerminal({b: ensure_time_ledger(_sim_from(s, profile))}, extra_review_epochs=admission_epochs(obs)) → ann = V3Announcer(built[\"schedule\"], end_s=built[\"sim_end_s\"]) → exec_policy, exc = _rule_policy(\"SF_SPT\", seed) → mbt.run(exec_policy, review_fn=lambda m,t: ann.review(m,t)) (시장은 안 열어 bridge 없이; 기록은 tests 처럼 sim.time_ledger 에서 직접 읽음). 초기 적재는 fill_ratio 0.65 → 936상자라 크지만 결정론이고 호스트 빌드라 그대로 둔다(작게 하려면 TerminalStreamParams(load_4h=5, fill_ratio=0.05)).
· 크기 확장 사다리: (1블록·1크레인·5오더·2h) → (1블록·2크레인·50오더·2h) → (2블록·2크레인·200오더·24h, PYTHONHASHSEED=0 고정) → (21블록·300오더·24h = test_world_equivalence 규모) → (21블록·7,500·24h).

■ 배열판 쪽 입력
같은 built 를 호스트 변환기 `to_arrays(built, orders)` 로 ScheduleArrays·ScenarioArrays·OrderArrays 로 만든다(변환은 순수 함수·문자열→색인 표를 같이 돌려줌). 시각은 int64 ms 또는 x64 float. 그 다음 배열 루프를 end_s = obs.observe_s + 7200 까지 굴린다.

■ 비교 대상 (순서대로, 앞이 깨지면 뒤는 보지 않는다)
1) 무대 동등 (변환기 검증): 오더별 (block, flow, arrival_s, lead_s, notice_s, travel_s, exit_travel_s, target 색인) 과 초기 occ 그리드·본선 표가 v5 dict 와 1:1 — 허용오차 0 (정수/ms 정수; float 는 == 1e-9).
2) 투입 동등: epoch 마다 admitted 집합·순서와 skip 사유 == V3Announcer.skips/n_admitted (허용오차 0). 소형 무대에선 n_skipped==0 이어야 한다.
3) 사건열 동등: sim.event_log (engine.py:157, (t, kind, payload) 리스트) 와 배열판이 꺼낸 사건열 (time, kind, target→문자열 역변환) 이 **같은 길이·같은 순서** — 시각은 |Δ| ≤ 1e-6 s(x64) 또는 == (ms 정수). 동시각 묶음 안의 순서가 다르면 타이브레이크(priority·seq) 결함.
4) 기록 동등: 오더별 (gate_in, block_arrival, service_start, job_done, gate_out) — v5 는 sim.time_ledger.records[jid] (TruckTimes) 와 mbt.ledger.records (a/b/c/o); 배열판은 OrderArrays 다섯 칸. 미발생은 None ↔ +inf 로 맞춰 비교, 시각 |Δ| ≤ 1e-6 s.
5) 집계 동등: n_admitted, Σ terminal_area_s, censored turn-time 표본 정렬열(gpu/state.py:131 vs time_contract.py:125-134), rehandle_count, empty_gantry_m — |상대 Δ| ≤ 1e-9 (x64) / 1e-5 (float32 모드).
6) 재현성: 같은 시드로 두 번 → 배열판 결과 바이트 동일 (test_episode_repro 의 배열판).

■ 판정 규약
CPU x64 에서 1)~5) 를 허용오차 안에 통과해야 '같은 세계'. float32/GPU 는 6) 재현성과 5) 의 느슨한 허용(1e-5)만 요구하고, 사건 순서(3)가 뒤집히는 건수를 세어 보고한다(0 이 아니면 학습용으로만 쓰고 판정은 CPU x64 로 다시 잰다 — test_gpu_core.py:159-175 규약과 동일).
gpu_hostile:
- [쉬움] orders.py:35-71 build_stage · vessels.py:104-139 sample_day_vessels · vessels.py:142-184 plan_streams · vessels.py:187-276 build_diurnal_v3 · scenario_gen.py:287-496 generate_terminal_scenario · terminal_stream.py:244-274 diurnal_arrivals: 파이썬 random.Random(문자열 시드) 메르센 트위스터 난수를 항목마다 순차 소비 (rng.random·gauss·uniform·shuffle·randrange), 절단정규 trunc_normal 의 최대 16회 재추출 루프(scenario_gen.py:132-136), 컨테이너 배치의 '꽉 찬 칸이면 다시 뽑기' while 루프(scenario_gen.py:226-242), free_targets.pop() 가변 리스트(vessels.py:255, scenario_gen.py:364,445) → ★무대 세우기는 호스트(CPU 파이썬)에 남긴다 — 결과만 배열(ScheduleArrays·ScenarioArrays)로 내보내는 '한 번 하는 빌드'. 시드→배열 함수는 순수하고 결정론이므로 캐시(npz)해 두면 된다. 장치 위에서 새로 뽑는 것은 v5 동등성 확인이 끝난 뒤 별도 축
- [쉬움] orders.py:62-69 (lead 샘플) · terminal_stream.py:969-986 sample_lead_s: 분위표 LEAD_TIME_DIST 를 for 루프로 훑다가 조기 return (구간 선형보간), 문자열 split 으로 블록 접두 제거, dict 제자리 수정 → jnp.interp(u, q_knots, h_knots)*3600 을 max(0,·) — 표가 8점이라 완전 벡터화. 접두 제거는 색인 사용으로 소멸
- [쉬움] orders.py:74-93 orders_from_schedule · order.py:31-57 · record.py:39-115: dict[str, Order]·dict[str, ExecutionRecord] — 문자열 키 dataclass, ExecutionRecord._stamped set 으로 '한 번만' 검사 후 LifecycleError → gpu/state.py OrderArrays (N,) 시각 5칸(+inf=미발생). 오더 = 색인 i. stamp = where(arr[i]==inf, t, arr[i]) 이고 '재기록' 은 assert 대신 위반 계수기 (N,) int32 로 표시
- [보통] orders.py:114-118 (by_epoch 구성) · orders.py:153-188 V3Announcer.review: dict[float,list].setdefault().append 로 epoch 버킷; review 에서 버킷 순회하며 try/except TransferError 로 건너뛰기; resolve_entry/retarget 파이썬 콜백; container_no 예외 검증 → notice_epoch (N,) int32 미리 계산. epoch k 에서 due = (notice_epoch==k)&~admitted. 유효성 6조건을 (N,) bool 로 한꺼번에: in_end=(arr+travel≤end) · time_ok=(gate_in≥clock)&(arr>clock)&(arr≤end) · cap_ok=where(flow==IN, free_slots[block]>margin, True) · tgt_ok=where(flow==OUT, occ_has[target], True) · dup_ok. skip_reason = 첫 실패 조건 번호. 투입 순서(seq)는 명단 색인 오름차순이므로 lax.fori_loop(N) 로 masked push_event (또는 due 의 cumsum 으로 빈 칸을 한꺼번에 배정)
- [보통] multiblock.py:243-291 admit_external_job · cargo_runtime.py:112-147 (오버라이드): 검사 단계 5~6개 raise TransferError, bisect.insort 로 _a_sorted 삽입 + _a_idx/_n_inside 포인터 보정, sim.jobs[jid]=job dict 삽입, job.actual_* 제자리 수정 → 위 벡터 유효성 + gate_in_s[i]=arr, block_in_planned[i]=arr+travel 설정. _a_sorted/_a_idx 는 아예 필요 없다 — '진입한 트럭 수' = sum(gate_in_s ≤ t). free_slots[b] = 1440 − count(occ[b]≠−1) − count(pending PLANNED IN/DISCHARGE in b) − reserved[b] 를 매 epoch 벡터로
- [어려움] multiblock.py:169-210 MultiBlockTerminal.run · month_engine.py:75-111 MonthTerminal.run · episode.py:348-350, 429: 블록 21개를 각자 시계로 굴리며 min(movable, key=clock) 으로 다음 블록 고르기, _parked dict·_terminal set, run_until_decision 의 isinstance 분기(None/ReviewEpoch/TerminalDecision), guard 상한 2,000,000 → 블록을 vmap 축으로: 블록별 EventArray (B, C) + block_clock (B,). 한 스텝 = 모든 블록의 '다음 사건 시각' 을 한꺼번에 보고, 다음 epoch 이전 사건이 남은 블록만 masked 전진(lax.while_loop cond=any(next_time < epoch)). 검토 시각에는 전 블록이 자연히 같은 t. 하루 무대는 epoch 사이 블록 간 상호작용이 없어(이송은 review 에서만) 이 병렬화가 v5 순서와 같은 결과를 낸다
- [어려움] cargo_runtime.py:195-276 CargoTerminal.run: 전역 시각 루프: candidates 리스트 min, while True 로 due=[(priority,b,s)…] 만들어 min(key=(priority, block_id)) 로 하나씩 처리, affected set·sorted(affected), 블록마다 run_until_decision 반복, 마지막에 물리 재고 등식 검사 RuntimeError → 전역 큐 하나 (C,) 로 합치고 정렬 키 = (time, priority, block_idx, seq) 4단. 사건 처리 후 '영향 블록' 은 bool (B,) 마스크 → vmap 된 블록 전진을 masked 로. 재고 등식은 검사 결과를 스칼라 플래그로
- [보통] cargo_moves.py:10-18 _gate_index · cargo_moves.py:39-103 move_dependent: bisect 후 예외, 힙을 리스트 컴프리헨션으로 걸러 heapify 재구성(90-92), _a_sorted pop/insert, _in_block·kpis._waiting dict 이동, setattr 루프로 필드 이동(81-83), transfer_history 튜플 연장, relocations.append → 사건 취소 = time[where((target==jid)&(kind∈{BLOCK_ARRIVAL,JOB_RELEASED,ETA_UPDATED}))] ← +inf (벡터 마스크, 크기 불변). 소유 블록 owner_block[jid]←dst, version+=1. 장부 소속은 owner_block 로 파생(별도 dict 없음). eta/est 이동은 두 float 칸 덧셈. reloc_log 고정 (R_max,5) + count
- [쉬움] time_contract.py:77-105 TimeLedger.integrate · 112-143 표본: 경계(next_a/next_o)까지 조각 적분하는 while 루프, heapq pop, _in_block dict 순회, None 검사로 continue → 닫힌 식 벡터 적분(위 state 의 TimeLedger 항목). '동시각이면 진입 먼저' (time_contract.py:100) 는 닫힌 식에서는 폭 0 구간이라 결과 동일. 표본은 gpu/state.py:131 censored_turn_time_s 그대로
- [쉬움] container_contract.py:21-35 container_no · 38-61 namespace_initial_inventory · 63-166 audit_container_plan · seed_inventory.py:12-76 · fixed_seed.py:42-167: f'IN_{key}'·f'{b}-{c}' 문자열 정체성, Counter/defaultdict, 예외 검증, sha256 감사, sorted 문자열 정렬, JSON 왕복 → 컨테이너 전역 번호: 초기 재고 M0 = 블록 오프셋 누적(Y01 부터 sorted(container_id) 순), 반입이 낳는 상자 = M0 + 오더 색인, 양하가 낳는 상자 = M0 + N + 스트림 오프셋 + m. cont_source_job (M,) int32 · cont_exit_job (M,) int32 · cont_block (M,) int32. 감사·sha 는 호스트 빌드 단계에 남긴다(배열로 옮길 이유 없음)
- [보통] cargo_runtime.py:63-87 CargoTerminal.__init__ · 92-98 update_order_location · 100-110 resolve_entry · 161-180 check_completion/completed: sources/locations/exit_jobs/ready_times/exit_times/vessel_home dict[str], 'IN_'+jid 문자열 조합, dataclasses.replace 로 오더 교체, RuntimeError 로 계약 위반 검출 → cont_ready_s (M,) float32(+inf) · cont_exit_s (M,) · cont_block (M,) · exit_job_of (M,) int32 · source_job_of (M,) · vessel_home (S,) int32. 반출 오더의 상자 = con_no[i]; ready 검사 = cont_ready_s[con_no[i]] ≤ clock (cargo_not_before 와 같은 자리에서 마스크). 계약 위반은 violation_count 스칼라
- [보통] episode.py:196-217 _rule_policy: gens: dict[id(sim)] 캐시, try/except Exception 으로 실패 시 전원 WAIT 대체 → 결정 유효성 마스크 (K,) bool → where(valid, choice, WAIT); policy_exceptions = sum(~valid). (실제 결정 로직은 dispatch 하위 시스템)
- [쉬움] episode.py:281-310 _CounterTape · 247-278 vessel_idle_of · 225-244 yc_empty_travel_s/rehandles_of: dict[round(t,6)] 스냅, read 에서 keys ≤ t 스캔 후 max, per_ship dict.setdefault(list).append, sorted(blocks.items()) 이중 순회, min(VESSEL_CLASSES, key=…) 대비책 → tape (E+1, ·) 배열을 epoch 색인으로 직접 채움; read(t)=tape[min(t//60, last)]. ship_idle = segment_sum(stream_wait_s, stream_ship, V_max) / ship_sts; yc_s = sum(empty_gantry_m[b]/gantry_speed); reh = sum(rehandle_count[b])
- [쉬움] episode.py:369-403, 413-451 (BranchPool·job_meta·pending·identity_check): 반사실 분기: 결정 전 스냅샷 deepcopy 를 작업자 프로세스에 보내 세계 둘을 굴림, job_meta dict[(round(t,6), doc_key)], pending list → v6 는 이 경로를 없앤다 — policy.py:92 counterfactual_advantage 가 순전파 한 번으로 대체. 배열판 run_episode 에는 budget/pool 인자 자체가 없다
- [쉬움] scenario_gen.py:454 jobs.sort(key=job_id) · engine.py:227 sorted(jobs, key=job_id) · 236 sorted(vessels) · 238 sorted(injected, key=(time,kind,target)): 문자열 정렬이 t=0 씨앗 사건의 push 순서(=seq)를 정한다 ('J-IN-000' < 'J-OUT-000' < 'J-V-DISC-0-00' 식 사전순) → 호스트에서 seed_order (B, J_max) int32 = argsort(job_id 문자열) 를 계산해 저장하고, 장치에서 그 순서로 push. 본선·injected 도 같은 방식(순열 배열)
- [보통] time_grid.py:12-15 on_grid · orders.py:117, 154 · multiblock.py:163-164 round(a,6) · episode.py:301, 398 round(t,6): float 나머지 t % 60 < 1e-6 판정, round(·,6) 을 dict 키로 → 시각을 int64 밀리초(또는 float64)로 들고, 검토 시각은 epoch_idx int32 를 별도 상태로 명시(비교는 정수). on_grid = (t_ms % 60000 == 0)
- [쉬움] scenario.py:24-38 TerminalScenario · engine.py:93-97 reset(deepcopy) · multiblock.py:669-690 _namespace_jobs: dict[str,Container]·list[Job]·list[VesselProcess]·meta dict; 블록마다 deepcopy; job_id 를 '{block}:{id}' 로 개명하며 힙 재구성 → ScenarioArrays(B 축). 개명은 색인이 전역이라 무의미(no-op). deepcopy 는 값 배열이라 불필요
- [쉬움] terminal_stream.py:134-143 allocate · vessels.py:231-235 (블록 배분·shuffle): 최대잉여법: sorted(p, key=(-(n·p−base), b)) 문자열 타이브레이크, slots 리스트 shuffle → 호스트 빌드. 결과 block (N,) int32 만 장치로
- [보통] month.py:266-281 build_month · 287-341 plan_month_vessels · month_engine.py:133-146 free_targets · 149-257 inject_vessel: f'D{d:02d}-' 접두 문자열, schedule.sort(arrival_s) 안정 정렬, 런 중 sim.vessels[key]=…·yard.jobs[jid]=… dict 삽입, random.Random(f'{size_seed}:size') 런 중 난수, TransferError → 30일치 배·연계 작업을 호스트에서 전부 미리 배열로 만들고(vessel_start_s, job release 등), 런 중 '붙이기' 는 활성 마스크를 켜는 것(active[s]=True)으로. 규격 난수는 미리 뽑아 inbound_size (S, moves_max) 로