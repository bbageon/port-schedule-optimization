__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.venv/
outputs/*
data/raw/
data/interim/
data/processed/
!outputs/.gitkeep
# 실험 리포트·산출물은 evidence 로 추적 (board done 링크 대상)
# (주의: `outputs/` 디렉토리 제외는 하위 재포함(!)이 무효 — `outputs/*` 여야 함)
!outputs/reports/
!outputs/reports/**
