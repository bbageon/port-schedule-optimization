# 수직형 단일 시드 보완 실험

2026-09-19. 담당: [기본 배치 검증 작업](../../../../.claude/docs/dashboard-task-specs/YR-317-h-v3-review-basic-layouts.md).

## 이번 결정

사용자는 기존 독립 실험의 적절한 기준을 수직형에 가져와 계산 시간을 줄이고, 두 정책을 병렬 실행하도록 요청했다.
수평형을 새로 구현하고 네 실행을 모두 다시 만드는 대신, 기존 환경 결과 두 개를 재사용하고 수직형 두 개만 추가한다.
이는 기존 공유형과 수직 끝단형에서 각각 시간 조정의 효과를 확인하는 사례다. 정교한 수평·수직 배치의 순수 인과 비교는 아니다.

## 재사용 기준

| 항목 | 고정 내용 | 해석 |
|---|---|---|
| 시드 | 원래 명단 첫 번째 20,000,000 | 이득이 큰 시드를 고르지 않음 |
| 입력 | 합성곡선·물량·초기 재고·보정 선박 계획 | 원본 지문을 기존 평가와 대조 |
| 정책 | 기준정책·시간정책, 기존 모델 고정 | 새 학습 없음 |
| 기간 | 30일 연속+2시간 잔여 처리 | 중간 28일 주 분석·30일 보조 분석 |
| 비용 | 기존 네 비용 항·단가·귀속 방식 | 새 일정 변경 비용 검증은 아님 |
| 관측 | 300초 간격·요청·장부·일별 비용과 상태 | 미완료와 손실도 함께 보존 |

수직형 도로 이동시간은 구조에 맞게 파생한다. 원본 입력이 같다는 것과 실행용 이동시간까지 두 환경이 같다는 것은 다르다.
수직형 두 정책끼리는 원본과 파생 입력이 모두 같아야 한다.

## 기존 전량 완료 검사와 구분

기존 독립 실험은 기록 오류와 종료 잔여를 나눠 판정한다. 수직형 초기 자격 검사는 전량 완료를 다음 정책의 시작 조건으로 두었다.
초기 실패는 그대로 보존하고, 이번 운영 사례의 기록 검사에는 원래 독립 평가의 기준을 적용한다.
요청 누락·물리 연결·비용 장부 오류는 중단 사유다. 남은 작업과 마감 초과는 결과로 저장하며 운영 개선 판정에 함께 사용한다.
단순 비용 감소만으로 전량 완료·안정 운영·일반적 우위를 선언하지 않는다.

## 실행과 해석

- 별도 진단 시드의 두 정책 연결 검사 후 수직형 본 실행 두 개를 CPU 18·19에서 병렬로 실행한다.
- 기존 실험과 모델·입력·감독 프로세스를 유지한다. 새 실행은 별도 고정 소스와 출력 경로를 쓴다.
- 기존 공유형의 같은 시드 두 결과는 완료·감사를 확인한 뒤 재사용한다. 완료 속도에 따라 시드를 바꾸지 않는다.
- 내부 운반시간·양하 정원·크레인 역할·후보 보존 규칙 차이를 공개한다. 각 환경의 기준정책 대비 시간정책 차이를 본다.
- 한 시드의 날짜들은 독립 반복이 아니다. 결과는 구조 변경에 대한 제한적인 적용 사례로 제시한다.

실행 전 상세 조건: [고정 계약](../../../../outputs/reports/yr317_v3_vertical_transfer/prereg.md).
## 실제 시작 확인

- 실행기 검사 46개와 병렬 감독 검사 6개, 총 52개 통과 후 `8c24c80` 소스를 고정했다.
- 짧은 두 정책 실행은 약 213초씩 걸렸고 원자료·입력·모델·대기열·컨테이너 연결 감사를 통과했다.
- 두 정책 모두 트럭120대 완료, 본선 잔여는 기준729건·시간728건이었다. 기준정책 변경0건, 시간정책 시간 변경99건으로 실제 행동 차이도 확인했다.
- 위 검사는 연결과 기록의 확인이다. 남은 본선 작업을 보존했으며 전량 완료나 성능 확증을 통과한 것으로 바꾸지 않는다.
- **9/19 14:28 KST 본 실행 시작**: 같은 시드20,000,000의 30일 기준/시간 두 실행을 CPU18·19에 배정했다. 별도 프로세스가 실제 실행 중임을 확인했다.
- 기존 독립·추가 감독기와 다른 세대 작업의 프로세스가 살아 있음을 확인했다. 새 학습은 없다.
- [실제 시작 증거](../../../../outputs/reports/yr317_v3_vertical_transfer/started.json), [짧은 검사 결과](../../../../outputs/reports/yr317_v3_vertical_transfer/run-8c24c80/smoke/pair-summary.json), [현재 본 실행 상태](../../../../outputs/reports/yr317_v3_vertical_transfer/run-8c24c80/main/progress.json).
- 기존 공유형의 같은 시드 시간정책은 시작 확인 당시 아직 실행 중이었다. 완료된 두 결과의 재사용과 두 환경 결과표 작성은 후속 단계다.

## 9/20 결과 — 두 환경 같은 시드 표

두 실행 모두 9/20 01:08·01:24 KST 정상 종료(exit 0). 기록·연결·일별·트럭/본선 손실 검사 6항목 통과, 입력 지문·모델 지문·짝 계약이 기존 독립 평가 시드 20,000,000과 일치했다.
기존 공유형의 같은 시드 기준/시간 결과는 완료·감사·지문을 검증한 뒤 재사용했다. [결과표](../../../../outputs/reports/yr317_v3_vertical_transfer/two-environment-comparison.md) · [검증 JSON](../../../../outputs/reports/yr317_v3_vertical_transfer/two-environment-comparison.json) · [생성 스크립트](../../../../outputs/reports/yr317_v3_vertical_transfer/build_two_environment_table.py).

비용은 요청일 귀속 최종값(`result.json`의 `days`, `daily-final.jsonl`과 같음)이며 하루 안 관측값(`days.jsonl`)이 아니다. 두 값의 차이는 종료 시점까지 못 나간 트럭의 체류 비용이다.

| 환경 | 기준정책 28일 | 시간정책 28일 | 시간−기준 | 미완료 트럭(기준/시간) | 본선 야드 잔여(기준/시간) |
|---|---|---|---|---|---|
| 기존 공유형 | 151.9억 | 1,255.9억 | **+1,104억 (+727%)** | 94 / 5,847 (미결 0 / 5,701) | 2,465 / 8,414 |
| 수직 끝단형 | 1,340.9억 | 1,330.5억 | **−10.4억 (−0.77%)** | 3,558 / 3,705 (미결 0 / 0) | 1,712 / 1,751 |

해석(시드 하나의 서술이며 통계 추론이 아니다):
- **수직 끝단형**은 기준정책부터 트럭 3,558대가 30일 안에 못 나가는 과부하 환경이다. 그 안에서 시간정책은 비용을 0.77% 줄였지만 미완료 트럭 147대·본선 잔여 39건이 늘었다. 사전등록대로 비용 감소만으로 운영 개선을 채택하지 않는다.
- **기존 공유형 같은 시드**에서는 시간정책이 트럭 5,847대를 못 끝내고 5,701대가 종료 시점에 미결(대상 컨테이너 미연결) 상태라 요청일 귀속 최종 비용이 기준의 8.3배가 됐다. 이 결과는 h의 환경 비교가 아니라 d(독립 반복)와 g(잔여 해석)의 대상이며, 원인 진단은 YR-317-j로 등록했다.
- 두 환경의 원비용(151.9억 vs 1,340.9억)을 배치 방향의 효과로 읽지 않는다. 내부 운반·양하 정원·크레인 역할이 다르며 같은 모델을 재학습 없이 옮긴 단일 사례다.
- 원고 반영은 d의 최종 수치와 함께 §5.4 배치 문단·답변서에 한 번에 넣는다(재컴파일·문서 검사 1회).
