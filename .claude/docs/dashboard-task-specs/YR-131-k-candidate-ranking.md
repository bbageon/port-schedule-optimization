# YR-131 — K-후보 오프라인 순위 학습 (상태당 다수 라벨)
> 상태: ready · 등록 2026-07-30 (YR-130 동결 분기 (a), 권고)
> 근거: [YR-130](../../../outputs/reports/yr130_offline_epochs/report.md) — 상태당 1표본
> 체제의 일반화 상한 r≈0.3~0.4 확정(과적합 즉발), 적합 정점에서도 순위 미형성.

## 목적
"표본 구성이 병목"의 직접 검증: 같은 고정 데이터셋(YR-129, 미선택 라벨 포함 — 신규
rollout 0)에서 **상태당 다수 후보 라벨 + 결정 내 순위 손실**로 오프라인 학습하면
held-out 순위가 사는가.

## 설계 방향 (착수 시 사전동결에서 확정)
- 데이터: YR-129 dataset_s*.pt 전 라벨(실행+미선택, 전원-WAIT 항등 제외), train =
  훈련 대역 / held-out = 신규 대역 (YR-130 과 동일 분리).
- 손실: 결정 내 pairwise 순위 손실(예: margin ranking) — 회귀(MAE)와 병행 보고.
- 판별(안): held-out top-1·결정 내 ρ·P(cw|qw) 가 YR-130 관찰(0.16~0.18/≈0/0.31~0.33)
  대비 사전 지정 폭 이상 개선 + 절대 임계(동결 시 확정). 순위 회복 시에만 에피소드
  실행 단계로 (외부 4차 피드백 검증 순서 준수).
- 주의: "망이 높게 평가한 후보" 선별 편향 없음(라벨은 전 후보 — 이미 생성됨).

## Evidence
등록 시점: YR-129 dataset·YR-130 results. 착수 시 동결 커밋·결과를 여기 갱신.
