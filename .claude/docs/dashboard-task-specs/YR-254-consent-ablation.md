# YR-254 — 상호 동의 절제 (거부권을 없애면 얼마나 나빠지나)

## 왜 이 작업이 필요한가

논문이 새로움으로 내세우는 것은 **제안과 수락을 독립 정책으로 갈라 두고, 양쪽이
동의한 후보만 중앙이 확정한다**는 구조다. 그런데 지금까지 측정한 것은

- 한쪽 동의만으로 확정된 재배치가 **0건** — 즉 *장치가 설계대로 돌아갔다*

뿐이고, **동의를 요구하는 것이 이득인가**는 한 번도 재지 않았다. 작동 확인은
절제 실험이 아니다. 심사자가 가장 먼저 묻는 자리이기도 하다.

## 무엇을 하는가

팔 `RL_NOVETO` — 학습된 두 망은 그대로 두고 **수락 정책의 학습된 거절만** 없앤다.

```python
# actors/buyer.py
self.always_buy = False        # 켜면 값 판단 없이 BUY
...
if slot_capacity_left is not None and slot_capacity_left <= 0:
    return Response(offer=offer, action=REJECT)   # ★용량 = 물리, 강제보다 세다
...
elif self.always_buy:
    action = BUY                                  # ★절제 — 제안만으로 확정
```

용량 검사가 위에 그대로 남아 있으므로 **물리적으로 불가능한 거래는 여전히 거절**된다.
없애는 것은 오직 *값을 따져 거절하던 부분*이라, 이 팔은 제약 위반이 아니라
**"상호 동의 중 학습된 판단"만 덜어낸 대조군**이다.

`stage/episode.py` 는 `RL_SPACE`·`RL_TIME` 과 같은 방식으로 팔을 편다 — 시장 구성은
`RL` 과 동일하게 두고 플래그만 넘긴다.

## 선행 실측 (2026-08-30) — ⚠️ 결론 아님

시드 9,900,970 · 부하 3,500 과 12,500 **이틀** · `ckpt_029` · 반사실 호출 0회 통과.

| 팔 | Φ 합계 | 거래 | 공간 | 시간 |
|---|---|---|---|---|
| 재배치 없음 | 15.47억 | 0 | 0 | 0 |
| RL | 11.74억 | 10,279 | 1,698 | 8,581 |
| **RL_NOVETO** | **11.24억** | 12,657 | 1,822 | 10,835 |

거부권을 없앤 쪽이 **4.27% 쌌고** 거래는 23.1% 늘었다. 날짜별로는 하루가 무승부,
하루가 `RL_NOVETO` 우세였다.

**n=2 다. 결론이 아니다.** 다만 방향이 논문 주장에 **불리**하다는 점이 중요하다 —
그래서 이 수치를 근거로 *동의 구조가 이득이다* 라고 쓰지 않았고, 반대로 지어내지도
않았다. 현재 논문은 **작동 확인까지만** 주장하고, 한계 절에 다음을 명시했다:

> 동의 구조 자체의 기여는 분리되지 않았다 — 한쪽 동의만으로는 아무것도 확정되지
> 않음을 확인했으나 수락 정책의 거부권을 없애면 비용이 얼마나 달라지는지는 재지
> 않았다. 따라서 제안과 수락의 분리는 절제 실험이 아니라 작동 확인으로 뒷받침되는
> 설계 선택으로 남는다.

## 다음 단계

판정 대역 28일에서 `RL` vs `RL_NOVETO` 짝비교. 세 갈래로 해석이 갈린다.

1. **RL 이 유의하게 싸다** → 동의 구조가 이득이다. 논문의 주장이 절제로 뒷받침된다.
2. **차이 없음** → 구조는 비용이 아니라 *실행 가능성·설명 가능성* 으로 정당화된다.
   그렇게 다시 쓴다.
3. **RL_NOVETO 가 싸다**(선행과 같은 방향) → 지금 수락망이 **과하게 거절**하고 있다는
   뜻이다. 수락망의 라벨 품질을 먼저 본다 — 구조를 버릴 근거가 아니라 학습이 덜 됐다는
   신호일 수 있다([[YR-246]] 의 학습 몫 문제와 같은 뿌리).

## 관련

- [[YR-246]] 학습 효과 — 수락망이 덜 배웠다면 두 row 의 원인이 같다
- [[YR-255]] 판정 저장 확대 — 이 팔도 하루치 상세가 함께 남는다

---

## 실행 설계 (2026-09-03) — 위 "다음 단계" 를 두 군데 바꿨다

### 1. 판정 대역이 아니라 **진단 대역**이다

위에서 *"판정 대역 28일"* 이라 적었지만 **9,400,000 은 [[YR-233]] 에서 이미 썼고**,
같은 커밋에서 `eval/guards.py` 의 `USED_BANDS` 에 넣어 재사용을 코드가 막는다.
한 대역은 한 번만 쓴다 — 두 번째 판정은 그 대역에서 고를 여지를 만든다.

절제 실험은 **진단 대역이 제자리다.** 여기서 무엇이 참인지 알아내고, 논문 주장으로
쓸 값이면 그때 **새 판정 대역에서 확증**한다. 순서가 뒤집히면 안 된다.

### 2. 한 무대가 아니라 **세 환경**이다

[[YR-286]] 이 보인 것: **혼잡 빈도가 바뀌면 결론이 바뀐다.** 한 무대만 재면 그 함정에
그대로 빠진다. 그리고 셋을 **동시에** 돌리면 벽시계가 가장 느린 하나(혼잡)와 같으므로
**시간을 더 쓰지 않고 세 배로 안다.**

```
시드     9,900,980            ([[YR-286]] 과 글자 그대로 같다)
부하     한산 2 · 적당 6 · 혼잡 14  (혼잡일/28, 같은 일정)
정책     month-02/ckpt_029.pt · 탐색 0
팔       RL (기준) · RL_NOVETO
기준선   `outputs/v3/env-*/arm_NO_REALLOC.json` 재사용 — 다시 굴리지 않는다
산출     outputs/v3/veto-{quiet,mixed,heavy}/
```

**덤**: `RL` 을 다시 굴리므로 [[YR-286]] 의 `RL` 과 값이 같아야 한다. 다르면 결정성이
깨진 것이고, 그 자체가 잡아야 할 결함이다.

실행: `scripts/v3/run_consent_ablation.sh` · 착수 2026-09-03 · 예상 약 14시간

---

## 결과 (2026-09-03) — 판정: 세 갈래 중 **①. 동의 구조가 이득이다**

n=2 연기시험과 **정반대**다. 84일 짝비교에서 거부권 있는 쪽이 확실히 싸다.

```
              기준(없음)     RL      NOVETO    RL 이 싼 날   부호p    순위p
한산 ( 2일)    63.83억   55.60억   55.72억    19/28      0.087   0.109
적당 ( 6일)   141.25억  115.74억  151.28억★   20/28      0.036   0.005
혼잡 (14일)   296.37억  245.90억  255.73억    21/28      0.013   0.008
──────────────────────────────────────────────────────────────────
합동 84일                                     60/84      0.0001  <0.0001
비용 격차 합계 +45.48억 (거부권 쪽이 그만큼 쌈) · 결정성: RL 세 판 모두 YR-286 과 일치
```

### 무엇이 벌어졌나 — 거부권은 혼잡 문지기다

- NOVETO 는 **30~35% 더 거래한다** (제안이 오면 다 받으니까). 거부권이 제안의
  약 4분의 1을 걸러내고 있었다는 뜻이다.
- **★적당에서 NOVETO 는 재배치 없음보다도 나쁘다**(−7.10%). 낱장을 보면 [[YR-251]] 과
  같은 지문이다 — **월말 초혼잡 25일 하나가 +12.67억으로 터지고** 26·27·28일이
  +7.00·+6.31·+1.64억으로 피를 흘린다. 나머지 24일은 오히려 기준보다 잘했는데
  (합 −15.9억) 한 번의 붕괴가 전부 지운다.
- 한산에서는 격차가 0.12억(0.2%)로 사실상 없다 — **막을 혼잡이 없으면 문지기도
  할 일이 없다.** 혼잡(14일)에서는 매일 수준에서 유의하게 갈린다(21/28, p=0.013).

### 왜 n=2 는 반대 방향이었나

연기시험은 이틀이라 **밀림이 자랄 무대가 없었다.** 수락을 다 받으면 당장은 거래가
늘어 싸 보이고, 목적 블록이 차오르는 대가는 뒤에 온다. 30일 연속 세계가 그 뒤를
보여준 것이다. 하루/이틀 무대의 결과로 구조 판단을 하면 안 된다는 교훈이
[[YR-239]] 의 존재 이유와 정확히 겹친다.

### 논문에 주는 것

- *"제안과 수락을 갈라 두고 양쪽이 동의해야 확정"* 이 **작동 확인을 넘어 절제로
  뒷받침된다.** 다만 이 수치는 진단 대역(9,900,980)이므로, 논문 본문 주장으로
  실으려면 **새 판정 대역에서 확증**해야 한다 → [[YR-296]] 등록.
- 기제가 [[YR-251]] 과 한 문장으로 이어진다: **수락 정책의 거절과 도착시각 조정은
  둘 다 "목적 블록 과밀"을 막는 안전장치**이고, 어느 쪽을 빼도 초혼잡일 하나가
  달 전체를 무너뜨릴 수 있다.

산출물: `outputs/v3/veto-{quiet,mixed,heavy}/` · 분석 `/tmp` 아닌 재현은
`scripts/v3/run_consent_ablation.sh` + 짝비교(judge json / arm json)
