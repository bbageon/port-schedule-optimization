# YR-317-h2 — 구조별 7일 학습·같은 시드 평가 (수평 공유형 vs 수직 끝단형)

- **상태**: done
- 등록: 2026-09-20 사용자 지시 *"7일로 진행해서 동일한 시드 데이터로 수직 학습 따로 수평 학습 따로 해서 동일한 테스트 데이터로 수평 테스트, 수직 테스트 진행"*. parent: YR-317, 리뷰어 2·1-5 대응. 선행: [[YR-317-h]](수평 모델의 수직 전이 사례, 완료).
- 무엇: 같은 학습 시드(7일)를 두 환경에 넣어 시간정책 가중치를 **환경별로 따로** 학습하고, 같은 평가 시드(7일)로 각 모델을 **자기 환경**에서 기준정책과 비교한다.
- 왜: h 전이 사례는 수평에서 배운 모델을 재학습 없이 옮긴 것이라 "수직형에서는 이득이 없다"가 모델 탓인지 환경 탓인지 가를 수 없다. 원래 h 명세의 주비교(구조별 별도 가중치)를 축소 예산(7일·1시드)으로 실행한다.
- [사전등록](../../../outputs/reports/yr317_v3_layout_training/prereg.md) · 설정 `outputs/reports/yr317_v3_layout_training/config.json` (코드 커밋 후 고정).
- 고정: 학습 시드 9,950,000(진단 대역) · 평가 시드 30,300,000(새 대역; 측정 5일이 한가·보통·혼잡을 모두 포함하는 첫 시드) · 초기값 시드 9,950,001 공통 · 라벨 64/일 · ε 0.50→0.05 · 요청 보존+공급 보정 · 시간정책만 · 마지막 날 체크포인트만 평가.
- 코드: 수직 환경 경로의 학습 금지 가드에 명시적 옵트인 `layout_training` 추가(`month_run.py`), 학습 루프·CLI에 후보 폭·공급 계약·환경 인자 추가(`month_loop.py`·`train/__main__.py`), 실행기 3종 `scripts/v3/{layout_experiment_io,run_layout_training,run_layout_evaluation,launch_layout_experiment}.py`. 기본 경로 동작은 불변.
- 검증: `tests/v3/test_vertical_environment.py` 거절 시험 6가지 + 수직 환경 학습 시험(분기 세계 ε 전달·라벨 생성·망 갱신) + 기존 분기 탐색률 시험 재활성 — 17 passed (2026-09-20).
- 한계: 학습 시드 1·평가 시드 1의 서술 사례. 독립 반복·유의성·배치 방향 인과·현장 성능 주장 없음. 두 환경 원비용 차이는 방향 효과가 아니다. 7일 중 측정 5일이라 표본이 얇다.
- 완료 기준: 두 환경 학습 완료 검사 통과(매일 체크포인트·라벨·갱신) → 네 평가 완료·기록 검사·감사 통과 → 환경별 짝 요약 `summary.json` → 결과 문서·Dashboard 기록.
- **3대 게이트 보정 대상: `scenario_validity`** — 현재 미통과한 환경 현실성(만든 상황이 실제 항만처럼 굴러가는가)을 구조별 학습·평가로 직접 보완하는 단일축 작업이다. 성능 판정·시드 선택·기존 평가 조건을 바꾸는 작업이 아니다.

## 9/20 연결 검사 통과·본 실행 시작

- 첫 연결 검사(소스 `31b092e`)에서 학습은 두 환경 모두 정상(라벨 12건·갱신 150회·매일 체크포인트)이었으나 완료 검사가 수평 환경의 빈 환경 지문을 잘못 비교해 실패로 기록됐다. `01dbd2d`에서 지문 계산을 통일하고 설정을 재고정(`245bac3`)했다. 첫 시도 증거는 `run-31b092e/`에 보존한다.
- 고친 소스의 연결 검사(`run-01dbd2d/smoke`): 학습 2개(약 5·11분) → 평가 4개, 기록·연결·감사·지문 검사 통과, 15분 소요. 수치는 3일·60대 입력이라 해석하지 않는다.
- 13:12 KST 본 실행 시작(감독 41957): 학습 수평 CPU {0,2,3,4,6,7,8,9}·수직 {10,12,…,18}, 이후 평가 4개. 기존 독립 반복 3건·v5 유지.

## 9/20 완료 — 결과

- 21:16 KST 학습 2개·평가 4개 모두 완료, 검사 통과. [결과 문서](../../../docs/paper/v3/paper-revision/28-구조별-학습-평가-결과.md) · [짝 요약](../../../outputs/reports/yr317_v3_layout_training/run-01dbd2d/main/summary.json).
- 측정 5일: 수평 공유형 자기 모델 −2.2%(6/7일 승), 수직 끝단형 자기 모델 −4.6%(7/7일 승). 수직은 못 끝낸 트럭 2,505→2,751 증가로 운영 개선 채택 안 함.
- h 전이 사례(수평 모델을 수직에 옮김, −0.77%)와 달리 환경 안에서 배운 모델은 그 환경에서 이긴다. 7일 학습은 30일 학습보다 이득이 작다(수평 −2.2% vs 독립 시드 중앙 약 −13%).
- 학습 시드 1·평가 시드 1의 서술 사례. 반복은 [[YR-317-h3]]. 원고 5.4절·답변서 반영은 d 최종 통합에서 한 번에.
