# YR-306 — 완료 실행의 잔여 작업 원인 진단

- **상태**: in-progress
- 사용자 지시 2026-09-13: "진행해봐" — 잔여 2,977건의 종료 시각·적체·처리 불능 구분.
- 상위: [연속 학습](YR-306-v5-first-continuous-training.md), [직전 재시작](YR-306-wait-restart.md).
- `36e4c3d`의 `gate-at-start.json`으로 신뢰성 직접 보정 착수 허가 `allowed=true`.

## 확인한 완료 기록

- 실행 `outputs/v5/yr306-wait-30d-4549ae9/`, 코드 `4549ae9`, 시드 9900306.
- 30일 + 정리 2시간 종료, 갱신 672회, 트럭 177,500건·본선 288묶음 등록, 누락 0.
- 30일 물리 검사와 최종 정책 파일 검증값 일치. 실제 실행 10,674.32초.
- 반출 48·선적 1,200·양하 1,729, 총 2,977건이 작업 미완료로 남았다.
- 완료 출고 145,715, 실제/계산 재고 13,927개 일치. 완료 상태를 작업 소진으로 해석하지 않는다.

## 진단 계약

1. 입력 파일·실행 원자료·정책 파일은 수정하지 않는다. 이전 실패·시작 기록도 보존한다.
2. 먼저 고정 명단의 작업 해제 및 공급 예정 시각을 종료 시각과 대조한다.
   종료 뒤에야 공급/해제가 가능한 작업은 정책이 빨라도 끝낼 수 없다는 하한 근거다.
3. 예정 시각이 종료 전이라는 이유만으로 처리 가능·정책 실패라고 판정하지 않는다.
   실제 이동·크레인·안벽 처리 및 상자 준비 시각은 계획보다 늦을 수 있다.
4. 본선 마감이 종료 뒤라는 사실만으로 모든 잔여를 정상 처리 중이라고 판정하지 않는다.
5. 날짜별 출문 미확인 집계는 개별 미완료 작업 목록과 다른 자료다. 일치한다고 원인을 확정하지 않는다.
6. 최종 저장물에 작업별 상태가 없으면 미식별 범위를 명시한다. 가중치 파일을 세계 상태로 취급하지 않는다.
7. 추가 실행이 필요하면 관측만 추가하고 정책·시드·명단·학습 설정을 유지하는 별도 진단 계약으로 기록한다.
   코드/명단 고정·CPU 1개·단기 연결 검사를 지킨다. 원래 완료 결과는 덮어쓰지 않는다.
8. 이번 범위는 원인 진단이다. 종료창 연장·정책 변경·강제 작업 처리로 기존 결과를 정상화하지 않는다.

## 수용 기준과 증거

- 원자료의 완료/미완료 수지와 파일 검증값을 대조하고, 종료 경계가 강제하는 최소 잔여를 계산한다.
- 단위 검사로 경계 시각·미래 공급·작업 해제의 중복 집계를 검증한다.
- 확정 가능한 범위와 추가 관측이 필요한 범위를 분리한다. 성능·실측 현실성 주장은 하지 않는다.
- 증거: `outputs/reports/yr306_residual_diagnosis/`.
- 다음은 같은 YR-306의 미식별 잔여 원인 확인이다. 신규 연구 가설은 추가하지 않는다.

## 1차 분석과 추가 재현 계약

- 예정 공급/작업 해제가 종료 시각 이상인 건은 0건. 이 하한만으로 2,977건을 설명하지 못한다.
- 출문 미확인 집계는 29일차 1·30일차 47건이다. 모든 잔여를 장기 교착으로 단정하지 않는다.
- 7개 경계/중복/누락 검사 통과. 원자료에 개별 잔여 작업·원인·물리 세계 저장이 없어 추가 관측이 필요하다.
- 별도 깨끗한 코드에서 동일 명단·동일 초기화·동일 학습을 재현한 뒤 작업별 상태를 읽어 저장한다.
  관측은 원래 실행 함수가 반환한 뒤에만 한다. 작업 선택·정책 입력·종료 시각은 변경하지 않는다.
- 기존 3일 명단에서 먼저 재현한다. 전체 결과(코드 출처 항목 제외)·날짜별 보고·모든 정책 파일이
  원 실행과 정확히 같아야 30일 재현을 시작한다. 30일에도 같은 비교를 적용한다.
- 대상 상자의 준비 여부·위치·운반 대기·작업 상태·크레인/본선/다음 사건을 저장한다.
  기본 처리 가능 여부와 전체 동시 작업 가능성을 구분하고, 순간 상태만으로 정책 실패를 단정하지 않는다.
- CPU 1개 제한, 다른 학습 프로세스 없음 확인. 예상 소요는 직전 실행 기준 약 3시간이다.
