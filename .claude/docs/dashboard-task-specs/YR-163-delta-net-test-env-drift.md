# YR-163 — 1세대 Δ-net 수렴 테스트의 환경(torch 버전) 민감 조사

- **Epic**: Infra / **Priority**: 🟡 / **등록일**: 2026-08-10 / **상태**: backlog
- **1줄**: `test_residual_delta_net.py::test_update_regresses_toward_residual_target`
  가 venv 재생성(torch 재설치) 후 허용오차 실패(−2.22 vs −2.5±0.15) — 대상 코드는
  20+ 커밋 무변경, 골든 궤적 테스트는 전부 통과.

## 왜 지금 고치지 않나 (정직 기록)

- YR-160 골든 회귀(2026-08-10, 820 통과/실패 1)에서 발견. 실패 원인 후보 = 신규
  venv 의 torch 버전/수치 경로 차이로 인한 학습 수렴 표류(코드 기인 아님 추정).
- **허용오차를 결과 보고 넓히는 것은 금지 행위** — 완화가 아니라 원인 규명이 먼저다.

## 할 일

1. torch 버전 고정 재현(구버전 pin)으로 "환경 기인" 가설 확증/기각.
2. 확증 시: 테스트를 시드·스텝 고정 결정론 판정으로 재설계(수렴 임계가 아니라
   갱신 방향·단조성 검사)하거나 torch 버전 pin 을 계약에 명시 — 사전등록로 변경.
3. 기각 시(코드 기인): 회귀 지점 이등분 탐색 — 별도 단일축.

## 참조

- 발견 런: YR-160 골든 회귀 (2026-08-10) · 대상: YR-012 계열(1세대 함수근사)
