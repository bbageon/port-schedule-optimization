# YR-281 — 제출본 12쪽 작성 (21 → 12쪽)

- **Epic**: Paper / **Priority**: 🔴 / **등록·완료일**: 2026-08-30

## 지시

*"바로 submission 으로 가서 최종 제출본을 작성해줘. 단 현재 논문-국문, 논문-영문 은
건드리지 말고. 제출 폴더에서는 논문-국문이 필요 없을 것이다."*

전달받은 20개 압축 항목 + 조정 3건을 반영한다.

## 결과

`docs/paper/v3/submission/` — **영문 단일 파일 12쪽**(참고문헌 포함), 폴더만 떼어 내도
컴파일된다. 작업본(`../latex-en` 21쪽)과 국문판은 **건드리지 않았다**.

| | 작업본 | 제출본 |
|---|---|---|
| 쪽수 | 21 | **12** |
| 그림 | 7 | **3** (아키텍처 · 분해 · 학습곡선 2패널) |
| 표 | 4 | **2** (설정 통합 · 주비교 통합) |
| 관련연구 | 논문별 소개 | 연구흐름 3문단 |
| 결과 | 6개 절 | 3개 절 |

## 조정 3건 (전달받은 계획에서 바꾼 것)

1. **MLP 그림 삭제** — 사용자 원본 그림이지만 계획대로 뺐다. 정보는
   `21→64→64→1` 한 문장으로 남는다. 되살리려면 그림 파일이 그대로 있다.
2. **붓스트랩 한 문장은 남겼다** — 지우면 "6.75억(45.9%)을 더하는데 18/28 (p=0.185)"
   만 남아 무효 결과로 읽힌다. 층화 붓스트랩 20,000회 구간 [−32,−17] 이 0을 제외한다는
   사실이 그 오독을 막는다. 나머지 상세(비층화 구간·99.3%·중앙값 대비)는 삭제.
3. **짝비교 그림은 지우되 예외 이틀 수치는 통합표 캡션에** — −17.1·−0.5백만원 대
   혼잡일 290백만원 초과.

## 내용 정정 둘 (압축과 함께)

- **`First-iteration model`** — "학습 전 모형" 은 틀린 이름이었다. 판정이 쓴
  `--ckpt-early` 는 `ckpt_000.pt` 이고 이 파일은 **1회차 갱신 뒤** 저장된다
  (`train/loop.py:TrainState.save` 가 `month_loop.on_day` 끝에서 호출).
- **상호작용 분해를 본문에 남겼다** — 시각만 1.344 · 블록만 −0.131 · 상호작용 0.258
  = 1.471 십억원. 표 4를 지워도 91.3% 의 근거가 남는다.
- **시간 축 비교의 두 검정** — 8/28(부호 p=0.036, 규칙 우세) vs 부호순위 p=0.399,
  총액은 학습 정책이 10.0억원 낮다. 새로 계산해 넣었다(`paired_tests.py` 규약과 동일).

## 검증

- pdflatex 2회 — **12쪽 · Overfull 0 · Underfull 0 · 오류 0 · 미정의 참조 0**.
- 인용 감사: 문헌 18 · 인용 키 18 · 고아 0 · 항목없음 0 · 참조/라벨 불일치 0.
- 삭제 대상(옛 그림·표 라벨·파일명) 잔여 **0건**.
- 아래 여백 16% 초과 쪽 **0개**, 부동체는 첫 참조로부터 0~2쪽.
- escape 전수검사 이상 없음(작업 중 `\footnotesize` 가 폼피드로 깨진 것을 잡아 고쳤다).
