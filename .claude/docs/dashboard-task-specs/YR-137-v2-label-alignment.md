# YR-137 — v2 비용계약 정렬 보완 + clean 재적합 + 맞춤 대조군 재평가
> 상태: **in-progress (2026-07-31 등록·정렬 수정 착수 — 외부 9차 피드백, 사용자 지시)**
> 근거: [YR-136 하향](YR-136-smooth-cost-contract-v2.md) — 구조 가능성은 실증(ρ 3/3·
> J3 3/3), 비용 라벨의 현실 정합 미완.

## 작업 4묶음 (각각 단일축·순서대로)

### ① 라벨·평가 정렬 (일부 즉시 수정 완료)
- **미래 gate-in 미열람** ✓ 수정: `observed_gate_in` — 실현 A 는 과거(≤now)만, 미래는
  공개 ETA 유도(eta − 평균 입문주행). 실측: t0 PLANNED 80대 전부 사전 기록 확인(누출 실재).
- **예측=softplus / 실현 평가=hard 분리** ✓ 함수 추가: `j_truck_realized`/`j_vessel_realized`
  (hard 초과비용) — 에피소드 평가·라벨 창끝 실현분은 hard, 예측 잔여만 softplus.
- **본선 후보-인과 조건부** (착수 시 동결): 라벨의 본선 항은 후보가 공급을 바꾸는 효과만 —
  paired 반사실(vessel_serves_delta 규약의 v2 판)로 재정의. 공통 드리프트 제외.
- **검열 일치** (착수 시 동결): 라벨 창끝과 평가창 끝의 미완 처리 규칙 명문화·동일화.
### ② clean 재적합
게이트→블록 여행 계약 정합(실측 161.7~258.3s vs 계약 180~420s — **사용자 선택**:
(a) 시나리오를 계약에 정합 = 전 대역 실현 변경 각오 / (b) 계약 문서를 실측으로 정정) 후,
**미열람 시드 + 본선 표본 확대**(≥32척)로 κ·b 재적합·동결.
### ③ v2 맞춤 대조군
같은 수정 라벨로 3망 재학습: 단순 회귀 / 순위 전용 / V/A+순위 보조 — 구조 효과 분리.
### ④ 확장 판정 (결과 열람 전 등록)
동점 허용 top-1(비용差 ≤ ε 동점 인정) · 에피소드 후회 + CI · 실제 총비용·완주·backlog ·
WAIT/REPOSITION 재퇴화 여부. YR-133 은 이 작업 완료 뒤로 연기.

## Evidence
등록: (커밋 예정). ①의 즉시 수정분 테스트 20 통과 유지.

## 진행 기록 (2026-07-31 — 10차 피드백 반영)

- **표현 정정**: 앞선 "결함 2건 수정 완료"는 절반 — 함수 추가만이었고 라벨 배선 미완이었음.
  이번에 **배선 완료**: _j_total_v2 재작성(관측 규칙·실현 hard·적하 한정).
- **양하 과금 오류 발견·수정**(10차 지적): 구판 라벨이 완료 양하선에도 J_V 부과 — LOAD 필터 추가.
- **5단 무효 사유 정정**: 미래 gate-in 누출은 공용 곡선에는 실재했으나 5단 라벨은 PLANNED
  제외라 직접 유입 단정 불가 — 직접 사유 = 실현값 softplus + 양하 과금.
- **(a) 확정(사용자 권고 수용)**: 시뮬레이션을 180~420s 연구계약에 정합. 발견 = 기반 기설
  (gate_block_contract 플래그·appointment_gate_time 필드가 YR-103 에 이미 구현 — 켜기만).
  observed_gate_in 은 공개 예약 1차로 갱신. 기존 결과 = 개발 이력 보존, 새 물리에서 재비교.
- **회귀 테스트 5 추가**(총 25): 미래 gate-in 변조 불변·실현 hard·양하 0·미진입 제외
  (검열 일치)·hard 공식. ②clean 재적합 = 계약 물리·미열람 시드(BASE+2000..)·본선 32척 실행.

## ② clean 재적합 완료 (2026-07-31)

계약 물리·미열람 시드(BASE+2000..)·본선 32척: 트럭 κ 383.7s·b +256.1s (n=2,156) /
본선 κ 919.7s·b +5,213.6s (n=32 — 낙관 편향 구조적 재현). kappa_fit_v2p.json 동결.
남은 작업 = ①-잔여(본선 후보-인과 항 동결·배선 + 테스트) → ③3망 대조군 → ④확장 판정.

## ① 라벨 계약 완결 + ③④ 설계 동결 (2026-07-31, 11차 피드백)

### ① 완결 3건 (구현·테스트 29)
- **κ 명시 배선**: KappaFit.load(KAPPA_V2P_PATH, require_contract_physics=True) — 필드·값·
  물리 검사 fail-fast + src_sha 감사 해시. 구 파일 로딩 시 즉시 실패 테스트 고정.
- **정확한 600초 창**: 엔진 review_epochs 훅으로 scratch 를 정확히 t0+600 에 동결
  (실측 600.0 정확) — 후보별 620/680s 불일치 제거. 이전 대역(dataset~dataset2) 라벨은
  이 결함을 공유(과대창 변동) — 재라벨 필수 근거 추가.
- **본선 paired 항**: 라벨 = ΔJ_truck(트럭만) + candidate_vessel_delta(v2 인자: ρ=10·
  κ=919.7s·margin=b_V) — 무영향 후보 정확 0·선박별 joint 1회·이중계상 제거(트럭 항에서
  본선 제외). 테스트: joint>=개별합·무영향 0·include_vessels 분리.
### ② 해석 하향 (외부 분석치 기록)
- b·κ 는 "계약 물리에서 적합한 매끄러운 비용 점수의 동결 후보"까지만 — **확률 보정·
  일반화는 미검증**. 외부 분석: 트럭 오차 중앙값 ~23s vs 평균 256s(비대칭), 곡선 50%
  지점 실제 비율 ~68%, 본선 셀별 중심 비율 25~87.5%. (원오차 미저장이 한계 — 이후
  적합런은 원오차 배열 저장 개선 예정.) 본선 ~87분 낙관 = 반복 확인이지 구조 상수 확정 아님.
### ③ 4망 요인 대조군 (2×2 + 참고 E — 동결)
| arm | 구조 | 손실 | | A | 단일 점수망 | 회귀 | | B | 단일 | 회귀+순위 | | C | V/A | 회귀 |
| D | V/A | 회귀+순위 | + E 순위 전용(참고). 같은 v3 라벨·같은 하이퍼 — 구조/손실 효과 분리.
### ④ 판정 보완 (동결)
- 절대 적합 = Pearson r + MAE + 보정 기울기[0.5,2] + 절편 (r 단독 금지).
- 동점 허용 top-1 = 선택 후보 실제비용 <= 최저 + ε, **ε=0.02 근거 동결**: 트럭 72초
  대기 = SLA(1800s)의 4%·주행 σ(60s) 미만 = 운영상 무시 가능.
- 후회는 **에피소드별 평균만**(중첩 600s 합산 금지) + 95% CI.
- 판정(D 주 후보): J1 3/3 ρ>=0.30 ∧ top1_eps>=0.35 · J2 r>=0.5 ∧ slope∈[0.5,2] ·
  J3 에피소드 평균 후회 D<A 3/3. B·C 대비 요인 분해는 관찰. 성공 시에만 ⑤ SF-SPT
  짝지은 완주 검정(총비용·완주·backlog·WAIT/REPO 퇴화). 판정 대역 = BASE+1700/1701
  (계약 물리 신규 실현·미열람).

## 적대검증 결과 (2026-07-31 — 워크플로 4 에이전트·3렌즈·재현검증)

**통과 확인**: 본선 단일 경로(이중계상 0)·ρ/margin/κ 이중곱 없음·선박별 joint 1회·
무영향 후보 리터럴 0·양하 이중 차단·margin=+b 부호 검산 일치·κ fail-fast 배선·판정
통계(에피소드 묶음·동점 정의·중첩 합산 없음)·판정 대역 미열람.

**확정 major 1 → 정정**: 2×2 요인에 제3 축 교락 — ranked 플래그가 손실과 함께 체크포인트
선택지표(A/C=Pearson r vs B/D=ρ)·조기종료 기준까지 바꿔 B−A·D−C 를 순위손실 효과로 귀속
불가. **정정 = 전 arm 공통 선택지표(select 대역 에피소드 평균 후회 — J3 과 동일 양)**.

**minor 잔존 1 → 정정**: 완료됐지만 출문이 창 밖인 트럭의 actual_gate_out(미래 exit 추첨)
열람 — 공개 평균 앵커(service_end+300s)로 대체(잔여 σ≈36s < ε=0.02, hard 사용 고지).
미래값 변조 불변 테스트 추가 (총 30 통과).

**한계 고지 (계약 내재 — 위반 아님)**: ①창이 에피소드 끝을 넘으면 end 에서 절단(전 후보
공통 절대시각·CLEAROUT 정합 — prereg "정확 600s" 문구의 예외로 명기) ②미진입 트럭은
라벨서 제외(진입 여부 판정만 미래값 사용·크기 미사용 — 창 직후 도착 트럭へ의 지연 전가가
무벌점인 사각 존재) ③ΔV 는 t0 후보의 서빙만 계상(창 내 base 정책의 후속 본선 서빙 미계상
— 순위 비대칭 가능성). 셋 다 결과 열람 전 고지.
