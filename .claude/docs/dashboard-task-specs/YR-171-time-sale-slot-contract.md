# YR-171 — **24시간 공통창 BUY 견적 + 블록×시간 최적화** (YR-161 대체)

- **Epic**: Exp / **Priority**: 🔴 / **등록일**: 2026-08-13 / **상태**: ready
- **3대 게이트 보정 대상**: 171-A=`scenario_validity`, 171-B/C=`performance` — 정보계약과
  견적 메커니즘을 한 판정에 섞지 않는다.
- **1줄**: source는 작업만 OFFER하고, receiver는 같은 24시간 공개계획에서 수용 전후 비용을
  계산하며, Resolver가 **수신 블록×30분 슬롯** 중 터미널 비용이 가장 낮은 안을 고른다.
- **구현 지위**: **설계 확정·코드 미완**. 현행 고정 `+15분` 좌표와 수기 receiver 견적을
  이 목표가 이미 구현된 것으로 해석하지 않는다.

## 왜 — 실측으로 급소가 특정됐다 (2026-08-13)

축 분리 진단(`yr170_axis_probe`, SF 집행·시드 8,400,000·판정 아님):

| 조건 | Φ | 기준선 대비 | 판매 1건당 |
|---|---|---|---|
| 전건 KEEP | 2,346.2 | — | — |
| 반입 지명(공간 위주) | **2,273.3** | **−72.9 (−3.11%)** | **−2.29분(이득)** |
| **반출 지명(시간축만)** | 2,784.5 | **+438.3 (+18.68%)** | **+14.28분(손해)** |
| 둘 다 | 2,784.0 | +437.8 (+18.66%) | +7.29분 |

**공간 판매는 이득이고 손해는 전부 시간 판매에서 나온다.** 학습 전 정책이
기준선보다 21% 나빴던 것의 정체가 갈렸다.

### 왜 구조적으로 손해인가

비용 계약상 **기사의 터미널 밖 대기가 안 대기와 같은 단가**로 계상된다
(`yr151_transfer_ppo.py:196`). 따라서 +15분 이연의 손익은

```
순이득 = (터미널 안 대기 절약분) − 15분
```

**절약분이 15분을 넘을 때만 이득**이고, 그건 혼잡 첨두를 정확히 비껴갈 때만
생긴다. 그런데 **고정 +15분은 고를 수가 없다** — 15분 뒤가 더 붐비면 그냥 손해다.
실측 +14.28분/건은 이 구조가 그대로 나타난 값이다.

## 설계 (42차 피드백 + 사용자 정정 · 실행 전 동결 필요)

30분은 "보는 범위"가 아니라 **"예약 슬롯 크기"**로 쓴다.

| 구분 | 현재 | 변경 |
|---|---|---|
| 미래 가시 범위 | 30분 | **같은 작업일 24시간(00:00~24:00)** |
| 선택 가능 시간 | 무조건 +15분 | **오늘의 30분 단위 슬롯(48개)** |
| 날짜 변경 | 해당 없음 | **금지** |
| 임박 구간 | 없음 | **잠금**(변경 마감) |

### 역할과 계산 순서

1. **공통 동결 스냅샷**: 모든 source·receiver·Resolver가 같은 기준시각, 같은 작업일
   24시간 공개 예약·ETA·본선계획, 같은 `plan_version`을 쓴다. 미래 실현값은 금지한다.
2. **source TransferHead**: `KEEP` 또는 작업 1건 `OFFER`만 고른다. 목적지와 시간은 고르지
   않는다. source 절감은 `J_source(유지) − J_source(제거)`로 계산한다.
3. **receiver BUY 견적 모드**: 별도 BUY 정책을 학습하지 않는다. 각 허용 슬롯에 작업을
   가상 삽입한 계획 복사본에서 **동결 ExecutionHead**를 실행하고 다음을 반환한다.

   ```text
   BUY부담(B,t) = J_B(t에 수용) − J_B(수용 안 함)
   ```

   `J_B`는 트럭 대기·본선 지연·크레인 이동을 같은 v2 비용단위로 합친 예상값이다.
4. **TransferResolver**: 가능한 `(receiver B, slot t)`를 한 저울에 올린다.

   ```text
   총 추가비용(B,t)
     = BUY부담(B,t) + 추가주행 + 기사 시간변경 + 불확실성 − source 절감
   ```

   가장 작은 안이 `KEEP=0`보다 쌀 때만 선택한다. 전부 0 이상이면 거래하지 않는다.
5. **일괄 matching**: 복수 OFFER는 작업 1회·receiver/슬롯 용량·소유권을 함께 지키며
   매칭한다. 한 건 확정 뒤 사용한 슬롯 용량을 차감하고 남은 후보 비용을 다시 계산한다.
6. **실제 집행**: BUY 견적은 미래 순서를 확정하는 예약이 아니다. 실제 도착 때 ExecutionHead가
   최신 상태에서 실행순서를 다시 고른다. ETA·본선계획·용량 version이 바뀌면 견적은 만료된다.

### 정보 입력 범위

- receiver의 가상 계획에는 예약 트럭 수·공개 ETA·크레인 처리 가능량·본선 작업계획·밀린
  작업·이미 옮겨진 수를 넣는다.
- source actor에는 3,600건 원자료를 직접 넣지 않고, 같은 24시간 스냅샷에서 만든 시간대별
  예상 혼잡곡선·본선여유처럼 압축한 공개 정보만 준다.
- Resolver만 24시간을 보고 블록은 30분만 보는 비대칭, 또는 서로 다른 version 견적을 한
  번의 matching에 섞는 것을 하드 실패로 처리한다.
- 20 receiver×48슬롯 가상 실행의 계산시간을 반드시 계측한다. 결과를 보지 않은 결정론적
  후보축소 없이 임의 pruning하여 최적 슬롯을 누락하지 않는다.

근거: 부산항 체인포털 VBS 가 적용시간 00~24시·슬롯 30분/1시간·예약 변경 가능시간과
마감시간 분리 구조를 쓴다. 하루 단위 가시성·슬롯 용량·임박 잠금은 실제 구조와 정합.

## 반드시 붙일 단서

- **예약 최적화 ≠ 재예약**: 최초 예약 전 추천은 최적화, 확정된 시간 변경은 재예약이며
  **기사 동의가 필요**하다. 현 시뮬레이션은 **전원 수락**을 가정하므로 결과는 실제 운영
  성과가 아니라 **"모두 수락할 때의 최대 가능 효과"**로만 읽는다.
- 이 변경은 **정보 계약을 바꾼다**(30분 전 공개 → 하루 명단 공개) → YR-167 자격이
  무효가 되므로 **재자격이 선행**이다. 상수 하나 교체가 아니다.
- 기존 연구의 입찰 구조는 조직적 근거일 뿐, **ExecutionHead 가상 실행 기반 BUY 견적의
  성능은 우리 실험에서 새로 증명해야 한다**. 이를 기존 연구가 검증한 정책으로 쓰지 않는다.

## 단일축 사다리

1. **171-A 정보계약**: 같은 작업일 24시간 공개 스냅샷·48슬롯·version/만료만 구현하고
   미래정보 누출·시나리오 현실성을 재자격한다. 성능 주장은 금지한다.
2. **171-B BUY 견적 shadow**: 소유권을 바꾸지 않고 동결 ExecutionHead의 수용 전/후 비용,
   결정론·가법성·실현 부담과의 방향성을 검증한다. 기존 수기 견적과 섞어 학습하지 않는다.
3. **171-C Resolver matching**: B를 통과한 견적만 블록×시간 공동선택에 연결한다. KEEP,
   현행 수기 견적, 새 정책기반 견적은 같은 미열람 시드에서 분리 비교한다.

각 단계는 앞 단계 evidence 뒤 별도 동결한다. 24시간 창과 BUY 견적과 matching을 한 번에
바꾼 성능 결과로 원인을 주장하지 않는다.

## 171-A 실행 기록 (2026-08-15)

### 선행 실측 — 왜 재자격 없이 48칸을 열면 안 되는가 (`yr171_horizon_probe`)

통지 선행 = **정확히 1 슬롯**(`ANNOUNCE_LEAD_S` 1,800초 = `SLOT_S` 1,800초).

| 시각 | 미래 칸/블록 | 트럭 보이는 미래칸 | 비율 | 블록당 |
|---:|---:|---:|---:|---:|
| 4시 | 39 | 3 / 819 | **0.37%** | 0~1칸 |
| 8시 | 31 | 3 / 651 | 0.46% | 0~1칸 |
| 11시 | 25 | 20 / 525 | 3.81% | 0~1칸 |
| 15시 | 17 | 20 / 357 | 5.60% | 0~1칸 |
| 20시 | 7 | 3 / 147 | 2.04% | 0~1칸 |

사후 정답: 하루 **48칸 전부**에 트럭이 온다. 즉 정책에게는 다음 1칸만 보이고 나머지는
"0대"로 보인다. 이 상태로 48칸을 열면 **"먼 슬롯일수록 한가하다"**를 배운다 — 실제로
한가한 것이 아니라 아직 통지되지 않은 것인데도. 최적화가 아니라 계측 결함이다.

### 구현 — 공개 예약 장부 (`integrated/day_plan.py`)

**공개하는 것은 예약 장부이지 트럭이 미리 오는 것이 아니다.**

| 공개 | 비공개 |
|---|---|
| 예약 gate-in(재예약 반영)·블록·반입/반출·계약 주행 기대값 | 실현 진입·실현 블록도착·서비스 시작/완료·재취급 수 |

- **엔진을 한 번도 읽지 않는다** — 자기 안의 예약 사본만 본다. 나중에 예약 준수오차를
  도입해도 구조적으로 누출이 불가능하다(실현값 접근 경로 자체가 없음).
- `plan_version` — 재예약 확정마다 상승. `check_fresh` 로 만료 견적은 **하드 실패**.
- 하루 격자를 넘는 재예약은 **거부**(명세: 날짜 변경 금지) → KEEP 과 동일.
- 최초 예약(`appointment`)은 재예약해도 불변 — 기사 외부 대기의 원점이라 바뀌면
  비용이 은닉된다.
- `slot_plan._scan_ledger` 는 장부가 붙어 있으면 특징 1·2를 **장부에서** 가져온다.
  수락 이송(특징 8)은 현재 관측이라 원장 그대로다.
- **명시적 opt-in** — `run_episode_diurnal(day_plan_public=False)` 가 기본값이라
  YR-174 판정이 조용히 바뀌지 않는다.

### 기계 검증 (`tests/test_day_plan_boundary.py` — 11건 전부 통과)

정적 2건(실현값 이름 미참조·엔진 미접근, AST 기준) + 내용 5건(재예약 없으면 불변·
버전 상승·최초 예약 불변·날짜 넘김 거부·미등록 작업 거부) + 만료 1건 + 부착 2건 +
격자 1건. **정보 계약을 바꾸는 변경이므로 사람이 읽어서가 아니라 기계로 확인한다.**

### 계약 전환 확인 (`yr171_contract_check`)

**① 구 계약 회귀 — 전부 동일** (`day_plan_public=False`, 박제 `eval_vs_keep.json` 대조)

| 팔 | 기대 Φ | 실측 Φ | 판정 |
|---|---:|---:|---|
| K 전건 KEEP | 2,475.2478 | 2,475.2478 | 동일 |
| P9_s8400000 | 2,453.0323 | 2,453.0323 | 동일 (n_space 218·n_time 350 도 일치) |

**② 새 계약 가시성 — 목적 달성** (`day_plan_public=True`)

| 시각 | 미래 칸 중 트럭이 보이는 비율 (전 → 후) |
|---:|---|
| 4시 | 0.37% → **76.07%** |
| 8시 | 0.46% → **84.18%** |
| 11시 | 3.81% → **82.48%** |
| 15시 | 5.60% → **74.23%** |
| 20시 | 2.04% → **51.02%** |

100%가 아닌 이유: 한 블록당 하루 약 171대(3,600 ÷ 21)라 **실제로 예약이 하나도 없는
칸이 존재**한다. 20시가 낮은 것은 남은 미래 칸이 7칸뿐이라 배수 구간에 걸리는 몫이
크기 때문이다. **"안 보이는 것"이 아니라 "정말 비어 있는 것"** 이 남았다는 뜻이다.

### 재현성 주의 (같은 실수 반복 방지)

첫 회귀 실행에서 K 팔만 Φ 가 0.0299 어긋났다(P9 는 완전 일치). 원인은 **torch 스레드
수**였다 — 박제값은 자식 프로세스에서 `torch.set_num_threads(1)` 로 돌았고 확인
하네스는 단일 프로세스 기본값(24)으로 돌았다. 집행 헤드 신경망의 부동소수점 축약
순서가 달라져 집행 결정이 갈린 것이다. 스레드 수를 맞추자 **두 팔 모두 완전 일치**.
→ **회귀 대조는 반드시 같은 스레드 설정에서 한다.** 코드 변경이 원인이 아니었다.

## 진단의 한계 (정직 고지)

- SF 집행 기준이다. 채택 PPO 집행 헤드에서는 비용 곡선이 다를 수 있다.
- **시드 1개**다. 방향은 크지만(+18.7% vs −3.1%) 크기는 재확인이 필요하다.
- "공간 위주" 조건에 시간 판매 780건이 섞였다 — resolver 가 반입 작업에도 시간 좌표를
  줄 수 있어 축 분리가 완전하지 않다. **"시간축만"(n_space=0)은 깨끗하다.**

## 하지 않는 것

- 자격 재검증 없이 정보 계약을 바꾸는 것.
- 기사 동의 없이 재예약이 100% 성립한다는 결과를 운영 성과로 보고하는 것.
- 결과를 보고 슬롯 크기·용량 상한을 조정하는 것.
- receiver용 별도 BUY PPO를 동시에 학습하거나 ExecutionHead를 함께 미세조정하는 것.
- 가상으로 만든 24시간 순서를 실제 실행 예약으로 강제하는 것.
