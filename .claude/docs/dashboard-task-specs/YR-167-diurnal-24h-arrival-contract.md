# YR-167 — 5차 부하 계약: 도착률 기반 24시간 이중 피크 환경

- **Epic**: Exp / **Priority**: 🔴 / **등록일**: 2026-08-11 / **상태**: in-progress
- **3대 게이트 보정 대상**: `scenario_validity` 하나 (환경 계약 변경 — 재자격 필요)
- **1줄**: 고정 재공량으로는 비용 벌금 구간에 못 갔다 → **도착 명단 사전 확정 +
  주야 이중 피크 24시간**으로 전환해 피크에서 혼잡, 야간에 한산한 무대를 만든다.

## 근거

[전환 결정](../strategy-history/2026-08-11-부하계약-도착률-24시간-이중피크-전환-사용자결정.md)
· [상수 유도 사전등록](../strategy-history/2026-08-11-24시간-이중피크-상수-유도-사전등록.md)

## 구현 (2026-08-11)

- `diurnal_rate`/`diurnal_arrivals` — 야간 상수 + 이중 정규(11h σ1 / 15h σ2·질량
  1:2)의 역CDF 층화균등 사상. 실측: 3,600대·평균 150/h·야간 22.5/h·순간 최대
  488.7/h·계획 시간대별 최대 461.2대.
- `vessel_schedule_24h` — 30 스트림을 창 전체에 등간격 배치, 시작 순서로 블록
  회전 → 같은 블록의 두 스트림이 21칸(≈13.9h) 떨어져 **겹치지 않음**. 실측:
  블록 21곳 전부 사용·최대 2/블록·양하 15/적하 15·동시 활성 평균 6.2.
- `_retime_vessels` — 본선 시작 이동 시 계획·ETD·물리하한에 더해 **연계 야드 작업의
  release_time 까지** 이동(구판 `_shift_vessels` 는 작업 시각을 안 옮겨 desync 소지).
- `build_diurnal` — 배경(초기 적재·본선) + **트럭 명단 사전 확정**(도착 시각·블록·
  flow·규격·주행·출문). 초기 채움(fill) 개념 없음 — 야간 저부하에서 자연 충전.
- `ScheduledAnnouncer` — 통지 시각(도착−lead)에 명단대로 투입하는 **개방 루프**
  스케줄러. 전역 계수 미참조 → 정책이 무엇을 하든 같은 트럭이 같은 시각에 온다
  (YR-165 가 지적한 투입기 오염 경로가 구조적으로 소멸).
- `OBS_24H` — 워밍업 2h + 측정 22h(스냅샷 300초의 264배).

## 남은 일

1. 자격 검사 갱신: **W1′**(명단 준수)·**W8′**(재고 소진 0)·**W11**(피크 도달
   461.2±10%) — W1/W8 은 폐기(고정 재공량·pool 개념 소멸), W4 는 24시간 교대 기준.
2. 24시간 자격 실행(런타임 셀당 15~30분) → 상태 분류·벌금 구간 도달 실측.
3. 통과 시 이 무대에서 공간 판매 재판정(YR-166 흡수 — 투입 고정이 계약에 내장됨).

## 하지 않는 것

- 자격런 결과를 보고 도착 곡선 파라미터(총량·창·σ·야간 저점)를 조정하는 것.
- 4차(고정 WIP) 결과와 직접 수치 비교하는 것 — 다른 계약이므로 이력 분리.
