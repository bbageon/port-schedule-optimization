# YR-167 — 5차 부하 계약: 도착률 기반 24시간 이중 피크 환경

- **Epic**: Exp / **Priority**: 🔴 / **등록일**: 2026-08-11 / **상태**: done (재자격 통과)
  (1차 자격 통과는 **철회** — 39차 감사. 아래 재자격 결과가 정본)
- **3대 게이트 보정 대상**: `scenario_validity` 하나 (환경 계약 변경 — 재자격 필요)
- **1줄**: 고정 재공량으로는 비용 벌금 구간에 못 갔다 → **도착 명단 사전 확정 +
  주야 이중 피크 24시간**으로 전환해 피크에서 혼잡, 야간에 한산한 무대를 만든다.

## 근거

[전환 결정](../strategy-history/2026-08-11-부하계약-도착률-24시간-이중피크-전환-사용자결정.md)
· [상수 유도 사전등록](../strategy-history/2026-08-11-24시간-이중피크-상수-유도-사전등록.md)

## 환경 구현 (2026-08-11)

- `diurnal_rate`/`diurnal_arrivals` — 야간 상수 + 이중 정규(11h σ1 / 15h σ2·질량
  1:2)의 역CDF 층화균등 사상. 3,600대·평균 150/h·야간 22.5/h·순간 최대 488.7/h.
- `vessel_schedule_24h` — 30 스트림을 창 전체에 등간격 배치, 시작 순서로 블록 회전.
- `_retime_vessels` — 본선 이동 시 **연계 야드 작업 release_time 까지** 이동.
- `build_diurnal` — 배경 + 트럭 명단 사전 확정. 초기 채움(fill) 개념 없음.
- `ScheduledAnnouncer` — 개방 루프(전역 계수 미참조) → 투입기 오염 경로 소멸.
- `OBS_24H` — 워밍업 2h + 측정 22h. **`DIURNAL_DRAIN_S` 배수 2h**(39차 감사):
  엔진이 `블록도착 > sim.end` 투입을 거부하므로 지평이 도착일보다 길어야 하루 끝
  게이트인까지 전건 투입된다. 배수 구간은 측정 대상이 아니다.
- `ensure_time_ledger` — 초기 트럭 0 인 sim 에 v2 장부를 런너에서 활성.
- `_sim_from(scn, profile)` — **시나리오 생성 프로파일을 엔진에 주입**(기본값 유지로
  기존 호출부 바이트 불변).

## 1차 자격 판정 철회 (39차 감사 · 2026-08-11)

`47ef922` 의 "11/11 PASS" 는 **허위 통과**다. 외부 감사 6건을 코드에서 독립 검증한
결과 **6건 전부 CONFIRMED**(1건 PARTIAL). 확인된 결함:

| 검사 | 무엇이 잘못됐나 |
|---|---|
| W1′ | SKIP_TAIL 2건을 판정에서 제외 — 비교 대상이 `admitted` 라 **구조적 항등식**. 계획 3,600 vs 투입 3,598 통과 |
| W6 | 지역변수만 검사. **엔진은 실제로 AGV 보정 프로파일**(SNP-ARMG-STD/AGV)로 실행 — 수치는 동일하나 코드↔대시보드 정합 증명 실패 |
| W5 | `len(blocks)==21` 뿐. `obs.snapshot_times()` 호출 **0회** |
| W8′ | W9 와 **문자 그대로 같은 식**(생성단계 fallback) |
| W4 | 배치 폭만 검사. 동시 활성(실측 1~8)·실현 작업률 미검사 |
| W2 | 같은 사건을 센 두 계수기 대조 — 장부 **면적 항등식** 미검사 |
| W3 | 명단 비교만 — 시뮬레이션 결정론 미보증 |
| W7 | 정보경계가 대수 항등식 + truthy 가드 fail-open + `provided_eta` 미검사 |

결정적 증거(감사): **트럭 3,598대를 받아 한 대도 처리하지 않은 런도 11개 검사를
전부 통과한다**. 따라서 1차 결과·산출물은 무효로 본다.

## 보정 (재자격 대상)

- W1′ **하드**: 계획=투입=등록=3,600 · SKIP=0 · SKIP_TAIL=0 · 시각 전건 일치.
- W2: 중복 0 + A≤B≤S≤C≤O + **완료 사슬 완전성** + **면적 항등식**(오차 ≤1e-6).
- W3: 빌드 결정론 + **런 digest**(장부 시각 + 엔진 사건열 해시)를 같은 시드 반복
  셀과 대조.
- W4: 동시 활성 실측(min≥1·max≤평탄 상한·평균) + **실현 작업률 앵커 [145,170]** +
  본선 완료율 ≥0.99. **평균은 판별력이 없다**(총 본선시간÷창 이라 배치 불변) —
  30척을 한 시각에 몰아도 평균 6.0. 판별항은 min·max 다(테스트로 박제).
- W5: 5분 격자 265시각 × 21블록 = 5,565행 **실제 수집**(재공·대기열·backlog·재고·
  여유칸) + 비퇴화 검사.
- W6: **엔진 실물** 프로파일이 H21-SHARED-YT/YT.
- W7: `is not None` 가드 + `provided_eta` 동시 검사 + 진실누출 실측 +
  **정책의 미래정보 선언** 확인 + 소유권 보존(이름 정정: 구 physical_constraints).
- W8′: 재고 하한 >0 · 여유칸 하한 · 런타임 취소 0 · **재고 보존 항등식**
  (Δ재고 == 완료 반입+양하 − 완료 반출+적하) — 임계값이 아니라 항등식.
- 부수 관측 분모 정정: **도착 코호트**(측정창 안 게이트인 중 완료분)가 정본.
  구판(출문 기준)은 워밍업 도착분이 섞이고 워밍업 내 완료분이 빠졌다 — 병기 보고.
- `tests/integrated/test_yr167_qual_detects.py` — 검사기가 **실패할 수 있는지**를
  고의 결함 주입으로 시험(7건).

## 재자격 결과 (2026-08-11 · clean tree `273ffc5` · 시드 8,100,000~2 + 시드0 반복)

**12/12 PASS** (W1′·W2·W3 빌드·W3 런·W4·W5·W6·W7 6종·W8′·W9·W11·정책예외 0).
산출물 `diurnal_qual.json` sha256 `61793868b905c688…`, `git_dirty=false`.

실측 근거 — 이번엔 전부 측정값이다:
- **계획 3,600 = 투입 3,600 = 등록 3,600 = 완료 3,600**, SKIP 0 · SKIP_TAIL 0 ·
  런타임 취소 0 (배수 구간 덕에 전건 완주).
- 장부 **면적 오차 1e-9초**(항등식 성립). 완료 사슬 결측 0.
- 엔진 실물 프로파일 `H21-SHARED-YT/YT` (3셀 전부).
- **재고 보존 항등식 성립** — 완료 반입 1,423 + 양하 1,739 − 반출 2,128 − 적하 1,732
  = 재고 Δ −698. 사전등록 §D 예측(21블록 × −34 = **−714**)과 2% 오차로 일치 —
  설계 산술이 독립적으로 확인됐다.
- 동시 활성 본선 min 1 / max 8 / **평균 6.01**(설계 c=6), 실현 작업률
  **148.2 moves/h**(앵커 [145,170]), 본선 완료율 1.00.
- 스냅샷 **5,565행 = 265시각 × 21블록**. 블록 재공 최대 26 · 대기열 최대 21 ·
  backlog 최대 96 · 재고 최소 764 · 여유칸 최소 374 · 터미널 재공 최대 281.
- 정보경계: 대상 3,600건 결측 0, **공개 예측 = 실현 도착인 트럭 0건**(진실 미누출),
  정책의 미래정보 선언 없음.
- 런 결정론: 시드 0 반복셀의 digest 일치(장부 시각 + 엔진 사건열).

### 부수 관측 (판정 아님 · 분모 = 측정창 안 게이트인 후 완료, 3,555대)

평균 체류 1,993.6초 · **벌금 구간(2,580초) 도달률 24.0%**(0.2402/0.2456/0.2349).
구판 분모(출문 기준 3,553~3,556)로도 24.0% — 분모 정정이 결론을 바꾸지 않았다.

시간대별 평균 체류 / 터미널 재공: 야간 2~8시 **15.7~20.0분 / 5~11대** →
11시 33.3분 / 220대 → **15시 40.8분(최대) / 276대** → 19시 27.1분 / 34대 →
23시 18.7분 / 7대. 피크에서 혼잡, 야간에 한산, 다음 날로 누적 없음.

## 측정창 (결함 아님 — 보고 정밀도)

실행 24h + 배수 2h, **측정은 [2h, 24h) 22시간**(사전등록 §E 동결). 워밍업 2시간은
하루의 야간 저점이라 계획 도착이 45대(1.25%)뿐이다. 야간 국면은 측정창 안의
2~8시·18~24시에서 관측되므로 손실되지 않는다.

## 하지 않는 것

- 자격런 결과를 보고 도착 곡선 파라미터(총량·창·σ·야간 저점·c)를 조정하는 것.
- 4차(고정 WIP) 결과와 직접 수치 비교하는 것 — 다른 계약이므로 이력 분리.
