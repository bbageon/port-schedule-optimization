# YR-167 — 5차 부하 계약: 도착률 기반 24시간 이중 피크 환경

- **Epic**: Exp / **Priority**: 🔴 / **등록일**: 2026-08-11 / **상태**: done (2026-08-11)
- **3대 게이트 보정 대상**: `scenario_validity` 하나 (환경 계약 변경 — 재자격 필요)
- **1줄**: 고정 재공량으로는 비용 벌금 구간에 못 갔다 → **도착 명단 사전 확정 +
  주야 이중 피크 24시간**으로 전환해 피크에서 혼잡, 야간에 한산한 무대를 만든다.

## 근거

[전환 결정](../strategy-history/2026-08-11-부하계약-도착률-24시간-이중피크-전환-사용자결정.md)
· [상수 유도 사전등록](../strategy-history/2026-08-11-24시간-이중피크-상수-유도-사전등록.md)

## 구현 (2026-08-11)

- `diurnal_rate`/`diurnal_arrivals` — 야간 상수 + 이중 정규(11h σ1 / 15h σ2·질량
  1:2)의 역CDF 층화균등 사상. 실측: 3,600대·평균 150/h·야간 22.5/h·순간 최대
  488.7/h·계획 시간대별 최대 461.2대.
- `vessel_schedule_24h` — 30 스트림을 창 전체에 등간격 배치, 시작 순서로 블록
  회전 → 같은 블록의 두 스트림이 21칸(≈13.9h) 떨어져 **겹치지 않음**. 실측:
  블록 21곳 전부 사용·최대 2/블록·양하 15/적하 15·동시 활성 평균 6.2.
- `_retime_vessels` — 본선 시작 이동 시 계획·ETD·물리하한에 더해 **연계 야드 작업의
  release_time 까지** 이동(구판 `_shift_vessels` 는 작업 시각을 안 옮겨 desync 소지).
- `build_diurnal` — 배경(초기 적재·본선) + **트럭 명단 사전 확정**(도착 시각·블록·
  flow·규격·주행·출문). 초기 채움(fill) 개념 없음 — 야간 저부하에서 자연 충전.
- `ScheduledAnnouncer` — 통지 시각(도착−lead)에 명단대로 투입하는 **개방 루프**
  스케줄러. 전역 계수 미참조 → 정책이 무엇을 하든 같은 트럭이 같은 시각에 온다
  (YR-165 가 지적한 투입기 오염 경로가 구조적으로 소멸).
- `OBS_24H` — 워밍업 2h + 측정 22h(스냅샷 300초의 264배).
- `ensure_time_ledger` — 초기 트럭 0 이면 엔진이 v2 시간 장부를 안 켠다(활성 조건이
  "시나리오에 v2 외부트럭 존재"). 엔진을 고치지 않고 **런너에서** 빈 장부를 붙인다.

## 자격 결과 (2026-08-11 · clean tree `7c478b6` · 시드 8,100,000~8,100,002)

**11/11 PASS** — W1′ 명단 준수 / W2 장부 보존 / W3 결정론 / W4 본선 24시간 분산 /
W5 21블록 스냅샷 / W6 H-21 YT / W7 내부타당성 4종 / W8′ 재고 소진 0 /
W9 flow fallback 0 / W11 피크 도달(관측 460·461·461 vs 계획 461.2) / 정책 예외 0.
투입 3,598/3,600 (2건은 창 꼬리 — 출문이 24h 를 넘겨 계약상 제외).

산출물: `outputs/reports/yr167_diurnal_qual/diurnal_qual.json`
(sha256 `400f643b9adf8be8…`), 셀별 `cell_rep{0,1,2}.json` 해시 동반 박제.

### 부수 관측 (판정 아님)

평균 체류 1,993.5초 · **벌금 구간(2,580초) 도달률 24.0%** (0.2401/0.2456/0.2348).
시간대별 평균 체류: 야간 1~8시 15~20분 → 상승 10시 26.8 → 11시 33.3 →
**14시 40.1·15시 40.8(최대)** → 하강 17시 34.0 → 야간 회복 23시 13.5분.
→ 피크에서 혼잡·야간에 한산이라는 설계 의도가 실현됐고, backlog 가 다음 날로
누적되지 않는다(야간 회복 확인).

## 정정 이력

- **피크 산술**: 사전등록 A7 초안의 429대/h 는 두 봉우리 겹침을 누락한 오류.
  동결 입력(총량·창·σ·야간 저점) 불변으로 산술만 정정 → 488.7대/h (추록 박제).
- **첫 자격런 폐기**: 로그 디렉터리 미생성으로 3셀 즉사 → 드라이버 스크립트로 교체.
- **두 번째 폐기**: v2 장부 미활성으로 3,598건 전건 거부 → `ensure_time_ledger`.
- **세 번째 폐기**: 미커밋 트리(`git_dirty=true`)에서 나온 합산은 증거 배제 —
  커밋 후 재실행했고 수치는 동일(결정론 재확인).

## 하지 않는 것

- 자격런 결과를 보고 도착 곡선 파라미터(총량·창·σ·야간 저점)를 조정하는 것.
- 4차(고정 WIP) 결과와 직접 수치 비교하는 것 — 다른 계약이므로 이력 분리.
