# YR-129 — 차분 실패 판별 진단: D̂ 회귀 적합도 (무학습)
> 상태: backlog · 등록 2026-07-30 (YR-125 2단계 기각 후속)
> 근거: [YR-125 2단계 report](../../../outputs/reports/yr125_diff_credit/report.md) 의
> 미분리 고지 — "표적 결함 vs 회귀 미적합(갱신 1,030회 = 표본 81k 의 0.8회 커버)".

## 목적
차분 기각의 두 해석을 분리한다. DIFF1 체크포인트 3개로 진단 에피소드를 실행하며
결정마다 실측 `D = C600(실행 조합) − C600(WAIT 기준)` 과 망 예측 `D̂` 를 짝지어
적합도를 잰다 (상관·부호 일치율·MAE — 임계는 착수 시 동결).

## 분기 (등록 시 방향만 — 착수 시 동결)
- 적합 **낮음** → 회귀 미적합: 갱신 예산 축(×20)이 다음 수 — 1-step 회귀라 YR-127 의
  전파 병목이 없고, P(cw|qw) 3/3 상승 추세가 학습 가능성의 근거.
- 적합 **높은데** 순위 실패 → 표적·정보 결함: (나) 종결(UNSERVED) 비용 계상 2차 축
  또는 (라) YR-124 상태표현 비교로.

## Evidence
등록 시점: YR-125 2단계 results.json. 착수 시 사전동결 커밋과 결과를 여기 갱신.
