# YR-317-d — 독립 반복과 구성요소 확증

- **상태**: in-progress
- 분류: 잠금평가. 선행: 관련 신뢰성·현실성·성능 게이트 통과 및 실행 전 평가 계약 동결.
- 성능이 미확정이면 게이트 절차에 따라 사전 고정 표본의 성능 직접 보정으로 먼저 등록한다.
- 무엇: 전체·시간만·공간만·거절 제거·수락망 완전 제거·동정보 규칙을 같은 월끼리 비교한다. 학습 효과는 별도 묶음이다.
- 왜: 공간 추가 1.28%포인트, 수락망 차이와 학습 기여가 독립 실행에서 남는지 아직 모른다.
- [정밀 평가 설계](../strategy-history/2026-09-14-v3-review-evaluation-detail.md).
- 고정 모형 20개월 또는 학습 5시드×평가 10개월은 예산용 제안이다. 진단 분산·최소 개선량으로 최종 표본을 동결한다.
- 월 단위 비용 차이·신뢰구간과 시드별 결과를 보고한다. 학습×평가 50칸을 독립 표본으로 세지 않는다.
- 같은 가중치의 행동 제한과 그 제한으로 별도 학습한 최적화 비교를 구별한다.
- 혼잡 빈도·연속성·비용·운영기간을 한 축씩 검증하고 평가창 밖으로 미룬 비용도 추적한다.
- 여러 우위 주장은 주비교·다중비교 규칙을 고정한다. 결과를 보고 시드를 추가하지 않는다.
- 완료 기준: 실패 포함 원자료, 운영 가드, 독립 반복 구간 추정, 구성요소별 지지/미확정/반대 판정.
- 결과가 약하면 주장을 축소한다. 전체 구조의 우위를 입증하기 위해 결과를 선별하지 않는다.
- **결과별 대응 문장은 [전략 §3.1](../../../docs/paper/v3/paper-revision/리뷰-대응전략.md)에 실행 전 확정해 둔다.**
  ⓐ모든 부하에서 전체 우위 · ⓑ낮은 부하에서만 우위 · ⓒ높은 부하에서 열위 — 세 갈래의 초록 문장이 이미 적혀 있다.
  실행 뒤에는 그중 한 갈래를 고르기만 하고 문장을 새로 만들지 않는다. 현재 단일 실행 관측치는 ⓒ에 가깝다.
- 기본 예산안은 독립 20개월×8정책=160개 정책-월. 진단 정밀도·계산량으로 최종 수를 실행 전에 고정한다.
- 기존 ckpt_000을 진짜 초기값으로 쓰지 않는다. 재학습 5회에서 초기/최종을 저장하고 같은 10개월로 비교한다.
- 기존 4비용 항 0.5/1/2배 재합산은 실행 재사용 가능. 새 일정 변경 비용은 사용자 지시로 이번 범위 밖이다.
- 수요 연속성과 90일 운영은 궤적이 달라 별도 실행한다. 단순 사용자 과업 평가는 필수 실험이 아니다.
- YR-317-g의 미투입 문제를 해결하거나 현재 평가의 건전성을 입증하기 전에는 대규모 반복을 시작하지 않는다.

- 9/15 [부하별 기존 결과·독립 평가 전략](../../../docs/paper/v3/paper-revision/01-독립반복-부하별효과.md)을 작성했다. 낮은 부하의 공간 이득과 최고 부하의 시간만 우위를 모두 검산했다.
- 기존 빈도 2/6/14일은 같은 단일 시드, 주실험 4일은 다른 시드다. 행동 비중 30일과 비용 28일의 분모를 개정에서 맞춘다.
- 월 전체 재표집 20,000회·시드별 점·사전 지정 부하별 차이·다중비교 규칙을 실행 전에 고정한다.
- 수평·수직 비교는 h의 구조 자격 이후 구조별 별도 가중치·동일 학습 예산과 20개월×4정책으로 제안한다. 기존 환경 비교의 재사용은 같은 계약임이 입증될 때만 허용한다.
- 기존 결과 재계산은 기술 통계 검산이다. 새로운 독립 평가·새 학습 결과로 보고하지 않는다.
- 9/16 사용자 지시로 d1의 독립 입력 구성 완료: 기존 합성곡선·물량 확률을 유지한 20개월·600일·4,038,000건. [고정 명단·검증](../../../docs/paper/v3/paper-revision/14-독립시드-합성곡선.md).
- 실제 평가 전 같은 월의 입력 지문 일치와 시드마다 새 엔진 생성을 확인한다. 평가 수를 줄일 경우 성능 관측 전 고정 명단 앞 N개로 정하고 결과에 따른 교체는 금지한다.
- 9/16 사용자 순서 재확정: g의 공급·반출 연결 보정 뒤 이 독립 반복을 먼저 진행한다. c의 순위 검사 완료를 추가 선행으로 요구하지 않는다.
- 첫 실행 묶음은 고정 20개월×기준/전체/시간만의 3정책을 우선한다. 나머지 구성요소 비교와 구분하며 전체 8정책 실험 완료로 보고하지 않는다.
- g의 `COUNT_BALANCED`는 선박 방향을 바꾸는 별도 입력 변형이다. 채택 시 원본과 보정본의 지문·변경 명단을 함께 고정하고 같은 월의 모든 정책에 동일하게 적용한다.
- 9/16 사용자 요청: d2의 [일별 관측 명세](../../../docs/paper/v3/paper-revision/18-독립반복-일별관측.md)를 모든 정책에 적용한다. 시작 잔여·실제/블록별 대기열·야드 작업 잔량·턴타임·공간/시간 비율·비용을 보존한다.
- 독립 실행 계약에는 `capture_daily=True`, `daily_sample_s=300`과 시드/정책/날짜 키를 고정하고 상태 시계열·확정 일별 파일의 지문을 확인한다. 과거 계측 없는 실행을 완료 자료로 대체하지 않는다.
- 9/16 사용자 실행 지시: 현재 공급 검증 종료 후 기존 가중치를 고정한 20시드×3정책 반복까지 이어서 진행한다. 새 학습은 선행하지 않는다.
- 3대 게이트 보정 대상: `scenario_validity` 직접 보정. 같은 입력의 독립 실행에서 처리량·잔여·물리 연결을 재검산한다. 성능 추정은 같은 실행의 결과이며 별도 새 가설을 추가하지 않는다.
- 기록·물리 연결·입력 보존 오류는 다음 실행을 막는다. 관측된 정책 열위나 종료 잔여 자체로 시드를 버리거나 바꾸지 않으며, 잔여는 비용과 함께 공개하고 운영 개선의 확증과 구분한다.

- [고정 실행 계약](../../../docs/paper/v3/paper-revision/20-독립반복-실행계약.md)과 후속 실행기를 준비했다. 진단 연결 검사 뒤 g 종료·원자료 대조를 기다리며, 본 평가 시작은 별도 진행 파일로 확인한다.
- `961d76a`의 세 정책 연결 검사는 입력·가중치·장부를 통과했으나 기준정책 선박 야드 5건 잔여를 감독기가 오류로 취급해 중단했다. 사전 계약의 기록 유효성과 작업 완료를 분리하도록 보정하고 같은 진단 입력으로 재검사한다. 독립 시드 사용 0개.
- `4068b4f` 고정 소스의 대기열을 23:24 KST에 등록했다(PID 21085). 세 정책 2일 연결·원자료 검사가 통과했고, 앞 실행과 날짜·요청·컨테이너·상태 파일 재생 동일성 18개도 통과했다. 기준정책 선박 잔여 5건은 그대로 보존했다.
- 9/16 등록 당시 `waiting_for_supply`였으며 g의 세 조건 종료·원자료 검사 후 본 평가를 시작하도록 설정했다. [당시 등록 증거](../../../outputs/reports/yr317_v3_independent_eval/started.json) · [재생 검산](../../../outputs/reports/yr317_v3_independent_eval/probe-replay-verification.json). 이후 실제 시작·중단·복구는 아래 9/17 기록을 따른다.

## 9/17 독립 반복 시작·검사기 오류와 복구

- 03:11 KST 본 실행 13개 시작, 07:25 첫 월 완료 뒤 같은 시각의 서비스 시작을 표본 이전으로 잘못 센 사후 검사 오류가 발생했다. 감독기가 다른 12개도 종료했다.
- 3대 게이트 보정 대상: `reliability` 직접 보정. 정책·시드·엔진·비용은 유지하고 기록 검사 및 실패 격리만 고친다. 새 연구 가설·학습·성능별 표본 선택 없음.
- 기록 시점대로 서비스 시작 `< t`를 적용한 30일 8,641표본 재검산 통과, 전체 실행 계약 동일성 통과. 완료된 1개는 검증 후 재사용하고 중단 12개+미시작 47개는 같은 입력으로 월 처음부터 실행한다.
- 단일 실패 때 새 실행만 중지하고 나머지 활성 실행은 끝까지 저장하도록 수정했다. 관련 검사 27개 통과. [복구 설명·증거](../../../docs/paper/v3/paper-revision/21-독립반복-검사오류와복구.md).
- `7e2fb14` 고정 소스에서 연결 검사·공급 자료 재검산·완료 월 복구를 통과하고 07:41 KST 본 평가를 재개했다. 완료 1개 보존·13개 실행·46개 대기이며 새 학습은 없다. [재개 증거](../../../outputs/reports/yr317_v3_independent_eval/restarted-20260917.json). 전체 60개 종료 후 월 단위 비교를 집계한다.
