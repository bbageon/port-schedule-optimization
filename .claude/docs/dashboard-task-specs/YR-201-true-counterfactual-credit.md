# YR-201 — 진짜 반사실 보상으로 교체 (이미 만들어놓고 안 쓰는 것)

- **Epic**: RL / **Priority**: 🟠 / **등록일**: 2026-08-19 / **상태**: ⏸ PENDING
- **아키텍처 분류**: v2 동결 비용표 보정 — v3 정책 본선의 선행조건이 아니며
  [[YR-204]] 판정 뒤 재검토
- **선행**: [[YR-200]] 판정 완료
- **1줄**: `sell_credit.py` 에 **진짜 반사실**이 구현돼 있는데 **아무도 import
  하지 않는다.** 학습은 더 거친 분해식을 쓴다.

## 지금 / 바꾼 뒤

| | 무엇 |
|---|---|
| 지금 (`yr174_txn_reward.realized_credit`) | `D_i = R_src − B_dst − ΔC_route` — 관측 가능한 항으로 **분해**한 근사 |
| 바꾼 뒤 (`integrated/sell_credit.py`) | `D_i = C(offer i 빼고 다시 매칭) − C(전체 매칭)` — **진짜 반사실** |

핵심 차이: **"i 가 없으면 그 자리를 다른 offer 가 대신 쓴다."** 지금 식은 그걸
반영하지 않아 **자기 공로를 과대평가**한다("내가 아니어도 누군가 챙겼을 이득"까지
자기 몫).

## 왜 정당한 변경인가

**"이겼어도 이 변경을 했을까?" → 네.** 더 정확한 정의이고, **이미 만들어놨는데
연결만 안 한 것**이다. 결과를 보고 만든 설계가 아니다.
([보상 재검토 기록](../strategy-history/2026-08-19-보상설계-재검토-매수매도-시장.md))

## 위험

- `sell_credit` 은 **동결 비용표** 위에서 반사실을 푼다(순수 함수 계약). 배정기
  본체는 배정마다 가상 대기열을 갱신하므로 **그만큼 근사**다 — 모듈 docstring 이
  명시한 한계.
- 보상이 바뀌면 [[YR-170]] 이후 판정과 **비교가 끊긴다.** 새 대조군이 필요하다.
- 계산 비용: offer 수 × 좌표 수의 재매칭. 실측 필요.

## 후속 장기 반사실과의 경계

본 row의 반사실은 **현재 epoch의 동결 좌표 비용표** 안에서만 재매칭한다. 미래 `H`시간
전체 시뮬레이터 분기는 [[YR-204]]이며, 본 row를 건너뛰거나 같은 실험에 섞지 않는다.

## 판정 (사전등록 시 동결)

`q(진짜 반사실) − greedy` 짝비교. **대조군 = `q(분해식)`** — 같은 무대·같은 문턱
으로 두 보상을 나란히. 평가 대역 신규.

**학습을 다시 해야 한다** — 보상이 바뀌면 라벨이 바뀐다. 약 3시간.

## 하지 않는 것

- [[YR-202]](`C_future`)와 동시에 바꾸는 것 — 한 번에 한 축.
- 반사실 계산이 느리다고 표를 근사로 더 뭉개는 것.
