# YR-306 — v5 최초 연속 학습 시작

- **상태**: in-progress
- 사용자 요청: 2026-09-10, "시작하고, 지금 구성이 어떻게 되어있는지 설명해줘".
- 대상은 v5 통합 블록 PPO다. 별도 진행 중인 v4 크레인 교사 작업과 섞지 않는다.
- 3대 게이트 보정 대상: `reliability` — 30일 연속 학습 연결·기록·실행을 검증한다.
- 현재는 **수정본 30일 완주·잔여 2,977건 관측용 재현 진행 중**이다.
  [재시작 계약](YR-306-wait-restart.md)의 실행은 672갱신·177,500건 등록·누락 0으로 종료했다.
  [잔여 진단](YR-306-residual-diagnosis.md)의 9검사·3일 원본 일치 확인 뒤 30일 관측용 재현을 시작했다.

## 확인된 현재 상태

- v5는 21블록의 판매·구매·크레인에 하나의 정책망을 공유한다.
- 실제 60초 구간의 비용 증가분을 보상으로 기록하고 PPO로 갱신한다.
- 반사실 세계 복제는 새 PPO 경로에서 사용하지 않는다.
- 현재 기본값: 60구간 수집, 2회 반복 갱신, 묶음 크기 64, 학습률 0.0003.
- 새 명령 `yard_rl.v5.ppo.continuous`가 준비일·학습일·정리일을 이어 실행한다.
- 날짜별 정책 저장·비용 보고·실패 기록 연결 완료. 물리 세계 자동 재개는 제공하지 않는다.
- 연구 시작 점검의 공용 최신 파일은 이전 정책에 대한 기록이다.
  이를 v5의 성능·현실타당성 통과 증거로 승계하지 않는다.

## 시작 조건

1. 연구 시작 점검을 실행하고 결과를 원자료로 남긴다. 거부되면 착수하지 않는다.
2. v5에 맞는 시작 조건과 연속 학습 실행 범위를 확정한다.
3. 실행기·설정·시드·관측/비용 계약을 고정한 커밋에서 짧은 배선 검사 후 시작한다.
4. 다른 무거운 학습이 실행 중인지 확인한다. CPU를 임의로 중복 점유하지 않는다.
5. 기존 모델·결과는 덮어쓰지 않는다. 비선형 본선 비용·6시간 통지창을 함께 추가하지 않는다.

## 근거

- [직전 디버깅](YR-303-v5-full-test-debug.md): 재검사 포함 199개 항목 통과.
- [현재 구현](../../../src/yard_rl/v5/README.md).

## 최초 시작 점검 결과 (사용자 승인 전 이력)

- 인가 거부: `allowed=false`, 저장된 시나리오 통과 기록 재계산 실패.
- 직접 원인은 외부 기준 파일의 CRLF/LF 줄바꿈 차이다. LF로 계산하면 기존 해시와 같다.
- 별도로 옛 점유율 65%와 v5 점유율 45%가 다르므로 이전 판정을 v5에 승계할 수 없다.
- 장기 실행기도 아직 별도 연결이 필요하다. 시작하지 않은 것을 시작했다고 보고하지 않는다.
- [보고서](../../../outputs/reports/yr306_v5_training_start/README.md)
  · [점검 원자료](../../../outputs/reports/yr306_v5_training_start/preflight.json).
- 사용자 방향 확인 전에는 기록·등록만 남기고 학습 프로세스는 실행하지 않는다.

## 사용자 승인 후 연결 계약 (2026-09-10)

- 사용자: "응 연결해서 진행해볼래?" — 30일 구성의 실행기 연결과 첫 실행 승인.
- 이번 단일축은 **연속 학습 실행 신뢰성**이다. 정책 구조·비용·정보를 최적화하지 않는다.
- 기존 게이트를 통과로 바꾸지 않고 v5의 미검증 상태를 별도 파일에 등록한다.
- 첫 실행은 비용 우위 판정이 아닌 연속 학습 작동 확인이다. 현실성 FAIL·성능 미확정은 유지한다.
- 30일을 한 세계에서 연속 실행. 첫날과 마지막날은 행동만 하고 가중치 갱신·학습 기록에서 제외.
- 진단 시드 9,900,306, 새 초기값. 정책 추첨 유지. 날짜별 부하는 기존 가중 추첨을 사용.
- PPO 기본값 유지: 60초 기록, 60구간 수집, 2회 갱신, 미니배치 64, 학습률 0.0003.
- 초기 점유율 45%, 기존 본선·트럭 비용·통지 분포·30분 재배정창 유지.
- 날짜별 가중치·최적화 상태, 1시간 진행 기록, 설정·코드·시드·파일 해시를 보존한다.
- 첫/마지막 날 제외는 물리 종료가 아니다. 미완료 일과 누적 비용을 보존한다.
- 중단 시 실패 상태와 마지막 완성 체크포인트를 보존한다. 물리 세계 자동 재개는 제공하지 않는다.
  재실행은 같은 고정 코드·시드로 새 출력 폴더에서 처음부터 한다.
- 실행 코드는 별도의 깨끗한 worktree에 고정하고 CPU 1스레드만 쓴다.
- 종료 조건: 연결 검증 통과·재현 가능한 커밋 원격 반영·30일 프로세스 시작·진행 기록 확인.
  실행이 시작됐다는 것을 30일 완주·성능 통과라고 보고하지 않는다.
- 착수 인가: 사전등록 `98b84c8`, v5 전용 게이트의 `reliability` 단일축 `allowed=true`.

## 연결 검증

- 관련 테스트 149건 통과: 새 연속 실행·기존 PPO 47건, 실제 엔진·복사·월 무대 102건.
- 3일·일일 트럭 60대 검사는 날짜별 갱신 `[0, 24, 0]`, 반사실 0회.
- 첫날 가중치 불변·학습일 변경·마지막날 불변, 비용 합·물리 상태·동일 세계 유지 확인.
- 단위 검사에서는 코드 출처 확인을 시험용으로 대체했다. 운영 실행은 대체 없이 별도 확인한다.
- [연결 보고서](../../../outputs/reports/yr306_v5_training_start/connection.md).

## 본 실행 시작

- 코드 `bcbcd00aa23c5c458d323180289969714ee2664d`, 원격 반영 후 별도 clean worktree 고정.
- 고정 코드 3일 시범 완주: 57.27초, 갱신 24회, 반사실 0회, 첫·마지막날 갱신 0회.
- 2026-09-10 10:45:54 KST 본 실행 시작. Windows 실행기 PID 30284, WSL Python PID 445.
- 본 실행 폴더: `outputs/v5/yr306-run-30d`; 고정 코드: `outputs/v5/yr306-source-bcbcd00`.
- 원자료는 `manifest.json`, `status.json`, `events.jsonl`, `days.json`, 날짜별 `.pt`에 남는다.
- 전체 입력 계획 177,500대, 첫날 3,500대·둘째 날 12,500대. 일별 부하 차이가 있어 비용 추이를
  곧바로 학습 개선으로 해석하지 않는다. 비교 정책과 성능 판정은 이번 범위가 아니다.
- 이 row는 진행 중으로 유지한다. 실행 시작과 30일 완주는 구분한다.
- 시작 증거 저장 시점(10:48:31 KST): 2일차 12시·갱신 12회, 첫날은 갱신 0회로 저장 완료.
  [기록](../../../outputs/reports/yr306_v5_training_start/launch.json).

## 2026-09-11 디버깅 사전등록

- 사용자: "지금 디버깅해서 작동하는지 확인하고 현재 어떻게 설계되어있는지 순차적으로 쉽게알려줘".
- 기존 실행 30일+2시간·학습 672회·정책 30파일은 보존된다. 최초 누락 사유는 저장되지 않았다.
- 첫 3일 경계까지 원본 궤적 재현에서는 누락이 없었다. 전체 30일의 누락 원인으로 확대 해석하지 않는다.
- 단일축은 계속 `reliability`: 투입 결과를 남기고, 최초 누락을 확인한 경계에서 즉시 실패 처리한다.
- 정상 경로의 오더·비용·정책·학습 설정·물리 엔진은 유지한다. 반사실 계산을 추가하지 않는다.
- 트럭·본선 누락 사례를 의도적으로 만든 검사에서 ID·블록·시각·이유·계수를 저장하고 이후 학습을 막는지 확인.
- 일반 예외와 종료 검증 실패에도 실행 결과·최근 정책·기존 날짜 기록을 잃지 않는지 확인.
- 정상 3일 저부하 실행은 첫/마지막날 갱신 0회·학습일 24회와 기존 실행 결과 일치를 확인.
- 정상 단기 통과를 원래 30일 누락 해결 또는 성능·현실성 통과로 보고하지 않는다.
- v4 작업은 보존. 이번 검사는 CPU 1스레드의 짧은 부품·연결 검사로 제한하고 장기 재현은 동시 실행하지 않는다.
- 설명은 생성→통지·투입→판매·구매→크레인→실제비용→PPO 갱신→날짜별 저장 순서로 코드와 대조한다.

## 디버깅 구현·부품 검사

- 사전등록 `0b358df` 원격 반영 뒤 신뢰성 보정 인가 `allowed=true` 확인.
- 최초 누락 경계에서 오더·블록·시각·사유·투입 수를 보존하고 이후 진행을 중단한다.
- 트럭은 다음 정책 경계보다 먼저, 본선은 당일 투입 직후 검사한다. 정상 실행 순서는 바꾸지 않는다.
- 월 실행 반환값을 최종 검사 전에 저장한다. 실패 시 최근 정책·부분 보고서·완료일 집계도 보존한다.
- 부품·회귀 검사 101건, 실제 3일 연속·통합·월 무대 검사 56건 통과(총 157건).
- 인위적 실패 4종은 기록·조기 중단이 목적이며 원본 30일 누락 원인 재현 증거가 아니다.
- 고정 코드 `8ab7d9d`의 3일 명령행 실행 완주: 180대 전부 투입, 본선 실패 0, 갱신 `[0,24,0]`, 반사실 0.
- 원래 3일 실행과 행동·비용·학습 수치 및 저장 모델·행동 추첨 상태가 모두 일치했다.
- 실행·저장 76.16초는 동시 v4 작업이 있는 단기 결과다. 속도·성능 비교 주장에 쓰지 않는다.
- 기록 장치의 단기 검증과 달리 원본 30일 누락 원인은 미확정이다. 장기 신뢰성 게이트는 미통과로 유지한다.
- 다음은 같은 row 안의 원래 30일 첫 누락 재현·직접 원인 보정이다. 동시 v4 작업 때문에 장기 재현은 시작하지 않았다.
- [쉬운 순차 설계·검증 보고서](../../../outputs/reports/yr306_v5_admission_debug/README.md).

## 30일 원본 입력 재실행 승인 (2026-09-11)

- 사용자: "지금은 돌려도 되지않아?" — 자원 상태 재확인 후 재실행한다.
- 13:49 KST 확인: v4 대규모 학습 프로세스 없음. CPU 1개를 쓰는 v4 진단만 실행 중.
  전체 CPU 24개, WSL 사용 가능 메모리 54,317 MiB. v4 진단은 중단·변경하지 않는다.
- v5는 별도 고정 코드 `8ab7d9d`, CPU 1스레드로 제한한다. 기존 단기 검증 코드를 그대로 사용한다.
- 기존 30일과 같은 진단 시드 9,900,306·기본 부하 추첨·정책 초기값·학습 설정으로 처음부터 실행한다.
- 출력은 새 폴더 `outputs/v5/yr306-replay-30d-8ab7d9d`. 기존 30일 결과와 모델은 덮어쓰지 않는다.
- 목적은 첫 투입 누락의 원인 재현이다. 첫 누락 시 자동 기록·중단하고, 없으면 30일 종료 검사를 한다.
- 비용·정책·시나리오 변경은 없다. 재현 중 학습은 원래 궤적을 따르기 위해 유지하며 성능 개선 판정은 아니다.
- 최신 v5 게이트의 신뢰성 단일축 인가 후 시작한다. 실행 시작 증거는 별도로 기록한다.

## 재실행 시작 증거

- 사전등록 `e7b6a70` 원격 반영·인가 `allowed=true` 확인 후 2026-09-11 13:50:51 KST 시작.
- 실행기 Windows PID 63756, 실제 WSL Python PID 682. CPU 1스레드·고정 코드 출처 검사 통과.
- 입력 계획 177,500대. 시드·날짜 계획·학습 설정·학습 구간·초기 적재·행동 추첨이 원본과 모두 같다.
- 13:51:40 KST 시작 증거: 첫날 15시, 트럭 2,630대 투입·누락 0·본선 투입 실패 0.
  첫날은 준비일이므로 갱신 0회가 정상이다. 진행 시작은 30일 완주·원인 해결을 뜻하지 않는다.
- [시작 기록](../../../outputs/reports/yr306_v5_admission_debug/replay-launch.json).

## 2026-09-12 컨테이너 연결 점검

- 원본 실행은 10일째 10:37 첫 반출 대상 부족으로 중단됐고, 같은 실패를 재현했다.
- 사용자 요청에 따라 v3 스키마와 실제 실행 대상을 대조하고 필요한 연결 검증을 수정한다.
- v3 스키마의 `con_no`는 있으나 합성 번호·엔진 대상·날짜별 명단 사이의 일치가 보장되지 않았다.
- [사전등록·수정 범위](YR-306-container-identity-audit.md). 입력 검사를 통과한 경우에만 재학습한다.

## 번호 연결 보정 후 판정 (2026-09-12)

- v3의 6필드는 유지했다. 실제 대상과 오더 번호 연결을 수정하고 임의 컨테이너 대체를 금지했다.
- 고정 코드 `8ab7d9d`와 `412a324`의 같은 30일 명단 비교에서 번호 불일치 177,500→0건.
- 초기 13,608개 컨테이너의 서로 다른 번호 수는 648→13,608개가 됐다.
- 원래 명단의 반복 출고 88,679건, 출처 없는 반출 8건, 선적 목록 없는 본선 묶음 108개는 남았다.
- 실제 운영 진입점도 이 입력을 세계 생성 전 거부했다. 학습 갱신 0회이며 장기 재학습은 시작하지 않았다.
- 작은 고정 명단으로 학습 연결을 검사하는 것과, 원래 30일 입력이 유효한 것은 별개다.
- 전체 v5 249건과 별도 추가 9건, 서로 다른 258건이 통과했다. 실패·건너뛰기는 없다.
- 번호 수정·입력 조기 검사까지 검증했다. 성능·현실성·유효한 30일 연속 실행은 미통과로 유지한다.
- 다음도 YR-306 안의 신뢰성 보정이다: 고정 운송 연결 입력과 실제 준비 상태·재배치 후 위치를 연결한다.
- [검증 보고서](../../../outputs/reports/yr306_container_identity/README.md) ·
  [원자료](../../../outputs/reports/yr306_container_identity/verification.json) ·
  [진행 판정](../../../outputs/reports/yr306_container_identity/gate-after-identity.json).

## 생성 규칙 고정·재생성 (2026-09-12)

- 사용자 요청에 따라 같은 난수 번호와 물량·도착 분포로 새 컨테이너 명단을 만든다.
- [재생성 규칙·검증 범위](YR-306-fixed-seed-regeneration.md). 생성·입력 검사까지이며 자동 재학습하지 않는다.
- 코드 `8539a92`에서 트럭 177,500건을 유지하고 재생성했다. 중복 출고·출처 없는 반출·대상 없는 선적은 0건이다.
- 같은 코드·시드로 두 번 생성한 압축 파일이 완전히 같고, 관련 검사 147건과 최종 40건 재검사가 통과했다(중복 합산 금지).
- 실제 미도착 대기·재배정 후 위치 추적은 아직 미검증이다. 같은 row의 다음은 이 실행 연결 검증이다.
- [생성·검증 보고서](../../../outputs/reports/yr306_seed_regeneration/README.md) · [현재 판정](../../../outputs/reports/yr306_seed_regeneration/gate-after-seed.json).
- 사용자 "그럼 다시 진행해봐"에 따라 [실행 연결 계약](YR-306-fixed-cargo-runtime.md)을 등록했다. 검사 통과 전 긴 학습은 금지한다.

## 새 고정 명단 실행 연결·재시작 (2026-09-12)

- 코드 `83de18d`, 관련 검사 190건 통과. 실제 명령 3일 실행에서 180건 전부 투입·갱신 24회·반사실 복제 0회.
- 미도착 상자 대기, 반입 재배정 뒤 후속 작업 위치·기존 대기 장부, 본선 운반 사건 연결을 확인했다.
- 3일 시험은 종료했지만 반입 1건이 남았다. 작업 전부 완료·정책 성능 개선을 주장하지 않는다.
- 새 30일 입력을 그대로 읽어 처음부터 재시작했다. CPU 1개, 기존 학습 설정 유지.
- 저장한 시작 확인: 첫날 15시간·2,630건 투입·누락 0·갱신 0(준비일). 실시간 상태는 출력 폴더에서 확인한다.
- 다음도 YR-306: 날짜별 기록·잔여 작업 원인을 확인한다. 30일 완주·성능·현실성은 아직 미통과다.
- [보고서](../../../outputs/reports/yr306_fixed_cargo_runtime/README.md) · [판정](../../../outputs/reports/yr306_fixed_cargo_runtime/gate-after-runtime.json).

## 용량 거절 정정 (2026-09-12)

- 기존 실행은 22일차 00:47에 반입 등록 거절 1건으로 중단됐다. 480갱신·정상 등록 125,269건이었다.
- 사용자 지시에 따라 `415e3e7`에서 주문을 보존하고 공간 부족은 대기·비용으로 처리한다.
- 관련 검사 156건 통과. 만재 유지·대기 비용 증가·공간 해제 뒤 처리·기존 학습 연결을 확인했다.
- 정책·보상 계수·데이터·고정 엔진 사본은 유지했다. 장기 실행은 재시작하지 않았다.
- 다음은 같은 YR-306의 30일 누락·물리 제약·잔여 작업 검증이다. [증거](../../../outputs/reports/yr306_admission_wait/README.md).
