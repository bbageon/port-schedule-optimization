# YR-302 — v5 통합 블록 PPO 구현·디버깅

- 요청: 2026-09-09 사용자 직접 지시. v4 코드를 복사하여 v5 구현, 디버깅까지만.
- **상태**: done — 구현·디버깅 범위 완료. 기존 v4·미추적 원본은 무수정.
- 구현 커밋: `5a9e8dd18750365f7f683fb74a12f1f625e62f59`.
- 범위: `src/yard_rl/v5`, `tests/v5`, `scripts/v5` 독립 사본과 신규 PPO 경로.
- PPO: 실제 선택 기록으로 정책을 조금씩 갱신하는 강화학습 방법.
- 판매·구매·크레인 작업 선택은 블록 간 하나의 정책망과 가치망 줄기를 공유한다.
- 시장 동의·물리 제약·후보 생성·기존 비용식은 유지한다.
- 반사실 교사·세계 복제·대안별 미래 실행 없이 본 실행 비용 차분으로 학습한다.
- 60초 동기화 구간 안의 역할별 선택을 블록의 묶음 행동으로 기록한다.
- 구간 비용은 터미널에서 한 번 계산하고 각 블록에 같은 협력 보상으로 제공한다.
- 학습 기록 수집 경계에서 정책을 갱신하되 세계·대기열·진행 작업은 유지한다.
- 시간 제한 종료에는 남은 가치 추정을 붙인다. 비용 발생이 끝났다고 간주하지 않는다.
- v4의 비선형 본선비용 추가, 사전 통지 6시간 확장, 장기 성능 실험은 제외한다.
- 검증: 복사 경계, PPO 수식·마스크·비용 차분·시간 경계·실행 연결 단위시험,
  짧은 21블록 실행과 가중치 저장·재로드. 통과 수와 실측은 완료 시 추가한다.
- 연구 게이트: 성능·현실타당성은 이번에 미평가. 구현 신뢰성만 검사한다.
- 사용자 직접 승인한 별도 구현 작업이며 기존 연구의 성능 채택 판정을 승계하지 않는다.
- 후속: 이번 요청은 디버깅에서 종료. 장기 학습은 별도 승인 범위다.

## 검증 증거

- [보고서](../../../outputs/reports/yr302_v5_ppo_debug/README.md).
- 관련 142건 통과(140 + 자정·엔진 대조 2). 전체 수집 179건의 전수 통과 주장은 하지 않는다.
- 21블록·하루 트럭 300대·12시간 축소 실행: PPO 6회 갱신,
  판매 118 / 구매 107 / 크레인 1,267건, 거래 확정 57건, 반영 실패 0건.
- 반사실 실행 0회. 세계 복제를 금지한 상태에서도 연결 시험 통과.
- 원화 비용과 구간 보상 합계 일치, 학습 경계의 세계 보존, 자정 정리 후 비용 보존 확인.
- 저장/재로드·동일 조건 재실행·추론 중 가중치 불변 확인.
- 탐색 없는 재실행에서는 거래 0건. 아직 정책 품질을 검증한 결과가 아니다.
- 학습 코드와 무대는 v5 전용. WSL에 NumPy 추가 설치, 패키지 목록 명시.
