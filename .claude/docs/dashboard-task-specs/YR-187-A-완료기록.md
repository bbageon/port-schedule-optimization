# YR-187 A단계 완료 기록 — 스키마만 (2026-08-18 · commit `2f0dbdf`)

> [[YR-187]] 본 spec 에서 분리(200줄 규약). 값의 실측화는 [[YR-187-B]].


**A = 그릇만 맞춘다** — 필드 이름·타입을 실규격에 맞추고 **값은 지금처럼 합성**.
데이터가 오면 [[YR-187-B]] 에서 값만 갈아끼운다.

### 만든 것 / 바꾼 것

| 파일 | 무엇 |
|---|---|
| `src/yard_rl/integrated/order_schema.py` (89줄, 신설) | `bnct_view()`·`attach()`·`record_swap()`·`size_class()` |
| `src/yard_rl/integrated/terminal_stream.py` | 스케줄 생성부 1곳에서 `attach_bnct()` 호출 |

**방식 = 가산 마이그레이션**. 기존 키를 지우지 않고 새 필드를 **더한다**.
사유: `"block"` 36곳·`"job_id"` 70곳 소비처가 있어 일괄 개명은 야간 자율 작업에
위험하다. 개명은 실데이터가 와서 소비처를 한 번에 옮길 때 한다.

추가 9필드: `doc_key` · `block_assigned` · `block_worked` · `block_previous` ·
`swap_reason` · `reserved_s` · `gate_in_s` · `size_class` · `schema`(=`bnct-a1`)

### 검증 (3,600건 전건)

| 검사 | 결과 |
|---|---|
| 구↔신 키 값 4쌍 일치 (`job_id`·`block`·`arrival_s`·`size_ft40`) | **전건 일치** |
| `reserved_s` == `gate_in_s` | **동일** — 현 무대는 준수오차 0. 이 축을 여는 것이 [[YR-190]] |
| 구키 지문 (2시드) | `9cf67389…` · `e724321177…` |
| 본선 작업 / `day_total` / `sim_end` | 3,600 / 3,600 / 93,600 — 불변 |
| 해시 안전성 | `realization_hash`=scenario · `run_digest`=실현시각·사건열 → **키 추가에 불변**(사전 확인) |

### 회귀

- `tests/integrated/` 전량 (WSL) — **571 passed** · 8 skipped
- 실패 8건은 **HEAD 에서도 동일** → 본 변경과 무관. [[YR-191]] 로 분리 등록
- 24시간 실에피소드 완주: `admitted=3600` · Φ 2271.87 · 536s

### 남은 것 → [[YR-187-B]]

값의 실측화 · `record_swap()` 을 판매 확정 경로에 연결(지금은 미호출) ·
합성 인공물 2종(`requested_flow`·`fallback_reason`) 삭제 · 추가권고 7필드.

**착수조건 ②("YR-183 판정 완료 대기")는 YR-183 취소(2026-08-18)로 소멸.**
