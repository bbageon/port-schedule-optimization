# YR-293 — submissionv2: 혼잡 빈도 민감도를 반영한 판

**상태**: done (2026-09-01)
**Epic**: Paper

## 발단

[[YR-286]] 이 미뤄 둔 문제 — §5.4 의 행동 분해가 대역 사이에서 **부호까지 뒤집혔다**
(시각 91.3% ↔ 53.8% · 블록만 −0.131 ↔ +0.394 십억원). 원인 후보는 **혼잡일 빈도**
였다(진단 4일 vs 판정 6일).

## 실험 설계

시드를 **셋 다 9,900,980** 으로 고정했다 — `plan_days` 가 날짜 시드를
`seed + 1000*(i+1)` 로 만들므로 같은 자리의 날은 세 환경에서 같은 난수를 쓴다.
혼잡 파도(12,500 → 15,000 이틀)의 **모양도 통일**하고 **빈도만 2 / 6 / 14 일**로
바꿨다. 어느 수준도 연달아 오지 않게 균등 위상으로 흩뿌렸다.
팔 다섯: RL · RL_SPACE · RL_TIME · RL_EARLY · NO_REALLOC (`run_env_sensitivity.sh`).

## 원자료 재계산 (전달받은 표를 그대로 쓰지 않았다)

규약대로 첫날·마지막날을 뺀 **가운데 28일**만 썼다.

| | 한산(2일) | 적당(6일) | 혼잡(14일) |
|---|---|---|---|
| 기준 비용 | 6.38 bn | 14.13 bn | 29.64 bn |
| 전체 감소 | 0.82 (12.9%) | 2.55 (18.1%) | 5.05 (17.0%) |
| 시각만 / 몫 | 0.55 (67%) | 2.01 (79%) | 4.11 (81%) |
| **블록만** | **+0.23** | **−2.40** | **+1.17** |
| 학습 몫 | 0.18 | 1.51 | 1.60 |
| **학습 중앙값/일** | +0.8 M | **−0.1 M** | **+9.7 M** |
| 학습 부호검정 | 17/28 p=0.345 | 14/28 p=1.000 | **22/28 p=0.004** |
| 무재배치 대비 | 27/28 | 27/28 | 28/28 |
| **공간 행동 비중** | **32.0%** | **21.5%** | **11.9%** |

**★보고서가 놓친 것**: 본편 실행(`judge-30d`)은 혼잡일 **4일** · 공간 비중
**25.7%** 로 이 선 위에 정확히 얹힌다 → **32.0 / 25.7 / 21.5 / 11.9%** 네 점 단조.
논문의 대표 실험이 민감도 곡선 위에 놓이는 셈이라 가장 튼튼한 근거가 됐다.

**★정정 둘**: 적당 환경의 학습 중앙값은 **−0.1 M**(보고서 +0.1), 혼잡은
**+9.7 M**(보고서 +10.7). 짝수 28일이라 중앙 두 값의 평균을 쓴 차이이고 판정에는
영향이 없다(p=1.000 / p=0.004).

## 판정

**흔들린 것 — 행동 분해.** 블록만 항은 부호가 뒤집히고, 적당 환경에서는
15,000대 하루가 −1.99 bn 을 혼자 만든다. **28일로는 추정되지 않는 양**이다.
잡음이 아니라 기제다 — 시간을 못 미루는 정책이 초혼잡일에 무너지고 밀림이
다음 날로 번진다.

**흔들리지 않은 것 넷** — ①공간→시간 전환(네 점 단조) ②시각이 어디서나 최대
③핵심 주장은 모든 환경에서 성립 ④학습은 혼잡할 때만 통계적으로 잡힌다.

## submission → submissionv2 로 고친 다섯

| 자리 | 이전 | 지금 |
|---|---|---|
| 초록 | *"accounted for **91.3%**"* | 단조 전환(32%→12%) + *학습은 가장 혼잡한 환경에서만 통계적으로 잡힌다* |
| §5.4 | *"therefore explains 91.3%"* | *"not stably estimated at 28 days"* + 세 환경 수치 + 단조 전환 |
| §5.6 | *"or **45.9%** of the total reduction"* | 절대액 셋 + **중앙값** 셋 + p 셋 |
| 결론 | *"carry no estimate of variation"* | 민감도 결과로 교체 |
| 그림 2 캡션 | — | *"specific to this demand mix"* |

**왜 두 비율을 뺐나**: 91.3% 는 다섯 자료 중 **바깥값**이고(67·79·81·53.8·91.3),
45.9% 는 분모가 환경마다 두 배씩 달라 학습 몫이 거의 같아도(1.51 vs 1.60 bn)
59% vs 32% 로 갈린다.

## `verify_submission.py` 확장

- 원고 경로를 **인자**로 받는다 — `submission` 과 `submissionv2` 를 같은 잣대로 잰다.
- 민감도 **21건**을 원자료 재계산으로 대조 목록에 추가.
- 91.3%·45.9% 검사는 `"controlled sweep" in TEX` 일 때 건너뛴다.

## 검증

`submission` **0건 실패(39)** · `submissionv2` **0건 실패(60)** ·
12쪽 · A4 595×842pt · Overfull 0 · Underfull 0 · 오류 0 · 미정의 참조 0 ·
인용순서 두 축 통과 · 본문 들여쓰기 0 · escape 이상 없음.

**남은 것**: v2 는 10쪽이 그림 둘만 있는 쪽이다 — `submission` 에서는 그림 2가
9쪽 아래였는데 민감도 서술이 늘며 밀렸다. 다른 배치 지정자도 시험했으나
그림 3이 사사 아래로 내려가 지금이 낫다.

## Evidence

`929402b` · `7ceaf28` (빌드 부산물 추적 해제)
