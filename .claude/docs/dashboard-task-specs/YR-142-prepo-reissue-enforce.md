# YR-142 — v4-B-2: PREPOSITION 재발행 금지 강제 (실행 이력 기반)

> **⚠ 아래 v1 계약(BASE+3500 대역·3군 비교)은 폐기됨** — 17·18차 감사로 시드 대역
> 오염(mid-loose 833500..833502 = YR-141 high-tight 열람 대역과 숫자 동일)과 B1 대조군
> 부재가 확정되어, 유효 계약은 **하단 "18차 감사 반영 — 사전등록 정정 v2"**(`24c7c7c`)다.
> 최종 판정(★공식 기각 — J7 만료 후 실행 1건)은 v2 계약으로 수행 (`d63ff87`).

## 무엇을 왜
YR-141(v4-B 구속적 PREPOSITION)은 7항 중 5항을 통과했고, 실패 2항(J2 장악·J7 반복)의
단일 원인은 **같은 작업을 위한 위치조정(PREPOSITION)이 반복 발행되는 것을 규칙으로
막지 않은 것**이었다 (근접 ≤1bay 소멸만으로는 미차단 — 스펙에 사전 고지한 단순화).
YR-142는 이미 동결된 J7 계약("같은 작업 반복 이동 = 0")을 **계수(사후 측정)에서
강제(사전 차단)로 승격**하는 구현 완결이다. YR-137 ①과 같은 "성립 조건 수리" 성격 —
튜닝이 아니다.

## 유일 변경 (단일축)
실행 이력 기반 재발행 금지:
1. **기록**: 결정이 실행될 때 결속 작업 id 를 `sim._prepo_history`(set)에 기록.
   - 학습 경로: `yr139_blockq_v4_ppo.run_episode` — 선택된 assign 적용 직전 기록.
   - 평가 경로: `yr141_bound_prepo._PrepoRecorder.decide` — 실행 계수와 같은 지점.
2. **차단**: `candidates._reposition` 의 BOUND_REPO 분기에서
   `jid in sim._prepo_history` 이면 후보 발행 스킵 (교착 탈출 REPO 는 불변 분리).

그 외 전부 YR-141 과 동일 (PPO 단위 정정판·표준 앵커·최종 정책·계약 물리·κ 동결).

## 판정 (동결 — YR-141 7항 동일, 신규 미열람 대역 BASE+3500..3502)
비교군 3: SF / v4-A(YR-140 체크포인트 재사용) / **v4-B-2**(신규 학습 3초기화).
실현 지문 박제. v4-B-2 기준 7항 전부 = 성공:
- J1 완주 100% ∧ backlog 0 (전판)
- J2 PREPOSITION(재배치 계열) 장악(에피소드 행동 60% 초과) 0
- J3 vs SF v2 실현 지연비용 감소 방향 ≥2/3 초기화
- J4 (B2−A) v2 짝 평균 ≤ 0 이 ≥2/3
- J5 (B2−A) 본선 초과분 짝 평균 ≤ 0 이 ≥2/3 (비열등)
- J6 (B2−A) v1 전체비용 짝 평균 ≤ 0 이 ≥2/3 (이동·재조작 악화 방지 대리)
- J7 같은 작업 반복 이동 = 0 ∧ 만료(도착) 후 이동 = 0 (실행 계수 — 이제 규칙이
  차단하므로 0 이 아니면 구현 결함)

## 실행
- 하네스: `yr141_bound_prepo.py` 재사용 (`--outdir outputs/reports/yr142_prepo_enforce`
  `--eval-offset 3500`). 학습 3초기화(88k/99k/123k) 병렬.
- 회귀: `test_yr141_bound_prepo.py::test_execution_history_blocks_reissue`(차단),
  flag off 불변·결속·근접·탈출 분리 기존 4건 유지.
- 통과 시 다음: 잠금평가(신규 시드 확대·신뢰구간·12시나리오×3초기화 의존성 계정)
  사전등록. J2 비용 크기 재요구는 YR-133(판매/수용 견적 배선)에 유지.

## 17차 감사 반영 (2026-08-02) — **평가 보류** · 보정 목록 (결과 미열람 유지)

적대 검증 워크플로 4축 + 직접 확인 2건으로 감사 주장을 대조한 결과:

1. **시드 대역 충돌 (맞음·치명)**: BASE 셀 간격이 100 인데 3500−3200=300 이라
   mid-loose 의 833500..833502 가 **YR-141 high-tight 열람 대역과 숫자 동일**. 시나리오
   난수는 시드 정수 단독 시딩(scenario_gen)이라 다른 셀·같은 숫자 = 상관 실현 →
   "미열람 신규" 성질 훼손. **보정: 판정 대역 BASE+4000..4002 로 교체**(과거 전 오프셋
   0~3500 과 셀 간격 ±300 조합 대비 간격 ≥200 — 충돌 없음 계산 완료), CLI 기본값
   3200 제거(명시 강제) + 과거 사용 숫자와 교집합 fail-fast.
2. **반올림 후 판정 (맞음)**: J4~J6 이 반올림 평균에 ≤0 적용 — 원정밀도로 판정하고
   반올림은 보고 출력만.
3. **건전성 미반영 (맞음)**: 퇴화 검출기(실작업 25% 미만·단일 비실작업 60% 초과)를
   계산만 하고 성공 조건에 없음 — 전판 healthy=True 를 하드 가드로 추가(강화 정정).
4. **관측 맹점 (맞음·지위 한정)**: 정책은 결속 작업 식별·ETA 를 관측하지 못함(adapter
   조회가 PREPO: 접두 ID 로 항상 None·ETA 특징은 선재조작 한정 — 같은 bay 두 PREPO 는
   동일 벡터). **허용 주장 = "작업 ID one-shot 마스크 규칙의 효과"까지**; "작업별
   ETA·긴급도 이해 최적화" 주장 불가. ETA 배선은 별도 단일축 실험(핵심 정보 우선).
5. **기록 시점 (부분 맞음)**: 유령 영구차단(미실행 기록)은 현 배선에서 불성립 —
   decide→_apply 직결·D-ORACLE 불변식 확인. 단 **같은 공동결정 내 두 크레인 동일 작업
   PREPO 는 차단 없음**(PREPO token=None 이 중복검사·DUP_JOB 을 건너뜀, J7 사후 검출만)
   — 보정: 공동조합 열거에서 동일 결속 작업 중복 조합 제외. 엔진 커밋 훅 이관은 후속.
6. **ID 파싱 (맞음·잠재)**: 다중 블록 "A:작업" 형식에서 split(":")[1] 붕괴(조용한
   무력화) — 현 단일 블록 실험은 무해(작업 ID 에 콜론 불가 확인). 보정: 양끝 파싱
   (`[len("PREPO:"):].rsplit(":",1)[0]`).
7. **텔레메트리**: PREPO 발행/실행/이력차단 계수 기록(반복 0 이 "차단 성공"인지
   "애초 미선택"인지 구분) + 결과 메타에 YR-142 라벨 명시.
8. **YR-141 재현성**: 현행 트리 재실행은 기록 훅 때문에 동작이 다름 — 재현 기준은
   판정 커밋 `837d11e` 체크아웃(박제 원칙)으로 명시.

**평가는 보정 1·2·3·5·6·7 적용 전 실행 금지.** 학습 3초기화는 완료(체크포인트 보존).
보정 5(중복 조합 제외)는 학습 계약에도 걸리므로 **보정 후 재학습**(스레드 상한 적용 시
~10분)으로 학습·평가 계약을 일치시킨다. 감속 원인 박제: 학습이 반복당 7.6분으로 느렸던
것은 규칙이 아니라 스레드 과잉 배정(3프로세스×24스레드/24코어) — 프로세스당 2스레드
상한으로 반복당 ~5초 회복(차단 켬 2.2s vs 끔 2.7s 로 규칙 무죄 실측).

## 18차 감사 반영 (2026-08-02) — 사전등록 정정 v2 (동결·결과 미열람)

17차의 보정 ⓐ(BASE+4000)가 추가 검증에서 **또 오염**으로 판명 — yr099_transfer_mvp 가
절대 시드 834000..834007 (+833000..833007) 을 사용, "BASE+오프셋" 족만 본 계산의 누락.
교훈: 산술 대역 배정 전면 폐기, 시드뱅크(YR-108 설계)로만 배정한다.

- **대역 (동결)**: `seedbank.assign_band` 시작 910000 (901k~905k·920k 이상은 이송 계열
  사용 중 — 미사용 정수 구간), 과거 기록 실현 지문 **1,638개 전수 제외** 대조. 결과
  = 셀별 3개, 910000..910011, `band.json` digest `29f0a45a78486b79`. 평가 직전
  fail-fast: 지문 재계산 일치 + 정수 범위 + 과거 지문 교집합 0 (`load_band_checked`).
  산술 offset CLI 인자 자체를 제거(기본값 실수 원천 봉쇄).
- **축 분리**: `BOUND_REPO`(결속 발행) ⊥ `PREPO_ONE_SHOT`(재발행 금지, 신설 기본 False).
  이력 기록(학습 run_episode·평가 recorder)도 ONE_SHOT 게이트 — B1·YR-141 재현 보호.
- **비교군 4** (전부 같은 대역): SF / A(자유 위치조정, YR-140 ckpt) / **B1**(결속·반복
  허용, 신규 학습) / **B2**(결속·one-shot, 신규 학습). one-shot 효과는 **(B2−B1) 짝
  대조로 분리 보고** — 성공 조건이 아니라 주장 근거 전용(동결 7항 유지 원칙).
- **판정** = 기존 7항(B2 기준, 원정밀도 — 반올림은 보고 출력 전용) + 강화 2: J1 에
  건전성 전판, J7 에 노출 유효(대역 전체 PREPO 실행 >0 — 공허 통과 방지).
- **중복 결속 조합 제외**: 같은 공동결정에서 두 크레인이 같은 결속 작업의 PREPO 를 드는
  조합은 `_feasible_joint` 에서 제외(발화는 `sim._prepo_dup_removed` 계수). 감사의
  "목록 비교 후 재학습 결정" 지시에 따른 probe(훈련 대역 4셀×16시드 argmax) 실측:
  **발화 B1 구성 1,042회 / B2 구성 361회 (64판 기준)** — 공동후보 목록이 실제로
  달라지므로 **B1·B2 모두 신규 학습 필수**(체크포인트 출처 commit·sha256 동봉).
  부수 확인: YR-141 판정 당시 같은 작업 PREPO 를 두 크레인이 동시에 들 수 있었음
  (J7 반복 88~150건의 한 원천).
- **파싱 견고화**: `prepo_bound_jid()` 양끝 파싱(다중 블록 "A:작업" 콜론 안전) 으로
  split(":")[1] 전면 교체. 텔레메트리: 발행/실행/차단/중복제거 계수 에피소드별 기록.
- **재현성**: YR-141 판정 재현 기준은 커밋 `837d11e` 체크아웃 (현행 트리는 계약 진화).
