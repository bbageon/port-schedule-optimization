# YR-292 — 그림 배치 해결: 문단 병합 + 그림 3 높이 축소

**상태**: done (2026-08-31)
**Epic**: Paper

## 발단

사용자 지적 셋.

1. *"fig2, fig3 이랑 문장 중간에 끊겨서 나오는거 같은데 차라리 관련있는 문단이 끝나고
   난뒤 ... 나오게 하는게 나을꺼같아"*
2. *"fig3 도 ... interpretation. 여기 문장 다음에 삽입하고 ... 그리고 acknowledgements
   위로 가야할 꺼같은데?"*
3. *"공간이 부족해서 그런거면 약간 본문의 내용을 축약하면 되는거아냐? ... 하나의 파트에는
   하나의 문단이 있는것처럼 띄어쓰기로 다 연결되면 돼"*

## 한 일 둘

### 1. 절 안의 문단 나눔 8곳 제거

`scratchpad/parascan.py` 로 훑어 서론 2 · 관련연구 2 · §3.4 1 · §4 1 · §5.1 1 ·
결론 1 을 이어 붙였다. 들여쓰기가 사라지고 자리도 조금 났다. 남은 빈 줄은 전처리 2곳뿐.

### 2. 그림 3 높이만 축소 (4.80×3.25 → 4.80×**2.70** in)

`scripts/v3/fig_results.py` 의 2패널 `figsize` 를 고쳤다. **폭과 글자 크기는 그대로**
두고 높이만 줄였으므로 가독성 손실이 없다. 이것만으로 조판 문제가 전부 풀렸다.

| | 이전 | 지금 |
|---|---|---|
| 그림 2 | 10쪽 y=332 | 10쪽 y=333 |
| 그림 3 | 11쪽 y=592 | **10쪽 y=609** |
| 결론 | 10쪽 y=487 | 11쪽 y=185 |
| 사사 | 11쪽 y=262 (**그림 위**) | 11쪽 y=509 (**그림 아래**) |

→ 그림 2·3 모두 **§5 안**, 결론·사사보다 **앞**. 10쪽은 그림 전용 쪽이 됐다
(LaTeX 의 float page — 본문이 섞이지 않아 문장을 쪼개지 않는다).

## 전달받은 피드백 중 쓰지 않은 것과 근거 (전부 실제로 빌드해 확인)

| 제안 | 결과 | 판정 |
|---|---|---|
| `placeins` + 그림마다 `\FloatBarrier` | **13쪽**, 9·10쪽에 363pt(약 30줄) 구멍 | 안 씀 |
| 결론 앞 차단선 하나만 | **13쪽**, 10쪽 구멍 197pt | 안 씀 |
| `\raggedbottom` 삭제 | 배치는 그대로인데 **Underfull 0 → 2** | 유지 |
| `pdfcrop` | 불필요 — 두 그림 모두 캔버스에 꽉 참(학습곡선 내용 0~345.6 × 0~231.4pt) | 안 씀 |
| `[H]` | 피드백 스스로 권하지 않음 | 안 씀 |

## 부수 확인 — `h`(문단 바로 뒤)가 왜 안 먹었나

**본문이 쪽을 끝까지 채우기 때문**이다. 본문 영역 바닥이 y=665 인데 9·10쪽 마지막
글줄이 **y=666.4** 다. 빈 자리가 없으니 `h` 가 실패한다. 폭 0.55 까지 줄여도,
`flafter` 를 꺼도 동일했다 (`figflow.py`·`figfit.py`).

## 3. 그림 2도 높이 축소 (4.80×2.86 → 4.80×**2.15** in)

사용자 물음: *"본문 내용을 조금 줄이면 가능하지않아?"*
→ **줄일 필요가 없었다.** 그림 2 높이만 낮추면 됐다.

그림 3만 줄였을 때는 10쪽이 **그림만 있는 쪽**이 됐다 — 두 그림이 위·아래 띠를
다 먹어 본문이 한 줄도 못 들어간 것이다. 그림 2를 25% 낮추자 둘이 갈라졌다.

| | 그림 3만 줄였을 때 | 그림 2도 줄인 뒤 |
|---|---|---|
| 그림 2 | 10쪽 y=333 (위 띠) | **9쪽 y=592** (본문 아래) |
| 그림 3 | 10쪽 y=609 (아래 띠) | **10쪽 y=610** (본문 아래) |
| 본문 없는 쪽 | 10, 12 | **12만** |
| 결론 / 사사 | 11쪽 y=185 / y=509 | 11쪽 y=161 / y=485 |

**★그림 2는 자기 인용과 같은 쪽(9쪽)에 있고, 바로 위 줄이
`...general property of spatial reallocation.` 로 문장이 끝난다** — 요청대로
"관련 문단이 끝난 뒤"에 놓였다.

## 확인한 것 둘 (실측)

1. **`\floatpagefraction` 은 원인이 아니었다** — 0.75 → 0.95 → 0.99 로 올려도
   배치가 그대로였다. 10쪽은 float page 가 아니라 두 그림이 위·아래 띠를 다 먹은
   상태였다.
2. **본문을 줄여도 그림 3은 안 움직인다** — §5.3 을 1·2·3단계로 줄여 빌드했지만
   그림 3이 같은 자리(10쪽 y=601~610, 문장 중간)에 앉는다. §5.3 이 이제 **한 문단**
   이라 그 안에 문단 경계가 없고, 쪽 나눔이 마침 문장 끝에 떨어져야 하는데 그건
   글자 수로 맞히는 일이다. → 내용을 깎아 봐야 얻는 게 없어 본문은 그대로 뒀다.

## 최종 배치

| 그림 | 위치 | 절 |
|---|---|---|
| 그림 1 (아키텍처) | 4쪽 아래 | §3 |
| 그림 2 (분해) | 9쪽 아래 — 인용과 같은 쪽, 문단 끝 뒤 | §5 |
| 그림 3 (학습곡선) | 10쪽 아래 | §5 |

결론 11쪽 y=161 · 사사 11쪽 y=485 — 세 그림 모두 그보다 앞.

## 4. 남아 있던 들여쓰기 세 곳 제거

사용자 물음: *"각 문단에 줄바꿈 되있는거 없애서 바로 연결되게 작성하면 괜찮지 않아?"*

원고의 줄바꿈은 LaTeX 이 공백으로 처리하므로 출력과 무관하다. 실제로 눈에 보이는
문단 나눔은 **들여쓰기**로만 드러나므로, PDF 를 글자 위치로 훑어
(`scratchpad/indentscan.py`) 본문 왼쪽 기준선(x=134.8)보다 들어간 줄을 찾았다.

| 쪽 | 줄 | 원인 |
|---|---|---|
| 6 | `The total cost over [d,d+1) is the sum of four terms` | 표 1 뒤 |
| 8 | `The learned policy costs less than the rule with...` | 표 2 뒤 |
| 10 | `The first-iteration model cost KRW 9.20 billion...` | 그림 3 뒤 |

**원인은 부동체 뒤가 아니라 앞의 빈 줄이었다.** LaTeX 에서 문단을 끊는 것은 빈
줄이므로, 부동체 **앞**에 빈 줄이 있으면 앞 문단이 거기서 끝나고 부동체 뒤의 글이
새 문단이 되어 들여쓰기가 붙는다. 뒤의 빈 줄만 지웠을 때는 그대로였고(실측),
앞의 빈 줄을 지우니 *"본문 + 부동체 + 본문"* 이 한 문단이 됐다.

앞뒤가 모두 본문인 **세 곳만** 처리했다. 절 머리말 바로 뒤의 그림 1과 절 경계의
그림 2는 이을 문단이 없어 그대로 뒀다.

**결과**: 본문 들여쓰기 **0**. 기준선보다 들어간 줄은 저자 이름(가운데 정렬)과
수식 번호(오른쪽 끝) 여섯뿐이고, 배치는 하나도 안 변했다.

## 검증

12쪽 · A4 · Overfull 0 · Underfull 0 · 오류 0 · 미정의 참조 0 · 인용순서 두 축 통과 ·
수치 41/41 일치 · 여백 16% 초과는 마지막 쪽 하나뿐 · escape 이상 없음 ·
절 안 문단 나눔 **0**.

## Evidence

`2eb47c6` (문단 병합·그림 3) · `641abf1` (그림 2) · `9e866d2` (들여쓰기 제거)
