# YR-149 — 견적·평가 정렬 → 파급 원인 진단 → 이송 효과 확증

- **Epic**: RL / **Priority**: 🔴 / **등록일**: 2026-08-04 / **범위 정정**: 2026-08-05
- **상태**: **done (2026-08-05 — 5셀 데이터 자격 통과, 판정 `7bb20c1`)**
  - 완료 범위 = 사용자 지정 5항(5셀 전용 생성기·중첩추출·동일성 확인·전 경로 assert·
    혼잡 상태 분류). **2~4단계(1축 보정·3군 파일럿·독립 확증)는 미실행 → YR-152 분리.**
  - **구조 지위(2026-08-05 결정)**: 이 5셀은 4h 유입→2h 비우기라 **유한 물량 처리·회복
    시험**이다. 본 성능시험은 **지속 유입 정상상태 시험(YR-150 재정의)** 이 담당한다.
  - **2026-08-06 터미널 단위 정정**: 이력의 `A50~A150+B50`은 실제 2블록 입력이며
    `50·75·100·125·150=터미널 전체`가 아니다. 수치와 판정은 이력 보존하고, 21블록
    terminal-wide 성능이나 혼잡등급으로 소급 해석하지 않는다.
  - 아래 1단계 본문의 "보정 축 = 본선 항 추가" 결론은 **28차 검산으로 철회**됐다
    (집계 반전·두 집계 열람 → 축 미확정). 이력으로만 보존한다.
- **선행 근거**: YR-133은 2블록·반입·SF 실행정책에서 견적 교환과 시뮬레이터 내부
  원자 이송을 기능 실증했다. 이송 14건·완주 전판·KEEP 이송 0이었지만, 8개 에피소드
  쌍의 `QUOTE−KEEP` 총비용은 `−15.58~+19.11`로 흔들려 비용효과는 미확증이다.

## 연구 범위

- 외부 TOS는 **최초 블록 배정이 이미 끝났다는 배경·입력 스냅샷**으로만 둔다.
- 실제 TOS/ECS API, ACK/NACK, route lock 동기화, 운영 배포 가능성은 구현·평가하지 않는다.
- `TransferResolver`가 설정된 허용 블록 안에서 시뮬레이터 owner·queue·event·시간장부를
  원자 변경하는 연구 추상만 사용한다.
- 1차 효과 주장은 SF-SPT 실행정책·2블록·반입 조건으로 한정한다. **후속 사용자 결정**으로
  계산견적 우월 자체는 PPO SELL의 선결에서 제외했다. YR-149는 정보시점·5셀 데이터·Q 기준선을
  고정하고, PPO 성능은 YR-151에서 KEEP·동일시점 Q30과 별도 판정한다.

## 선결 정렬 — 결과 열람 전 고정

1. 견적과 실현 주지표를 YR-136의 **v2 실현 총비용**으로 통일하고 v1은 참고로만 보고한다.
2. YR-109의 달성 가능한 본선 마감을 켠다. 물리적으로 불가능한 `deadline_mult=0.5`를
   효과 판정에 쓰지 않는다.
3. `0.107`은 기능 발화를 위한 보정값으로만 보존한다. `κ_T=383.7초`는 잔차 1σ가 아니므로,
   최종 여유폭은 미열람 자료의 `예측 NetGain−실현 한계효과` 오차에서 별도로 동결한다.
4. source 이름순 첫 양수 선택이 아니라 같은 epoch의 허용 제안 중 최대 NetGain을 고르고,
   quote 발행 version과 commit version 일치를 직접 검사한다.

## 단계 — 각 단계는 별도 동결

### 1. 결정시점 반사실 진단

- YR-133의 이송 결정시점 상태를 복제해 같은 난수로 `KEEP`과 **그 이송 1건만** 각각 실행한다.
- 이후 이송은 끄거나 같은 행동으로 고정해 해당 결정의 효과를 분리한다.
- `truck / vessel / move / other`, source 기존 작업 이득, receiver 기존 작업 손실,
  완주·backlog를 나눠 기록한다.
- 14건은 진단 상태 수이지 독립 통계표본 14개가 아니다. 같은 에피소드의 여러 결정은 묶고,
  최종 통계 단위는 에피소드 seed 쌍으로 둔다.
- 사후 실현정보는 진단 라벨에만 쓰며 배포 견적 입력에는 넣지 않는다.

### ★27차 감사 정정 (2026-08-05) — 1단계 판정 하향·정정 재실행 동결

- **계약 불일치 2건**: 1단계가 v1 비용(본선 가중 33 — 계약은 v2 가중 10)·달성 불가능
  마감(ACHIEVABLE=False) 환경에서 수행됨 — 선결 정렬 위반. 가중 환산만으로도 본선 잔차
  ~0.20 으로 소스(0.54)보다 작아질 수 있어 **지배 축 미확정**.
- **판정 안정성**: 통계 단위 6 에피소드·leave-one-out 시 본선 1위 3/6·수신 2/6·소스
  1/6 — **허용 표현 = "구 v1·불가능 마감 환경 선택 표본에서 동결 규칙상 본선 잔차
  잠정 1위"까지**. "본선 주범 확정"·"수신 과소 가설 일반 기각"·"v2 에서도 본선 항이
  최선" 전부 금지. 14건 v2 효과는 유리 7·불리 7 — 성능 개선 증명 아님(진단 단계).
- **compare_completion_cost 는 완성품 아님**: 반입 이송→크레인 부하→적하 완료시각→v2
  본선 비용 연결 사슬 + v2 계약(가중 10·κ≈920s·편향≈5,214s) 별도 계산 계약 필요
  (현 기본값은 구 계약 33·κ300).
- **정정 재실행(동결)**: ①달성 가능 마감 켬 ②소스·수신·본선 전 축 v2 분해 ③이동비용
  route/3600 을 v2 총비용에 명시 가산 ④각 런 완주·backlog·정책 예외 저장+하드 검사
  ⑤구 14건은 개발 이력 보존 — 정렬 환경 **새 표본**(신규 대역 906100+·현행 resolver =
  최대 NetGain·version 검사) ⑥**2단계 진행 조건 = 재실행에서 지배 축이 평균 1위 ∧
  LOO 과반 1위(안정 1위)** — 아니면 보류. 2단계 비교군은 KEEP / BASE_ALIGNED /
  VESSEL_REFINED (마지막 두 군은 본선 항 하나만 차이).

### 1단계 정정 재실행 결과 (2026-08-05 — 27차 계약 환경·박제)

- 정렬 환경(달성 가능 마감·v2 분해·route 포함·새 표본 906100+): 이송 17건·8에피소드
  전부 발화, 리플레이 재현 실패 0·정책 예외 0. 저장된 base·prefix 런의 backlog는 0이나,
  전 경로 `backlog==0` assert는 구현되지 않아 **하드 검사 통과 표현은 철회**한다.
- **축별 v2 잔차 (★31차 집계 명칭 정정)**: 27차가 "이송 1건 평균"이라 부른 값
  (소스 +0.351 / 수신 +0.199 / 본선 −0.615·LOO 6/8)은 실제로 **에피소드 평균을 다시
  평균한 매크로 집계**였다. **진짜 이송 1건 단위(micro·전 결정 pooled)는 소스 +0.567 /
  수신 +0.011 / 본선 +0.046 으로 소스 지배(LOO 8/8)**, 누적합도 소스 지배(+1.204·8/8).
  ⇒ **"1건 평균은 본선, 누적합은 소스"라는 집계 반전 서사는 철회**한다.
- 구 환경의 본선 악화 부호는 정렬 환경에서 재현되지 않았다. 마감·비용계약·시드·선택된
  이송이 함께 바뀌었으므로 “불가능 마감이 부호 반전의 원인”이라는 인과 표현은 금지한다.

### ★28차 독립 검산 정정 — 주판정 집계·2단계 축 재개방

- 최종 목표인 터미널 에피소드 총비용처럼 한 에피소드 안 이송 잔차를 **합산**하면 평균이
  소스 `+1.204`, 수신 `+0.024`, 본선 `+0.097`이고 LOO 승자는 **소스 8/8**이다.
- 따라서 27차 결과는 “평균 이송 1건의 견적오차에서는 본선이 컸다”로만 허용한다.
  “터미널 누적오차의 지배축=본선”·“본선 항이 자동 다음 수”는 철회한다.
- 실제 v2 한계효과는 17건 중 개선 6·악화 11, 에피소드 사슬은 개선 4·악화 4이므로
  “좋은 이송을 과소평가한다” 또는 “본선 항을 넣으면 절감된다”는 아직 주장할 수 없다.
- 2단계 전에 ①에피소드 누적합을 주판정 ②이송 1건 평균을 보조 교정지표로 동결하고,
  ③모든 replay 경로의 backlog·정책예외를 assert한 뒤 단일 보정축을 다시 고른다.

- 사슬 한계효과 리플레이 20런 완주(리플레이 실패 0 — 재현 계약 성립). 14건·6에피소드.
- **에피소드 묶음 평균 잔차: 소스 +0.541 / 수신 −0.509 / 본선 +0.657(중앙 +1.28)**
  → **지배 축 = 본선(vessel)** (동결 규칙 |평균| 최대). 총 잔차 +0.588(실현이 예측보다
  악화 방향).
- 해석: ①견적에 본선 항이 전무한 것이 최대 누락 — 이송이 본선 비용을 예측 밖에서
  움직임(에피소드별 −5.07~+3.81 요동) ②소스 이득은 예측보다 작게 실현(과대 견적 경향)
  ③수신 부담은 예측보다 작게 실현(보수적 — 사전 예상과 반대 방향, "수신 파급 과소"
  가설 기각). 에피소드 간 부호 요동이 커서 축 판정은 동결 규칙 산출로만 채택.
- ~~**2단계 보정 축 = 본선 항 추가**~~ — **철회(28차)**: 에피소드 누적합 집계에서는 소스
  8/8 로 뒤집혔고, 두 집계를 모두 열람했으므로 이 표본에서 축을 확정하지 않는다.
  축 선정은 미열람 자료에서 수행하며 잔여 단계는 YR-152 가 승계한다.

### ★28차 정정 이행·5셀 자격시험 동결 (2026-08-05 — 실행 전)

- **집계 계약 동결(코드 반영)**: 진단 산출물이 **①에피소드 누적합 = 주판정**(터미널 총비용과
  같은 단위) **②이송 1건 평균 = 보조 교정지표** 를 각각 별도 블록으로 보고한다. 두 집계
  모두 이미 열람된 906100 표본에서는 **보정축을 확정하지 않는다** — 축 확정은 미열람 5셀
  자료에서 수행(사후 선택 회피).
- **assert 이행**: 모든 replay 경로(base·prefix 전 j)에서 `backlog==0`·`정책예외==0` 을
  assert. 구판은 저장만 했으므로 "하드 검사 통과" 표현이 성립하지 않았다(27차 지적 반영).
- **5셀 전용 생성기(동결)**: 트럭 수를 바꿔 재생성하면 도착식 `horizon×(i+u)/n` 과 공유
  난수열 탓에 초기 적재부터 전 궤적이 달라진다(= spec 이 금지한 가짜 pairing). 그래서
  **A150 master 를 한 번만 생성하고 외부트럭만 150→125→100→75→50 균등 솎기**해
  `A50 ⊂ A75 ⊂ A100 ⊂ A125 ⊂ A150` 을 만든다. 컨테이너·초기 적재·본선·B 는 master 그대로.
- **동결 template**: 두 블록 동일 — 본선 2×15 move·장치율 0.30·도착 피크·게이트 210s·
  `gaussian=False`(트럭 수 정확 고정)·시간계약 v2·게이트→블록 계약·달성 가능 마감.
  **★29차 정정**: "A/B 의 유일 차이 = 트럭 수"는 **과한 표현이라 철회**한다. A 와 B 는
  **서로 다른 seed** 를 쓰므로 생성 파라미터(template)만 같고 초기 적재·본선 실현에는
  무작위 차이가 있다. 정확한 표현은 **"각 seed 쌍의 5개 부하 셀 사이에서 A 의 트럭 수만
  바뀌고 B 는 동일하다"**. 계약 테스트 8종은 이 셀 간 불변성을 고정한다.
- **자격 하드 검사 8종**: 중첩·유지 작업 필드 바이트 동일·B 5셀 동일·초기 적재 동일·
  본선 지문 동일·정확 개수·정책 예외 0·장부 항등(A 등록 수 = m + 50).
- **원자료**: 시간가중 재공량(A≤t<O)·블록 점유·결정시점 최대 대기·평균 A→O·YC 가동률·
  처리량·4h/6h 잔여·비움시각. **용량상태는 결과 후 기계 분류**(CLEAR/BUSY/OVERLOADED) —
  50~150 이라는 숫자로 혼잡을 미리 선언하지 않는다.
- 표본: master 4쌍(시드뱅크 907000+·지문 독립성 검사) × 5셀 = 20런, SF-SPT·이송 없음.

### 2. 누락항 하나만 보정

- 1단계 전에 판정 규칙을 고정하고, 괴리를 가장 많이 설명한 `source / receiver / vessel`
  파급 중 **한 축만** 선택한다.
- 현재처럼 receiver 기존 트럭 지연을 결과 전에 원인으로 확정하지 않는다.
- 목표식은 다음 양측 한계비용 계약을 유지한다.

```text
OutRelief(A,j) = J_A(KEEP j) - J_A(REMOVE j)
InBurden(B,j)  = J_B(ADD j) - J_B(NO ADD)
NetGain        = OutRelief - InBurden - TransferCost - uncertainty_margin
```

### 3. 보정 파일럿

- 비교군은 `KEEP / OLD_QUOTE / REFINED_QUOTE`이며 실행정책·시나리오·본선 가드·이송 상한은
  동일하게 둔다.
- **사용자 부하 5셀(2026-08-05)**: 4시간 유입·2시간 배출, 블록당 2YC를 고정하고
  `A/B=50/50·75/50·100/50·125/50·150/50`으로 소스 A의 유입만 단계 상승시킨다.
  숫자는 블록별 4시간 유입 작업 수이며 터미널 합계는 `100·125·150·175·200`이다.
  트럭 수는 `gaussian=False`로 정확히 고정하고, 블록별 본선 process 2개×15 move
  (두 블록 합계 4 process·60 move)·장치율 0.30·도착 피크·물리·비용계약은 모두 동일하게
  둔다. **A/B의 본선 일정·마감도 같은 동결 template**을
  사용해 트럭 부하 외 차이를 없앤다. 150대 master trace의 결정론적 층화 부분집합으로
  50/75/100/125를 만들어, 같은 정수 seed만 쓰고 RNG 흐름이 달라지는 가짜 pairing을 금지한다.
- **정책 비교 전 데이터 자격시험을 반드시 실행한다**: A150에서 A50/75/100/125의 job-id
  중첩, B50의 작업·ETA·실현값 5셀 byte 동일, 정책군별 roster 동일을 직접 검사한다. 현
  realization hash만 믿지 않고 초기 container layout, 본선 start/planned completion/ETD/moves/
  cadence/physical-minimum, deadline·fill·flow비율을 포함한 config digest를 각각 assert한다.
  시간가중 `A≤t<O` WIP,
  queue 평균/최대, 평균 A→O, YC busy율, 처리량, 4h/6h backlog, drain-to-empty를 원자료로 남긴다.
- 부하 이름은 입력 셀일 뿐 실제 혼잡 판정이 아니다. 결과 뒤 `CLEAR`(4h·6h backlog 0),
  `BUSY`(4h backlog>0·6h backlog 0), `OVERLOADED`(6h backlog>0 또는 사전 drain cap 초과)로
  기계 분류한다. 즉 50·75·100·125·150이라는 숫자만으로 양호·보통·혼잡을 미리 선언하지 않는다.
- 기능 가드와 함께 실제로 서로 다른 결정을 충분히 냈는지 `NO_EXPOSURE`를 검사한다.
- 운영상 최소 의미 절감폭 `δ`는 결과 전에 비용계약에서 정하고, 파일럿은 분산·최소검출효과
  (MDE)·필요 확증 표본 수만 계산한다.

### 4. 독립 확증

- 파일럿과 겹치지 않는 신규 시드뱅크를 사용하고 top-up은 금지한다.
- 표본 단위는 에피소드 seed 쌍, 주판정은 v2 실현 총비용의 짝지은 차이다.
- 같은 master seed의 5셀을 한 cluster로 함께 재표집하고 5셀 동시 신뢰구간 또는 다중검정
  보정을 쓴다. 각 부하 셀은 합쳐 평균내지 않는다. raw 총비용과 함께 작업당 비용,
  A→O, 본선 척당 비용, 처리량/YC시간, 이송·route 비용을 보고한다.
- 모든 셀에서 미완·backlog를 누락하지 않는 장부, 정책 예외 0, epoch당 이송 1건, 작업당 1회,
  owner·version·rollback 연속성, route 비용 포함, 미래정보 미열람을 하드 가드로 둔다.
  비용판정 가능 셀에만 완주 100%·최종 backlog 0을 추가 요구한다.
- 트럭 평균 A→O와 본선 초과는 사전 허용손실 안이어야 한다. P95는 보고만 한다.
- 어느 셀이든 후보만 미완이면 후보 기각, KEEP만 미완이면 완주 우위만 주장하고 비용 비교는
  보류, 모두 미완이면 **현재 2YC 용량 초과 셀**로 분류해 처리량·backlog·drain-to-empty만 본다.
- YR-149는 SF-SPT 위 계산견적 기준선과 데이터 자격을 검증한다. PPO SELL 성능 주장은
  [YR-151](YR-151-block-ppo-sell-head.md)에서만 한다.

## 허용 주장과 금지 주장

- 확증 전 허용: “견적·원자 이송의 시뮬레이터 기능 경로가 발화했다.”
- 확증 전 금지: “블록 간 이송이 총비용을 줄였다”, “0.107이 최적 여유폭이다.”
- 확증 후에도 실제 TOS 연동·현장 배포 가능성은 주장하지 않는다.

## 참조

- YR-133: `648d0c9`(동결)·`b9b8fd9`(기능 보정)·`76c62d1`(판정)
- [보고서](../../../outputs/reports/yr133_sell_quote/report_pilot.md) ·
  [TOS 연동 제외 결정](../strategy-history/2026-08-05-YR-133-TOS-연동-범위-제외-사용자결정.md)
- [PPO SELL·5부하 데이터 사용자 결정](../strategy-history/2026-08-05-YR-149-151-PPO-SELL-혼잡5셀-사용자결정.md)
