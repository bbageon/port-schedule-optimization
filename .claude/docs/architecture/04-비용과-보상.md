# 04 — 비용과 보상: 평가 잣대와 학습 잣대

> **확인일 2026-08-20** · 코드: `integrated/cost_curve_v2.py` ·
> `experiments/yr151_transfer_ppo.py`(Φ) · `experiments/yr174_txn_reward.py`(보상)

## 0. 잣대가 둘이고, **시야가 다르다**

| | 무엇 | 시야 |
|---|---|---|
| **Φ** | 평가 잣대 — 터미널 하루 총비용 | **24시간** |
| **`D_i`** | 학습 잣대 — 그 거래 하나의 공로 | **약 40분** |

**이 불일치가 현재 연구의 핵심 문제**다 → §4

## 1. Φ — 평가 잣대

```
Φ(t) = Σ블록 (트럭 비용 + 본선 비용)  +  주행/3600  +  기사 외부 대기/3600
```

### 트럭 (softplus 확률 지연 · YR-136 v2.1)

```
J_T(Ô) = (Ô − A)/3600  +  (κ_T/3600) · softplus((Ô − D_T)/κ_T)
          └ 체류 시간      └ SLA 초과 **확률** × 벌점
```

**계단이 아니라 softplus 인 이유**: 마감 직전인 작업은 이미 "넘길 위험"을 안고
있는데, `max(0, x)` 는 그걸 결정 시점에 안 보여준다. 그리고 **미분 가능**하다.

### 본선

```
J_V(F̂) = (10 · κ_V/3600) · softplus((F̂ − P)/κ_V)      ρ = 10
```

**본선 지연은 트럭의 10배**. κ 는 0단계에서 적합 후 `kappa_fit.json` 으로 동결 —
판정런 내 조정 금지.

### 단위

**비용시간(cost-hour)**. 원(KRW) 아님. `ρ=10` 은 사실상 임의 가중치이며, 원 단위
환산은 실데이터로 단가를 실측할 수 있을 때가 적기다.

## 2. `D_i` — 학습 잣대 (차분 신용)

```
공간:  D_i = R_src − B_dst − ΔC_route
시간:  D_i = relief − burden − defer_s/3600
```

| 항 | 계산 |
|---|---|
| `B_dst` 수신 부담 | 자기 비용 + **뒤차 지연**(그 시각 대기 수 × 점유시간) + **본선 압력**(ρ=10) |
| `R_src` 소스 절감 | 같은 형태로 src 에서 — "남았더라면"은 미관측이라 **실현 서비스 시간 대입**(가정) |
| `ΔC_route` | (게이트→dst) − (게이트→src) |

**대칭성**: 양쪽 모두 "점유시간 × 그때 대기 수 (+ 본선 몫)" 라는 **같은 근사**를
일부러 쓴다. 한쪽만 정밀하면 편향이 생긴다.

**학습 목표** = `−D_i / Q_SCALE` (Q_SCALE = 20.0). KEEP 은 표본이 아니라 **상수 0**.

## 3. ★독립 검증을 통과했다

| 방법 | 시간 판매 1건의 비용 |
|---|---|
| 비용식에서 **해석적 유도** ([[YR-171]] 2026-08-13) | **14.28 분** |
| 차분 신용 **실측** (2026-08-19) | **13.75 분** |

**완전히 다른 두 경로가 4% 안에서 만난다.** [[YR-174]] 분해식이 만들어진 뒤
처음 받은 외부 검증이다. 축 격차도 라벨 +0.2480 vs 모델 +0.2546 로 맞는다.

부수 정황: `Φ + 신용합` 이 회차마다 2,413~2,588 로 좁게 모인다(엄밀 검증은
[[YR-194]]).

## 4. ★40분 시야 — 무엇이 빠져 있나

`realized_credit` 이 세는 범위:

```python
occupy = job_done − service_start      # 크레인 점유
own    = gate_out − gate_in            # 그 트럭의 체류
blocked_dst = 그 순간 대기 트럭 수
```

**전부 "그 트럭 한 대의 방문"(약 40분) 안**이다. 에피소드는 24시간.

| 세는 것 | 안 세는 것 |
|---|---|
| 그 트럭의 체류 | 그 배치가 만들 **재취급** |
| 그 순간 뒤차 지연 | 이후 트럭들의 **추가 대기** |
| 그 순간 본선 압력 | 그 컨테이너를 **나중에 꺼낼 때**의 비용 |

**그리고 규칙(`sell_gain`)도 근시안이다** — "지금 순이득"만 본다. 따라서
**RL 에게 구조적 우위가 없다**: 같은 질문에 규칙은 공식, RL 은 근사로 답한다.

## 4-1. ★지평(H)과 할인율(γ)은 다르다

혼동하기 쉬워 못박아 둔다.

```
H = 1시간, γ 없음     ├──── 다 똑같이 100% ────┤ 딱 자름
                     0분                    60분   61분부터 0

γ = 0.99             ╲___ 매끄럽게 줄어듦 ___╲___
                     0분                            무한
```

| | 무엇 | 성격 |
|---|---|---|
| **H (지평)** | **얼마나 멀리 굴려볼까** | **계산 예산**의 문제 |
| **γ (할인율)** | **먼 미래를 얼마나 쳐줄까** | **가치관**의 문제 |

`H` 는 굴리기를 멈추는 지점이라 **비용이 직접 걸린다**(3시간 굴리면 3시간치 계산).
`γ` 는 계산 비용과 무관하다.

### 우리는 `γ` 를 쓰지 않는다

**비용이 실제로 발생한 시간이기 때문이다.** 트럭이 1시간 뒤에 10분 기다렸으면
그건 진짜 10분 손해다. 깎을 근거가 없다.

(`γ` 는 원래 무한히 이어지는 문제에서 값이 발산하지 않게 하려고 쓴다. 우리는
하루짜리 유한 문제라 필요가 없다. 크레인 정책도 `γ=1` 이다 — [[YR-139]] 계약:
"γ=1 (유한 에피소드 총비용 정합)".)

### 대신 딱 자르는 부작용이 있다

```
59분에 생긴 영향  →  100%
61분에 생긴 영향  →    0%
```

거의 같은 두 상황의 라벨이 크게 달라져 **학습이 어려워질 수 있다.**

**절충 후보**: 경계 근처를 선형으로 깎는다(예: 50~60분 구간). 근거는
*"60분에서 자르는 게 임의적이니 절벽을 없앤다"* 이지 *"먼 미래가 덜 중요하다"*
가 아니다. → [[YR-204]] 착수 시 결정.

### 지금 보상에는 둘 다 없다

정확히는 **`H` 가 "그 트럭의 체류"로 자동 결정된다** — 트럭이 나가면 계산이 끝난다
(약 40분). [[YR-204]] 는 그것을 **명시적 1시간**으로 바꾼다(사용자 결정 2026-08-20,
근거는 연산량: 반사실이 오더 1건당 발생하므로 `H` 가 곧 배수).

## 5. 왜 시야를 못 늘렸나 — 신용 배분

[[YR-170]] 에서 **하루 총비용**을 보상으로 줬다가 실패했다. 21블록이 같은 60초에
동시에 결정하는데 결과가 하나로 뭉쳐 나와 귀속이 안 됐다:

```
행동 귀속 분산 = 0.0000038   (사실상 0)
```

| | 신용 배분 | 시야 |
|---|---|---|
| 하루 총비용 보상 | ❌ | ✅ |
| **지금 (거래별)** | ✅ | ❌ 40분 |
| 목표 | ✅ | ✅ |

**둘 다 가진 설계**가 다음 단계의 문제다 → [[YR-204]] (학습 전용 장기 반사실
rollout) · [[YR-205]] (Seller·Buyer 학생모델) · 근거 [[YR-206]]

## 6. 빠져 있는 비용 항목

사용자가 제안한 범용 항만 비용 5항목과 대조(2026-08-19):

| 제안 | 우리 | 상태 |
|---|---|---|
| 차량 대기 | `(Ô−A)/3600` | ✅ |
| 작업 지연 | softplus SLA 초과 | ✅ (제안보다 정교) |
| 본선/STS 대기 | ρ=10 본선 마감 초과 | ✅ |
| 장비 이동 | `route_cost_s` — **트럭 주행만** | ⚠️ **크레인 이동 없음** |
| **재조작** | — | ❌ **없음** (엔진에 동작은 있으나 비용항 없음) |

**야드 배치 품질이 목적함수에 안 들어간다** — 3단 밑에 깔아놔도 비용이 같다.

## 검증

```bash
PYTHONPATH=src python -c "
from yard_rl.integrated.cost_curve_v2 import RHO_VESSEL_V2
from yard_rl.experiments.yr174_txn_reward import RET_SCALE
from yard_rl.experiments.yr189_q_train import Q_SCALE
print('rho',RHO_VESSEL_V2,'RET_SCALE',RET_SCALE,'Q_SCALE',Q_SCALE)"

grep -n "def phi_terminal" -A 12 src/yard_rl/experiments/yr151_transfer_ppo.py
grep -n "def realized_credit" -A 30 src/yard_rl/experiments/yr174_txn_reward.py
# 재조작 비용항이 없음을 확인
grep -c "rehandle" src/yard_rl/integrated/cost_curve_v2.py
```
