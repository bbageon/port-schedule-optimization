# 05 — 정보 경계: 무엇을 볼 수 있고 무엇을 못 보나

> **확인일 2026-08-20** · 코드: `integrated/time_sell.py`(`notified_gate_in`) ·
> `transfer_head.py`(특징) · `sell_gain.py`(규칙) · `buy_estimator.py`

## 0. 원칙

> **결정 시점에 존재하지 않는 값을 읽으면 반칙이다.**

미래 실현값을 보면 정책이 "이 트럭은 유난히 늦게 오겠군"을 알게 되는데, 실제
터미널은 그걸 모른다. 그러면 성능이 좋아 보여도 **실현 불가능한 성능**이다.

## 1. 정책이 볼 수 있는 것

| 종류 | 예 |
|---|---|
| **통지 시각** | `notified_gate_in(job)` — 공개 통지분만 |
| 현재 상태 | 블록 내부 대수 · 대기열 · 크레인 잔여 · 적치율 |
| 공개 ETA | `public_block_eta` — 통지 + 기대 주행 |
| 지도 정보 | `travelBaseSec`(거리÷속도) · 블록 간 거리 |
| 이력 | 이송 횟수 · 이연 횟수 |
| 본선 계획 | `planned_completion_s`(계획값) |

## 2. 정책이 **못 보는** 것

| 종류 | 왜 |
|---|---|
| `gateInTime` (아직 안 온 트럭) | **미래** |
| `travelSec` · `exitTravelSec` | 그 트럭이 실제 얼마 걸릴지는 **미래** |
| `blockInTime` · `jobDoneTime` · `gateOutTime` | 시뮬 **출력** |
| 본선 `actual_completion_s` | **미래** |
| 30분 창 밖의 트럭 | 아직 **통지 전** |
| 다른 블록의 이번 epoch 제안 | **동시 결정** — 구조적으로 불가 |

## 3. 학습 목표에는 실현값을 쓴다 — 이건 반칙이 아니다

```
입력(특징)  → 공개 정보만
목표(라벨)  → 실현값 사용 (D_i 는 실제로 일어난 일에서 계산)
```

**학습은 사후에 한다.** "그때 그렇게 했더니 이렇게 됐다"를 나중에 알려주는 것은
정상이다. 반칙은 **결정 시점에** 미래를 보는 것이다.

## 4. 감사 이력 — 실제로 새던 곳

| 시점 | 무엇 | 조치 |
|---|---|---|
| 2026-08-09 (감사 치명 6) | `block_features` 가 실현 `a_gate_in` 을 읽음 | `notified_gate_in` 으로 교체 |
| 2026-08-09 (감사 치명 1) | critic 입력을 학습 직전 zeros 로 덮어씀 | 결정 시점 저장으로 정정 |
| YR-173 | BUY 견적망 입력 | 공개 정보만 — 실현 미래값 **한 줄도 안 읽음** 명시 |

**규칙(`sell_gain`)도 같은 경계를 지킨다**: "통지된 gate-in · 공개 ETA · 현재 내부
대수 · 통지된 미래 도착. 미래 대기열 예측은 결정론 배수 + 통지 도착만으로."

→ **규칙과 학습이 같은 정보로 겨룬다.** 규칙은 오라클이 아니다.

## 5. ⚠️ 경계를 지키느라 **덜 준** 것

경계와 무관하게 **줄 수 있는데 안 주는** 정보가 있다. 이건 결함이다.

| 정보 | 결정 시점 관측 가능? | 정책에 있나 |
|---|---|---|
| **지금 몇 시인가** (`t/end`) | ✅ 당연히 | **❌ 없음** |
| **실제 대기 대수** (`_waiting_at`) | ✅ 공개 관측 | ❌ `inside` 로 대체 |
| **이 트럭의 예상 서비스 시각** | ✅ 크레인이 계산 중 | ❌ 없음 |
| 목적지 블록의 크레인 부하 | ✅ | ❌ (출발지만) |

**시각**은 구 critic 입력에 `min(1, t/end)` 로 있었으나 [[YR-189]] Q 전환에서
critic 을 없애며 **완전히 소멸**했다. 무대에 이중 피크가 있는데 정책이 시계가
없다.

**대기 대수**는 정의가 어긋난다:

```
정책이 보는 inside      : gate_in ≤ t < gate_out     (블록 안 전부)
보상이 쓰는 _waiting_at : block_arrival ≤ t < service_start  (줄 선 것만)
```

**채점은 "줄 선 대수"로 하면서 정책에는 "총 대수"를 준다.**

이것이 [[YR-199]] 잡음 하한(신호의 77.6%)의 일부를 설명할 수 있다 — 같은 특징
값인데 실제 상황이 다르면 라벨이 달라지고, 그건 "못 줄이는 잡음"으로 계측된다.

→ [[YR-198]] 이 이 축이며, **잡음 하한 재측정(20분·학습 불필요)으로 값어치를
선확인**할 수 있다.

## 6. 검증

```bash
# 실현값을 읽는 곳이 없는지 (정책 경로)
grep -rn "actual_gate_in\|actual_block_arrival\|actual_completion" \
  --include=*.py src/yard_rl/integrated/transfer_head.py \
                 src/yard_rl/integrated/sell_q.py \
                 src/yard_rl/integrated/sell_gain.py

# 통지 시각만 쓰는지
grep -n "notified_gate_in" src/yard_rl/integrated/transfer_head.py \
                           src/yard_rl/integrated/sell_q.py

# 정책 특징에 시각이 없음 (결함 확인)
grep -n "return \[inside" -A 2 src/yard_rl/integrated/transfer_head.py

# inside vs _waiting_at 정의 차이
grep -n "def block_inside" -A 6 src/yard_rl/integrated/sell_review.py
grep -n "def _waiting_at" -A 8 src/yard_rl/experiments/yr174_txn_reward.py
```
