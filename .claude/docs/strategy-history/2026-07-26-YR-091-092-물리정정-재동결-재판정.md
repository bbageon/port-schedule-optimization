# YR-091+092 물리 정정 — 비통과 크레인·초기 pile 규격 + 골든 재동결 + 헤드라인 재판정

> 외부감사 결함2·3 (2026-07-26). 순서 = 감사 권고: 각각 구현·시험 → 골든 1회 재동결 →
> SF·rollout 재판정 → 유지 시 YR-075-c. **문헌보정 시뮬(D5)·assumed 분포.**

## 1. YR-091 비통과 크레인 (감사 결함2)

- **idle/down 상시 장벽**: ReservationTable `_idle_pos` — 예약 없는 크레인의 현재 bay 를
  점-corridor 로 corridor_conflict 에 포함 (예약 중이면 corridor 가 대체·자기 위치는 제외).
  후보 mask·resolver·commit 3중 방어가 같은 소스라 자동 전파.
- **초기 분산**: 같은 담당구간 크레인을 id 순=물리 순 등간격 배치 (이전: 전원 service_bay_min
  동일 시작 = 간격 0). calibrated: 6.75/18.25.
- **순서·간격 불변식**: check_invariants 에 CRANE_ORDER_SWAP·CRANE_MIN_GAP — 매 이벤트 검사.
- 감사 재현(bay20 idle 관통 허용) → 이제 CRANE_INTERFERENCE 거부 (테스트 7).

## 2. YR-092 초기 pile 규격 (감사 결함3)

초기 배치 규격을 **pile 당 1회 추첨** — 실행 place() 규칙(빈 바닥 또는 같은 규격)과 동일
계약. `_validate` 에 PILE_SIZE_MIX 거부 추가. 12 seed 혼합 pile 0·용량검사 "blocker
동일규격" 가정 성립 (테스트 4). size_mix_ft40 은 pile 단위 혼합비로 재해석.

## 3. 골든 1회 재동결 (`b6e3eff`)

재동결 6파일(각 사유 주석): yr002 지문 3·yr048 eta 핀·stage0 스냅샷 3(완주 1.0 유지)·
golden_terminal(**사건 구조 n_events/결정수/완료수 불변 — 이동 기하만 변화** = 정정이 의도
범위만 건드림)·store_seam 해시. yr050 fixture 는 물리 정합(busy 좌/우 분배)으로 수정.
전체 스위트 **486 passed** (잔여 1 deselect = YR-058 기지 머신민감).

## 4. 헤드라인 재판정 (동일 셀·seed, 정정 전 수치와 짝 대조)

| 헤드라인 | 정정 전 | 정정 후 | 판정 |
|---|---|---|---|
| rollout ΔB−C (v2, 양 창) | −4.45 유의 | **−4.76/−4.39 유의** | ✅ 견고 유지 |
| rollout@3600 Δberth | −27.67 [−42.1,−13.3] | **−9.49 [−16.4,−2.6] 유의** | ⚠️ 유지·**1/3 축소** |
| rollout 총비용 절감 (v2) | +14.1/+22.0 유의 | **+9.7/+12.3 유의** | ✅ 유지·축소 |
| 현실형 vs 오라클 (v1) | −2.05 구분안됨 | **−4.64 [−9.9,+0.6] 구분안됨** | ✅ 유지 |
| **현실형@1800 Δberth vs SF** | −16.41 유의 | **−7.19 [−19.8,+5.4] 무의** | ❌ 유의성 상실 |

- **원인**: 물리 정정이 SF 기준선을 강화 — 초기 분산(커버리지)·균일 pile(병적 재조작 제거)로
  SF v2 총비용 91.2→84.9·berth 114.2→106.4. 이전 rollout 이득의 일부는 **결함 물리(동일 bay
  시작·혼합 pile)가 만든 인공 이득**이었음이 드러남 — 감사의 "정책 순위를 바꿀 수 있다" 예측 적중.
- **유지**: 트럭 블록처리(B−C) 우위·총비용 절감·현실형≈오라클(공개정보 충분성)은 새 물리에서도
  유의 유지. **YR-075-c 개방 조건 충족**(관련 헤드라인 = rollout>SF 유지).
- **약화(정직)**: 본선 이득 축소(@3600 −9.5 유의로 생존·@1800 무의)·**현실형@1800 본선 유의성
  상실** — 하이브리드의 "본선 위험 시 rollout" 근거는 **현실형@3600 재검 전까지 보류**.
  하이브리드 작업 등록 시 이 재검을 선결로 명시한다.

## 5. 남은 물리 한계 (주장 게이트)

bay 축 비통과만 모델(트롤리 축·동일 bay 간섭 상세 없음)·게이트/도로 외생·YT 총대수 모델 등
감사 "그 밖의 부족" 목록은 그대로 유효 — **"물리·안전 위반 0" 주장은 이번 정정 범위(관통·
혼합 pile·평가창)에 한정**해서만 성립. 원자료: [physfix report](../../outputs/reports/yr091_092_physfix/report.md).
