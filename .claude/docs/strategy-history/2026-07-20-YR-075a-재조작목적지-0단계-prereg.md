# YR-075-a 0단계 사전등록 — 재조작 목적지 오라클 천장 (2026-07-20)

> 실행 전 동결. 결과는 `## 실행 결과` 로 append. 사용자 승인 "이렇게 진행해줘".
> 설계 3단계(게이트형) 중 0단계 = **만들기 전에 상금이 있는지 먼저 잰다**
> (YR-070/072 규율). 헤드룸 ≈ 0 이면 "greedy 목적지 near-optimal" 박제·종료,
> 실재하면 1단계(목적지 후보 확장)·2단계(증류 통합) 진행.

## 1. 질문

방해 컨테이너를 치울 때 목적지를 정하는 현 고정 규칙(`find_slot` = 최근접·저더미
greedy, [stack.py:101])이 **미래를 아는 배치보다 유의하게 나쁜가.** 나쁘면 "어디에
놓을지"를 결정으로 열 가치(1·2단계)가 있고, 대등하면 greedy 가 이미 최적.

## 2. 오라클 (동결)

- 주입: 통합 엔진 `_plan` 의 재조작 목적지 선택([engine.py:394])에 opt-in 훅
  `slot_selector`. None(기본)이면 `find_slot` 그대로 = **골든 바이트 동일** (sim/
  stack.py 무수정 — 통합 레벨 주입).
- 오라클 = **미래 재조작 회피 배치**: 각 blocker 를, 후보 슬롯 중 (① 목적지 더미에
  이 blocker 보다 **먼저 반출**될 컨테이너 수 [= 새 미래 blocker 생성 위험] ②
  greedy 비용) 사전식 최소 슬롯에 놓는다. 미래 반출시각은 시나리오 jobs 에서
  전지적 추출(오라클 지위 — CTDE·평가 전용, 정책 아님). blocker 자신이 무예약이면
  반출시각 ∞ (예약된 컨테이너 위 적재를 최대 회피).

## 3. 조건·비교 (동결)

- 정책 고정 = **SF_SPT** (목적지 외 전 결정 동일 — 차이를 목적지로 격리).
- 지형: v2 × 고장치율 (재조작 밀도 확보 필수 — 기본 fill 0.30 은 재조작 희소).
  셀: fill {0.55, 0.70} × 부하 {mid, high}. seed **신규** 748000~748019 (수준·fill
  공유, 차이는 목적지 규칙뿐 — paired).
- arms: GREEDY(현 find_slot) · ORACLE(미래인지). paired bootstrap 10k(75168).

## 4. 판정 (동결)

- **헤드룸 지표**: paired Δtotal_cost(ORACLE−GREEDY) + **Δrehandle_count**(직접
  기전) + Δmean_wait. 절대차+CI 보고.
- **게이트**: 어느 셀이든 Δtotal CI 상한 < 0 (오라클 유의 개선) **또는** Δrehandle
  CI 상한 < 0 이면 **헤드룸 실재 → 1단계 진행 권고**. 전 셀에서 두 CI 모두 0 포함/
  양수면 **near-optimal 박제 → YR-075-a 종료**, 다음 축(전문가 반복 등)으로.
- guard: 완주 전 seed 1.0·backlog 0·결정론(오라클 2 seed 재실행 동일)·골든 불변
  (fill 0.30·selector None 시 기존 지문 불변 회귀).
- 의미: 문헌 보정 조건·오라클은 상한 근사(전지적 미래·per-blocker greedy — 진
  최적 아님, 헤드룸의 하한 추정). 실재 판정은 강함, 부재 판정은 "이 오라클로는
  못 찾음" 한정.

## 5. 하네스

- `integrated/engine.py` slot_selector 훅 · `integrated/rehandle_oracle.py`
  (오라클 selector + 미래 반출시각 추출) · `experiments/yr075a_ceiling.py`.
  출력 `outputs/reports/yr075a_ceiling/`. tests: 골든 불변·오라클 결정론·
  미래인지 배치 계약.

## 실행 결과 (2026-07-20 기록 — 원자료 `outputs/reports/yr075a_ceiling/`)

- 실행: `e624937`. 160 에피소드·결정론 OK·완주 전 셀 1.0·골든 불변 6 passed.
  구현 중 결함 1건: LEVEL import 가 torch 의존 모듈 경유 → 직접 정의로 정정.
- **게이트 통과 — actionable 헤드룸 실재, 단 최고 혼잡 셀에 집중**:

| 셀 | greedy 총비용 | oracle | Δtotal [CI] | 재조작 g→o | Δrehandle [CI] | 판정 |
|---|---|---|---|---|---|---|
| mid/f0.55 | 148.1 | 145.7 | −2.35 [−5.40, +0.58] | 63.9→61.2 | −2.7 [−3.7,−1.8] | 재조작만 |
| mid/f0.70 | 165.7 | 163.8 | −1.93 [−5.38, +1.49] | 81.7→78.5 | −3.25 | 재조작만 |
| high/f0.55 | 232.2 | 228.5 | −3.71 [−12.9, +6.2] | 83.2→79.4 | −3.8 | 재조작만 |
| **high/f0.70** | **287.6** | **263.5** | **−24.13 [−33.25, −15.82]** | 107.9→102.0 | −5.85 | **✅ actionable** |

- **해석**: 스모크의 단일-seed "오라클이 더 나쁨"은 대표성 없었음 — 20 seed 평균은
  전 셀 점추정 음수(오라클 우세). **재조작은 전 셀 유의 감소하지만, 총비용 개선은
  최고 혼잡(high·fill 70%)에서만 유의** (−24.1, CI 전부 <0, ~8%). 나머지 3셀은
  재조작이 줄어도 총비용은 대등 — greedy 근시안이 그 영역에선 이미 near-optimal.
- **기전**: fill 70%·고부하에선 더미가 높고 바쁨 → 방해물을 잘못 놓으면 연쇄 미래
  재조작이 커짐. 목적지 선택 가치는 **혼잡에 집중** (프로젝트 일관 서사 — 상금은
  혼잡에서, YR-054/073-b/077 정합). 한산·저밀도에선 어디 놓든 비슷.
- **판정: 0단계 통과 → 1단계(목적지 후보 확장) 진행 근거 성립.** 단 상금이 혼잡
  regime 국소이므로 1·2단계 학습·평가는 **고혼잡(high·fill 0.65~0.70) 셀 중심**으로
  설계해야 효율적. 한계: 오라클은 미래 blocked 최소화형(사전식)이라 총비용 직접
  최적화 아님 — 실제 상한은 더 클 수 있음(하한 추정).

## 1a단계 실행 결과 (2026-07-20 — 배포형 H1 vs 오라클, 원자료 `yr075b_deploy/`)

> 설계 refinement (사용자 "우선 진행해봐"): K-후보 RL 확장 전에, **관측정보만 쓰는
> 배포형 목적지 규칙 H1** 이 오라클 이득을 잡는지 먼저 확인 (`765f820`).

| 셀 | 정책 | greedy | H1 | oracle | H1 Δ [CI] | 오라클 대비 잡음율 |
|---|---|---|---|---|---|---|
| high/f0.65 | FT | 275.0 | 265.7 | 266.1 | −9.29 [−35.8, +17.0] ns | **104%** |
| high/f0.65 | SF | 267.6 | 250.6 | 251.1 | **−16.93 [−27.4, −6.7]** | 103% |
| high/f0.70 | FT | 279.1 | 251.9 | 251.9 | **−27.13 [−49.5, −7.3]** | **100%** |
| high/f0.70 | SF | 263.4 | 258.4 | 258.4 | −4.99 [−14.0, +4.6] ns | 100% |

- **핵심: H1(배포형)이 오라클 천장을 전 셀 ~100~104% 잡음** — 관측 ETA·마감만으로
  전지적 오라클과 동일 배치(고혼잡 FT·SF fill0.70 은 완전 동일값). ETA ±300s 노이즈가
  있어도 "반출 순서"는 보존돼 방해물 배치엔 지장 없음.
- **함의: RL K-후보 확장(1b)은 무의미** — H1 과 오라클 사이 격차가 0 이라 학습이
  메울 여지가 없다. 이득은 **규칙 교체(greedy→H1)만으로** 확보된다.
- 개선 유의성: **채택본(FT) fill0.70 −27.1 (~10%) 유의**·SF fill0.65 −16.9 유의.
  FT fill0.65 는 점추정 −9.3 우호나 고분산으로 미유의(20 seed 검정력 한계, 부호역전
  아님). 전 셀 점추정 음수·H1 이 더 나쁜 셀 없음·완주 100%.
- **자동 게이트 `H1_helps_FT=False`는 "전 FT 셀 유의" 엄격 기준 탓** (fill0.65 FT
  검정력 부족)이며, 게이트의 본래 목적(H1 이 오라클에 못 미치면 RL 필요)은 명확히
  답됨: **H1==오라클 → RL 불요.** 기계적 False→1b 는 오독이므로 따르지 않는다.
- **판정: H1 을 목적지 규칙으로 채택 권고 (RL 1b 생략).** 저혼잡 중립·고혼잡 유의
  개선·배포 가능·다운사이드 없음. 채택 = 배포 시 `slot_selector=deployable_future_
  selector` (opt-in·골든 불변). 채택 전환은 사용자 결정.

## 채택 박제 (2026-07-20, 사용자 승인 "박제하자")

- **채택본의 표준 재조작 목적지 규칙 = H1 (배포형 미래인지)** — `joint_distill.
  adopted_slot_selector()` = `deployable_future_selector`. 배포 시 sim.slot_selector
  로 설정. 미설정 기본은 greedy(골든 불변).
- **RL K-후보 확장(1b)은 채택 안 함** — H1==오라클로 학습 격차 0. YR-075-a 종료.
- 잔여(비차단): fill0.65 채택본 셀 검정력 부족(−9.3 미유의)은 필요 시 seed 확대로
  보강 가능하나, 70% 유의·전 셀 다운사이드 0 이라 채택에 무관.
- 계약 회귀: `tests/integrated/test_yr075a_destination.py` (기본 greedy 불변·H1
  결정론·먼저 반출 위 회피 3건).
- **반입 배치(YR-075-b)는 별개 미구현** — 생애주기 모델 선행, PoC 이후.
