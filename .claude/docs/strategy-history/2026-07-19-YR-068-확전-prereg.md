# YR-068 사전등록 — 차분 표적 QMIX 본 시나리오 확전 (2026-07-19)

> 실행 전 동결. 결과·해석은 `## 실행 결과` 절로 append (원문 수정 금지 — 규약 준수).
> 배경: [YR-013c](2026-07-19-YR-013c-차분표적QMIX-prereg.md) — 진단 소형(외부 16)에서
> G1 통과 (65.97, mixer 보정 −9.41 [−12.81,−5.86], 비-BC 신기록). 질문: **이 이득이
> 본 시나리오(외부 40)에서 재현되는가** — 규모 의존성 검증이 다음 관문.

## 1. 가설 H-068

승자 처방(차분 D 앵커 + mixer 팀 보정)은 신용 희석·교차항이라는 **규모 불변의
구조 문제**를 겨냥하므로, 본 시나리오에서도 TD-RL 최고(INDEP@2000-norm 80.51)를
유의하게 넘는다. (JR 68.26 초과는 상위 목표 — 별도 게이트.)

## 2. 처방 (동결) — 승자 구성 그대로, 재조정 없음

- 손실·구조: YR-013c 동결 그대로 — `L = Σ huber(Q_i, D_i) + 1.0·huber(Mixer(Q, g_norm),
  C_W_team)`, 창 2400s·1-step·target 망 없음·ddqn head·lr 1e-3·ε=1/√ep·λ_mix=1.0.
  **knob 재조정 금지** — 확전의 목적은 "승자 레시피의 전이" 검증이다.
- 변경은 규모 축뿐: 시나리오 외부 40·본선 2 (TerminalGenParams 기본 = YR-045/059 규격),
  seed 대역 train 530000~530149 (150ep)·val 540000~540007 (8)·test 550000~550059 (60)
  — **yr059_qmix_norm_ladder 와 동일 test 대역** (paired 재사용 조건).
- state_norm: YR-059 기계(P90)로 train 선두 5-seed 재적합·박제 (val/test 미접촉).
- 하네스: `experiments/yr068_scale_diff_qmix.py` (`run_yr068`) —
  run_diff_qmix_episode 재사용, 차이는 config·비교군 로딩.

## 3. 비교군 (동일 test 대역 550000~550059 기존 판정 행 재사용)

- **INDEP@2000** (norm ON, 80.51 — TD-RL 최고, G1 기준): `yr059_qmix_norm_ladder`
- **JOINT_ROLLOUT** (68.26 — 강 baseline, G2 기준): 〃
- QMIX@2000 (norm ON, 암묵 QMIX 실패군 — 서사 대조용): 〃
- **SF_SPT**: 이 대역에 기존 행 없음 → **신규 평가** (비학습·결정적 — 60 seed 실행,
  prereg 에 명시). paired bootstrap 10k (seed 75,168).

## 4. 사전 판정 기준

- **G1 (primary — 확전 재현)**: vs INDEP@2000(norm) paired Δtotal CI 상한 < 0.
- **G2 (관문)**: vs JOINT_ROLLOUT — 상한 < 0 이면 **RL 이 JR 초과 (트랙 최초)**,
  CI 0 포함이면 동급, 하한 > 0 이면 미달로 기록.
- **G3 (참고)**: vs SF_SPT — 규칙 대비 위치 박제.
- **guard**: 완료율 ≥ 0.95·swa ≥ 0.25·발산 서명 없음 (val 곡선).
- **실패 조건**: G1 미달 → 이득은 소형 진단 규모 특이 — 규모 의존성을 박제하고
  후속(YR-069 민감도·YR-066 차분 개량)으로 이관. knob 재조정으로 구제하지 않는다.
- 노이즈 주의: 단일 학습 궤적 — 판정은 CI·guard 축.
