# YR-061 사전등록 — 미완료 잔존 페널티 (학습 표적 전용) 판정 실험 (2026-07-18)

> 실행 전 동결. 결과·해석은 `## 실행 결과` 절로 append (원문 수정 금지 — 규약 준수).
> 배경: [단일 DQN 11조건 진단 판정](2026-07-18-단일DQN-11조건-진단-보상신용할당-판정.md).

## 1. 가설과 처방

- **진단**: 전 11조건에서 학습 후 SERVE 붕괴(0.04~0.09)·REPOSITION 퇴화. 원인 특정 =
  학습신호(총비용)가 미서비스·미완료를 과소처벌 — 에피소드가 시간 한도에서 끝나면 대기
  청구가 멈추고 미완료 벌점이 없어 "일 안 하는" 정책이 총비용을 게이밍한다. 평가 게이트
  (완료율·건전성)는 이를 탈락시키지만 **학습 중에는 신호로 들어가지 않는 불일치**.
- **가설 H-061**: 학습 표적에만 미완료 잔존 페널티를 넣으면 — 평가 지표·ledger·게이트
  불변 — 퇴화가 사라지고 SERVE·완료율이 회복된다.
- **처방**: `LearnerConfig.unserved_terminal_cost` (기본 0.0 = off). 에피소드 종료 시
  `페널티 × 미완료 job 수`를 종결 구간 비용에 가산(total_normalized 단위, cost_scale 로
  나눠 표적화) → 각 크레인의 말단 SMDP 표본이 자기 결정시점 할인으로 흡수, 부트스트랩
  사슬로 역전파. **checkpoint 선택도 같은 목적함수(val 실제비용 + 페널티×backlog)** 로 —
  게이밍된 지표로 정책을 뽑는 순환을 선택 단계에서도 끊는다.

## 2. 동결 설정 (실행 전 고정)

- 하네스: `src/yard_rl/experiments/yr061_reward_redesign.py` (`run_yr061`, full mode).
- arm = 페널티 사다리 **{0.0(control), 2.0, 5.0, 10.0}** — 소형 시나리오 에피소드 실제비용
  ~65-100·job당 ~4-6 기준으로 "완만→지배" 스펙트럼. control 은 기존 거동 재현.
- 학습: ddqn·lr 1e-3·cost_scale=train 5-seed BaselinePreference fit·γ 0.95·hidden 128 —
  진단이 "기본값 최선"으로 판정한 그대로. 전 arm 동일 초기화 seed(61,000)·동일 탐험
  rng(61,100)·동일 train seed 열 (paired).
- 시나리오: `TerminalGenParams(n_external=16, n_vessels=2)` (나머지 기본 — horizon 4h+drain
  2h·가우시안 변주·ETA ±300s). 소형 = 빠른 판정 루프 (진단과 같은 취지).
- seed 대역 (신규 600k — 기사용 0~250k·300k·400k·500k~560k 배제, config guard 강제):
  train 600000~600149 (150ep, 에피소드당 신규 시나리오), val 610000~610007 (8),
  test 620000~620019 (20). checkpoint 15ep 마다 val 평가, val 목적함수 최소 선택.
- 판정 지표: test 20-seed — 실제(비페널티) total_cost·완료율·backlog·serve_share·
  `serve_when_available`(swa, YR-044 건전성 문턱 0.25)·paired bootstrap (10k resamples,
  seed 75,061). 비교군: SF_SPT·FIFO (동일 후보·resolver·비용 config).
- 비목표: QMIX 경로(joint_sink)에는 페널티 미적용 — 별도 트랙(YR-013/059, 병행 세션).
  본선 λ 중립·전략 WAIT 기본 제외(YR-052) 등 기존 계약 그대로.

## 3. 사전 판정 기준

- **P1 (퇴화 해소, primary)**: 페널티 arm 중 ≥1개에서 test swa ≥ 0.25 **그리고** 평균
  완료율 ≥ 0.95. control 은 진단 재현(swa < 0.25 예상) — control 이 이미 건전하면
  "이 프로토콜에선 퇴화 미재현"으로 P1 판정 불가, 재설계.
- **P2 (성능, secondary)**: P1 통과 arm 의 실제 total_cost paired vs control — 문서화.
  퇴화 정책의 게이밍된 낮은 비용과 비교이므로 **P2 열세는 실패가 아니다** (완주 비용 vs
  미완주 비용). vs SF_SPT 가 실질 기준: 이기면 강한 성공, CI 겹침이면 "회복 성공·성능
  동급", 크게 열세면 "회복은 성공, 성능 추격 과제 잔존".
- **실패 조건**: 전 페널티 arm swa < 0.25 → "미완료 과소처벌이 퇴화 원인" 가설 기각,
  진단 문서의 되돌릴 조건 발동 (상태/후보 표현 재수사).
- 노이즈 주의: 소형 시나리오·단일 학습 궤적 — 수치 몇 단위 차이는 해석 금지, 정성
  패턴(swa·완료율·행동분포)이 1차 판정 축.

## 4. 계약 검증 (실행 전 통과 확인)

`tests/integrated/test_yr061_reward.py` 4건: 음수 거부·기본 off·checkpoint 직렬화
왕복·**페널티는 말단 창 표본만 증가시키고 평가 total_cost 는 불변** (backlog>0 보장
시나리오 drain 120s 실측 고정). 기존 dqn 계약 테스트 회귀 없음 (10 passed 확인).

## 실행 결과 (2026-07-18 기록 — phase 1, 원자료 `outputs/reports/yr061_reward/`)

- 실행: commit `1fae73e` 격리 worktree·clean tree, full mode, 1168s. 판정: **H-061 기각 —
  단, "페널티 무효"가 아니라 "전제 불성립"**. §3 실패 조건(전 arm swa<0.25)이 발동했으나
  그 경위가 결정적으로 다르다:
  1. **4 arm(pen0/2/5/10)의 val 곡선 10개 checkpoint·test 원자료가 바이트 단위 완전 동일**
     (pen0==pen10 JSON 동일 실측) — 페널티 항이 학습 전 구간에서 한 번도 발동하지 않았다.
  2. 이유: **미완료가 구조적으로 발생하지 않는다** — 전 arm·전 정책 완료율 1.000·backlog 0
     (val·test 전 seed). resolver 의 mandatory-우선 골격과 넉넉한 마감(6h)이 완료를 강제한다.
     진단 문서의 "미완료 방치 게이밍" 해석은 이 프로토콜에선 재현되지 않음 — **퇴화의 실체는
     방치가 아니라 지연**: control 대기 1.59분 vs SF_SPT 0.45분(3.5배), 결정 수 4.7배(공회전).
  3. control 은 진단 퇴화를 정식 하네스에서 정확히 재현: REPOSITION 61.0%·SERVE 9.1%·
     swa 0.094 (<0.25) — **재현 가능한 퇴화 벤치마크 확보** (하네스 정식화 목적 달성).
  4. 실제비용: 전 RL arm 70.11 vs SF_SPT 53.12 (Δ+16.99 [+13.59, +20.31]) — val 은
     94→70 으로 학습이 개선하지만 SF_SPT 대비 32% 열세인 지연·공회전 국소해에 수렴.
- **새 1차 용의자 = 할인 근시 (discount myopia)**: 학습 표적은 γ^(Δt/60s) 할인 —
  10분 뒤 비용 ×0.60, **1시간 뒤 ×0.046, 2시간 뒤 ×0.002 (사실상 소멸)**. 평가는 비할인
  총비용. "지금 SERVE = 이동·간섭 비용 즉시 청구" vs "회피 = 대기비용이 할인 지평 밖으로
  밀림" → **미루기가 학습 목적함수상 이득**. 관측 전부와 정합: 지연형 퇴화·완료는 유지·
  11조건 knob 무효(할인은 어느 knob 에도 없음)·cost_scale 무효(할인과 직교)·loss 정상 수렴.

## 5. 2차 사전등록 — 할인 정합(γ) 사다리 (2026-07-18, phase 2 실행 전 동결)

- **가설 H-061b**: 학습 할인을 평가 목적(비할인)에 정합시키면 지연 유인이 사라져 SERVE·
  대기가 회복된다.
- **동결 설정**: 하네스 동일(`--gamma`), arm = **γ ∈ {0.95(control), 0.99, 0.999, 1.0}**,
  ref_s 60 고정·페널티 0. 나머지 전부 phase 1 과 동일 (seed 대역·선택 규칙·비교군·
  bootstrap). control 은 phase 1 pen0 과 결정론 동일해야 함 (하네스 무결성 자체검증).
- **판정**: P1(swa ≥ 0.25 + 완료율 ≥ 0.95)·P2(vs SF_SPT) 는 §3 과 동일. 가설 특이 예측 —
  γ↑ 에 따라 swa·대기·실제비용 **단조 개선**, γ=1.0 최량. 전 arm 미회복이면 H-061b 도
  기각하고 신용할당 잔여 갈래(공유비용 균등귀속)로 재수사.

## 실행 결과 (2026-07-18 기록 — phase 2, 원자료 `outputs/reports/yr061_gamma/`)

- 실행: commit `a421ce0` 격리 worktree·clean tree. **H-061b 기각 — 할인 정합은 퇴화를
  해소하지 못한다.** 단조 개선 예측 정반대:

| arm | total_cost | swa | mean_wait(분) | vs control |
|---|---|---|---|---|
| g0.95 (control) | 70.11 | 0.094 | 1.59 | — (pen0 과 결정론 동일 — 무결성 ✓) |
| g0.99 | 79.06 | 0.068 | 2.87 | **+8.96 [+2.63, +14.85] 유의 악화** |
| g0.999 | 72.33 | 0.088 | 2.03 | +2.22 [−2.17, +6.57] |
| g1 (비할인) | 76.00 | 0.072 | 2.38 | **+5.90 [+1.30, +10.81] 유의 악화** |

- 전 arm swa < 0.25 (P1 전멸)·완료율 1.0 유지. γ↑ 는 val 곡선 불안정도 키운다
  (g0.99 ep30 104 스파이크·g0.999 말미 87 반등) — return 지평이 길어질수록 분산 증가,
  교과서적 trade-off 만 재확인.
- **주목 사실**: 전 arm·전 phase 에서 **학습 개시 직후 첫 checkpoint(ep15)부터 이미
  swa 0.09~0.10** — 미학습 0.45 에서 학습이 켜지는 순간 붕괴. 점진적 drift 가 아니라
  즉각적·계통적 편향이다.
- **종합**: 보상신호측 지렛대 2종(미완료 페널티·할인 정합)이 사전등록 기준으로 모두
  기각됐다. 남은 용의자 2갈래 — (i) **신용 희석**: 팀 공유 return(에피소드 잔여 총비용
  ~70)에 비해 행동 간 Q 차이가 미세해 함수근사 잡음에 묻힘, (ii) **표현/인코딩 결함**:
  후보 feature 가 "SERVE 우선" 순위 자체를 표현 못 함. 이 둘은 모방 검정으로 갈라진다.

## 6. 3차 사전등록 — 모방(behavior cloning) 이분 검정 (2026-07-18, phase 3 실행 전 동결)

- **질문**: 같은 Q-망·같은 인코딩이 SF_SPT 의 선택을 **지도학습으로는** 배울 수 있는가.
  배우면 → 표현력은 충분, 병목 = TD 학습신호(신용 희석) 로 특정. 못 배우면 → 인코딩/
  후보 feature 결함으로 특정. 어느 쪽이든 다음 지렛대가 확정된다.
- **동결 설정**: SF_SPT 를 train seed 열(600000~600149)에서 구동해 (인코딩, 선택 후보)
  쌍 수집 → 동일 CandidateQNet 을 cross-entropy(−Q 를 logit 으로, argmin Q = 모방)로
  지도학습 (Adam lr 1e-3·batch 128·30 epoch·seed 61,200). checkpoint 5 epoch 마다
  val 8-seed 실평가(비용) 선택. test 20-seed — phase 1/2 와 동일 대역·비교군·bootstrap.
- **판정**: **I1** = test swa ≥ 0.25 + 완료율 ≥ 0.95 (모방 성공 여부). **I2** = vs SF_SPT
  paired Δtotal (동급 여부 — CI 가 ±5 이내면 "표현력 충분" 강한 증거). I1 성공 시 병목 =
  TD 신호 확정 → 후속 = 신용 개별화(로컬/차분 보상)·모방 초기화 + RL 미세조정 등록.
  I1 실패 시 → 인코딩/후보 feature 재수사 (YR-031-b 집합 feature 교훈 재적용 검토).
