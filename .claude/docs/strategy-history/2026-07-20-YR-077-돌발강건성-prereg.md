# YR-077 사전등록 — 돌발(크레인 고장) 강건성 1단계 (2026-07-20)

> 실행 전 동결. 결과는 `## 실행 결과` 로 append. 사용자 승인 "응 진행해줘".
> 배경: FT 채택 전환 직후 — 전 실험이 돌발 0 세계였고, 채택본은 rollout 교사
> 증류라 예측 의존. 고장에서 이득 유지가 채택 유효성의 남은 관문 (YR-076 §5).

## 1. 조건 (동결)

- 주입: `TerminalGenParams.fault_outages` ∈ {0(대조), 1, 2} × 지속 900s(15분) —
  전용 스트림 `fault:{seed}` (기존 시나리오 draw 불변·기본 0 은 바이트 동일).
  크레인 정지 = 엔진 기존 EQUIPMENT_DOWN/UP 계약.
- 지형: v2 × calibrated mid/high. seed **신규**: mid 747000~747011·high
  747100~747111 (수준별 12, 셀 간 동일 seed — 차이는 주입뿐).
- arms: SF_SPT · STUDENT_FT(채택본 `student_ft.pt`). paired bootstrap 10k(75168).

## 2. 판정 (동결 — 진단형)

- **R1 (강건 유지)**: 전 주입 셀(수준×outage 1·2)에서 Δ평균대기(FT−SF) CI
  상한 < 0. 상한 ≥ 0 셀 존재 시 "부분 유지/붕괴" 로 셀별 정직 박제 — 구제 없음,
  후속(고장 포함 DAgger 재수집) 등록 제안만.
- guard: FT 완주율 ≥ SF −0.05 (동일 셀 상대)·정책단위 집계 mix 건강·결정론 1seed.
- 보고: outage 별 격차 곡선·완주율·P95. 문헌 보정 조건 한정.

## 3. 하네스

- `experiments/yr077_robust.py` (`run_yr077`) → `outputs/reports/yr077_robust/`.
- tests: 기본 0 주입 골든 불변·주입 결정론·이벤트 쌍 정합.

## 실행 결과 (2026-07-20 기록 — 원자료 `outputs/reports/yr077_robust/`)

- 실행: `c6228aa` + load_student 포맷 허용(ft 판) 정정. 144 에피소드·결정론 OK.
  실행 사고 1건: load_student 가 distill 포맷만 허용 → ft 채택본 로드 AssertionError,
  포맷 화이트리스트에 `yard-rl-joint-ft-v1` 추가.
- **R1 통과 (전 주입 셀) — 강건성 확인, 오히려 강도↑ 격차↑**: Δ평균대기(FT−SF)
  mid out1 **−1.84 [−2.37,−1.36]**·out2 **−2.07 [−2.75,−1.47]** / high out1
  **−3.80 [−4.80,−2.78]**·out2 **−4.09 [−5.78,−2.80]**. 무고장(out0)과 비교해
  **고장이 심할수록 이득이 커짐** (mid −1.57→−2.07·high −3.07→−4.09). 완주 전
  셀 1.0/1.0·mix 건강·결정론.
- **해석**: "rollout 교사 증류라 예측 깨지면 붕괴" 우려는 기각. 기전 = 크레인 한
  대가 15분 멈추면 남은 한 대의 순서 결정이 더 중요해지는데, 규칙(SF-SPT)은 그
  국면에서 대기를 더 흘리고 학습 정책은 계층목적(대기 우선)대로 남은 자원을 트럭에
  집중 → 혼잡할수록 벌어지던 무고장 격차(YR-073-b)와 동일 서사가 고장 국면에서
  재현. FT 는 단발 예측이 아니라 상태 조건부 우선순위를 배운 것이라 국소 예측
  붕괴에 강함.
- **채택 유효성 관문 종료**: 성능(YR-073-b)·품질(YR-074)·강건성(YR-077 1단계)
  3축 통과. 남은 강건성 축 = 장부 불일치(2단계, 미구현 모델)·노쇼(YR-019).
  문헌 보정 조건 한정·본선 보호 미검증(YR-041).
