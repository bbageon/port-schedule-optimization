# 📋 Ready (선택됨)

> 착수 준비된 작업 (착수 신호 대기). [index](README.md) · 인접: [backlog](backlog.md) → 여기 → [in-progress](in-progress.md).
> **설계감사 정정 (2026-07-27)**: 아래는 [감사 기록](../docs/strategy-history/2026-07-27-설계감사-검정력붕괴-오라클오표기.md)
> 반영본이다. 구 YR-105-b/c/d 는 **같은 지표·같은 n=8** 설계라 집행해도 같은 이유로 "무의"가
> 나오므로 **YR-106(지표·검정력 재설계) 이후로 보류**한다.

| ID | Epic | Title | Priority | Blocked by / Note |
|---|---|---|---|---|
| YR-106 | Exp | **판정 지표·검정력 재설계 — 모든 이송 축의 선결** | 🔴 | **감사 critical**: 주지표 총비용의 70%가 본선비용이고 짝지은 분산의 대부분(sd 18.75 vs 트럭 5.49)이 거기서 온다 → 총비용 유의판정에 시드 ≈97 필요한데 전 판정이 n=8. 실측상 트럭 채널은 −3.25 [−7.84,+1.35] 로 일관된 이득 방향인데 본선 잡음에 묻힌다. **처방**: ①주판정을 **채널 분해**(트럭측 Δ 를 1차 판정, 본선측·총비용은 병기) ②사전등록에 **MDE 계산 필수** ③확증 대역 n 을 선택 대비 ≥2배 ④"무의"와 "효과 없음"을 라벨 단계에서 분리. 이 뒤에야 YR-105-b/c/d 가 의미를 갖는다 |
| YR-107 | Exp | **JR1800 오라클 오표기 정정 + 영향 판정 재서술** | 🟠 | **감사 major**: `baselines.py:163` 의 rollout 은 sim 을 deepcopy 해 실제 엔진으로 진행 → 큐의 `actual_block_arrival`(진짜 미래)을 소비 = 프로젝트 자신이 "오라클·배포 금지"로 규정한 계열(`predictive_rollout.py:3-4`). 그런데 `yr100_candidate_eval.py:5` 는 "공개정보 rollout 참고군"으로 표기했고 `yr041_locked` 는 **배포 후보로 잠금평가**했다. 통과했다면 미래를 아는 정책을 채택할 뻔했다. 조치: 라벨·docstring 정정, YR-100-[3]·YR-041-a·README 서술 정정, 공개정보 rollout 은 predictive 경로로만 |
| YR-108 | Infra | **시드 대역 충돌 해소 — 실현 독립성 보장** | 🟠 | **감사 major**: 셀 BASE 간격(100) < 대역 offset 간격(200)이고 `dmult` 는 난수를 소비하지 않아, 다른 셀·다른 실험이 **같은 트럭 실현**을 공유한다. 실측: YR-041-a "신규 +900" 24표본 전부가 YR-098 확증대역과 동일 실현·18/24 는 YR-100-[3] 선택대역과 동일 / YR-098 자체도 확증 40쌍 중 20쌍이 선택과 동일. 조치: 실현 해시 기반 대역 배정 유틸 + 사전등록 시 중복 검사 강제 + 과거 판정 실현 독립성 재감사 |
| YR-109 | Sim | **시나리오 물리 정합 — 달성 불가능한 본선 마감 제거** | 🟠 | **감사 major(근본 원인)**: `vessel_deadline_mult=0.5` 는 계획완료를 물리 최소완료(start+moves·cadence)보다 **앞에** 두어 야드가 무한히 빨라도 반드시 초과한다(구조적 최소초과 33~38분 = 선석초과 합의 19~29%). 이것이 ①vessel_delay 를 총비용의 70%로 밀어올리고 ②`slack_s<0` 을 초기조건으로 만들어 YR-105-a 의 VGUARD 를 정보량 0 으로 퇴화시켰다. 조치: dmult 하한을 물리 달성가능선(≥1.0)으로 올리거나 구조적 최소초과를 비용에서 분리(정책 무관 상수 제거) |
| YR-110 | Infra | **guard 기계화 + 대조군 템플릿 표준화** | 🟡 | **감사 major/gap**: 평가계약이 0순위 하드 guard 로 정한 완주 1.0·backlog 0 이 최근 하네스에서 기록만 되고 게이트가 아니다(YR-103 은 수집조차 안 함). 또 "혼잡 관측 덕"과 "그냥 A→B 덕"을 가르는 **볼륨매칭·신호미사용 대조군**이 표준으로 없다(YR-105 에서 임시 구현). 조치: guard 검사 함수화 + 대조군 템플릿을 공용 모듈로 |
| YR-105-b/c/d | RL | (보류 — YR-106 이후 재설계) | ⚪ | 볼륨·타이밍(`C_HIGH`)·지속 혼잡·역가설. **구 근거는 철회됨**: 정본 YR-105-a 는 d_total/d_berth/교락통제 **전부 무의**이며 report 가 "방향 주장 안 함"을 명시했다. 구 ready 의 "볼륨이 지배 확정"·"반대 방향 반증"은 철회된 1차(오염판) 수치였다 |

---

운영: [backlog.md](backlog.md) 에서 승격. 착수 시 [in-progress.md](in-progress.md) 로 이동.
