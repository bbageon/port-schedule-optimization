# 🟢 In Progress

> 현재 진행 중. **한 번에 1개** 권장. [index](README.md) · 인접: [ready](ready.md) → 여기 → [done](done.md).

| ID | Epic | Title | Priority | 착수 | Note |
|---|---|---|---|---|---|
| YR-087 | RL | 증류 격차 해소 — 학생 관측 본선 feature 강화 / 룩어헤드 (YR-080 2b negative 파생) | 🟠 | 2026-07-22 | **착수 (사용자 결정 2026-07-22)**: YR-080 2b 진단 = 기준재 교사 배 보호가 feed-forward 학생으로 전이 안 됨(학생 berth 2~5배 악화·완주 실패·top1_disagree 0.167), 원인 = 교사 우위가 1800s rollout **미래**에 있는데 학생 관측은 현재 집계만·본선 그룹 rich feature(여유시간·위험·예상지연) 부재. **1단계 완료 — 관측 강화 가설 기각 (negative)**: ①진단 = encode_observation 이 본선 그룹 8필드(slack/risk/잔여작업/버퍼)를 통째 드롭·후보 훅 2개 죽음(vessel_risk_delta 항상 None·deadline_slack naive) 확인. ②v1(후보 훅 2개 관측값 교정, 214불변) → 개선 없음(희소·후행·후보한정). ③v2(본선 그룹 전역 인코딩 top-K, 214→246, use_vessel opt-in 하위호환) — 본선 블록 실신호 확인(slack −1344s·risk 0.75)에도 **개선 없음**. 3판본 학생 berth 교사 대비 2~4배 악화·완주 실패·top1_disagree ~0.16 동일. **결론: 병목은 관측 아님** — feed-forward 가 교사 1800s rollout 을 단일 forward 로 amortize 못하는 근본 한계(방법론 검증 강형 예측 적중). **train-fit 진단(2026-07-22, 사용자 결정)**: v2 학생이 **학습셋조차** 분기결정 재현 0.192(test 0.160·격차 +0.03) → **근본 확정: 관측별칭**(같은 관측에 교사 다른 선택 — rollout 이 의존하는 야드 스택/타이밍을 압축 관측이 미담). 데이터·과적합 아님 → **더 학습·관측·DAgger/FT 전부 무익**(라벨 불학습). **함의: feed-forward 증류는 구조적으로 막힘 · 배포정책은 룩어헤드(shallow rollout/beam) 유지 필요.** 관측 수정은 correct enrichment 로 보존(룩어헤드 학생용). **얕은/선택적 rollout 배포 탐색 (2026-07-22~23)**: SelectiveRolloutPolicy(본선 급할 때만 rollout) 구현. firm-up(10-seed 짝지은 CI): **순수@3600 rollout 은 SF 대비 출항지연(berth) −10~−22분 4/4 유의(B★)+트럭 P95 유의개선** = rollout 정책은 실효(구조적만 아님)·SF보다 나음. sts5@1800 은 SF 대비 무의(짧은 창 무익). **핵심 재구성: distillation 실패는 치명 아님 — 빠른 net 없이 rollout 직접 배포가 경로**(실터미널 분단위 결정 → ~1-2s/결정 무방). 선택적 firing 은 평균비용 절감(loose 셀 발동 0.4). **RL 전환 (사용자 결정 2026-07-23, YR-088)**: "지도학습은 새 환경 일반화 불가" → 중앙 공동정책을 **보상 기반 RL**(joint-combo Q-학습, 조합 하나=값 하나라 신용 희석 없음)로 학습. 반복 v1(퇴화)→v4(REPO 행동벌점=건전·SF하회, 트럭대기 전셀 우수)→v5(본선 선행신호 sts_wait). **1차(단일 run·점추정): healthy·완주 전 셀 + num SF 하회.** 정직 대조(학습 net vs 랜덤 net·동일 제약): **트럭대기 학습 net 대폭 우수(3.5 vs 10)=학습 실효**, 그러나 berth(본선)는 학습≈랜덤 → 본선 개선은 제약·shaping 몫(학습 불확실)·best_ep=20 반복(학습 얕음). **일반화 검증 (2026-07-23, 사용자 결정 '가', [결과](../../outputs/reports/yr088_generalize/results.json))**: 학습 RL zero-shot(재학습 0)으로 안배운 부하·마감 + **다른 터미널 PNIT·PNC** — **전 환경 healthy·완주 1.0·에러 0 + 트럭대기 6/6 새환경 SF 하회** → **RL 전제 입증**(지도학습 불가했던 구조적 일반화·학습 트럭 skill 전이). berth(본선)는 대부분 SF 하회 못함 → 얕은 본선학습 새환경서 확인. 미검: 다중 train seed·CI·**본선 학습 심화**. 커밋 `yr088_joint_rl.py`·`yr088_generalize.py`·rl_net.pt |

---

운영: 시작 시 [ready.md](ready.md) 에서 pull. 종료(commit) 시 [done.md](done.md) 로 이동 + commit 링크 박제.
