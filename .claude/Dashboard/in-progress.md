# 🟢 In Progress

> 현재 진행 중. **한 번에 1개** 권장. [index](README.md) · 인접: [ready](ready.md) → 여기 → [done](done.md).
>
> **사후 비용계약 변경(2026-07-30)**: YR-135의 현재 라벨은 계단형 트럭비용·본선 33의
> v1 계약이다. 결과는 V/A 구조 진단으로만 보존하며 새 정책 채택 근거로 승계하지 않는다.
> 구조가 통과하면 YR-136 점증비용 v2로 신규 라벨을 생성해야 한다.

| ID | Epic | Title | Priority | 착수 | Note |
|---|---|---|---|---|---|
| YR-235 | RL | **v3 감사 — 두 행위자의 시야를 맞춘다 (A안 채택)** | 🔴 | 2026-08-26 | **발단**: 사용자 물음 *"Buyer 가 보는 창은 Seller 와 같아야지, 그래야 Seller 가 제안한 시간대를 Buyer 가 검토하지 않나"*. 맞다. **감사 8건**(`probe/audit.py` 신설 · 기계 훑기 + 문서 대조): **A1** `Offer.slot_load` 를 아무도 안 채움(시간 offer 445건 전부 0.0) → ✅**고침** · **A2** Φ 네 항이 다른 창(항1 24h 검열 vs 항2/3/4 **26h 누적**, +1.0%, 본선유휴 +8.1%) — `04 §항1`·`phi.py` 규약 위반 · **A3** Buyer 가 `defer_s` 를 못 봄(15분/60분 제안이 **같은 행**) · **A4** `seller_action_features(slot_load)` 죽은 인자 · **A5** `TODO_ARMS=()` 인데 오류 메시지는 여전히 "YR-211" · **A6** `_space_coords(quay_of)` 죽은 인자 · **★A7 이연 격자가 H=1시간 시절 값**(최대 60분) — H 는 [[YR-217]] 에서 3시간이 됐는데 격자는 안 따라감. 넣을 수 있는 칸 **10~11개 중 4개만** 사용. 증거: 최대치(60분) 선택 비율 **82~97%** = 천장에 붙어 있음 · **★A8 시야 비대칭** — Seller 는 제안 시각을 **±30분**(도착 압력, [[YR-230]])으로 보는데 Buyer 는 **±7.5분**(`slot_load`)으로 본다. **⚠️ 블록의 미래 상태는 아무도 모른다** — `inside_count` 는 `gate_in≤t` 만 세므로 미래엔 0, 크레인 밀린 일은 음수. 미래에 대해 공개 정보로 아는 건 **"그 시각에 통지된 물량"** 뿐이다. **★채택안 A(사용자 결정)**: Buyer offer 특징에 `arrival_pressure`(±30분) 추가 **19→20칸**. 대칭이 맞고, **도착 압력이 "언제 떨어지나"를 담으므로 `defer_s` 를 따로 안 넣어도 된다(A3 해결)**. **묶어서 간다**: A8 + A7(격자를 150분 근처까지) + A4·A5·A6 정리 — 셋 다 재학습이 필요하다. **A2 는 따로** (라벨은 정상, 판정 수치만 걸림 → 배수 편향이 팔마다 다른지 재고 판단). **[[YR-234]] 6차 재학습을 3회차에서 중단**(잃은 시간 1h30m) — A7·A8 이 학습 전제를 바꾼다. [spec](../docs/dashboard-task-specs/YR-235-actor-symmetry-audit.md) |
| YR-233 | Exp | **최종 판정 — 새 대역 · 딱 한 번 (⏸ 6차 무대로 재설계)** | 🔴 | 2026-08-26 | **⏸ 실행 전 중단(2026-08-26)** — ①시드 대역 오류: `9,910,000` 은 `(s//100_000)*100_000 = 9,900,000` 이라 **비판정 대역 안**이었다(폭 10만). `check_bands()` 가 있는데 **내가 안 불렀다** → 실행 첫 줄에서 부르도록 고친다. 새 대역은 **9,400,000**. ②그 사이 **6차 도착 곡선**으로 무대가 바뀌어 정책·판정을 다시 만들어야 한다. 재설계 후 다시 사전등록한다. [[YR-210]] 의 **재배치 주판정 축**을 실행한다. 지금까지 판정은 전부 **비판정 대역**(9,900,920~935)이었고 거기서 나는 ①팔별 최선 `k` 를 **같은 16일 위에서** 골랐고 ②정책을 결과 보고 골랐고 ③*"부하가 오를수록 강해진다"* 서사를 **결과를 본 뒤** 만들었다. 06 §3: **평가 대역은 새 대역·재사용 금지**. **★사전등록(동결)**: 대역 **9,910,000~015**(한 번도 안 쓴 새 대역) · 부하 3,500·5,000·7,500 · 정책 [[YR-230]] `ckpt_015` 탐색 0 · 팔 7종 + 진단 2종 · 셀 **432**. **팔별 트리거 `k` 는 비판정에서 정한 값을 그대로** (7,500 은 전부 0.15 · 3,500 은 FCFS/NEAREST 0.15 나머지 0.30 · 5,000 은 NETGAIN 만 0.30). **새 대역에서는 아무것도 고르지 않는다.** **판정식**: 날 단위 짝비교 · 부호검정 양측 p<0.05 · 주판정 = 부하별 "RL 이 고전 5종 중 몇 종을 넘는가". **★세 결과의 대응을 미리 적었다**: 7,500 에서 5/5 → *"고전 5종을 전부 이겼다"* / 3~4/5 → *"N종을 이겼다"* + 못 넘은 팔 이름 명기 / 0~2/5 → *"비판정 우세가 재현되지 않았다"*. **어느 경우든 세 부하를 다 적는다 — 이긴 부하만 고르지 않는다.** **금지**: 결과 보고 `k`·정책·부하·팔 변경 금지 · 이 대역 재사용 금지 · 진단 팔은 주판정에 안 넣는다(분해 보고용). 22코어 약 2시간 45분. [spec](../docs/dashboard-task-specs/YR-233-final-judgment.md) |
| YR-236 | Exp | **Dashboard 정본 기반 논문 작업초고 v2 — 확정·잠정 결과 분리** | 🟠 | 2026-08-26 | 새 실험 없이 현재 Dashboard·spec·원자료로 `docs/paper/v2/`를 작성한다. **확정 결과·5차 진단·6차에서 무효화된 수치·진행 중 판정**을 분리하고, [[YR-233]] 완료 전에는 v3 최종 우세를 주장하지 않는다. 문서 전용 작업이라 [[YR-235]] 코드 작업과 파일 범위가 겹치지 않는다. [spec](../docs/dashboard-task-specs/YR-236-dashboard-paper-v2.md) |



---

운영: 시작 시 [ready.md](ready.md) 에서 pull. 종료(commit) 시 [done.md](done.md) 로 이동 + commit 링크 박제.
