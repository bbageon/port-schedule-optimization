# 🟢 In Progress

> 현재 진행 중. **한 번에 1개** 권장. [index](README.md) · 인접: [ready](ready.md) → 여기 → [done](done.md).
>
> **사후 비용계약 변경(2026-07-30)**: YR-135의 현재 라벨은 계단형 트럭비용·본선 33의
> v1 계약이다. 결과는 V/A 구조 진단으로만 보존하며 새 정책 채택 근거로 승계하지 않는다.
> 구조가 통과하면 YR-136 점증비용 v2로 신규 라벨을 생성해야 한다.

| ID | Epic | Title | Priority | 착수 | Note |
|---|---|---|---|---|---|
| YR-183 | Exp | **세 팔 모두 수렴 전에 끊겼다 — 팔 비교가 학습곡선 위치를 재는 중일 수 있음** | 🔴 | 2026-08-18 | **★착수 2026-08-18 — 사전등록 동결 후 실행**. **바뀌는 축은 회차(10→40)와 시드(1→5) 뿐**, 나머지(교정 설정·팔·집행·학습률·클립·엔트로피·미니배치)는 YR-185 와 동일. **20코어**(시드 5 × 워커 4, 4코어는 타 세션 몫) — 같은 벽시계에 5배 증거. **★YR-185 사전등록 실패 2건을 명시적으로 고침**: ①**주판정 = 규칙 대비**(안 팔기 대비는 대조군도 이미 넘던 축이라 판별력 0이었다) ②**모드 = argmax 명시**(배포 방식), 추첨도 **함께 보고**로 사전 약속. **평가**: 새 대역 **9,500,000~9,505,000(6일)**·`K`/`greedy`/`fix40`(5시드)·같은 날 짝지어·독립단위=날. **규칙도 이 새 대역에서 다시 잰다**(기존 −170.05 는 다른 6일 값이라 인용 불가). **판정**: `fix40−greedy` CI 가 0 미만=넘음(서사 A 재검토) / 0 포함=구분 불가(B 유지) / 0 초과=**서사 B 확정·마지막 구멍 닫힘**. 하드가드 4종. **대응 계획 3종 사전 기재**. **비용** 약 10~13시간 + 평가 40분. [사전등록](../docs/strategy-history/2026-08-18-YR-183-교정설정-40회차-사전등록.md) · **★RL 전용 세션 인계 (2026-08-18)**: 이 row 는 [rl-training-session](../skills/rl-training-session.md) 레인 담당이다. **범위가 YR-185 결과로 바뀌었다** — 단순 40회차가 아니라 **②③ 교정 설정(`yr185_retrain` 의 0 보상 하위표집 + 정책·가치 비대칭 가중)으로 40회차**를 돌린다. 교정 없이 40회차를 돌리면 이미 아는 병목(critic 붕괴)을 다시 겪는다. **답할 질문 하나**: 교정본이 10회차에서 규칙과 **40.9** 차이였는데, **40회차면 그 40.9 를 메우나**. 넘으면 서사 A 재검토, 못 넘으면 서사 B 의 마지막 열린 구멍이 닫힌다(논문 `03-한계와-주장-범위.md` §2). **실행 전 필수**(오늘 다 겪은 것): ①코드 먼저 커밋·`dirty=[]` 확인 ②배선 사전 검증 ③**판정축을 규칙 대비로, 모드를 명시해** 사전등록 후 커밋 ④물결 수 올림으로 소요 추정. **비용**: 팔 1개 기준 약 10시간(회차당 약 27분 × 40, 워커 4). 코어 4개만 쓰므로 남는 20개로 다른 세션이 가벼운 작업 가능. **예산 25%**: 동결 예산은 40회차×4에피소드=160 에피소드/시드인데 YR-170·174·171-B/C **전부** `N_ITER=10, EPS_PER_ITER=4`(40 에피소드)로 돌았다. 축소 사유는 기록돼 있으나(1회 10분→160편 27시간) **축소 실행 결과가 그대로 판정으로 쓰였다**. **수렴 안 함**(시드 8,400,000 Φ): fixed15 2914.2→2544.6 · slots48 **3656.9→2571.1** · buy 2441.6→2372.6 — **세 팔 다 마지막 회차까지 단조 개선 중**이고 시간 판매 건수도 전부 아직 감소 중(2650→1134·2754→903·769→446). slots48 은 10회차에 Φ 1,086 하락하며 여전히 가파름 → **서로 다른 속도의 주자를 임의 지점에서 세워 순위를 매긴 것**. **★더 중요 — 팔 차이의 대부분이 학습 전에 이미 있다**: `iter 0`(학습 0회·같은 시드·같은 초기화라 정책은 사실상 동일, 다른 건 행동공간과 값매김뿐)에서 Φ 가 slots48 **3656.9**(시간 2754) / fixed15 2914.2(2650) / buy **2441.6**(시간 **769**) — **이미 1,215 벌어져 있고** 방향이 시간 판매 건수와 정확히 일치. 견적망 팔은 시간 슬롯을 **3.6배 적게** 판다. → **YR-177 해석 B(값매김 비대칭이 손해축을 억제)를 거의 직접 확증** — 좋은 칸을 고른 게 아니라 비싸게 매겨 막은 것이고, 학습 전부터 그렇다. 같은 표가 `48칸이 더 나쁨`도 학습 전 존재함을 보여준다. **조치**: ①YR-177 판별 먼저(재학습 전 기준 통일) ②기준 확정 후 동결 예산 재학습 ③그때까지 팔 대비를 수렴 후 결론으로 인용 금지. **예산 결정 요청**: 40회차 그대로(팔·시드당 27h) / **수렴 기준 사전등록 후 조기종료(권장)** / 에피소드 단축(`find_slot` 최적화) 선행. [spec](../docs/dashboard-task-specs/YR-183-undertrained-arm-comparison.md) |

---

운영: 시작 시 [ready.md](ready.md) 에서 pull. 종료(commit) 시 [done.md](done.md) 로 이동 + commit 링크 박제.
