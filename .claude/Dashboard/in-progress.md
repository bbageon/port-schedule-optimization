# 🟢 In Progress

> 현재 진행 중. **한 번에 1개** 권장. [index](README.md) · 인접: [ready](ready.md) → 여기 → [done](done.md).
>
> **사후 비용계약 변경(2026-07-30)**: YR-135의 현재 라벨은 계단형 트럭비용·본선 33의
> v1 계약이다. 결과는 V/A 구조 진단으로만 보존하며 새 정책 채택 근거로 승계하지 않는다.
> 구조가 통과하면 YR-136 점증비용 v2로 신규 라벨을 생성해야 한다.

| ID | Epic | Title | Priority | 착수 | Note |
|---|---|---|---|---|---|
| YR-185 | RL | **학습 설정 감사 — 데이터는 멀쩡, 최적화 대상과 채점 대상이 달랐다** | 🔴 | 2026-08-17 | **★①착수 2026-08-17 — 평가 모드 진단(재학습 없음)**: 같은 가중치·같은 6일(YR-171-C)을 **추첨 모드**로 재평가. 추첨 씨앗 **11·22·33 고정**(결과 보고 변경 금지)·3팔×2시드×6일×3씨앗=**108판**. 기준선 K·규칙 판매(−170.05)는 기존 산출물 **인용**(재실행 없음, K 일치 확인). **사전 동결: 두 모드를 둘 다 보고한다**(유리한 쪽 고르기 금지)·**진단이지 성능 판정 아님**·배포 후보 만들지 않음. 산출 = `추첨−최선`(모드 효과)·`추첨−규칙`·`최선−규칙`. **사용자 판단(2026-08-17) "학습 실패로 보인다"에 따른 바닥 감사.** ✅**데이터 무결**: 학습 날짜 `ts+it*4+e` → 8400000~8400039 **40일 전부 다름**(같은 날 반복 아님)·학습 8.4M 대 평가 9.2/9.3M **누출 없음**·경사 step **5,208**·PPO 구현 표준(이점 정규화·클립 0.2·엔트로피 0.01·grad clip 1.0). **데이터를 잘못 만든 문제가 아니다.** ❌**①학습 정책 ≠ 평가 정책(가장 큼)**: 학습 `sample=True`·평가 `sample=False` 인데 같은 가중치가 행동이 몇 배씩 다르다 — slots48 **6.3x·8.6x**(896·903 → 141.8·104.5) / fixed15 4.1x·3.4x / slots48_buy **1.4x·2.3x**. **괴리가 작은 팔일수록 성적이 좋다(−40.4 < −73.4 < −175.9, 세 팔 순서대로 정렬)** — 최적화한 것(추첨 정책 기대수익)과 채점한 것(최선 정책)이 다른 물건이었다는 증거. 기전: 덜 학습돼 logit 이 평평 → 추첨은 많이 팔고 argmax 는 KEEP 이 근소 우세라 거의 안 판다. ❌**②보상 희소·악화**: 0 아닌 표본 11.6%→10.7%→**8.9%**(설계상 KEEP·거절은 보상 0). ❌**③critic 붕괴**: critic rms 0.0567→**0.0199**(3배 수축)인데 목표는 0.133→0.108 유지 → 비율 2.35→**5.43**. 목표 91% 가 0 이라 **"항상 0"이 MSE 최적해**에 가까워 빠진 것. 결과 `adv≈ret` 로 기준선 소멸 → PPO 가 REINFORCE 로 퇴화. 부수 위험(미확증): 0 보상 표본의 정규화 이점 부호가 배치 평균에 좌우되는데 그 평균이 학습 중 뒤집힘(`credit_sum_mean` −46.5→**+16.9**). ❌**④바깥 회차 10번뿐**: step 5,208 은 같은 40 에피소드를 10 덩어리로 4 epoch 씩 돈 것 — **정책이 자기 변화 결과를 다시 본 횟수는 10**(YR-183). **교정 순서(싼 것부터·한 번에 한 축)**: **①평가를 추첨 모드로도 병행 측정(재학습 없음·평가 1회)** → ②보상 0 표본 하위표집 → ③critic 붕괴 방지 → ④동결 예산 40회차. **①을 먼저** — 재학습 없이 원인을 가른다. **사전 동결**: ①은 두 모드 결과를 **둘 다** 보고한다(유리한 쪽 고르기 금지). **★①판정 (2026-08-17) — 가설 기각. 추첨 모드가 오히려 훨씬 나쁘다.** 108판 완주(`b1d26dc`·`dirty=[]`·18:48:33→20:23:06)·투입 3600 전건·씨앗 11·22·33 고정 준수. **두 모드 병행 보고(사전 동결 이행)** — fixed15 최선 −73.43±14.81 / **추첨 +14.62±44.74** · slots48 최선 −40.43 / **추첨 +94.20** · slots48_buy 최선 −175.89 / **추첨 −60.25**. `추첨−최선` = **+88.06(t=+2.25) · +134.63(t=+3.18) · +115.64(t=+4.29) — 3팔 전부 유의하게 나쁨**. `추첨−규칙(−170.05)` = +184.67 · +264.25 · +109.80 으로 **추첨 모드는 규칙에 더 크게 진다**. fixed15·slots48 은 추첨에서 **안 파는 것보다도 나쁘다**(양수). **모드 일치 확인**: 추첨 평가 판매량이 학습 마지막 회차와 거의 같다(fixed15 924·930 vs 학습 928·1134 / buy 1248·439 vs 1294·446) — 같은 얼굴을 재고 있음이 확인됐다. → **①은 원인이 아니다.** 최적화 대상(추첨)과 채점 대상(최선)이 다른 것은 맞지만, **채점한 쪽이 더 좋은 얼굴**이었다. argmax 평가는 정책에 오히려 관대했다. **앞서 적은 "괴리가 작을수록 성적이 좋다" 상관은 인과가 아니라 증상**으로 격하한다 — 괴리 크기는 정책이 얼마나 확신 있게 학습됐는지의 지표일 뿐이다. **남은 원인은 ②③④**: PPO 가 최적화하던 확률정책이 10회차 뒤에도 **여전히 안 팔기 수준(±0) 언저리**였다(학습곡선과 정합: iter9 Φ 2544.6 vs K 2392.98). 즉 학습은 되고 있었으나 **출발점이 +507 로 너무 나빴고 10회차로는 못 건넜다**([[YR-183]] 강화). **부수 관측**: 추첨 씨앗 간 흩어짐 33~51 로 커서, 확률정책 배포는 운영 변동성 자체가 문제다. **다음 = ②보상 0 표본 축소 → ③critic 붕괴 방지 → ④동결 예산 재학습.** [산출물](../../outputs/reports/yr185_eval_mode/eval_mode.json) [spec](../docs/dashboard-task-specs/YR-185-training-setup-audit.md) |
| YR-171 | Exp | **24시간 공통창 BUY 견적 → Resolver의 블록×시간 공동선택 (YR-161 대체)** | 🔴 | **상태=설계 확정·코드 미완 / 게이트=171-A `scenario_validity` → 171-B/C `performance`**. **왜**: 고정 +15분 시간 판매가 +438.3(+18.68%·1건당 +14.28분) 손해였고, Resolver와 블록의 미래 창이 다르면 수신 시점 부담을 잘못 견적한다. **무엇**: 같은 작업일 24시간 공개 스냅샷에서 source TransferHead는 작업만 OFFER하고, 각 수신 블록은 동결 ExecutionHead로 `수용 전/후`를 가상 실행해 30분 슬롯별 추가비용을 응답한다(별도 BUY 학습행동 아님). Resolver가 `수신 블록×시간`의 판매절감−수신부담−주행−기사변경−불확실성을 비교해 최저비용안을 고르고, 전부 KEEP보다 나쁘면 거래하지 않는다. **현 코드는 고정 +15분·수기 견적 중심이라 이 구조가 아직 아니다**; 24시간 정보 재자격→BUY 견적 shadow→matching 순으로 한 축씩 검증 · [spec](../docs/dashboard-task-specs/YR-171-time-sale-slot-contract.md) · [결정](../docs/strategy-history/2026-08-13-YR-171-24시간-공통창-BUY견적-사용자결정.md) · **착수 2026-08-15 — 171-A(정보계약+재자격)부터**. YR-174 1차 판정 통과 뒤 남은 급소가 "시간 선택지가 +15분 1칸뿐"으로 특정됐다 (time_sell.py:36 DEFER_DELTA_S=900, sell_review.py:352 에 반복문 없음 — 공간은 20블록을 다 도는데 시간은 한 칸). buy_estimator·slot_plan(48칸)은 **어떤 실험도 import 하지 않는 미연결 상태**. **171-A 범위**: 같은 작업일 24시간 공개 스냅샷(예약명단·공개 ETA·본선계획)·48슬롯 좌표·plan_version/만료 구현 + **미래정보 누출·시나리오 현실성 재자격**. **성능 주장 금지**(사다리 규약). **선행 실측**: yr171_horizon_probe — 현 계약(통지 30분=슬롯 1칸)에서 48칸 계획표의 미래 칸이 얼마나 비어 있는지 확정 → 재자격이 필요한 이유의 증거. · **★171-A 완료 (2026-08-15)** — 정보 계약 전환 + **재자격 16/16 통과**. `integrated/day_plan.py` 신규(하루 공개 예약 장부: 예약 gate-in·블록·flow만 공개, 실현값 비공개, 엔진 미접근, plan_version/만료, 날짜 넘김 거부). `slot_plan` 이 장부에서 특징 1·2를 받고, `try_time_sell` 이 장부 검사 후 갱신. `run_episode_diurnal(day_plan_public=False)` 명시적 opt-in 이라 YR-174 판정 불변. **증거**: ①미래 칸 가시성 0.4~5.6% → **51~84%** ②구 계약 회귀 K 2475.2478·P9 2453.0323 **완전 일치** ③경계 단위검사 11/11 ④재자격 16/16 — W12 신규 3항(장부=명단·자기 재예약 0·**구 계약 run_digest 셀별 완전 일치=읽기 전용 증명**). 산출은 `yr167_diurnal_qual_public/` 로 분리(계약별 자격 증거 구분). **성능 주장 없음**(사다리 규약). **다음: 171-B 견적 shadow**. 실행 기록: [171-A log](../docs/dashboard-task-specs/YR-171-A-execution-log.md) · **★171-B/C 판정 (2026-08-16 · 통계 정정 2026-08-17)** — 2시드×6일, 전건 KEEP 기준 같은 날 짝지어. 평균차는 `eval_slots.json` 과 일치: fixed15 **−73.4** / slots48 **−40.4** / **slots48_buy −175.9**(12/12 우위). **±·t 정정** — 초판은 하루-시드 12건을 독립으로 봤으나 두 시드가 **같은 6일**을 쓰므로 독립단위는 날이다. 날 기준: fixed15 −73.4±14.8(t=−4.96) · slots48 −40.4±32.0(t=−1.26) · buy −175.9±31.6(t=−5.56). **대비도 정정** — 초판은 두 팔을 독립표본으로 뺐으나 같은 날·같은 시드라 **짝지어야** 한다: 48칸 열기 **+33.0±10.0(t=+3.31, 날 기준)** — 초판의 "이득 없음(t=+1.40)"은 틀렸고 **유의하게 나빠졌다** · 견적망 값매김 **−135.5±10.7(t=−12.73, 날 기준)**. 재계산은 저장된 `rows` 에서 전부 재현된다(초판 t 는 산출물에 없던 임시 계산). → **이득의 원천은 선택지가 아니라 하루치 정보다**(계산식은 지금 대기열만 봄) — **단 YR-177 미해소 전까지 잠정**. **단, 메커니즘은 예상과 다름**: 최대 변화는 시간 판매 개선이 아니라 **공간 판매 4배 증가**(241→901건) — 정확한 시간 견적이 "미루는 건 비싸다"를 알려주자 공간이 matching 에서 이김. **"48칸 자체의 이득"은 분리되지 않았다**. **견적망 검증 2/3**: 결정론 OK · 방향성 순위상관 **0.8024**(미열람 7,132건) OK · **단조성 실패 99.8%**(실제 계획표 재검사, Δ평균 −0.0237) — 원데이터는 예약↑→부담↑(순위상관 +0.2953, n=28,530)인데 망은 반대 부호. **검증된 견적기로 쓸 수 없음**. **★값매김 기준 불일치 발견(2026-08-17 · YR-177)**: 공간 좌표와 `relief` 는 자기 비용만 재는 계산식인데 시간 좌표만 견적망(자기 51%+뒤차 30%+본선 19%) — 시간 후보에만 약 +0.6 비용시간이 세금처럼 붙는다. 시간 좌표 비중 60.0%→42.0%→**17.1%** 로 단조 하락하고 시간 판매는 1건당 +14.28분 손해 축이라, **−135.5 가 "정보 가치"가 아니라 "손해축 억제"일 수 있다**. 후속: YR-177(기준 통일·판별 실험)·YR-175(단조성)·YR-176(TIME 슬롯 용량). 정답지 28,530행/8일, 48칸·21블록 전부 덮음(부담 = 자기 51%+뒤차 30%+본선 19%). |
| YR-174 | Exp | **BUY 견적망 + 폐쇄형 학습 구조 (43차 설계안)** | 🔴 | 2026-08-14 | **구조**: 공통 24h 계획 → BUY 견적망(작업별 20블록×48슬롯 부담 예측) → 작업별 시장요약 → SELL PPO(KEEP/OFFER 1건) → 중앙 Resolver 공동 matching(KEEP 보다 이득일 때만 집행) → 동결 ExecutionHead 실행 → **거래별 실제 손익을 판매한 source PPO 에만** 반환. **역할 분리 필수**: BUY=수신 부담 예측(지도학습 후 동결)·SELL PPO=무엇을 내놓을지(학습)·Resolver=어디로 언제(결정론)·ExecutionHead=동결. **BUY 예측값을 SELL 보상으로 쓰지 않는다**(견적이 틀리면 정책도 같이 틀림) — 보상은 실현 손익 D_i = C(source i 만 KEEP) − C(실제). 여러 source 동시 판매 시 **하나를 KEEP 으로 바꾼 반사실 matching 재계산**(빈 슬롯을 다른 작업이 쓰는 효과 반영). **왜 차이 보상인가**: 전역은 행동 신호 0.0004%·로컬은 떠넘기기 구조(판 +5.8분/받은 −7.5분). 차이 보상은 시각 잡음을 **구조적으로 상쇄**한다. QMIX 는 없는 정보를 못 만들고 PPO 를 버려야 해 부적합. **★선행 결함 2건 확인(2026-08-14)**: ①SpaceOnly 가 시간 판매를 못 막음 — resolver 가 반입 작업에도 TIME 좌표를 붙임(sell_review.py:352 "반입·반출 공통"), 실측 754~793건/일 ②YR-172 Φ 에 기사 외부 대기 누락 — 0회차부터 195.3 낮고 이는 시간판매 781건×15분=195.3 과 소수점까지 일치. → **YR-172 실행 결과(8% 개선 주장 포함) 전부 무효**. **구현 ①②③ 완료(23680ff)**: slot_plan(블록별 48슬롯 계획표·특징 9 동결·정보누출 기계검증 통과)·buy_estimator(작업×20블록×48슬롯 부담망 62,546 파라미터·순열불변·누출검사 통과)·sell_credit(차이 공로 — "없었으면 남이 그 슬롯을 썼다"를 반영: 자기이득 3.0 이나 대체 가능분 제외 시 공로 0.5, 단위시험 전건 통과). **★실행 가능성 확인**: 960후보 일괄 견적 2.5ms → 하루 14,400 OFFER 기준 36초. 시뮬레이션 가상실행(45ms×960=43초/건 → 172시간/일) 대비 **1.7만 배** 싸다 — YR-171 의 계산량 문제가 이 경로로 해소된다. **구현 ④ 완료(de10921)**: 거래별 실현 손익을 그 결정에 되돌려 붙이는 배선. D_i = R_src − B_dst − 주행 − 시간변경 을 **반사실 없이** 한 번의 실행에서 분해 — B_dst 에 "점유 시간 × 그때 밀린 트럭 수"(견적식이 놓치던 외부효과)를 넣고 R_src 에 같은 형태의 근사를 써 편향을 만들지 않는다. 조인 키 (round(t,6), src). KEEP·거절은 보상 0(실제 상태가 KEEP 과 동일) → 하루 결정의 약 7%만 신호를 받는다. **눈금 3종**: RET_SCALE=1.0(거래 손익은 자연히 O(1) — 전역 판의 2만 7천 배 어긋남이 구조적으로 소멸)·assert_scale_sane(목표 과대/과소·조인 실패 시 **학습 시작 전 실격**)·scale_report(첫 회차 목표·critic·비율 기록). 자기참조 발산은 V(s_end) 항이 없어 재발 불가. **남은 일**: 구현(slot_plan·buy_estimator·sell_credit 병렬 작성 중) → 통합 → BUY 지도학습 → SELL PPO 학습 · [spec](../docs/dashboard-task-specs/YR-174-buy-market-closed-loop.md) · **★44차 피드백 반영(2026-08-14)** — 본선 작업은 판매 대상 아님(위치가 선박·야드에 묶임)이나 **트럭 수용이 본선을 밀어내는 비용은 부담에 필수**. 견적식·내 실현손익 모두 본선 항 누락 확인 → ① vessel_pressure(본선 압력) 항 추가(10배 가중, 판·받은 양쪽 대칭) ②시간 이연에 **미룬 시각의 새 혼잡** 항 추가(없으면 이연이 항상 이득으로 보이는 편향). 실측 검증: 같은 에피소드에서 credit_sum +452.3 → **−470.3**, 음수 거래 1,378 → **2,273**건 — 축 분리 진단(시간 판매 1건당 손해)과 부호가 일치. 눈금 ratio 2.83(<100 통과). · **★1차 판정 통과(2026-08-15)** — 보상 정정 후 학습 6시드×10회차 완주(4h16m). **학습됨**: 시간 판매 2,658→1,082건(−59%, 6/6 시드 단조), 공간 판매는 거의 유지(977→942) — 축 분리 진단(공간=이득·시간=1건당 14.28분 손해)을 정책이 스스로 재발견. **매칭 기준선 대조**(`yr174_eval.py` — 같은 집행설정 adopted+C0_GUARD, 학습 미사용 6일, 결정론 실행, 같은 날 짝지어): 전건 KEEP Φ=2,423.37 / 학습 전 P0 Φ=2,930.72(+507.4, t=+34.4) / **학습 후 P9 6시드 −84.49 ± 9.22 (t=−9.16, 개선 −3.49%, 36건 중 35건 KEEP 우위, 6/6 시드 우위)**. Φ 에 기사 외부 대기 포함 확인(yr151_transfer_ppo.py:193-196) — YR-172 결함 없음. **주의**: 결정론 실행은 판매를 훨씬 적게 함(공간 258·시간 350 vs 학습 중 표본추출 942·1,082) — 정책이 보수적으로 수렴. **남은 일**: BUY 견적망 지도학습 → 48칸(30분×24h) 시간좌표 열기. 현재 시간 판매 선택지는 **+15분 고정 1칸뿐**(time_sell.py:36 DEFER_DELTA_S, sell_review.py:352 반복문 없음)이고 buy_estimator·slot_plan 은 **아무 실험도 import 하지 않음**(미연결 확인). |
| YR-150 | Exp | **H-21 21블록 — ★4차 재정의: 고정 재공량(WIP) 계약 전환, 재자격 미판정** | 🔴 | 2026-08-06 | **★부하 정의 전환(사용자 결정 2026-08-08)**: L∈{50..150} = 터미널 안에 **유지하는** 외부트럭 대수(고정 WIP). 초기 채움 L 대 + 나간 만큼 60초 주기 교체 투입. 구현 = `admit_external_job`(런 중 투입 수술·fail-closed) + `build_fixed_wip`(pool 사전 추첨·결정론) + `WipAdmissionController` + `yr150_h21_wip_pilot`(자격 검사 W1~W8). smoke 실측: WIP 60 유지구간 min=max=60·건너뜀 0·불변식 OK·혼잡비 1.204. **공정성 한계 박제**: 빠른 정책일수록 트럭을 더 받아 정책별 처리 물량이 달라짐 → 판정은 **같은 재공량에서 처리량+시간당 v2 비용 공동 판정**만 허용(에피소드 총비용 단독 금지). walk-in(lead 0)이라 **PRE_GATE 창 없음** — 0B 전 lead>0 설계 선결. [결정](../docs/strategy-history/2026-08-08-YR-150-고정WIP-4차재정의-사용자결정.md) · **구 유입량 계약 이력**: 0단계(N=3 계약 9종, evidence `f37414d`)·1단계(자격 10종, `3f7275b`) 통과 보존 — [0단계](../../outputs/reports/yr150_nblock_contract/report.md) · [1단계](../../outputs/reports/yr150_h21_pilot/report.md). **★본선 물량 재보정(2026-08-08)**: 척당 15건은 2블록 시절 값의 무비판 승계 — HJNC 확인 처리량(231만 TEU/년)에서 터미널 본선 작업률 145~170 moves/h 를 유도해 12 process×120 moves(계획 161/h·앵커 안·양하 6/적하 6)로 동결, 앵커 등록부에 vessel_workload 승격. smoke: WIP 60 유지 그대로·혼잡비 1.20→1.405(본선 경쟁 실화). **★최종 재자격 통과(2026-08-09, 트리 `8e8eba4`·dirty=false — lead 30분·내부+확약=L 최종 SELL 환경)**: W1~W9 전항 PASS — 전 셀 계약량(내부+확약) min=max=L 완벽 유지·fallback 투입분 0(W9)·배경 공통 시드·60초 격자 · [재자격 JSON](../../outputs/reports/yr150_h21_wip_pilot/pilot_h21_wip.json). **★사후 관찰(판정 아님·임계 불변)**: 상태 분류 **전 셀 CLEAR**(혼잡비 1.36~1.43·물리 내부 트럭은 L 의 절반 이하: L=150 에서 27~114대·처리량 64.75~189.5대/h) — 확약분(30분 예고)이 L 슬롯을 차지하는 lead 계약의 구조적 귀결. 정책 이득이 실측 가능한 BUSY 대역이 현 L∈{50..150}에 없으므로 **YR-157 대역 재정의(이론 μ·ρ 유도·사전등록)가 0B 전 필수로 승격** — 결과를 보고 대역을 올리지 않는다(사전등록로만). **중간 자격(lead=0·트리 `11c58c1`) 이력**: 75~150 BUSY(1.407→1.812)·처리량 157~367.5/h — lead=0 환경 기록으로 보존. **★환경 신뢰성 보정(외부 감사 2026-08-09)**: ①본선 배치 독립화 — 구판은 이름순 Y01~Y12 에만 본선 + 시작시각이 블록 순서와 단조 상관(인공 편향) → `vessel_placement` 3축(블록·슬롯·종류) 전용 난수열 완전 추첨·슬롯 5%~95% 정정(계획 작업률 161→150 moves/h·앵커 안)·배치 원장 저장 (`d2cf6c0`) ②flow fallback 원장(requested/realized/reason)+공식 자격 **W9=0** 요구·flow/규격 난수열 분리 (`d2cf6c0`) ③투입 60초 격자 가드 — `time_grid.on_grid` 공용 유틸 분리 (`ba4ba53`). 계약 테스트: 신규 14종 포함 관련 67종 통과·자격 smoke WIP 60 유지 min=max=60 재확인. **선결 이력**: YR-158 정본 사용자 결정(게이트인→장치장 5분·30km/h·턴타임 15분, `11c58c1`)으로 재자격 선결 해소. **★현실성 게이트 PASS(2026-08-10, `9754c92`)** — 앵커 5종 전부 정합(YR-158 done): 주행시간 정본·본선 작업률·장치율 0.65 재보정·게이트 처리율 재정의·크레인 서비스 런 실측(사이클당 146.7~147.3s — 문헌 정중앙). 잔여 축 = performance(반사실 0B 부터). **다음**: 반사실 0B(무대 = 주 w3-L150·대조 w1-L150) · [spec](../docs/dashboard-task-specs/YR-150-continuous-inflow-steady-state.md) · **금지**: 정책 성능 비교 |

---

운영: 시작 시 [ready.md](ready.md) 에서 pull. 종료(commit) 시 [done.md](done.md) 로 이동 + commit 링크 박제.
