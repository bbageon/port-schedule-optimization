# 🟢 In Progress

> 현재 진행 중. **한 번에 1개** 권장. [index](README.md) · 인접: [ready](ready.md) → 여기 → [done](done.md).
>
> **사후 비용계약 변경(2026-07-30)**: YR-135의 현재 라벨은 계단형 트럭비용·본선 33의
> v1 계약이다. 결과는 V/A 구조 진단으로만 보존하며 새 정책 채택 근거로 승계하지 않는다.
> 구조가 통과하면 YR-136 점증비용 v2로 신규 라벨을 생성해야 한다.

| ID | Epic | Title | Priority | 착수 | Note |
|---|---|---|---|---|---|
| YR-254 | Exp | **상호 동의 절제 — 거부권을 없애면 얼마나 나빠지나** | 🔴 | 2026-09-03 | **논문이 새로움으로 내세우는 구조인데 한 번도 절제하지 않았다.** 지금까지 잰 것은 *장치가 돌아갔다*(한쪽 동의 확정 0건)뿐이다. 팔 `RL_NOVETO` — 망은 그대로 두고 **값 판단으로 거절하던 부분만** 끈다(용량 거절은 위에 남아 물리 제약은 유지). **설계를 둘 바꿨다**: ①spec 의 *판정 대역* → **진단 대역 9,900,980**. 9,400,000 은 [[YR-233]] 에서 이미 썼고 `USED_BANDS` 가 막는다 — 절제는 진단에서 하고, 논문 주장으로 쓸 값이면 그때 새 판정 대역에서 확증한다. ②한 무대 → **세 환경 동시**(혼잡일 2·6·14). [[YR-286]] 이 *혼잡 빈도가 바뀌면 결론이 바뀐다* 를 보였으므로 한 무대만 재면 같은 함정이다. 동시 실행이라 **벽시계는 그대로**. 기준선은 [[YR-286]] 것을 재사용하고 `RL` 재실행은 **결정성 확인**을 겸한다. ⚠️ **선행 실측(n=2)은 논문에 불리한 방향** — 거부권 없는 쪽이 4.27% 쌌다. 세 갈래 해석을 spec 에 미리 적어 뒀다(이득 / 차이 없음 / 수락망이 과하게 거절). 실행 `scripts/v3/run_consent_ablation.sh` · 예상 14시간 · [spec](../docs/dashboard-task-specs/YR-254-consent-ablation.md) |
| YR-263 | Paper | **논문 v3 핵심 그림 교체와 절 배치 교정** | 🟠 | 2026-08-30 | 제공된 시스템 개념도·상세 아키텍처·MLP 그림을 국·영문 LNCS 원고에 넣는다. 개념도는 서론의 문제→해법 연결을 돕도록 문장을 다듬어 배치하고, 상세 아키텍처는 3장 제안 아키텍처로 옮긴다. MLP 그림은 실제 구현의 ReLU와 맞춘 뒤 기존 그림을 대체한다. 다른 세션이 고치는 결과표와 결과 그림은 건드리지 않는다. [spec](../docs/dashboard-task-specs/YR-263-paper-core-figure-placement.md) |
| YR-239 | RL | **30일 연속 무대 — 세계가 밤에 끊기지 않는다** | 🔴 | 2026-08-26 | **사용자 지시**: *"30일치 시드 데이터를 만들어놓고 … 첫날과 마지막날은 세계를 연결하는 용도로만 사용해서 나머지 28일을 학습해봐. 단 중간보고는 하루가 끝날때마다 평가"* + 후속 결정 셋(부하 **가중 추첨 30/30/25/10/5%** · **완전 연속 + 엔진은 v3 폴더에 새 모듈로 추가** · **부하별 판정도 같이**). **왜**: 하루 무대는 ①매일 인공 초기 적재에서 출발 ②배수 2시간 도착 0 ③하루 끝 계수기 창이 어긋남. **구현**(v3 사본 무수정): `stage/month.py` · **`stage/month_engine.py`**(루프 상한 + **본선 실시간 투입**) · `stage/month_run.py` · `train/month_loop.py`(하루=한 회차 온라인 학습) · `eval/month_judge.py`(부하별 부호검정). **★새 엔진이 필요한 이유**: 사본 `_validate` 가 *"모든 job 대상이 t=0 야드에 있을 것"* 을 강제하는데 초기 적재 대비 하루 소모가 커 **사흘 반이면 바닥** → 배를 그날 아침에 붙인다(`admit_external_job` 과 같은 fail-closed 계약). **비용 벽**: `bridge._sync` 가 60초마다 트럭 20만 줄 = **86억 회** → 하루 끝에 네 장부 **동시 정리**. **★30일이 잡은 결함 여섯** (하루 무대는 매일 리셋돼 구조적으로 못 보던 것 · 상세 [YR-239b](../docs/dashboard-task-specs/YR-239b-thirty-day-defects.md)): **①배가 내리기만 함**(야드 +26%/일 · Φ 17.8M→**138.9M원**) — 하루 무대는 이 칸을 안 읽어 **과거 결과 영향 없음**(미완 job **0/6,008** 실측) · **②배를 너무 일찍 치움**(남은 일감 131→**1,566** · 72초→**325초**) · **③반출 대상이 날마다 겹침** — 상자 이름이 `C0001…` 로 매일 반복 → **트럭이 조용히 사라짐**(300 중 288) · **④블록이 차서 양하가 멈춤** — 양하 스트림 하나가 블록 용량의 27%를 하루에 쌓음 · 92% 찬 블록에서 막힌 양하 93→**570**, 빈 블록은 **0** · **⑤야드가 하루 1,000상자씩 마름** — `gate_out_share=0.6` 이라 반출 3,018 vs 반입 1,982 → **12일이면 텅 빔**. 현실은 이 구멍을 본선 양하가 메우므로 배의 양하/적하 목표를 **트럭 수지에 맞춤** · **★⑥분기 세계가 탐색을 안 함** — `open_teacher` 가 본 세계 객체에만 ε 를 걸고 `ctx.explore` 는 0 인 채로 둬서 분기가 **ε=0** 으로 굴렀다(본 세계 하루 거래 487 vs 분기 3시간 **1건**). *"사실"* 가지가 사실을 재현 못 해 **라벨의 97% 가 정답 0**. 동일성 불변식은 학습 경로 전체에서 꺼져 있어(하루 무대 `loop.py:196` 도 0) 아무도 못 잡았다 — 세계를 더 안 쓰므로 켜는 비용 0. 고침 `ctx.explore = eps` + `identity_checks=4` → **0 비율 97%→62%** · 실질 신호 1→13건 · **동일성 통과 4·실패 0**. **영향**: 30일 학습 4일치 무효 · 판정 경로 무관(분기 0회 + 하드가드) · 하루 무대 학습 무관. **고친 뒤 4일 실측**: 남은 일감 **18→16→27→1** · 하루 **40→32→42→44초**(4일 합 626→**158초**) · 투입 **300/300 전부** · 막힌 양하 **0** · 야드 12,882↔12,070(제자리) — **되돌릴 조건 셋 전부 통과**. **시험 25건** + rudder 동일성 4건(A2 반영) · 아키텍처 교차검증 **일치**(부하 5수준·셀 880 갱신). **속도**: `Market.newly_eligible` 이 epoch 마다 오더 21만 건을 전량 정렬 → 예약시각 색인으로 (`4c17fc0`, 70.9→51.8분/일). ⚠️ 그 진단에서 **틀린 인과 셋을 보고했다가 정정**(짐 pickle·메모리·탐색) — 비교군의 ε·망 초기값·오더 개수를 안 맞춘 채 "숫자가 줄었다"를 원인으로 읽었다. **★A11 정정**: *"30일이면 `vessel_slack` 이 살아난다"* 는 **틀렸다** — 실측 표본 2,723건이 **100% 천장(+2h)**. 원인은 창 밖 결측이 아니라 **여유 배수 `dmult=2.0`**(계획 완료 = 순수 작업의 2배)이라 배가 늘 넉넉한 것. 배가 일정의 50% 이상 밀려야 내려온다 → **혼잡 부하(12,500·15,000)에서 재고 판정**, 거기서도 천장이면 제거. **검증**: `tests/v3`+`tests/rudder` **91 passed** · 월 무대 시험 27건 · 아키텍처 교차검증 일치 · 학습 경로 연기시험 통과(라벨→반사실 3세계→망 갱신→체크포인트, ε 0.50→0.05). 무대 `d43484d` · 속도 `4c17fc0` · ε 수정 `687f7aa`. **다음**: 28일 학습 실행 → 부하별 판정 → 혼잡 부하에서 죽은 칸 재감사. [spec](../docs/dashboard-task-specs/YR-239-thirty-day-stage.md) · [이력](../docs/strategy-history/2026-08-26-30일-연속무대-결정.md) |
| YR-235 | RL | **v3 감사 — 두 행위자의 시야를 맞춘다 (A안 채택)** | 🔴 | 2026-08-26 | **발단**: 사용자 물음 *"Buyer 가 보는 창은 Seller 와 같아야지, 그래야 Seller 가 제안한 시간대를 Buyer 가 검토하지 않나"*. 맞다. **감사 8건**(`probe/audit.py` 신설 · 기계 훑기 + 문서 대조): **A1** `Offer.slot_load` 를 아무도 안 채움(시간 offer 445건 전부 0.0) → ✅**고침** · **A2** Φ 네 항이 다른 창(항1 24h 검열 vs 항2/3/4 **26h 누적**, +1.0%, 본선유휴 +8.1%) — `04 §항1`·`phi.py` 규약 위반 · **A3** Buyer 가 `defer_s` 를 못 봄(15분/60분 제안이 **같은 행**) · **A4** `seller_action_features(slot_load)` 죽은 인자 · **A5** `TODO_ARMS=()` 인데 오류 메시지는 여전히 "YR-211" · **A6** `_space_coords(quay_of)` 죽은 인자 · **★A7 이연 격자가 H=1시간 시절 값**(최대 60분) — H 는 [[YR-217]] 에서 3시간이 됐는데 격자는 안 따라감. 넣을 수 있는 칸 **10~11개 중 4개만** 사용. 증거: 최대치(60분) 선택 비율 **82~97%** = 천장에 붙어 있음 · **★A8 시야 비대칭** — Seller 는 제안 시각을 **±30분**(도착 압력, [[YR-230]])으로 보는데 Buyer 는 **±7.5분**(`slot_load`)으로 본다. **⚠️ 블록의 미래 상태는 아무도 모른다** — `inside_count` 는 `gate_in≤t` 만 세므로 미래엔 0, 크레인 밀린 일은 음수. 미래에 대해 공개 정보로 아는 건 **"그 시각에 통지된 물량"** 뿐이다. **★채택안 A(사용자 결정)**: Buyer offer 특징에 `arrival_pressure`(±30분) 추가 **19→20칸**. 대칭이 맞고, **도착 압력이 "언제 떨어지나"를 담으므로 `defer_s` 를 따로 안 넣어도 된다(A3 해결)**. **묶어서 간다**: A8 + A7(격자를 150분 근처까지) + A4·A5·A6 정리 — 셋 다 재학습이 필요하다. **A2 는 따로** (라벨은 정상, 판정 수치만 걸림 → 배수 편향이 팔마다 다른지 재고 판단). **[[YR-234]] 6차 재학습을 3회차에서 중단**(잃은 시간 1h30m) — A7·A8 이 학습 전제를 바꾼다. [spec](../docs/dashboard-task-specs/YR-235-actor-symmetry-audit.md) |



---

운영: 시작 시 [ready.md](ready.md) 에서 pull. 종료(commit) 시 [done.md](done.md) 로 이동 + commit 링크 박제.
