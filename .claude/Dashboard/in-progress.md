# 🟢 In Progress

> 현재 진행 중. **한 번에 1개** 권장. [index](README.md) · 인접: [ready](ready.md) → 여기 → [done](done.md).

| ID | Epic | Title | Priority | 착수 | Note |
|---|---|---|---|---|---|
| YR-087 | RL | 증류 격차 해소 — 학생 관측 본선 feature 강화 / 룩어헤드 (YR-080 2b negative 파생) | 🟠 | 2026-07-22 | **착수 (사용자 결정 2026-07-22)**: YR-080 2b 진단 = 기준재 교사 배 보호가 feed-forward 학생으로 전이 안 됨(학생 berth 2~5배 악화·완주 실패·top1_disagree 0.167), 원인 = 교사 우위가 1800s rollout **미래**에 있는데 학생 관측은 현재 집계만·본선 그룹 rich feature(여유시간·위험·예상지연) 부재. **1단계 완료 — 관측 강화 가설 기각 (negative)**: ①진단 = encode_observation 이 본선 그룹 8필드(slack/risk/잔여작업/버퍼)를 통째 드롭·후보 훅 2개 죽음(vessel_risk_delta 항상 None·deadline_slack naive) 확인. ②v1(후보 훅 2개 관측값 교정, 214불변) → 개선 없음(희소·후행·후보한정). ③v2(본선 그룹 전역 인코딩 top-K, 214→246, use_vessel opt-in 하위호환) — 본선 블록 실신호 확인(slack −1344s·risk 0.75)에도 **개선 없음**. 3판본 학생 berth 교사 대비 2~4배 악화·완주 실패·top1_disagree ~0.16 동일. **결론: 병목은 관측 아님** — feed-forward 가 교사 1800s rollout 을 단일 forward 로 amortize 못하는 근본 한계(방법론 검증 강형 예측 적중). **train-fit 진단(2026-07-22, 사용자 결정)**: v2 학생이 **학습셋조차** 분기결정 재현 0.192(test 0.160·격차 +0.03) → **근본 확정: 관측별칭**(같은 관측에 교사 다른 선택 — rollout 이 의존하는 야드 스택/타이밍을 압축 관측이 미담). 데이터·과적합 아님 → **더 학습·관측·DAgger/FT 전부 무익**(라벨 불학습). **함의: feed-forward 증류는 구조적으로 막힘 · 배포정책은 룩어헤드(shallow rollout/beam) 유지 필요.** 관측 수정은 correct enrichment 로 보존(룩어헤드 학생용). **얕은/선택적 rollout 배포 탐색 (2026-07-22~23)**: SelectiveRolloutPolicy(본선 급할 때만 rollout) 구현. firm-up(10-seed 짝지은 CI): **순수@3600 rollout 은 SF 대비 출항지연(berth) −10~−22분 4/4 유의(B★)+트럭 P95 유의개선** = rollout 정책은 실효(구조적만 아님)·SF보다 나음. sts5@1800 은 SF 대비 무의(짧은 창 무익). **핵심 재구성: distillation 실패는 치명 아님 — 빠른 net 없이 rollout 직접 배포가 경로**(실터미널 분단위 결정 → ~1-2s/결정 무방). 선택적 firing 은 평균비용 절감(loose 셀 발동 0.4). **RL 전환 (사용자 결정 2026-07-23, YR-088)**: "지도학습은 새 환경 일반화 불가" → 중앙 공동정책을 **보상 기반 RL**(joint-combo Q-학습, 조합 하나=값 하나라 신용 희석 없음)로 학습. 반복 v1(퇴화)→v4(REPO 행동벌점=건전·SF하회, 트럭대기 전셀 우수)→v5(본선 선행신호 sts_wait). **1차(단일 run·점추정): healthy·완주 전 셀 + num SF 하회.** 정직 대조(학습 net vs 랜덤 net·동일 제약): **트럭대기 학습 net 대폭 우수(3.5 vs 10)=학습 실효**, 그러나 berth(본선)는 학습≈랜덤 → 본선 개선은 제약·shaping 몫(학습 불확실)·best_ep=20 반복(학습 얕음). **일반화 검증 (2026-07-23, 사용자 결정 '가', [결과](../../outputs/reports/yr088_generalize/results.json))**: 학습 RL zero-shot(재학습 0)으로 안배운 부하·마감 + **다른 터미널 PNIT·PNC** — **전 환경 healthy·완주 1.0·에러 0 + 트럭대기 6/6 새환경 SF 하회** → **RL 전제 입증**(지도학습 불가했던 구조적 일반화·학습 트럭 skill 전이). berth(본선)는 대부분 SF 하회 못함 → 얕은 본선학습 새환경서 확인. 미검: 다중 train seed·CI·**본선 학습 심화**. 커밋 `yr088_joint_rl.py`·`yr088_generalize.py`·rl_net.pt. **본선 STATE 재학습 (itc-v5, 2026-07-24~25, [상세](../docs/strategy-history/2026-07-24-본선STATE재학습-flowmargin-itcv5.md))**: flow_margin(버퍼여유 피드백)을 스키마(itc-v4→v5)에 배선·재학습 → **본선 유의개선**(berth −11.77 CI, shaping 실패였던 걸 STATE 로 해결)·단 트럭P95 악화(Pareto 이동). **의무처리 하드가드**(SLA임박 트럭만 표적 캡)로 berth −8.12 유지+꼬리 비열등(assumed δ=1/3분)+턴타임(B-C)P95 비열등 = **첫 채택가능 본선인지 정책**. 소프트가중치(long_wait 1→5)는 본선 이득 소멸(berth +8.55) = 꼬리보호엔 표적 하드제약>전역 가중치. **가드없는 RL 총비용 −9.8%**[+3.07,+9.17] 유의(4/4셀). **절감 메커니즘(§10)**: 출처는 사실상 선석초과 한 항(Δ−6.44/−6.12, ρ33 증폭) — RL이 사전재조작(PRE_REHANDLE) SF의 5배로 유휴슬랙을 본선준비에 전환→STS굶주림 −704초→선석초과 −11.7분. 볼륨 아닌 타이밍(재조작총량 불변). 대가는 트럭꼬리 P95 +6.7분. **가드채택 반납 3자분해 ([상세](../docs/strategy-history/2026-07-25-가드채택-총비용반납-3자분해.md), 적대검증 3-agent)**: SF/무가드/가드 동일 15seed — 가드 반납(가드−무가드)=+0.01 [−1.80,+1.83]·독립대역(+700) +0.07 재현 = **총비용 무가드와 구분불가 수준 유지 + 트럭P95 SF수준 복구(31.49 vs SF 31.84)**. 단 "공짜" 아님(적대검증 교정): CI 0포함=미탐지일뿐 상한 절감의 ~30% 반납 가능·TOST 미실시. 실체=**구성이 본선→트럭 이동**(가드가 선석초과 이득 31% 반납+3.58분→트럭 truck_wait −0.92·long_wait −1.03 매입). 셀별 부호갈림(high-loose 실질반납 −15→−10.3%·high-tight 오히려이득 −6→−9.2%, 평균~0은 상쇄). 절감크기 취약(ρ33 가중+seed대역 조건부 −6~−10%). 전부 문헌보정 시뮬·실운영 주장 아님. **정책 최적화 (사용자 지적 "사후가드≠최적화", [상세](../docs/strategy-history/2026-07-25-정책최적화-학습안정화-점증대기비용.md))**: ①진단 = 채택RL은 사실상 미학습(발산 — val_berth 73→137·best_ep20=랜덤시기, 본선이득=후보집합 산물). ②안정화 = 하이퍼 실패 → 보상스케일(r/20)+DoubleDQN 부분성공(best_ep20→100·본선 학습 실효). ③목적함수 불균형(ρ33 vs 선형트럭) 진단 → 트럭 SLA직전 방치·불안정. ④점증-대기비용(사용자 step4, 훈련전용 shaping: f≥0.5 convex램프) → frontier RAMP0(본선극단)→1(균형)→3(트럭극단). ⑤RAMP=1.0 15-seed 검정(시드88000): berth −7.77·평균 −1.64·총비용 −9.2% 유의·P95 비열등. **⑥강건성(시드99000) → 이득 절반이 시드 운**: 트럭평균 유의·P95 비열등은 **견고 재현**(가드없이 트럭보호 학습 내재화=사용자 접근 실증) but **본선·총비용은 비재현**(berth −0.48·총비용 +3.7% 무의) → "가드없는RL=가드있는RL 동급" 철회. 뿌리=학습노이즈. ⑦노이즈감축(Polyak+EMA+3시드 앙상블) 시도 → **실패**: 앙상블 berth −7.62·총비용 −9.5% 유의처럼 보였으나 Leave-one-out 이 **seed88000 견인 확증**(88000 빼면 berth −2.93 비열등·+2.3% 무의 붕괴), P95 비열등도 전체3net만(2net 전부 열세)=1-instance 창발. 적대검증(2agent, stats 독립재현 CONFIRMED·adversary high1+medium3) 반영 "앙상블=가드RL동급" 철회. **최종: 트럭평균만 견고 재현·본선/총비용/P95는 단일시드 운(재현불가)** → 반응형 net 은 트럭 신뢰학습·본선 재현불가(증류 관측별칭·"본선=lookahead 필요"와 정합). 다음=rollout/하이브리드 본선 or RL 트럭한정. 임계값 assumed 문헌보정 시뮬. ⑧**오라클 vs 현실형 예측 rollout (2026-07-25, 사용자 지시, [상세](../docs/strategy-history/2026-07-25-오라클vs현실형-예측rollout.md))**: 코드확증 = 기존 rollout(`_rollout_cost` deepcopy→실제엔진)은 큐의 `actual_block_arrival`(진짜미래) 소비 = **오라클(상한)**. 현실형 = 미래 트럭 도착을 `provided_eta±Uniform(300)` 예측 K=4표본 교체(단일 leak=도착시각, 본선은 이미 공개 cadence). 2셀×5seed 짝지음: **현실형 berth vs SF −16.41 [−24.2,−8.61] 유의개선 · vs 오라클 −2.05 [−9.93,+5.83] 구분안됨 · 트럭평균 −5.03 유의 · 총비용 −18.6% 유의**. **판정: 본선 이득이 공개정보(ETA±오차·본선계획)만으로 유지 = 하이브리드 근거 성립** — 반응형RL(본선=시드운)과 정면 대비, rollout은 본선 신뢰개선. 한계: 도착시각 불확실성만(노쇼·물량구성 미검=YR-019)·2셀5seed·K4·eta300. 다음: YR-089 B-C 트럭시간 → 하이브리드 별도 등록 → YR-041 재검정 |

---

운영: 시작 시 [ready.md](ready.md) 에서 pull. 종료(commit) 시 [done.md](done.md) 로 이동 + commit 링크 박제.
