# 🟢 In Progress

> 현재 진행 중. **한 번에 1개** 권장. [index](README.md) · 인접: [ready](ready.md) → 여기 → [done](done.md).
>
> **사후 비용계약 변경(2026-07-30)**: YR-135의 현재 라벨은 계단형 트럭비용·본선 33의
> v1 계약이다. 결과는 V/A 구조 진단으로만 보존하며 새 정책 채택 근거로 승계하지 않는다.
> 구조가 통과하면 YR-136 점증비용 v2로 신규 라벨을 생성해야 한다.

| ID | Epic | Title | Priority | 착수 | Note |
|---|---|---|---|---|---|
| YR-211 | Exp | **고전 배차 규칙 대조군 — 재배치 축으로 번역** | 🟢 진행중 | 2026-08-25 | **★구현 완료 2026-08-25 (사용자 지시) · 판정 미실시.** `actors/classical.py` 의 `ClassicalMarket` 이 `Market` 과 **같은 자리**에 꽂힌다(상속 — `decided`·`newly_eligible`·창 규칙 재사용, **누가 고르느냐만** 다름). `stage.ARMS` 7종(`TODO_ARMS` 비었음). **동결**: `TRIGGER_TOP_K=0.30`(공개 예상 대기 상위 30% 블록만 후보) · `SERVICE_REF_S=180`. **시험 24건 통과(36분)**: 반사실 0 · `n_time==0`(공간만) · `0<거래<결정`(트리거 작동) · 재현성 · 팔 구분(최소 3개 서로 다른 Φ). **★설계문서와 다르게 한 것 셋 — 전부 명시**: ①**트리거를 고전 팔에만** — RL 은 `KEEP` 을 스스로 고를 수 있고 이미 그 없이 학습됐다([[YR-229]] 비교 보존). 이 방향은 **고전 팔에 유리 = 우리 주장에 보수적**. ②**`SPT` 는 이 무대에서 판별력 0** — `Order` 에 작업시간 필드가 없고 엔진 서비스 시간은 기하학이며 규격은 적치 탐색에만 쓰인다. 야적 좌표를 주면 **SPT 가 RL 보다 정보가 많아져** 비교가 반대로 기운다 → 정보 계층을 맞췄다. **팔에서 안 뺀다**(사전등록). ③**고전 팔은 공간 이동만** — 시간 이연에 대응물이 없다. **RL(공간+시간) vs 고전(공간만)** 행동 폭 차이가 남으므로 **논문에 명시**하고 필요시 RL 공간전용 진단 팔을 돌린다. **남은 것**: 1단계 고전 팔끼리 서열(비판정) → 2단계 RL 붙여 **주판정**([[YR-229]] 방식). [[YR-230]] 이 20코어를 쓰는 중이라 끝난 뒤 한 번에. **★2026-08-25 승격 — 이게 없으면 주장을 못 한다.** [06 §3](../docs/architecture/06-학습과-판정.md) 동결 규약: *주판정 = **규칙 대비**, 안 팔기 대비는 **판별력 0**([[YR-185]])*. [[YR-229]] 가 안 팔기 대비 이득(14/16일·p=0.0042)을 냈지만 **그 축은 기각된 축**이라 *"RL 이 유효하다"* 를 못 말한다. 고전 규칙 팔 5종(FCFS·SPT·LEAST_SLACK·NEAREST·NETGAIN)을 구현해 **[[YR-229]] 와 같은 방식**(정책 고정·16일 부호검정)으로 RL 과 견줘야 비로소 주판정이 성립한다. ⚠️ `run_episode` 가 미구현 팔을 **즉시 실패**시키므로 조용한 대체 위험은 없다. **★사용자 지적 2026-08-20**: *"greedy 를 규칙이라 하기엔 애매하다. RL 성능 비교는 No Reallocation·FCFS·SPT·EDD/Least Slack·Nearest 와 해야 한다."* **문제는 성능이 아니라 위치다** — 현행 `UnifiedNetGain` 은 **우리 비용식 `J` 에서 직접 유도한 자체 휴리스틱**이라 순환적이고 문헌에 이름이 없다. "RL 이 이 하나를 못 넘었다"는 문장은 일반화되지 않는다. **★번역이 필요**: 나열된 규칙은 전부 *"다음에 어느 작업을 처리할까"*(sequencing)인데 우리 축은 *"어디로·언제"*(assignment)다. 게다가 재배치는 결정이 **둘**(누구를 + 어디로)이라 **목적지 규칙을 짝지어야** 한 팔이 완성된다 — FCFS/SPT/Least-Slack 은 `누구를` + Least-Loaded, Nearest 는 `어디로`. **★공통 트리거 고정**: 고전 규칙에는 "안 옮긴다"가 없다. 출발 블록 공개 예상 대기 상위 `k`% 일 때만 후보 생성하고 **`k` 는 전 팔 동일 동결** — 안 그러면 고전 팔이 무차별 재배치를 해 실제보다 나빠 보인다(불공정 대조). **기존 자산 주의**: `baselines.py` 의 `FIFOPreference`·`ServiceFirstSPTPreference` 는 **크레인 층 전용**이고 FIFO 는 실현 도착을 읽는 미래정보 누출([[YR-107]])이라 진단 전용 — 재배치 축은 새로 짜고 **전 팔 공개 정보만** 쓴다. **SPT 는 판별력이 거의 없을 수 있다**(서비스 180초 상수) — 그래도 팔에 넣고 "이 무대에서 SPT 는 무의미"도 결과로 보고한다. 결과 보고 팔을 빼지 않는다. **부하 3수준 전부**에서 잰다. 1단계(고전 팔끼리)는 학습 없어 비판정. **선행 [[YR-210]]** — 과포화에서 재배치가 무가치하면 대조군을 세워도 전부 No Reallocation 에 진다. [spec](../docs/dashboard-task-specs/YR-211-classical-dispatch-baselines.md) |
| YR-230 | RL | **정책이 "미룬 곳이 붐빌지"를 못 본다 — 정보 축** | 🟢 진행중 | 2026-08-25 | [[YR-227]] 이 *오전 라벨의 부호가 반대*임을 밝혔는데, 코드를 열어 보니 **원인이 둘**이었다. 둘째가 이 row 다: **정보가 아예 없다.** **실측(2026-08-25)**: Seller 행동 8칸 = `[KEEP, SPACE, TIME, 목적블록부하, 목적블록여유, 경로차이, ★몇 분 미루나, 안벽거리]` — **미룬 그 시각이 붐빌지 나타내는 칸이 없다**. 실제 행 `[0,0,1, 0,0,0, 0.25, 0]`(TIME·15분). **막힌 곳 셋**: ①`announced_soon` 이 **30분 앞까지만** 센다(`_ANNOUNCE_HORIZON_S=1800`) — 2시간 뒤로 미루면 그 시각을 못 본다 ②`slot_load` 를 Seller 함수가 인자로 받지만 **출력에 안 넣는다(죽은 인자)** ③`slot_capacity` 실측 `{}` — `_slot_left(3)→None`([[YR-176]] 미구현). **그래서**: 라벨을 고쳐도 정책이 *"미루면 피크에 떨어진다"* 를 **배울 수단이 없다.** 정보 축이 라벨 축([[YR-227]])보다 **먼저**일 수 있다. **설계 원칙 2 준수**: 한 번에 한 축 — 후보 정보를 한꺼번에 넣지 않고 **①목적 시각의 예상 혼잡 1칸**부터 넣고 자기 실증한다. **판정**: [[YR-229]] 와 같은 방식(정책 고정·16일 부호검정)으로 부하 5,000 이 기준선 아래로 내려오는가. | **★착수 2026-08-25 (사용자 지시)**. **처방**: `announced_around(블록, 중심시각±30분)` = 그 시각 그 블록에 **통지된** 물량 수. 세 행동 모두에 값을 준다 — KEEP=예정시각·현재블록 · SPACE=예정시각·**목적블록** · TIME=**예정+이연시각**·현재블록. `in_out_reserve_s` 는 통지값이라 `announced_soon` 과 **같은 출처** — 정보 경계 안전. `SELLER_ROW_DIM` 23→24 · 파라미터 5,761→5,825. **★착수 전 확인(학습 없이 · 부하 5,000 · 결정 4,310건)**: 값 0~100대·표준편차 7.46 · 0 아닌 비율 **97.9%** · **한 결정 안 TIME 후보들의 폭 평균 8.31대**(폭 0 인 결정 0.7%) · '가장 한산한 이연' vs '그대로 두기' 중앙 **−8.0대**(90.9% 음수) → **정책이 고를 수 있다**. **사전등록**: [[YR-222]] 와 **완전 동일**(회차16·라벨200·검증20%·시드 9,900,600) — 바뀐 것은 입력 1칸뿐. 작업자 20. **판정([[YR-229]] 방식·정책 고정 16일 부호검정)**: **주 = 부하 5,000 이 기준선 아래로 내려오는가**(대조군 +6,273,522원·1/16일) · 부 = 3,500 이득 유지(−1,551,913원·14/16일) · 진단 = 오전 창 손해 감소(대조군 +5,546만원/4일). ⚠️ 이전 체크포인트는 23칸 계약이라 새 코드로 못 읽는다 — [[YR-229]] 대조 수치는 박제돼 있어 재실행 불필요. [spec](../docs/dashboard-task-specs/YR-230-arrival-pressure-feature.md)



---

운영: 시작 시 [ready.md](ready.md) 에서 pull. 종료(commit) 시 [done.md](done.md) 로 이동 + commit 링크 박제.
