# 🗂️ Backlog (미래)

> 미착수·미래 작업. 방향이 자주 바뀌면 재정렬. [index](README.md) · 다음 상태: [ready](ready.md).
> 순서는 [최종전략 전환 이력](../docs/strategy-history/2026-07-15-YR-034-final-integrated-strategy-pivot.md)과 각 task spec의 의존관계가 기준이다. 과거 Phase/Exp 명칭은 완료 이력에만 남긴다.
> **평가계약 정정(2026-07-27)**: 아래 과거 서술의 P95·`δ=3분`은 역사적 결과다. 향후 작업은 [P95 보고용 전환 결정](../docs/strategy-history/2026-07-27-P95-평가계약-정정.md)을 따른다.
> **비용계약 정정(2026-07-30)**: YR-133의 실제 견적 선결은 YR-123 v1 API 존재만이 아니라
> [YR-136 점증비용 v2](../docs/dashboard-task-specs/YR-136-smooth-cost-contract-v2.md)의 학습·평가 계약 정렬까지다.
> **TOS 연동 범위 제외(2026-08-05 사용자 결정)**: 외부 TOS는 최초 배정 입력의 배경 가정으로만 둔다. 실제 API·ACK/NACK·상태 동기화는 구현·평가하지 않으며, 다중 블록 연구는 시뮬레이터 내부 `TransferResolver`의 비용효과까지만 검증한다.

| ID | Epic | Title | Priority | Note |
|---|---|---|---|---|
| YR-175 | Exp | **BUY 견적망 단조성 부호 오류 교정** | 🔴 | 견적망이 "그 칸 예약이 늘면 부담이 준다"고 답한다(실제 계획표 재검사 위반 99.8%, Δ평균 −0.0237). 원데이터는 반대다(예약 대수 vs 실현 부담 순위상관 +0.2953, n=28,530; 5분위 평균 부담 0.639→1.508). 전체 순위는 0.8024 로 잘 맞히지만 **결정에 가장 직접 쓰이는 입력의 부호가 틀렸다** — resolver 가 "내 트럭을 이 칸에 더하면?"을 물을 때 정확히 그 방향을 묻기 때문. 검증 통과 전에는 "검증된 견적기"로 부르지 않는다. 후보: 그 칸 예약 특징에 단조 증가 구조 제약 / 특징·라벨 재설계 / 라벨을 한계부담(추가 1대의 증분)으로 바꾸기. [spec](../docs/dashboard-task-specs/YR-175-buy-monotonicity.md) |
| YR-177 | Exp | **세 선택지의 값매김 기준 불일치 — YR-171-C 판정 해석이 걸림** | 🔴 | resolver 가 한 줄에 놓고 비교하는 세 값이 서로 다른 자로 잰 값이다(`sell_review.py:345-399`). 공간 좌표의 `add` 와 원래 자리 `relief` 는 `cost_given_queue` → **자기 비용만**(`j_truck` 한 항, `sell_gain.py:59-71`)인데, 시간 좌표만 견적망 값 → **자기 51% + 뒤차 30% + 본선 19%**(`dataset_meta.json`, n=28,530)다. 눈금 확인: 라벨 평균 1.21 비용시간 중 자기 몫 ≈0.62 ≈ 계산식 트럭 1대 비용 0.65(Φ2,346÷3,600) → **어긋난 건 정확도가 아니라 기준이고, 시간 후보에만 약 +0.6 비용시간이 고정 세금처럼 붙는다**. 실측 정합: 시간 좌표 비중 60.0%(fixed15) → 42.0%(계산식) → **17.1%**(견적망), 공간 판매 240.8 → 170.3 → **901.3**/일. 시간 판매는 이미 1건당 +14.28분 손해 축이므로 **−135.5 는 "손해축 억제"만으로도 전부 설명된다** → "이득의 원천은 하루치 정보"는 **잠정**. 판별 = 정책 고정·값매김만 교체(E1~E4, 정책 재학습 없음). **핵심 대조 = E2(계산식·자기비용) 대 E4(견적망을 자기비용 라벨로 재학습)** — 기준·정책이 같고 "지금 대기열만 봄 대 하루치 장부를 봄"만 달라 **정보의 순수 가치**를 잰다. 정답지 28,530행이 own·blocked·vessel 을 항별로 갖고 있어 시뮬 재수집 없이 라벨만 교체 → **1시간 이내**(YR-171-C 평가 실측 33분 기준). 교정 권장 = 공간·relief 도 외부효과 포함으로 올림(YR-174 `realized_credit` 과 같은 기준). YR-175 와 별건(그건 견적망 **안**의 부호, 이건 견적망과 **바깥 계산식 사이**의 기준). [spec](../docs/dashboard-task-specs/YR-177-quote-basis-mismatch.md) |
| YR-184 | RL | **최종 목표인 "단일 통합정책"을 한 번도 학습·평가한 적이 없다** | 🔴 | **사용자 지적(2026-08-17)으로 확인**. ①**크레인 정책은 학습 대상이 아니다** — `yr151_transfer_ppo.py:151-153` `actor.eval()`·`requires_grad_(False)`, 옵티마이저(:464)에 들어가는 건 판매 머리뿐이고 매 회차 해시로 불변 검사까지 한다. ②**그 크레인 정책은 다른 무대 산출** — 체크포인트 메타가 `train_band: BASE+16..31`·셀 `mid/high × loose/tight` 로 **구 2블록 부하셀 체계**(`yr090_dense_vessel`)다. 현 무대는 21블록·24시간 이중피크·고정 재공량. **옛 세계에서 배운 두뇌를 새 세계에 얹어 쓰고 있다**. **최종 목표와의 거리**: README 기록 목표는 "처음부터 같은 State·Action·Total Cost 계약으로 다루는 **단일 통합정책**"(2026-07-15 사용자 결정)인데, 현 구조는 BUY 동결·SELL 학습·Resolver 결정론·**집행 동결**이다. **역할 분리 자체는 정당**했다(연구 설계 원칙 2 — 한 번에 한 축씩). 문제는 **분리한 채로 최종 목표를 향해 전진했다고 보고해 온 것**이다. **그래서 지금까지 잰 것의 정확한 이름** = "통합정책의 비용 개선"이 아니라 **"옛 무대에서 학습된 고정 크레인 정책 위에 판매 층 하나를 얹었을 때의 차이"** 이고, 그 판매 층조차 규칙에 졌다(YR-179). **세 갈래(결정 요청)**: A 현 무대에서 크레인부터 재학습 / B 크레인+판매 동시 학습(최종 목표 그대로) / C 범위를 "고정 배차 위의 재배치 층"으로 정직하게 좁히고 통합은 향후 과제로 명시. **선결(어느 안이든) = YR-180** — 현 무대에서 고정 크레인 정책이 규칙(SF-SPT)을 이기는지부터. 옛 두뇌가 새 무대에서 규칙보다 나쁘면 A·B 는 위에 뭘 얹어도 무의미하고 C 의 전제도 무너진다. **현재 전체 연구의 최우선 미측정 항목**. [spec](../docs/dashboard-task-specs/YR-184-integrated-policy-never-trained.md) |
| YR-183 | Exp | **세 팔 모두 수렴 전에 끊겼다 — 팔 비교가 학습곡선 위치를 재는 중일 수 있음** | 🔴 | **예산 25%**: 동결 예산은 40회차×4에피소드=160 에피소드/시드인데 YR-170·174·171-B/C **전부** `N_ITER=10, EPS_PER_ITER=4`(40 에피소드)로 돌았다. 축소 사유는 기록돼 있으나(1회 10분→160편 27시간) **축소 실행 결과가 그대로 판정으로 쓰였다**. **수렴 안 함**(시드 8,400,000 Φ): fixed15 2914.2→2544.6 · slots48 **3656.9→2571.1** · buy 2441.6→2372.6 — **세 팔 다 마지막 회차까지 단조 개선 중**이고 시간 판매 건수도 전부 아직 감소 중(2650→1134·2754→903·769→446). slots48 은 10회차에 Φ 1,086 하락하며 여전히 가파름 → **서로 다른 속도의 주자를 임의 지점에서 세워 순위를 매긴 것**. **★더 중요 — 팔 차이의 대부분이 학습 전에 이미 있다**: `iter 0`(학습 0회·같은 시드·같은 초기화라 정책은 사실상 동일, 다른 건 행동공간과 값매김뿐)에서 Φ 가 slots48 **3656.9**(시간 2754) / fixed15 2914.2(2650) / buy **2441.6**(시간 **769**) — **이미 1,215 벌어져 있고** 방향이 시간 판매 건수와 정확히 일치. 견적망 팔은 시간 슬롯을 **3.6배 적게** 판다. → **YR-177 해석 B(값매김 비대칭이 손해축을 억제)를 거의 직접 확증** — 좋은 칸을 고른 게 아니라 비싸게 매겨 막은 것이고, 학습 전부터 그렇다. 같은 표가 `48칸이 더 나쁨`도 학습 전 존재함을 보여준다. **조치**: ①YR-177 판별 먼저(재학습 전 기준 통일) ②기준 확정 후 동결 예산 재학습 ③그때까지 팔 대비를 수렴 후 결론으로 인용 금지. **예산 결정 요청**: 40회차 그대로(팔·시드당 27h) / **수렴 기준 사전등록 후 조기종료(권장)** / 에피소드 단축(`find_slot` 최적화) 선행. [spec](../docs/dashboard-task-specs/YR-183-undertrained-arm-comparison.md) |
| YR-178 | Infra | **게이트의 "지금 허가된 축" 이 하드코딩 — 이미 닫힌 축을 가리킨다** | 🟡 | 2026-08-17 재발행(`805f9f8`)에서 발견: 세 축은 reliability PASS·scenario_validity **PASS**·performance INCONCLUSIVE 인데 `currently_authorized` 는 `"reliability PASS → YR-150: scenario_validity 단일축"` — **이미 통과한 축을 보정하라고 한다**. 같은 JSON 의 `conditional_sequence` 는 performance 라고 적어 **한 파일 안에서 두 칸이 다른 말**을 한다. 원인은 `yr151_gate_report.py:122-127` 의 세 칸이 전부 하드코딩이고 `currently_authorized` 는 `reliability` 하나만 보고 갈리는 것 — scenario_validity 가 FAIL 이던 2026-08-06~09 시절 문자열이 그대로 남았다. **왜 위험한가**: 이 칸은 "다음에 무슨 작업을 해도 되나"를 알려주는 자리다. 틀리면 헛일을 하거나, 더 나쁘게는 **게이트 안내를 통째로 무시하는 습관**이 든다(진짜 막아야 할 때도). 지금까지 사고가 없던 건 사람이 원 판정 세 줄을 직접 읽어서지 이 칸이 맞아서가 아니다. **교정**: 문자열 상수를 없애고 판정에서 유도 — 미해소 축 중 의존 순서(`reliability→scenario_validity→performance`)상 가장 앞선 것이 허가 축, 전 축 PASS 면 확증·잠금평가. 8조합 단위시험 + **판정값은 한 글자도 안 바뀔 것**(안내 문구만 고침). [spec](../docs/dashboard-task-specs/YR-178-gate-authorized-axis-hardcoded.md) |
| YR-176 | Exp | **TIME 좌표 슬롯 용량 미반영** | 🟡 | matching 이 `TIME@k` 에는 가상 용량(`vcap`)을 갱신하지 않는다 — 공간 좌표만 갱신(`sell_review`). 같은 epoch 의 여러 제안이 같은 칸을 고를 수 있고 아무도 막지 않는다. 현재 실측은 하루 186건이 18~20칸에 흩어져 쌓임이 없었으나 **구조적 구멍**이며, 판매량이 늘면 드러난다. YR-171 명세의 "receiver/슬롯 용량을 함께 지키며 매칭" 요구와도 어긋난다. [spec](../docs/dashboard-task-specs/YR-176-time-slot-capacity.md) |
| YR-173 | Infra | **견적 모델용 데이터 적재 (지금은 쌓기만)** | 🟡 | **★진행 중 → backlog 이동 (2026-08-17)**: 등록 이후 **한 번도 실행되지 않았다** — `outputs/reports/yr173*` 산출물 0건. 그동안 YR-170·YR-174·YR-171 학습을 여러 판 돌렸으므로 사용자 지시("일단 쌓아둬")가 노린 **공짜 수집 기회를 놓쳤다**. 다만 폐기는 아니다 — YR-171-B 가 만든 정답지(트럭별 실현 부담 28,530행)와 **입도가 다르다**(이쪽은 60초 격자 **결정 단위** 상태). 코드(DecisionRecorder·`_extra_review=` 훅)는 그대로 살아 있고 기본값 None 이라 학습 경로 바이트 불변. **재개 조건**: 다음 학습·평가 실행 때 훅만 켜서 동승 수집. **사용자 지시(2026-08-13)**: "일단 쌓아둬". **왜**: YR-171 의 BUY 견적을 시뮬레이션 가상 실행으로 하면 OFFER 1건당 960후보 × 하루 14,400건 = **1,380만 회**이고 실측 결정 1회 45ms 기준 **173시간/에피소드**(현재 9분)라 실행 불가. 대안 사다리 ①범위 축소 ②계산식 직접 개선 ③**견적 예측 모델** 중 ③으로 갈 경우 데이터가 필요한데, 실험을 돌리는 김에 공짜로 나온다. **왜 지도학습이 유리한가**: 견적은 정답이 있다(굴려보면 나온다) → 예측 대 실제 직접 대조 가능, 960 후보 일괄 질의는 GPU 유익(현 병목 find_slot 은 GPU 무익 — 별개). **★데이터 지위**: 시뮬레이션 산출물이지 **실제 터미널 운영 기록이 아니다** — 산출물 _header 에 박제. 학습한 모델은 이 시뮬레이터의 근사이며 실제 항만 성능 주장 불가. **구현**: DecisionRecorder(60초 격자 블록 상태 → 종료 시 원장·장부 조인해 실현 체류·벌금·비용 부착, 입력은 공개 정보만)·run_episode_diurnal(_extra_review=) 관측 전용 훅(기본 None → 학습 경로 바이트 불변). **남은 일**: 학습 종료 후 수집 실행 · code 1ff6cf9 · [spec](../docs/dashboard-task-specs/YR-173-estimator-dataset.md) |
| YR-169 | Exp | **판매 정책 하루 총비용 비교 (안 팔기 vs 계산식 vs 학습)** | 🔴 | **무엇**: 판매 1건이 아니라 **하루 종일 판매하는 정책**과 안 파는 정책의 하루 총비용을 시드 단위로 비교한다. **왜**: YR-168 파일럿이 보인 것은 판매의 무익함이 아니라 **단건 설계의 측정 한계**다(잡음 σ=11.15 vs 단건 효과 0.139). 정책 단위로 가면 하루 1,800~2,100건의 효과가 누적되는 반면 연쇄 잡음은 그만큼 빨리 커지지 않아 신호대잡음이 개선될 여지가 있다 — 다만 **이것도 사전에 파일럿으로 확인해야 한다**(자동 개선 가정 금지). 41차 감사가 "최종 운영 가치는 여기서 판단"으로 지정한 범위이기도 하다. **선행**: 하루 단위 σ 눈가림 파일럿 → 표본 동결 → 실행. 5차 계약 개방 루프라 두 정책이 같은 트럭·같은 시각을 받아 정책 단위 비교의 전제는 이미 확보돼 있다. [spec](../docs/dashboard-task-specs/YR-169-sell-policy-daily-cost.md) |
| YR-166 | Exp | **투입 고정 짝 비교 계약 + 공간 판매 0B 재판정** | 🔴 | **YR-165 판정 A 의 직접 후속(`2a13e68`)**: 짝 비교에서 투입기가 두 세계에 **다른 트럭**을 넣어 판매 효과와 섞였다 — 계약을 **투입 고정**(기준 세계의 투입 계획 재생)으로 바꾸고 그 환경에서 **공간 판매 0B 를 다시 판정**한다. 선결: ①투입 고정 계약을 사전등록으로 동결(표본 수·판정 축·하드 가드는 기존 0B 계약 승계, 무대 = w3-L100·새 잠금 시드) ②authorize-next 재인가 ③40쌍 재실행. **주의**: YR-165 예비 관측에서 고정 투입 하 이득이 양수 3/8 로 작아졌다 — 재판정 결과가 STOP 이어도 그대로 박제(완화 금지). 기존 0B STOP 판정은 **투입 미고정 환경의 판정**으로 이력 보존 · [YR-165 spec](../docs/dashboard-task-specs/YR-165-space-sell-global-effect-isolation.md) |
| YR-163 | Infra | **1세대 Δ-net 수렴 테스트 환경(torch) 민감 조사** | 🟡 | **YR-160 골든 회귀(2026-08-10, 820통과/실패1)에서 발견**: test_residual_delta_net 수렴 판정이 venv 재생성(torch 재설치) 후 허용오차 실패(−2.22 vs −2.5±0.15) — 대상 코드 20+커밋 무변경·골든 궤적 전부 통과라 환경 기인 추정. **허용오차 완화 금지** — torch pin 재현으로 가설 확증 후 결정론 판정으로 재설계(사전등록) · [spec](../docs/dashboard-task-specs/YR-163-delta-net-test-env-drift.md) |
| YR-162 | Exp | **예약 준수오차·ETA 오차 probe — ★A 통과(2026-08-09)·B 는 0B 후** | 🟠 | **★A 판정(2026-08-09, 트리 `e478eac`·dirty=false·전항 PASS)**: σ=600s 동결(저장소 선례)·w5-L100 무대 — 투입 517대 중 516대 오차 실재(평균 474초), **방어 발화 실측: 원자 확정 거부 40회**(공개값으론 진입 전·실현은 이미 진입한 조기 도착의 이연 시도를 거부)·SKIP 12회 — **죽은 경로 우려 해소**. 정보경계 누출 0·결정론·불변식 ✓. 1차 실행에서 A2 검사식 결함(판매·이연 정당 갱신 트럭 오검사) 발견·정정 이력 박제 · [probe JSON](../../outputs/reports/yr162a_eta_probe/probe_a.json) · **B(0B 후)**: 무오차 GO/STOP 방향 유지 검사 + K/Q30/S 잠금평가 오차 축. 그 전까지 성능 주장은 "예측 무오차 조건 한정" 문구 의무 유지 · [spec](../docs/dashboard-task-specs/YR-162-eta-error-probe.md) |
| YR-161 | RL | **시간 판매(재예약) — ★축 확정: 반입=공간·반출=시간·양하/적하 제외 (선구현·테스트 유예)** | 🟠 | **사용자 결정(2026-08-08)**: 판매 축 = **반입(공간) + 반출(시간)** 두 개뿐. **양하·적하는 판매 제외 확정** — 적하: 본선 지연 직결·기사 없음·기존 크레인 행동(작업 순서·DEFER)과 중복. 양하 시간축 동일 사유, 양하 **공간축**만 반입 실증·블록 간 운반 배선 후 확장 후보로 보존. 검토 중 만든 적하 embargo 계층은 **제거**(정책 미편입 계층은 대시보드↔코드 정합을 깨뜨림). **구현 상태(빌드 우선 모드)**: `defer_admitted_entry`(진입 전 재예약 수술·이연 상한 엔진 fail-closed·**비용 원점 = 최초 통지 시각 보존** — 이연이 기사 대기를 장부에서 못 지움) + `time_sell.py`(반출 전용 후보·전원 수락 적용·이연 원장). 전원 수락 = D5 가정("수락률 100% 상한 효과"로 주장 한정). **선결**: lead>0 사전 통지(walk-in lead 0 이면 창 없음 — 공간 판매 0B 와 공유). **검증 부채**: 이연 수술 원자성·비용 원점 보존·전체 회귀 미실행(빌드 우선 지시) · [spec](../docs/dashboard-task-specs/YR-161-time-sell-outbound-deferral.md) · **⚠️ YR-171 로 대체 예정(2026-08-13)** — 고정 +15분 이연이 축 분리 실측에서 1건당 +14.28분 손해로 확인됐다. 새 목표는 같은 작업일 24시간 공개창에서 receiver가 동결 ExecutionHead로 슬롯별 BUY 부담을 계산하고 Resolver가 블록×30분 슬롯을 고르는 구조다. |
| YR-154 | Infra | **게이트 진입 후 이송 경로에도 이송 상한을 엔진에서 막기** | 🟠 | **YR-151 0A 발견(2026-08-06)**: 작업 1건의 이송 횟수 상한(1회)이 후보 생성기에만 있고 엔진에 없었다 — 생성기를 우회하면 같은 작업을 몇 번이고 옮길 수 있다. 0A 에서 만든 **게이트 진입 전** 경로에는 fail-closed 를 넣었지만 **기존 진입 후 경로는 그대로**다. 0A 에서 함께 고치지 않은 이유는 그 경로가 판정 끝난 실험(골든)의 경로여서 — 계약 검사와 판정 코드 변경을 섞으면 원인 분리가 불가능. 착수 시 **상한 위반 이송이 과거에 실제 있었는지 먼저 세는 것**이 판정 · [spec](../docs/dashboard-task-specs/YR-154-post-gate-transfer-cap-enforcement.md) |
| YR-155 | Infra | **재현 스탬프 자기 해시 규약 정정 — 결과 파일 안의 `self_sha256` → sidecar 분리** | 🟡 | **YR-151 0A 게이트 보고에서 발견(2026-08-06)**: 결과 JSON 안에 자기 해시를 적으면 덧쓰는 순간 내용이 바뀌어 **기록값이 덧쓰기 전 파일의 해시**가 된다(0A 실측: 기록 `4862ae71…` vs 실제 `c287a9d5…`). 검증하는 쪽이 쓸 수 없는 값이다. 0A 판정은 커밋된 실제 파일 해시로 고정해 유효하나, 규약을 두면 다음 실험도 같은 값을 계속 남긴다. 정정안 = `<result>.json.sha256` sidecar, 과거 필드는 지우지 않고 뜻풀이만 박제 · [spec](../docs/dashboard-task-specs/YR-155-repro-stamp-self-hash-convention.md) |
| YR-152 | RL | **계산견적 1축 보정·효과 확증 (YR-149 2~4단계 잔여)** | 🟡 | 사용자 결정으로 **YR-151 선결 아님** — YR-151 이후 필요성이 남으면 연다. 승계 동결: 누적합 주판정·906100 표본에서 축 확정 금지(두 집계 열람)·소스 잔차 부호 일관(체계적 편향) vs 본선 부호 혼재(분산) · [spec](../docs/dashboard-task-specs/YR-152-quote-axis-refine.md) |
| YR-151 | RL | **Block PPO SELL — H-21 우선 성능, V-21 별도 가중치 후속** | 🔴 | 0A는 A/B 기술계약 8종 20/20 통과(`ae6005b`)했지만 성능 근거가 아니다. YR-150 H-21 현실성 PASS 뒤 0B-H에서 SELL 상금·공개정보 구분력과 학습 GO/STOP을 먼저 판정한다. V-21은 YR-083 역할분리 런타임·자격 후 같은 알고리즘을 **별도 가중치**로 학습·판정한다. 구조별 `정책−동일 구조 SF`만 주판정하며 H/V 원비용 평균 금지. **TransferHead 🟡 실행 골격 선구현(2026-08-09 — 외부 감사 치명 7건 정정: critic zeros→결정시점 저장·Φ 행동직전 기록·GAE 주장 삭제(MC−V λ=1 명시)·repro_stamp 인자·60초 격자 가드·공개 통지 접근자·이연 원자성 사전검사. 구조 반영: KEEP 좌표 ΔJ=0·matching 용량 검사·시드 순서. 학습계약 완성·검증 미완 — 장시간 학습 금지). **ExecutionHead 연결 완료(2026-08-09)**: 채택 실행 PPO(YR-143 confirm C0 ppo_s221000 + YR-125 StateNorm + 대기 허가증 + C0 플래그) 동결 로딩·requires_grad=False·매 iter hash 불변 검사·스탬프에 hash 박제. SF 는 배선 디버그 opt-in 으로 강등(기본 = 채택 PPO·체크포인트 부재 시 fail-closed). 로딩·동결 실검증 완료(hash 0457efbd…)**: 의미 규정 = 이 작업을 포함한 현 계획이 나쁘다 · B-(b)(축 선택 = resolver 비용 저울·가중치 1벌) · 보상 (i) 실현 전역 증분(떠넘기기 자동 청구) · transfer_head.py + UnifiedSellOrchestrator + yr151_transfer_ppo.py. **★shadow 실측 검증 통과(2026-08-09, 트리 `0808f45`·dirty=false)**: resolver 관통 dry-run — **S1 본 실행 불변**(기준 런 대비 343개 작업 시간 장부 전수 일치)·S2 배선 흐름(trail 2,090·would-commit 886·critic 입력 비영)·S3 결정론·S4 채택 실행 동결 해시 불변 — [검증 JSON](../../outputs/reports/yr151_shadow_verify/shadow_verify.json). 짝 강제(shadow↔dry_run)·would-commit 원장·환경 무변이 테스트 3종 포함. lead 해석은 32차 사용자 채택으로 동결(30분·내부+확약=L). **★0B "GO" 철회 → 계산식 노출 smoke 로 강등(33차 감사 2026-08-09)**: 구 판정은 **순환**이었다 — 축1은 resolver 가 KEEP(0)을 이긴 것만 기록하므로 자동 통과, 축2의 상관 0.569 는 이득 공식의 입력(혼잡 q)과 출력(볼록 비용 이득)의 자기 상관이며 "어느 작업"의 구분력은 미검사. 결과([smoke JSON](../../outputs/reports/yr151_0b_signal/ob_signal.json))는 삭제하지 않고 **"계산식·배선이 일관 작동한다"는 smoke 증거로만** 보존 — 학습 GO 근거로 사용 금지. **재설계 등록**: 진짜 0B = 반사실 실험 — 같은 상태·같은 난수에서 (안 판 세계 vs 그 이송 1건만 실행한 세계)를 각각 완주해 **실현 총비용 차**를 정답으로, ①실현 이득 부호 ②계산 견적이 실현 이득 순위를 구분하는지(새 시드)를 판정 — [재설계 사전등록](../docs/strategy-history/2026-08-09-YR-151-0B-반사실-재설계-사전등록.md). **★반사실 0B 판정 = STOP(2026-08-10, `404dd01`·dirty=false·인가 allowed=true)**: 40쌍 전부 유효 집행(실격 0·stale 0·상태 해시 사전 동일 40/40). **축1 실패** — 런별 실현 이득 합 5개 중 2개 음수(+15.58/−3.64/+7.63/+5.18/−11.96). **축2 실패** — 런별 견적-실현 순위상관 중앙값 **−0.204**(임계 0.3·개별 +0.44~−0.56 로 부호조차 뒤집힘). **★수치·해석 정정(37차 감사 2026-08-10 — 원자료 재계산으로 확인)**: ①위 런별 값은 **처리량 가드 반영 조정값**이고 순수 비용 합은 +15.58/−3.64/+7.63/**+7.09**/**−11.72**(어느 쪽이든 2개 음수 — STOP 불변) ②처리량 감소 10건 중 **비용도 이득이던 건은 3건뿐**, 나머지 7건은 비용·처리량 동반 악화(구 보고 "10건 모두 비용 이득"은 오류) ③**40건 전부 공간(블록 간 이송) 판매** — 시간 이연·TransferHead 전체를 반증한 결과가 아님 ④"판매 직전 완전 동일"은 전 시뮬레이터 바이트 비교가 아니라 **선택된 핵심 상태(작업·크레인·소유권·투입)의 해시 일치**. **정확한 결론**: 판매 자체가 무의미한 것이 아니라(40건 중 비용 이득 22건·순수 이득 총합 +14.95·처리량까지 반영 시 성공 19/실패 21), **현재 견적식이 이로운 공간 판매를 안정적으로 구분하지 못해** 현 설계의 SELL PPO 학습을 중단한다. 구 smoke 의 ρ=0.569 가 자기 상관이었음은 실현 대조로 확증. **완화 재시도 금지** — 후속은 원인 규명 단일축만(YR-164, 공간 판매 견적 진단으로 한정) · [판정 JSON](../../outputs/reports/yr151_0b_counterfactual/ob_counterfactual.json) · **정식 인가(게이트 재발행) 후 실행 이력**. **★학습 배선 완결(33차 ④, 2026-08-09)**: ①무대 동결 — 학습·판정 = pairing 정정 정본 **w3-L150**(구 균등·L100 은 CLEAR 무대 결함 — run_episode 배선 정정) ②공동 기록→학습 연결 — 배치의 Φ 를 공동 epoch 기록에서 읽고 resolver 결과(축·좌표·ΔJ·확정/거부)를 결정별 join 박제·결정 epoch 누락 시 즉시 실격 ③장부 보존 깨지면 에피소드 즉시 실격. 종단 smoke(라이브 1에피소드·w3-L150): 결정 4,468 전량 조립·resolver join 2,785·공간 336/시간 211 집행·예외 0. 테스트 8종. **★무대 최종 재확정(사용자 2026-08-10, 짝 체계 정본)**: 주 무대 = **w3-L100**(유일 BUSY×3 안정 — 예선·학습)·대조 = **w1-L150**(최종 비교 축·과부하 시드 제외). **★최종 환경 검증 사슬 완결(`da16f50`)**: shadow 본 실행 불변을 최종 환경(0.65·짝 체계·w3-L100)에서 클린 재검증(S1~S4 전항 PASS·330개 장부 전수 일치·would-commit 704)·크레인 계측 146.3~146.5s(문헌 정중앙)·현실성 재제출 PASS 재확정·공식 게이트 = reliability+scenario PASS(잔여 performance). 잔여: YR-160 본체 → authorize-next → 반사실 0B(계약 동결 완료: 5런×8건·런 단위 군집 통계·처리량 하드 가드) → 통과 시에만 Q30·본 학습 · [spec](../docs/dashboard-task-specs/YR-151-block-ppo-sell-head.md) · [0A report](../../outputs/reports/yr151_pre_gate_0a/report.md) |
| YR-134 | Exp | **다음 공동행동 shadow 이중계산 — 작업시작 예측 vs 완료시 실제상태** | 🟠 | **사용자 결정(2026-07-30): 정책 성능 검증 전 제약 추가 금지.** 현재 공동작업 시작 때 다음 완료사건 뒤 예상 `crane_ids`의 공동후보 top-K를 계산하되 별도 진단 로그에만 저장하고, 완료 때 기존 Block Q가 최신 상태에서 자유롭게 다시 선택한다. shadow ON/OFF의 본 실험 행동·비용·난수상태는 동일해야 한다. 실제 정책 선택은 최적 정답이 아니므로 정책선택·C600 참고최선의 top-K 포함률과 선택 후 손실을 분리 측정한다. 통과 뒤에만 soft cache를 별도 단일축으로 등록하며 예약·자동실행은 후순위 · [spec](../docs/dashboard-task-specs/YR-134-next-action-shadow.md) · [결정이력](../docs/strategy-history/2026-07-30-YR-134-다음작업-shadow-이중계산-사용자결정.md) **backlog 유지(7차 피드백)**: 새 공동 Advantage-Q 정책 확정까지 |
| YR-126 | Infra | **1세대 Archive 완료분 태스크 스펙 소급 작성** | 🟡 | spec 필수 규칙(2026-07-29, AGENTS.md) 소급분 — 2세대·활성 row 는 당일 전량 작성 완료, 1세대 Archive(YR-016~078 대 다수)가 잔여. evidence 보고서가 이미 상세하므로 각 spec 은 15줄 내 요약+링크로 일괄 생성 |
| YR-096 | RL | **배포 후보 = 공개정보 예측 rollout 단일 계층 — 트럭·본선·목적지 통합 최적화** | 🟠 | **ready→backlog 하향 (2026-07-26, 사용자 확인: 착수 미확정)** — 이력: 하이브리드는 명시 기각(레이어·레이턴시 비판), "만든다면 단일 rollout" **구조 결정만** 있었고 착수 결정은 없었음. 내용은 유효 보존: 단일 메커니즘(예측 rollout)이 트럭·본선·재조작 목적지(YR-075-c 상금 −4.54)를 한 계획기에서 평가+resolver 제약, RL 은 참고군. 선결 3(①현실형@3600 본선 재검 ②관측기반 목적지 통합 ③YR-093 정보안전). **새 이론(YR-099/100 축)과의 순서·비교관계 재확정 후 진행/폐기 결정** — YR-100 의 4개 비용계약 정정과 겹치는 부분 검토 필요. 최종 판정은 YR-041 |
| YR-102 | RL | **분해 quote — J=J_계산식+J_잔여 (창중 review 우선 — t=0 여지는 잔존)** | 🟡 | **YR-103 G1 반영·사용자 최종평가 정정 (2026-07-27)**: t=0 표본규칙(R=5) 기대상금 +0.91 [−4.62,+6.44] = GO 게이트 실패 — 단 **낙관 하한 −8.5 잔존 = 더 강한 t=0 선택기(분해 quote 포함) 여지가 닫히지 않음**(공개정보 가치 부재는 미확정). 실현별 최적 상이는 확정이나 "숨은 정보 유일 원인" 단정은 순환성 탓 미확정. 순서: 창중 review(YR-099 브리지)가 우선(구조적으로 더 유망) — 분해 quote 는 창중 위에서 1차 재정의, t=0 재도전은 그 결과 후 |
| YR-093 | RL | **예측 rollout 정보안전 — 미통지 고장·계획변경 이벤트 마스킹** | 🟠 | **외부감사 결함4 (2026-07-26)**: 현실형 예측 rollout 이 deepcopy 로 시뮬 전체를 복사해 **미래의 미통지 고장(EQUIPMENT_DOWN)·계획변경(PLAN_CHANGE) 이벤트를 알고** rollout — 조건부 미래정보 누출. ETA 결측 트럭의 실제도착 대체 누출은 fail-closed 로 즉시 정정(`4b44737` 후속 커밋). 잔여 수정: scratch 에서 미공지 injected/PLAN_CHANGE 이벤트 제거 또는 공지분포 대체. **현 YR-087 결과는 돌발 없는·ETA 완비 실험이라 영향 없음(주장 한정 유지)** — 고장·계획변경·ETA 결측 있는 하이브리드 실증 전 필수. [predictive_rollout.py](../../src/yard_rl/integrated/predictive_rollout.py) |
| YR-095 | Sim | **현실 적재 제약 최종 도입 — 중량·냉동·위험물·선사·그룹 규칙** | ⚪ | [spec](../docs/dashboard-task-specs/YR-095-real-stowage-constraints.md) · **사용자 순서 정정(2026-07-26)**: 반입·양하 블록배치 PoC는 공간·높이·규격 등 최소 물리로 먼저 검증. YR-099 상금과 Level 2~3 실제 자료를 확보한 뒤 맨 마지막에 현실 규칙을 추가해 성능·안전 유지 여부를 재판정한다. 현재 PoC 선결 아님 |
| YR-094 | Sim | **엔진 견고성 소갭 묶음 — 감사 지적 부수결함** | 🟡 | 외부감사 (2026-07-26) 부수: ①assign() 외부 API 가 위조·낡은 후보 완전 재검증 안 함(정상 resolver 경로는 안전 — fail-closed 화) ②다중 배정 중 후속 실패 시 선배정 자동 rollback 없음 ③죽은 상태필드(last_move_dir·recent_empty_travel_s 미갱신, recent_throughput 은 누적값) — 제거 또는 실갱신 ④겹친 고장구간에서 첫 UP 이 조기 복구(boolean → 카운트). **⑤ ✅정정됨(감사 2차)**: unfinished_backlog 가 ASSIGNED·RUNNING 미집계(runner·direct_job_env 소비) — 양 엔진 DONE·CANCELLED 외 전부 집계로 수정+테스트 (AUDIT-0726 2차 보완 commit). 잔여 ①~④ 각각 소규모·행동 영향 낮음이나 계약 정직성 건 |
| YR-088 | RL | **일반화-우선 강화학습 아키텍처(B) — 계획법 품질을 학습 정책 속도로, 규모/설정 무관** | 🟠 | **사용자 결정 (2026-07-23)**: A(계획법 직접 배포) 아닌 **B(일반화 학습 정책)** 채택 — 목표 "계획법 품질 × 학습 속도 × 규모 일반화". [결정 문서](../docs/strategy-history/2026-07-23-일반화우선-강화학습-아키텍처-B채택.md). **판정: FT 는 순수 RL 아님(계획법 모방+반사실 조각)·실패는 관측별칭.** 첫 관문 = 별칭 해소: ①라벨 충돌률 측정(별칭=표현 문제 확정 게이트) ②요약벡터→집합/그래프 입력 ③크레인 수 무관(순열불변) 구조 + 도메인 랜덤화 학습. 교사=계획법(품질 실증). A 는 품질교사+소규모 안전망으로 유지. 의존 YR-081·본선설계v2 §7·YR-082/085/086. 단계 게이트로 손절 · **본선 관측 구체화 (2026-07-23)**: 중심을 risk 1개→"실질 마감여유+병목위치+후보 병목해소 효과"로. per-candidate 본선효과가 1차 신호(크레인 액션이므로). **선결=양하 역압력 엔진 수정 ✅ done (2026-07-23 `c93918e`)** — `yard_handover_cap` opt-in(기본 off=골든 불변), YC 적재 지연이 STS 역압력으로 배 지연에 반영(회귀 131+역압력 4 통과). **최소 본선 신호 ✅ done (2026-07-24 `2a65516`)** — 사용자 최소화 반영: 정책 상태=컨테이너+schedule_slack(스케줄 급함·트럭 ETA 본선판)+flow_margin(STS 굶/막힘 여유 1개), STS/YT raw 미노출. `vessel_signals.py`·tests 5(flow_margin STS막힘 100%≤0). **본선판 ETA 후보 ✅ done (2026-07-24 `d7fd4d1`)** — `CandidateGenerator(vessel_prep=)` opt-in(기본 off=골든 불변), 다가올 적하 blocker 스케줄 기반 선제정리 PRE_REHANDLE 발행(트럭 PRE의 본선판). golden 5+tests 4. payoff(rollout 1800 본선보호목적, 3seed) 선석초과 넷 −5.5분(67.7→62.2)·노이즈큼·트럭 중립. [병목중심→스케줄중심 최소 설계](../docs/strategy-history/2026-07-23-본선상태-후보설계-병목중심.md) · **②인코딩 배선 ✅ done (2026-07-24 `245927a`, 병렬 세션 조율승인)** — flow_margin 을 vessel 그룹에 추가, SCHEMA `itc-v4→itc-v5`(vessel 8→9필드·인코딩 자동감지 246→250차원), adapter 계산·golden 3파일 재생성·numeraire_v1.yaml·버전 assertion 갱신, **전체 회귀 378 통과**. **인수인계: rl_net.pt(구 246) 무효 → itc-v5 재학습, 하네스 dim 자동감지**. **itc-v5 재학습 판정 ✅ (2026-07-24)** — flow_margin STATE 추가 후 재학습(같은 리워드·seed): berth 평균 RL 87.6→82.8분(SF 90.6), **SF 를 3/4셀+평균 이김**·트럭 유지·전셀 healthy/완주. **판정: 본선은 리워드(v6 실패)가 아니라 STATE 문제 — 관측 개선이 열음**. **확증(신선 12seed·paired CI): berth −11.77분 [−17.92,−5.62] 유의**(48seed pooled·high셀 2/4 유의), 단 **★트럭 P95 유의 악화(3/4셀 +8~12분)** = 본선↔트럭꼬리 Pareto 이동(공짜 아님). [재학습 결과](../docs/strategy-history/2026-07-24-본선STATE재학습-flowmargin-itcv5.md) · **★P95 가드·판정 교정 (2026-07-24, 사용자)**: itc-v5+mandatory 하드가드로 berth −7.73 유의 유지·P95 악화 완화(+1.03[−1.44,+3.50])이나 **"채택 게이트 통과"는 철회** — 비열등은 CI 0 포함이 아니라 δ+단측상한<δ 증명 필요(P95·평균 미확정). **P95=S-B(대기, 턴타임 아님)**. **판정: 본선 개선 원리=성공 / 다목적 정렬·채택=미완**. **★RL>규칙 성립 (2026-07-24)**: flow_margin 임계규칙 대조 — RL 이 규칙을 Pareto 지배(berth 76<77.9+트럭평균 8.62≪11.27), RL+가드는 트럭 P95 32.4(≈SF)로 규칙보다 훨씬 우위 = **단순 임계가 아니라 세밀한 교환 학습**(평균만·CI 별개). **★채택 요건 재판정 = 충족 (2026-07-24, δ 사전정의+턴타임)**: RL+가드 vs SF 15seed — berth −8.12[−12.80,−3.44] 유의 · 트럭평균 −1.24 우월 · **P95 B-C(턴타임) −1.16[−3.44,+1.11] 비열등**(상한<δ3.0) · 완주1.0. **판정: assumed δ 하 채택 가능** — 앞선 "채택 불가"는 비검정 탓. 남은 것=학습 아닌 자료·확장: δ 확정(실SLA)·gate턴타임·개별셀 유의·하이브리드 추가이득 · **★하드가드 vs 소프트가중치 (2026-07-24, 사용자실험)**: 규칙삭제+long_wait 1→5 재학습 → 트럭 강력개선이나 **본선 이득 소멸(berth +8.55 무의)** = 가중치는 전역레버(둘다 못잡음)·하드가드는 표적제약(둘다 잡음) → **꼬리보호엔 하드제약>소프트가중치** · **★본선 축 종결 (2026-07-26, 사용자)**: 5축 소진 경계발견으로 RL-본선 학습 심화 종결([종결 문서](../docs/strategy-history/2026-07-26-RL본선트랙-종결-경계발견-5축소진.md)) — 별칭 해소 관문(①~③)은 재개 조건으로만 유효, 트럭 축 성과·zero-shot 일반화는 보존물. 성능 트랙은 YR-096(rollout 단일계층) |
| YR-079 | Exp | 적응형 내다보기 창 재도전 — 소진시간 추정식 보정(재조작량·미도착 유입 반영) 후 고정 1800s 와 재대결 | ⚪ | **사용자 결정 (2026-07-20, YR-078 파생)**: 적응 기준이 불명확해 지금은 고정 30분 채택 — 적응형은 추후 과제. v1 패인: 대기 작업 수만 세는 추정이 밀린 일 과소평가(창 14~23분 vs 필요 30분). 채택 조건: 보정판이 고정 1800s 와 전 셀 대등 이상 + 한산 구간 계산비용 우위 유지 |
| YR-077-2 | Sim | 돌발 강건성 2단계 — 장부 불일치(컨테이너 실제≠기록 위치) 모델 + 노쇼 통합 | ⚪ | **PoC 이후로 연기 (2026-07-20, 사용자 결정)**: "휴먼 에러는 채택 검증 축이지 원리 증명 아님" — 신규 엔진 모델(기록≠실제) 필요·정책 구조 확정 후 재야 정확(YR-075 확장 시 재측정 회피). 진짜 겨누는 것 = rollout 교사 세계모델 오류 붕괴. 실배치 고민 단계에서 재개 |
| YR-041 | Exp | **본선 마감 조건 locked 시험 — 본선·트럭 목적계약 동결 후 검증** | 🟠 | **1차 집행 완료 (2026-07-27, done.md YR-041-a)**: JR1800은 berth 상한 +9.9>δ5 비열등 실패로 기각·total 무의. 당시 `P95 −10.4`는 구 `S→B` 보고값이라 현 `B→C/A→O` 채택근거가 아니다. 다음 재집행은 새 후보 확정(YR-096 또는 YR-102/099 후속) 뒤 P95 보고용 계약으로 수행. [spec](../docs/dashboard-task-specs/YR-041-vessel-deadline-locked.md) · [prereg+결과](../../outputs/reports/yr041_locked/prereg.md) |
| YR-019 | Exp | ETA 품질 조건 일반화 locked 시험 — BIASED/NO_SHOW/STALE | ⚪ | **실증 단계로 보류 (2026-07-20, 시간계약 의존 추가 2026-07-23)**: ETA 편향·노쇼·낡음은 실배치 강건성 축이라 PoC 단계 비대상. 재개할 때는 **YR-089의 예약시각·단계별 예상 블록도착·실제 gate-in 분리 계약 위에서만** BIASED/NO_SHOW/STALE을 정의한다. [spec](../docs/dashboard-task-specs/YR-019-eta-quality-locked.md) |
| YR-021 | Exp | 학습 밖 부하·혼잡 조건 일반화 locked 시험 | ⚪ | **실증 단계로 보류 (2026-07-20, 사용자 결정)**: 학습 밖 극단 부하·혼잡은 외부 돌발 강건성(실증) 축 — PoC 원리 증명 비대상(019 와 동일 기준). 실증 단계에서 재개. [spec](../docs/dashboard-task-specs/YR-021-ood-load-locked.md) |
| YR-082 | Data | **부산항 10개 터미널 프로파일 자격심사·PoC 전환 게이트 (A 완료 · B/C 잔존)** | 🟠 | [spec](../docs/dashboard-task-specs/YR-082-busan-terminal-poc-profile.md) · **A 공개 registry done (2026-07-22, `e21b4e8` — done.md)**: manifest+선택기+구조군 게이트+과대주장 감사 통과. **B(Level2 블록 자격)·C(현장 PoC)는 실측 구조·운영로그 확보 후** — 현재 Level2 적격 0개라 YR-042 실제구조 arm 은 `DATA-BLOCKED`; 현장 실증은 Level 3~4 뒤 |
| YR-083 | Sim | **V-21 수직 역할분리형 런타임·자격 — step1·5 완료, step2~4 잔존** | 🟠 | [spec](../docs/dashboard-task-specs/YR-083-level2-runtime-contract.md) · H-21 성능 신호 뒤 두 번째 고정 환경을 연다. 같은 21블록에서 육측 YC=외부트럭·LSTP, 해측 YC=본선 LOAD/DISCHARGE·WSTP/AGV로 역할을 분리하고 후보 mask·resolver·버퍼·경로가 실제 궤적을 바꾸는지 검사한다. 현 DGT 선택은 `UNSUPPORTED`이며 이름표 stress를 V-21 성능으로 쓰지 않는다. V용 PPO는 H 가중치 재사용이 아니라 별도 재학습하고 동일 구조 SF와 비교한다. 실제 DGT 재현 주장은 금지 · [결정](../docs/strategy-history/2026-08-06-H21-V21-두-합성구조-고정.md) |
| YR-075-b | RL | 결정권 확장 2: 반입 장치위치 (생애주기 연결 선행) | ⚪ | 현 시뮬은 반입 컨테이너가 같은 에피소드에서 재반출되지 않아 보관품질 검증 불가 — **반입→보관→미래 반출 생애주기 시나리오 연결 후에만** 개방. YR-075-a 후 |
| YR-066 | RL | 차분 신호 개량 — on-policy counterfactual base·창 확대·amortized-JR 회귀 | 🟠 | **우선순위 상향 (2026-07-19, YR-068 기각 근거)**: 본 시나리오에서 D_mean 급감(−5~−7→−1.6~−2.5) — 결정 밀도 상승이 창 내 반사실 차이를 희석. **규모에서 살아남는 차분 신호 설계가 협조 트랙의 선결 병목**. rollout 비용 주의 · **개정 전략 재정의 (2026-07-19)**: OLD 비용 기준 차분 개량은 철회 — 대체 경로 = YR-073(JR_NEW 교사·NEW 목적), 반사실 기법은 YR-074 미세조정 표적으로 흡수 |
| YR-069 | RL | 차분 표적 QMIX 민감도 — λ_mix·창 그리드 (별도 사전등록) | ⚪ | YR-013c prereg 가 금지한 knob 탐색의 정식 경로. **2026-07-26 조건 축소**: 다중 블록이라는 이유만으로 재개하지 않음. YR-099의 local 한계비용 합이 direct terminal 반사실의 부호·순위를 반복적으로 틀리고, 결정론적 공동평가도 규모상 불가능하다는 증거가 있을 때만 비교. QMIX를 써도 작업매칭·예약·commit/rollback은 TransferResolver가 담당 |
| YR-060 | RL | QMIX 타깃 PopArt 보완 (return 크기 비정상성) | 🟡 | [적용전략](../docs/상태정규화-보상가중치-적용전략.md) §6-2 · **조건 발동 (2026-07-19, YR-059: 입력 정규화로 미해결)** — 단 신용 축(YR-061~065)이 1차 용의자로 승격돼 순서는 그 종합 뒤. state_norm 결합 필수 |
| YR-057 | RL | checkpoint 선택 안정화 — val seed 확대·이동평균 선택 | ⚪ | **YR-054 부차 발견** — val 20-seed 곡선 변동폭이 평균의 1.5배, val→test 격차 +3~+9. 격차 주인 아님(우선순위 낮음) |
| YR-058 | Infra | 머신 민감 수렴 테스트 안정화 — residual Δ-net 허용오차·반복수 재조정 | ⚪ | **YR-052 검증 중 발견 (2026-07-18)**: geonu 클론 새 WSL 에서 −2.5±0.15 기대에 −2.22 (변경 전 코드도 동일 — 회귀 아님, CPU 부동소수점 경로 차). 닫힌 단일야드 트랙 테스트라 낮은 우선순위 |
| YR-042 | Exp | **다른 단일 블록·2크레인 profile-shift 게이트 — 동결 정책 무재학습 → 조건부 재적응** | 🟠 | [spec](../docs/dashboard-task-specs/YR-042-cross-terminal-generalization.md) · 정책 동결 + YR-082 Level 2 프로파일 2개 + YR-083 런타임 지원이 선결. 미충족이면 `DATA/STRUCTURE-BLOCKED`; 공개 Level 0~1·임의 수치는 stress test로만 표기 |
| YR-005 | Data | Phase 1 후반: 원천자료 loader·익명화·품질 플래그·날짜 split | ⚪ | [spec](../docs/dashboard-task-specs/YR-005-data-pipeline.md) · 현재는 D5 협약 폐기로 휴면. 향후 YR-082 Level 3 운영로그 접근권한이 실제 확정될 때만 재개하고, 그 전에는 공개자료를 실측로그로 대체하지 않음 |
| YR-020 | RL | Exp-2/3 열세 원인 분석 — 상태공간 희석 vs 정보 무익 판별 | 🟡 | [수렴진단](../docs/YR-020-수렴진단-2026-07-14.md): 희석 방문통계 증거 확보. **YR-030 전환 결정으로 "함수근사 판단재료" 역할 종결** — 학술적 원인 규명 가치로만 유지 (🟠→🟡, 2026-07-14) |
| YR-024 | Sim | 취급시간 확률화 — DGT 육측 원격 인계 분산(PEMA) 반영, 결정적 모델 확장 | 🟡 | YR-023 발견: 공개정보만으론 HJNC·DGT 프로파일이 수치 동일 — 케이스 차별화의 유일한 문헌 경로 |
| YR-015-b | UI | 통합정책 설명·동기비교·운영자 승인/반려 UI | 🟡 | [spec](../docs/dashboard-task-specs/YR-015-verification-ui.md) · 현재는 읽기전용 replay. YR-014 채택 bundle과 YR-082 Level 4 범위가 확정되면 실제 제어 없는 Shadow Mode의 승인·반려 기록으로 확장 |
| YR-014 | Exp | **채택 정책 locked 종합평가·주장 확정** | 🟡 | [spec](../docs/dashboard-task-specs/YR-014-final-evaluation.md) · 새 정책 bundle이 생긴 뒤 수행. 주판정=terminal total cost·평균 B→C/A→O·본선+안전/완주 guard, P95는 보고용. YR-082 자료등급·YR-083 런타임·YR-042 구조 분류 후 기준/stress/무재학습/재적응 주장을 분리 |
| YR-081 | RL | **구조 확장 게이트 — 가변 크레인·이질 다중 블록** | ⚪ | **범위 분리**: H-21 동질 공유형은 YR-150, V-21 육·해측 역할분리형은 YR-083으로 선행한다. 본 row에는 1·3+ 크레인, 블록별 이질성, 두 구조 밖 실제 터미널 일반화가 남는다. [spec](../docs/dashboard-task-specs/YR-081-variable-crane-yard-scaling.md) |
| YR-084 | Infra | **장기 운영화 북극성 — LLM 읽기전용 설명·예외 관제 Shadow PoC** | ⚪ | [spec](../docs/dashboard-task-specs/YR-084-llm-terminal-orchestration.md) · **2026-07-26 범위 축소, 지금 착수하지 않음**. TOS 대체·독자 터미널 계획기·작업 재배정 권한은 제외. 외부 TOS+BlockPolicy+YR-099 resolver의 상태와 결정을 읽고 비정형 예외 해석·설명·운영자 에스컬레이션만 검증. Level 3 로그·Level 4 Shadow 뒤 개방 |

---

운영: 우선순위 오르면 [ready.md](ready.md) 로 승격. 폐기 시 [cancelled.md](cancelled.md).
